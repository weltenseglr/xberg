// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify
package io.xberg;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.Linker;
import java.lang.foreign.SymbolLookup;
import java.lang.foreign.ValueLayout;
import java.lang.invoke.MethodHandle;
import java.io.File;
import java.net.URL;
import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.nio.file.StandardCopyOption;
import java.util.ArrayList;
import java.util.Enumeration;
import java.util.List;
import java.util.jar.JarEntry;
import java.util.jar.JarFile;

@SuppressWarnings({"PMD.AvoidCatchingGenericException", "PMD"})
final class NativeLib {
    private static final System.Logger LOGGER = System.getLogger(NativeLib.class.getName());
    private static final Linker LINKER = Linker.nativeLinker();
    private static SymbolLookup LIB;
    private static final String NATIVES_RESOURCE_ROOT = "/natives";
    private static final String LIBRARY_PATH_PROPERTY = "xberg_ffi.library.path";
    private static final String LIBRARY_DIRECTORY_PROPERTY = "xberg_ffi.library.dir";
    private static final String SKIP_BUNDLED_PROPERTY = "xberg_ffi.skipBundled";
    private static final String LIBRARY_PATH_ENV = "XBERG_FFI_LIB_PATH";
    private static final String LIBRARY_DIRECTORY_ENV = "XBERG_FFI_LIB_DIR";
    private static final String SKIP_BUNDLED_ENV = "XBERG_FFI_SKIP_BUNDLED";
    private static final String[] REQUIRED_SYMBOLS = {
        "xberg_acceleration_config_free",
        "xberg_acceleration_config_from_json",
        "xberg_archive_entry_free",
        "xberg_archive_entry_from_json",
        "xberg_archive_metadata_free",
        "xberg_archive_metadata_from_json",
        "xberg_audio_metadata_free",
        "xberg_audio_metadata_from_json",
        "xberg_b_box_free",
        "xberg_b_box_from_json",
        "xberg_bedrock_config_free",
        "xberg_bedrock_config_from_json",
        "xberg_bibtex_metadata_free",
        "xberg_bibtex_metadata_from_json",
        "xberg_bounding_box_free",
        "xberg_bounding_box_from_json",
        "xberg_browser_config_free",
        "xberg_browser_config_from_json",
        "xberg_build_decoder_prompt_tokens",
        "xberg_cache_stats_free",
        "xberg_cache_stats_from_json",
        "xberg_captioning_config_free",
        "xberg_captioning_config_from_json",
        "xberg_cell_change_free",
        "xberg_cell_change_from_json",
        "xberg_chunk_classification_config_free",
        "xberg_chunk_classification_config_from_json",
        "xberg_chunk_classification_definition_free",
        "xberg_chunk_classification_definition_from_json",
        "xberg_chunk_classification_enrichment_config_free",
        "xberg_chunk_free",
        "xberg_chunk_from_json",
        "xberg_chunk_info_free",
        "xberg_chunk_info_from_json",
        "xberg_chunk_metadata_free",
        "xberg_chunk_metadata_from_json",
        "xberg_chunking_config_free",
        "xberg_chunking_config_from_json",
        "xberg_citation_free",
        "xberg_citation_from_json",
        "xberg_citation_metadata_free",
        "xberg_citation_metadata_from_json",
        "xberg_classification_label_free",
        "xberg_classification_label_from_json",
        "xberg_classify_chunks",
        "xberg_code_chunk_info_free",
        "xberg_code_chunk_info_from_json",
        "xberg_code_data_attribute_free",
        "xberg_code_data_attribute_from_json",
        "xberg_code_data_node_free",
        "xberg_code_data_node_from_json",
        "xberg_code_metadata_free",
        "xberg_code_metadata_from_json",
        "xberg_content_config_free",
        "xberg_content_config_from_json",
        "xberg_content_filter_config_free",
        "xberg_content_filter_config_from_json",
        "xberg_contributor_role_free",
        "xberg_contributor_role_from_json",
        "xberg_conversion_options_free",
        "xberg_conversion_options_from_json",
        "xberg_core_properties_free",
        "xberg_core_properties_from_json",
        "xberg_crawl_config_free",
        "xberg_crawl_config_from_json",
        "xberg_csv_config_free",
        "xberg_csv_config_from_json",
        "xberg_csv_metadata_free",
        "xberg_csv_metadata_from_json",
        "xberg_dbf_field_info_free",
        "xberg_dbf_field_info_from_json",
        "xberg_dbf_metadata_free",
        "xberg_dbf_metadata_from_json",
        "xberg_detect_response_free",
        "xberg_detect_response_from_json",
        "xberg_detection_result_free",
        "xberg_detection_result_from_json",
        "xberg_diff_hunk_free",
        "xberg_diff_hunk_from_json",
        "xberg_diff_options_free",
        "xberg_diff_options_from_json",
        "xberg_djot_content_free",
        "xberg_djot_content_from_json",
        "xberg_djot_image_free",
        "xberg_djot_image_from_json",
        "xberg_djot_link_free",
        "xberg_djot_link_from_json",
        "xberg_doctor",
        "xberg_doctor_check_free",
        "xberg_doctor_check_from_json",
        "xberg_doctor_report_free",
        "xberg_doctor_report_from_json",
        "xberg_doctor_report_to_json",
        "xberg_document_boundary_free",
        "xberg_document_boundary_from_json",
        "xberg_document_counts_free",
        "xberg_document_counts_from_json",
        "xberg_document_metadata_free",
        "xberg_document_metadata_from_json",
        "xberg_document_node_free",
        "xberg_document_node_from_json",
        "xberg_document_relationship_free",
        "xberg_document_relationship_from_json",
        "xberg_document_revision_free",
        "xberg_document_revision_from_json",
        "xberg_document_structure_free",
        "xberg_document_structure_from_json",
        "xberg_document_summary_free",
        "xberg_document_summary_from_json",
        "xberg_docx_app_properties_free",
        "xberg_docx_app_properties_from_json",
        "xberg_docx_metadata_free",
        "xberg_docx_metadata_from_json",
        "xberg_element_free",
        "xberg_element_from_json",
        "xberg_element_metadata_free",
        "xberg_element_metadata_from_json",
        "xberg_email_attachment_free",
        "xberg_email_attachment_from_json",
        "xberg_email_config_free",
        "xberg_email_config_from_json",
        "xberg_email_extraction_result_free",
        "xberg_email_extraction_result_from_json",
        "xberg_email_metadata_free",
        "xberg_email_metadata_from_json",
        "xberg_embed_multi_vector_async",
        "xberg_embed_sparse_async",
        "xberg_embedded_changes_free",
        "xberg_embedded_changes_from_json",
        "xberg_embedded_diff_free",
        "xberg_embedded_diff_from_json",
        "xberg_embedded_file_free",
        "xberg_embedded_file_from_json",
        "xberg_embedding_config_free",
        "xberg_embedding_config_from_json",
        "xberg_ensure_initialized",
        "xberg_entity_free",
        "xberg_entity_from_json",
        "xberg_epub_metadata_free",
        "xberg_epub_metadata_from_json",
        "xberg_error_metadata_free",
        "xberg_error_metadata_from_json",
        "xberg_excel_metadata_free",
        "xberg_excel_metadata_from_json",
        "xberg_excel_sheet_free",
        "xberg_excel_sheet_from_json",
        "xberg_excel_workbook_free",
        "xberg_excel_workbook_from_json",
        "xberg_extract",
        "xberg_extract_batch",
        "xberg_extract_input_free",
        "xberg_extract_input_from_json",
        "xberg_extracted_document_free",
        "xberg_extracted_document_from_json",
        "xberg_extracted_image_free",
        "xberg_extracted_image_from_json",
        "xberg_extracted_uri_free",
        "xberg_extracted_uri_from_json",
        "xberg_extraction_confidence_free",
        "xberg_extraction_confidence_from_json",
        "xberg_extraction_config_free",
        "xberg_extraction_config_from_json",
        "xberg_extraction_diff_free",
        "xberg_extraction_diff_from_json",
        "xberg_extraction_error_item_free",
        "xberg_extraction_error_item_from_json",
        "xberg_extraction_result_free",
        "xberg_extraction_result_from_json",
        "xberg_extraction_result_to_json",
        "xberg_extraction_summary_free",
        "xberg_extraction_summary_from_json",
        "xberg_fiction_book_metadata_free",
        "xberg_fiction_book_metadata_from_json",
        "xberg_file_extraction_config_free",
        "xberg_file_extraction_config_from_json",
        "xberg_find_unmarked_claims",
        "xberg_footnote_anchor_free",
        "xberg_footnote_anchor_from_json",
        "xberg_footnote_config_free",
        "xberg_footnote_config_from_json",
        "xberg_footnote_definition_free",
        "xberg_footnote_definition_from_json",
        "xberg_footnote_free",
        "xberg_footnote_from_json",
        "xberg_formatted_block_free",
        "xberg_formatted_block_from_json",
        "xberg_formula_free",
        "xberg_formula_from_json",
        "xberg_free_string",
        "xberg_grid_cell_free",
        "xberg_grid_cell_from_json",
        "xberg_header_metadata_free",
        "xberg_header_metadata_from_json",
        "xberg_heading_context_free",
        "xberg_heading_context_from_json",
        "xberg_heading_level_free",
        "xberg_heading_level_from_json",
        "xberg_heuristics_config_free",
        "xberg_heuristics_config_from_json",
        "xberg_hierarchical_block_free",
        "xberg_hierarchical_block_from_json",
        "xberg_hierarchy_config_free",
        "xberg_hierarchy_config_from_json",
        "xberg_html_metadata_free",
        "xberg_html_metadata_from_json",
        "xberg_html_output_config_free",
        "xberg_html_output_config_from_json",
        "xberg_image_extraction_config_free",
        "xberg_image_extraction_config_from_json",
        "xberg_image_metadata_free",
        "xberg_image_metadata_from_json",
        "xberg_image_metadata_type_free",
        "xberg_image_metadata_type_from_json",
        "xberg_image_preprocessing_config_free",
        "xberg_image_preprocessing_config_from_json",
        "xberg_image_preprocessing_metadata_free",
        "xberg_image_preprocessing_metadata_from_json",
        "xberg_inline_element_free",
        "xberg_inline_element_from_json",
        "xberg_install_pdf_render_diagnostics",
        "xberg_jats_metadata_free",
        "xberg_jats_metadata_from_json",
        "xberg_keyword_config_free",
        "xberg_keyword_config_from_json",
        "xberg_keyword_free",
        "xberg_keyword_from_json",
        "xberg_language_confidence_free",
        "xberg_language_confidence_from_json",
        "xberg_language_detection_config_free",
        "xberg_language_detection_config_from_json",
        "xberg_last_error_code",
        "xberg_last_error_context",
        "xberg_late_interaction_config_free",
        "xberg_late_interaction_config_from_json",
        "xberg_late_interaction_match_free",
        "xberg_late_interaction_match_from_json",
        "xberg_late_interaction_preset_free",
        "xberg_late_interaction_preset_from_json",
        "xberg_layout_detection_config_free",
        "xberg_layout_detection_config_from_json",
        "xberg_layout_detection_free",
        "xberg_layout_detection_from_json",
        "xberg_layout_region_free",
        "xberg_layout_region_from_json",
        "xberg_link_metadata_free",
        "xberg_link_metadata_from_json",
        "xberg_list_document_extractors",
        "xberg_list_embedding_backends",
        "xberg_list_ocr_backends",
        "xberg_list_post_processors",
        "xberg_list_renderers",
        "xberg_list_reranker_backends",
        "xberg_list_supported_formats",
        "xberg_list_tokenizer_backends",
        "xberg_list_validators",
        "xberg_llm_budget_config_free",
        "xberg_llm_budget_config_from_json",
        "xberg_llm_cache_config_free",
        "xberg_llm_cache_config_from_json",
        "xberg_llm_config_free",
        "xberg_llm_config_from_json",
        "xberg_llm_provider_config_free",
        "xberg_llm_provider_config_from_json",
        "xberg_llm_rate_limit_config_free",
        "xberg_llm_rate_limit_config_from_json",
        "xberg_llm_usage_free",
        "xberg_llm_usage_from_json",
        "xberg_map_result_free",
        "xberg_map_result_from_json",
        "xberg_map_result_to_json",
        "xberg_map_url",
        "xberg_max_sim_rank",
        "xberg_max_sim_score",
        "xberg_meta_schema_compile",
        "xberg_meta_schema_free",
        "xberg_meta_schema_parse_preset",
        "xberg_metadata_free",
        "xberg_metadata_from_json",
        "xberg_model_paths_free",
        "xberg_model_paths_from_json",
        "xberg_multi_vector_embedding_free",
        "xberg_multi_vector_embedding_from_json",
        "xberg_multidoc_input_free",
        "xberg_multidoc_input_from_json",
        "xberg_multidoc_thresholds_free",
        "xberg_multidoc_thresholds_from_json",
        "xberg_ner_config_free",
        "xberg_ner_config_from_json",
        "xberg_ocr_confidence_free",
        "xberg_ocr_confidence_from_json",
        "xberg_ocr_config_free",
        "xberg_ocr_config_from_json",
        "xberg_ocr_element_config_free",
        "xberg_ocr_element_config_from_json",
        "xberg_ocr_element_free",
        "xberg_ocr_element_from_json",
        "xberg_ocr_extraction_result_free",
        "xberg_ocr_extraction_result_from_json",
        "xberg_ocr_metadata_free",
        "xberg_ocr_metadata_from_json",
        "xberg_ocr_pipeline_config_free",
        "xberg_ocr_pipeline_config_from_json",
        "xberg_ocr_pipeline_stage_free",
        "xberg_ocr_pipeline_stage_from_json",
        "xberg_ocr_quality_thresholds_free",
        "xberg_ocr_quality_thresholds_from_json",
        "xberg_ocr_rotation_free",
        "xberg_ocr_rotation_from_json",
        "xberg_ocr_table_bounding_box_free",
        "xberg_ocr_table_bounding_box_from_json",
        "xberg_ocr_table_free",
        "xberg_ocr_table_from_json",
        "xberg_orientation_result_free",
        "xberg_orientation_result_from_json",
        "xberg_paddle_ocr_config_free",
        "xberg_paddle_ocr_config_from_json",
        "xberg_page_boundary_free",
        "xberg_page_boundary_from_json",
        "xberg_page_classification_config_free",
        "xberg_page_classification_config_from_json",
        "xberg_page_classification_free",
        "xberg_page_classification_from_json",
        "xberg_page_config_free",
        "xberg_page_config_from_json",
        "xberg_page_content_free",
        "xberg_page_content_from_json",
        "xberg_page_hierarchy_free",
        "xberg_page_hierarchy_from_json",
        "xberg_page_info_free",
        "xberg_page_info_from_json",
        "xberg_page_range_free",
        "xberg_page_range_from_json",
        "xberg_page_signals_free",
        "xberg_page_signals_from_json",
        "xberg_page_span_free",
        "xberg_page_span_from_json",
        "xberg_page_structure_free",
        "xberg_page_structure_from_json",
        "xberg_pattern_match_free",
        "xberg_pattern_match_from_json",
        "xberg_pdf_annotation_free",
        "xberg_pdf_annotation_from_json",
        "xberg_pdf_config_free",
        "xberg_pdf_config_from_json",
        "xberg_pdf_form_field_free",
        "xberg_pdf_form_field_from_json",
        "xberg_pdf_metadata_free",
        "xberg_pdf_metadata_from_json",
        "xberg_post_processor_config_free",
        "xberg_post_processor_config_from_json",
        "xberg_pptx_app_properties_free",
        "xberg_pptx_app_properties_from_json",
        "xberg_pptx_extraction_result_free",
        "xberg_pptx_extraction_result_from_json",
        "xberg_pptx_metadata_free",
        "xberg_pptx_metadata_from_json",
        "xberg_preprocessing_options_free",
        "xberg_preprocessing_options_from_json",
        "xberg_preset_free",
        "xberg_preset_from_json",
        "xberg_preset_sample_free",
        "xberg_preset_sample_from_json",
        "xberg_preset_summary_free",
        "xberg_preset_summary_from_json",
        "xberg_preset_to_json",
        "xberg_processing_warning_free",
        "xberg_processing_warning_from_json",
        "xberg_property_change_free",
        "xberg_property_change_from_json",
        "xberg_proxy_config_free",
        "xberg_proxy_config_from_json",
        "xberg_pst_metadata_free",
        "xberg_pst_metadata_from_json",
        "xberg_qr_bounding_box_free",
        "xberg_qr_bounding_box_from_json",
        "xberg_qr_code_free",
        "xberg_qr_code_from_json",
        "xberg_rake_params_free",
        "xberg_rake_params_from_json",
        "xberg_recognized_table_free",
        "xberg_recognized_table_from_json",
        "xberg_redaction_config_free",
        "xberg_redaction_config_from_json",
        "xberg_redaction_finding_free",
        "xberg_redaction_finding_from_json",
        "xberg_redaction_pattern_free",
        "xberg_redaction_pattern_from_json",
        "xberg_redaction_report_free",
        "xberg_redaction_report_from_json",
        "xberg_redaction_term_free",
        "xberg_redaction_term_from_json",
        "xberg_registry_extend_from_dir",
        "xberg_registry_free",
        "xberg_registry_get",
        "xberg_registry_is_empty",
        "xberg_registry_len",
        "xberg_registry_load_embedded",
        "xberg_registry_sample_bytes",
        "xberg_registry_summaries",
        "xberg_reranked_document_free",
        "xberg_reranked_document_from_json",
        "xberg_reranker_config_free",
        "xberg_reranker_config_from_json",
        "xberg_resolved_preset_free",
        "xberg_resolved_preset_from_json",
        "xberg_revision_delta_free",
        "xberg_revision_delta_from_json",
        "xberg_security_limits_free",
        "xberg_security_limits_from_json",
        "xberg_server_config_free",
        "xberg_server_config_from_json",
        "xberg_sitemap_url_free",
        "xberg_sitemap_url_from_json",
        "xberg_sparse_embedding_config_free",
        "xberg_sparse_embedding_config_from_json",
        "xberg_sparse_embedding_free",
        "xberg_sparse_embedding_from_json",
        "xberg_sparse_embedding_preset_free",
        "xberg_sparse_embedding_preset_from_json",
        "xberg_ssrf_policy_free",
        "xberg_ssrf_policy_from_json",
        "xberg_structured_data_free",
        "xberg_structured_data_from_json",
        "xberg_structured_data_result_free",
        "xberg_structured_data_result_from_json",
        "xberg_structured_extraction_config_free",
        "xberg_structured_extraction_config_from_json",
        "xberg_summarization_config_free",
        "xberg_summarization_config_from_json",
        "xberg_supported_format_free",
        "xberg_supported_format_from_json",
        "xberg_svg_options_free",
        "xberg_svg_options_from_json",
        "xberg_table_cell_free",
        "xberg_table_cell_from_json",
        "xberg_table_diff_free",
        "xberg_table_diff_from_json",
        "xberg_table_free",
        "xberg_table_from_json",
        "xberg_table_grid_free",
        "xberg_table_grid_from_json",
        "xberg_take_pdf_oxide_render_warnings",
        "xberg_tesseract_config_free",
        "xberg_tesseract_config_from_json",
        "xberg_text_annotation_free",
        "xberg_text_annotation_from_json",
        "xberg_text_extraction_result_free",
        "xberg_text_extraction_result_from_json",
        "xberg_text_metadata_free",
        "xberg_text_metadata_from_json",
        "xberg_timestamp_token_to_ms",
        "xberg_token_counter_free",
        "xberg_token_counter_new",
        "xberg_token_reduction_config_free",
        "xberg_token_reduction_config_from_json",
        "xberg_token_reduction_options_free",
        "xberg_token_reduction_options_from_json",
        "xberg_transcription_config_free",
        "xberg_transcription_config_from_json",
        "xberg_translation_config_free",
        "xberg_translation_config_from_json",
        "xberg_translation_free",
        "xberg_translation_from_json",
        "xberg_tree_sitter_config_free",
        "xberg_tree_sitter_config_from_json",
        "xberg_tree_sitter_process_config_free",
        "xberg_tree_sitter_process_config_from_json",
        "xberg_url_extraction_config_free",
        "xberg_url_extraction_config_from_json",
        "xberg_user_chunk_config_free",
        "xberg_user_chunk_config_from_json",
        "xberg_verify_excerpt",
        "xberg_xlsx_app_properties_free",
        "xberg_xlsx_app_properties_from_json",
        "xberg_xml_extraction_result_free",
        "xberg_xml_extraction_result_from_json",
        "xberg_xml_metadata_free",
        "xberg_xml_metadata_from_json",
        "xberg_yake_params_free",
        "xberg_yake_params_from_json",
        "xberg_year_range_free",
        "xberg_year_range_from_json"
    };
    private static final Object NATIVE_EXTRACT_LOCK = new Object();
    private static String cachedExtractKey;
    private static Path cachedExtractDir;

    static {
        Path loadedLibraryPath = loadNativeLibrary();
        try {
            Arena arena = Arena.ofShared();
            // Try the loaded library path (for System.load() with absolute path)
            if (loadedLibraryPath != null) {
                try {
                    LIB = SymbolLookup.libraryLookup(loadedLibraryPath, arena);
                } catch (RuntimeException | Error inner) {
                    // If Path variant fails, fallback to defaultLookup
                    LIB = LINKER.defaultLookup();
                }
            } else {
                LIB = LINKER.defaultLookup();
            }
            validateRequiredSymbols(loadedLibraryPath);
        } catch (RuntimeException | Error e) {
            ExceptionInInitializerError error = new ExceptionInInitializerError(
                "Failed to initialize library symbols: " + e.getMessage());
            error.initCause(e);
            throw error;
        }
    }

    private static Path loadNativeLibrary() {
        Path loadedLibraryPath = null;
        String osName = System.getProperty("os.name", "").toLowerCase(java.util.Locale.ROOT);
        String osArch = System.getProperty("os.arch", "").toLowerCase(java.util.Locale.ROOT);

        String libName;
        String libExt;
        if (osName.contains("mac") || osName.contains("darwin")) {
            libName = "libxberg_ffi";
            libExt = ".dylib";
        } else if (osName.contains("win")) {
            libName = "xberg_ffi";
            libExt = ".dll";
        } else {
            libName = "libxberg_ffi";
            libExt = ".so";
        }

        String nativesRid = resolveNativesRid(osName, osArch);
        String nativesDir = NATIVES_RESOURCE_ROOT + "/" + nativesRid;

        String explicitPath = firstNonBlank(System.getProperty(LIBRARY_PATH_PROPERTY), System.getenv(LIBRARY_PATH_ENV));
        if (explicitPath != null) {
            return loadAbsolutePath(Paths.get(explicitPath));
        }

        String explicitDirectory = firstNonBlank(
                System.getProperty(LIBRARY_DIRECTORY_PROPERTY),
                System.getenv(LIBRARY_DIRECTORY_ENV));
        if (explicitDirectory != null) {
            return loadAbsolutePath(Paths.get(explicitDirectory, libName + libExt));
        }

        String skipBundledValue = firstNonBlank(
                System.getProperty(SKIP_BUNDLED_PROPERTY),
                System.getenv(SKIP_BUNDLED_ENV));
        if (!Boolean.parseBoolean(skipBundledValue)) {
            Path extracted = tryExtractAndLoadFromResources(nativesDir, libName, libExt);
            if (extracted != null) {
                return extracted;
            }
        }

        try {
            System.loadLibrary("xberg_ffi");
            // Find the full path by searching java.library.path
            Path libPath = findLoadedLibraryPath(libName, libExt);
            if (libPath != null) {
                loadedLibraryPath = libPath;
            }
        } catch (UnsatisfiedLinkError e) {
            String msg = "Failed to load xberg_ffi native library. Expected resource: " + nativesDir + "/" + libName
                    + libExt + " (RID: " + nativesRid + "). "
                    + "Ensure the library is bundled in the JAR under natives/{os-arch}/, "
                    + "place it on java.library.path, set -D" + LIBRARY_PATH_PROPERTY
                    + "=/absolute/path, or set " + LIBRARY_PATH_ENV + ".";
            UnsatisfiedLinkError out = new UnsatisfiedLinkError(msg + " Original error: " + e.getMessage());
            out.initCause(e);
            throw out;
        }
        return loadedLibraryPath;
    }

    private static String firstNonBlank(String preferred, String fallback) {
        if (preferred != null && !preferred.isBlank()) {
            return preferred;
        }
        if (fallback != null && !fallback.isBlank()) {
            return fallback;
        }
        return null;
    }

    private static Path loadAbsolutePath(Path configuredPath) {
        Path absolutePath = configuredPath.toAbsolutePath().normalize();
        if (!Files.isRegularFile(absolutePath)) {
            throw new UnsatisfiedLinkError("Configured native library does not exist: " + absolutePath);
        }
        System.load(absolutePath.toString());
        return absolutePath;
    }

    private static void validateRequiredSymbols(Path loadedLibraryPath) {
        List<String> missing = new ArrayList<>();
        for (String symbol : REQUIRED_SYMBOLS) {
            if (LIB.find(symbol).isEmpty() && LIB.find("_" + symbol).isEmpty()) {
                missing.add(symbol);
            }
        }
        if (missing.isEmpty()) {
            return;
        }

        int exportedCount = REQUIRED_SYMBOLS.length - missing.size();
        String loadedFrom = loadedLibraryPath == null
                ? "System.loadLibrary(xberg_ffi) / java.library.path"
                : loadedLibraryPath.toString();
        throw new UnsatisfiedLinkError(
                "The loaded xberg_ffi native library is stale relative to this binding: it exports "
                        + exportedCount + " of " + REQUIRED_SYMBOLS.length + " required symbols. Missing: "
                        + String.join(", ", missing) + ". Loaded from: " + loadedFrom
                        + ". Rebuild the native library, point this binding at a current one with -D"
                        + LIBRARY_PATH_PROPERTY + "=/absolute/path, or bypass bundled resources with -D"
                        + SKIP_BUNDLED_PROPERTY + "=true.");
    }

    private static Path tryExtractAndLoadFromResources(String nativesDir, String libName, String libExt) {
        String resourcePath = nativesDir + "/" + libName + libExt;
        URL resource = NativeLib.class.getResource(resourcePath);
        if (resource == null) {
            return null;
        }

        try {
            Path tempDir = extractOrReuseNativeDirectory(nativesDir);
            Path libPath = tempDir.resolve(libName + libExt);
            if (!Files.exists(libPath)) {
                throw new UnsatisfiedLinkError("Missing extracted native library: " + libPath);
            }
            Path absPath = libPath.toAbsolutePath();
            System.load(absPath.toString());
            return absPath;
        } catch (Exception | Error e) {
            LOGGER.log(System.Logger.Level.WARNING, "Failed to extract and load native library from resources", e);
            return null;
        }
    }

    private static Path extractOrReuseNativeDirectory(String nativesDir) throws Exception {
        URL location = NativeLib.class.getProtectionDomain().getCodeSource().getLocation();
        if (location == null) {
            throw new IllegalStateException("Missing code source location for xberg_ffi JAR");
        }

        Path codePath = Path.of(location.toURI());
        String key = codePath.toAbsolutePath() + "::" + nativesDir;

        synchronized (NATIVE_EXTRACT_LOCK) {
            if (cachedExtractDir != null && key.equals(cachedExtractKey)) {
                return cachedExtractDir;
            }
            Path tempDir = Files.createTempDirectory("xberg_ffi_native");
            tempDir.toFile().deleteOnExit();
            List<Path> extracted = extractNativeDirectory(codePath, nativesDir, tempDir);
            if (extracted.isEmpty()) {
                throw new IllegalStateException("No native files extracted from resources dir: " + nativesDir);
            }
            cachedExtractKey = key;
            cachedExtractDir = tempDir;
            return tempDir;
        }
    }

    private static List<Path> extractNativeDirectory(Path codePath, String nativesDir, Path destDir) throws Exception {
        if (!Files.exists(destDir) || !Files.isDirectory(destDir)) {
            throw new IllegalArgumentException("Destination directory does not exist: " + destDir);
        }

        String prefix = nativesDir.startsWith("/") ? nativesDir.substring(1) : nativesDir;
        if (!prefix.endsWith("/")) {
            prefix = prefix + "/";
        }

        if (Files.isDirectory(codePath)) {
            Path nativesPath = codePath.resolve(prefix);
            if (!Files.exists(nativesPath) || !Files.isDirectory(nativesPath)) {
                return List.of();
            }
            return copyDirectory(nativesPath, destDir);
        }

        List<Path> extracted = new ArrayList<>();
        try (JarFile jar = new JarFile(codePath.toFile())) {
            Enumeration<JarEntry> entries = jar.entries();
            while (entries.hasMoreElements()) {
                JarEntry entry = entries.nextElement();
                String name = entry.getName();
                if (!name.startsWith(prefix) || entry.isDirectory()) {
                    continue;
                }
                String relative = name.substring(prefix.length());
                Path out = safeResolve(destDir, relative);
                Files.createDirectories(out.getParent());
                try (var in = jar.getInputStream(entry)) {
                    Files.copy(in, out, StandardCopyOption.REPLACE_EXISTING);
                }
                out.toFile().deleteOnExit();
                extracted.add(out);
            }
        }
        return extracted;
    }

    private static List<Path> copyDirectory(Path srcDir, Path destDir) throws Exception {
        List<Path> copied = new ArrayList<>();
        try (var paths = Files.walk(srcDir)) {
            for (Path src : (Iterable<Path>) paths::iterator) {
                if (Files.isDirectory(src)) {
                    continue;
                }
                Path relative = srcDir.relativize(src);
                Path out = safeResolve(destDir, relative.toString());
                Files.createDirectories(out.getParent());
                Files.copy(src, out, StandardCopyOption.REPLACE_EXISTING);
                out.toFile().deleteOnExit();
                copied.add(out);
            }
        }
        return copied;
    }

    private static Path safeResolve(Path destDir, String relative) throws Exception {
        Path normalizedDest = destDir.toAbsolutePath().normalize();
        Path out = normalizedDest.resolve(relative).normalize();
        if (!out.startsWith(normalizedDest)) {
            throw new SecurityException("Blocked extracting native file outside destination directory: " + relative);
        }
        return out;
    }

    private static String resolveNativesRid(String osName, String osArch) {
        // Classifier names match go_java_platform(): `linux-aarch64`, `macos-arm64`,
        // `windows-x86_64`, etc. macOS distinguishes arm64 (ARM-based), while other
        // platforms use aarch64. Align with src/publish/platform.rs::go_java_platform().
        boolean isMac = osName.contains("mac") || osName.contains("darwin");
        boolean isWindows = osName.contains("win");

        String arch;
        if (osArch.contains("aarch64") || osArch.contains("arm64")) {
            // macOS uses "arm64" for aarch64; Linux uses "aarch64"
            arch = isMac ? "arm64" : "aarch64";
        } else if (osArch.contains("x86_64") || osArch.contains("amd64")) {
            arch = "x86_64";
        } else {
            arch = osArch.replaceAll("[^a-z0-9_]+", "");
        }

        String os;
        if (isMac) {
            os = "macos";
        } else if (isWindows) {
            os = "windows";
        } else {
            os = "linux";
        }

        return os + "-" + arch;
    }

    private static Path findLoadedLibraryPath(String fullLibName, String libExt) {
        // Search java.library.path for the library file
        String javaLibPath = System.getProperty("java.library.path");
        if (javaLibPath != null) {
            for (String path : javaLibPath.split(File.pathSeparator)) {
                Path libPath = Paths.get(path, fullLibName + libExt);
                if (Files.exists(libPath)) {
                    try {
                        return libPath.toRealPath();
                    } catch (java.io.IOException e) {
                        return libPath.toAbsolutePath();
                    }
                }
            }
        }
        // Library not found in java.library.path
        return null;
    }


    static final MethodHandle XBERG_EXTRACT = LINKER.downcallHandle(
        LIB.find("xberg_extract")
            .or(() -> LIB.find("_xberg_extract"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extract"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extract"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_extract" +
                " (tried: xberg_extract, _xberg_extract). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACT_BATCH = LINKER.downcallHandle(
        LIB.find("xberg_extract_batch")
            .or(() -> LIB.find("_xberg_extract_batch"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extract_batch"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extract_batch"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_extract_batch" +
                " (tried: xberg_extract_batch, _xberg_extract_batch). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_MAP_URL = LINKER.downcallHandle(
        LIB.find("xberg_map_url")
            .or(() -> LIB.find("_xberg_map_url"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_map_url"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_map_url"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_map_url" +
                " (tried: xberg_map_url, _xberg_map_url). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LIST_SUPPORTED_FORMATS = LINKER.downcallHandle(
        LIB.find("xberg_list_supported_formats")
            .or(() -> LIB.find("_xberg_list_supported_formats"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_supported_formats"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_supported_formats"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_supported_formats" +
                " (tried: xberg_list_supported_formats, _xberg_list_supported_formats). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ENSURE_INITIALIZED = LINKER.downcallHandle(
        LIB.find("xberg_ensure_initialized")
            .or(() -> LIB.find("_xberg_ensure_initialized"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ensure_initialized"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ensure_initialized"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_ensure_initialized" +
                " (tried: xberg_ensure_initialized, _xberg_ensure_initialized). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid()
    );


    static final MethodHandle XBERG_LIST_EMBEDDING_BACKENDS = LINKER.downcallHandle(
        LIB.find("xberg_list_embedding_backends")
            .or(() -> LIB.find("_xberg_list_embedding_backends"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_embedding_backends"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_embedding_backends"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_embedding_backends" +
                " (tried: xberg_list_embedding_backends, _xberg_list_embedding_backends). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LIST_DOCUMENT_EXTRACTORS = LINKER.downcallHandle(
        LIB.find("xberg_list_document_extractors")
            .or(() -> LIB.find("_xberg_list_document_extractors"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_document_extractors"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_document_extractors"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_document_extractors" +
                " (tried: xberg_list_document_extractors, _xberg_list_document_extractors). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LIST_OCR_BACKENDS = LINKER.downcallHandle(
        LIB.find("xberg_list_ocr_backends")
            .or(() -> LIB.find("_xberg_list_ocr_backends"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_ocr_backends"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_ocr_backends"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_ocr_backends" +
                " (tried: xberg_list_ocr_backends, _xberg_list_ocr_backends). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LIST_POST_PROCESSORS = LINKER.downcallHandle(
        LIB.find("xberg_list_post_processors")
            .or(() -> LIB.find("_xberg_list_post_processors"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_post_processors"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_post_processors"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_post_processors" +
                " (tried: xberg_list_post_processors, _xberg_list_post_processors). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LIST_RENDERERS = LINKER.downcallHandle(
        LIB.find("xberg_list_renderers")
            .or(() -> LIB.find("_xberg_list_renderers"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_renderers"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_renderers"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_renderers" +
                " (tried: xberg_list_renderers, _xberg_list_renderers). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LIST_RERANKER_BACKENDS = LINKER.downcallHandle(
        LIB.find("xberg_list_reranker_backends")
            .or(() -> LIB.find("_xberg_list_reranker_backends"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_reranker_backends"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_reranker_backends"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_reranker_backends" +
                " (tried: xberg_list_reranker_backends, _xberg_list_reranker_backends). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LIST_TOKENIZER_BACKENDS = LINKER.downcallHandle(
        LIB.find("xberg_list_tokenizer_backends")
            .or(() -> LIB.find("_xberg_list_tokenizer_backends"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_tokenizer_backends"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_tokenizer_backends"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_tokenizer_backends" +
                " (tried: xberg_list_tokenizer_backends, _xberg_list_tokenizer_backends). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LIST_VALIDATORS = LINKER.downcallHandle(
        LIB.find("xberg_list_validators")
            .or(() -> LIB.find("_xberg_list_validators"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_validators"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_validators"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_validators" +
                " (tried: xberg_list_validators, _xberg_list_validators). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CLASSIFY_CHUNKS = LINKER.downcallHandle(
        LIB.find("xberg_classify_chunks")
            .or(() -> LIB.find("_xberg_classify_chunks"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_classify_chunks"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_classify_chunks"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_classify_chunks" +
                " (tried: xberg_classify_chunks, _xberg_classify_chunks). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FIND_UNMARKED_CLAIMS = LINKER.downcallHandle(
        LIB.find("xberg_find_unmarked_claims")
            .or(() -> LIB.find("_xberg_find_unmarked_claims"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_find_unmarked_claims"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_find_unmarked_claims"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_find_unmarked_claims" +
                " (tried: xberg_find_unmarked_claims, _xberg_find_unmarked_claims). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_VERIFY_EXCERPT = LINKER.downcallHandle(
        LIB.find("xberg_verify_excerpt")
            .or(() -> LIB.find("_xberg_verify_excerpt"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_verify_excerpt"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_verify_excerpt"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_verify_excerpt" +
                " (tried: xberg_verify_excerpt, _xberg_verify_excerpt). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMBED_SPARSE_ASYNC = LINKER.downcallHandle(
        LIB.find("xberg_embed_sparse_async")
            .or(() -> LIB.find("_xberg_embed_sparse_async"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_embed_sparse_async"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_embed_sparse_async"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_embed_sparse_async" +
                " (tried: xberg_embed_sparse_async, _xberg_embed_sparse_async). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_MAX_SIM_SCORE = LINKER.downcallHandle(
        LIB.find("xberg_max_sim_score")
            .or(() -> LIB.find("_xberg_max_sim_score"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_max_sim_score"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_max_sim_score"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_max_sim_score" +
                " (tried: xberg_max_sim_score, _xberg_max_sim_score). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_DOUBLE, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_MAX_SIM_RANK = LINKER.downcallHandle(
        LIB.find("xberg_max_sim_rank")
            .or(() -> LIB.find("_xberg_max_sim_rank"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_max_sim_rank"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_max_sim_rank"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_max_sim_rank" +
                " (tried: xberg_max_sim_rank, _xberg_max_sim_rank). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMBED_MULTI_VECTOR_ASYNC = LINKER.downcallHandle(
        LIB.find("xberg_embed_multi_vector_async")
            .or(() -> LIB.find("_xberg_embed_multi_vector_async"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_embed_multi_vector_async"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_embed_multi_vector_async"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_embed_multi_vector_async" +
                " (tried: xberg_embed_multi_vector_async, _xberg_embed_multi_vector_async). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle XBERG_DOCTOR = LINKER.downcallHandle(
        LIB.find("xberg_doctor")
            .or(() -> LIB.find("_xberg_doctor"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_doctor"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_doctor"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_doctor" +
                " (tried: xberg_doctor, _xberg_doctor). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_INSTALL_PDF_RENDER_DIAGNOSTICS = LINKER.downcallHandle(
        LIB.find("xberg_install_pdf_render_diagnostics")
            .or(() -> LIB.find("_xberg_install_pdf_render_diagnostics"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_install_pdf_render_diagnostics"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_install_pdf_render_diagnostics"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_install_pdf_render_diagnostics" +
                " (tried: xberg_install_pdf_render_diagnostics, _xberg_install_pdf_render_diagnostics). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG)
    );


    static final MethodHandle XBERG_TAKE_PDF_OXIDE_RENDER_WARNINGS = LINKER.downcallHandle(
        LIB.find("xberg_take_pdf_oxide_render_warnings")
            .or(() -> LIB.find("_xberg_take_pdf_oxide_render_warnings"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_take_pdf_oxide_render_warnings"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_take_pdf_oxide_render_warnings"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_take_pdf_oxide_render_warnings" +
                " (tried: xberg_take_pdf_oxide_render_warnings, _xberg_take_pdf_oxide_render_warnings). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_BUILD_DECODER_PROMPT_TOKENS = LINKER.downcallHandle(
        LIB.find("xberg_build_decoder_prompt_tokens")
            .or(() -> LIB.find("_xberg_build_decoder_prompt_tokens"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_build_decoder_prompt_tokens"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_build_decoder_prompt_tokens"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_build_decoder_prompt_tokens" +
                " (tried: xberg_build_decoder_prompt_tokens, _xberg_build_decoder_prompt_tokens). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(
            ValueLayout.ADDRESS,
            ValueLayout.JAVA_LONG,
            ValueLayout.JAVA_LONG,
            ValueLayout.JAVA_LONG,
            ValueLayout.JAVA_LONG,
            ValueLayout.JAVA_LONG
        )
    );


    static final MethodHandle XBERG_TIMESTAMP_TOKEN_TO_MS = LINKER.downcallHandle(
        LIB.find("xberg_timestamp_token_to_ms")
            .or(() -> LIB.find("_xberg_timestamp_token_to_ms"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_timestamp_token_to_ms"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_timestamp_token_to_ms"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_timestamp_token_to_ms" +
                " (tried: xberg_timestamp_token_to_ms, _xberg_timestamp_token_to_ms). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.JAVA_LONG, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle XBERG_FREE_STRING = LINKER.downcallHandle(
        LIB.find("xberg_free_string")
            .or(() -> LIB.find("_xberg_free_string"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_free_string"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_free_string"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TOKEN_COUNTER_NEW = LINKER.downcallHandle(
        LIB.find("xberg_token_counter_new")
            .or(() -> LIB.find("_xberg_token_counter_new"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_token_counter_new"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_token_counter_new"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_token_counter_new" +
                " (tried: xberg_token_counter_new, _xberg_token_counter_new). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_META_SCHEMA_COMPILE = LINKER.downcallHandle(
        LIB.find("xberg_meta_schema_compile")
            .or(() -> LIB.find("_xberg_meta_schema_compile"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_meta_schema_compile"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_meta_schema_compile"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_meta_schema_compile" +
                " (tried: xberg_meta_schema_compile, _xberg_meta_schema_compile). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_META_SCHEMA_PARSE_PRESET = LINKER.downcallHandle(
        LIB.find("xberg_meta_schema_parse_preset")
            .or(() -> LIB.find("_xberg_meta_schema_parse_preset"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_meta_schema_parse_preset"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_meta_schema_parse_preset"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_meta_schema_parse_preset" +
                " (tried: xberg_meta_schema_parse_preset, _xberg_meta_schema_parse_preset). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle XBERG_REGISTRY_LOAD_EMBEDDED = LINKER.downcallHandle(
        LIB.find("xberg_registry_load_embedded")
            .or(() -> LIB.find("_xberg_registry_load_embedded"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_registry_load_embedded"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_registry_load_embedded"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_registry_load_embedded" +
                " (tried: xberg_registry_load_embedded, _xberg_registry_load_embedded). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGISTRY_GET = LINKER.downcallHandle(
        LIB.find("xberg_registry_get")
            .or(() -> LIB.find("_xberg_registry_get"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_registry_get"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_registry_get"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_registry_get" +
                " (tried: xberg_registry_get, _xberg_registry_get). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGISTRY_SUMMARIES = LINKER.downcallHandle(
        LIB.find("xberg_registry_summaries")
            .or(() -> LIB.find("_xberg_registry_summaries"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_registry_summaries"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_registry_summaries"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_registry_summaries" +
                " (tried: xberg_registry_summaries, _xberg_registry_summaries). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGISTRY_LEN = LINKER.downcallHandle(
        LIB.find("xberg_registry_len")
            .or(() -> LIB.find("_xberg_registry_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_registry_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_registry_len"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_registry_len" +
                " (tried: xberg_registry_len, _xberg_registry_len). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGISTRY_IS_EMPTY = LINKER.downcallHandle(
        LIB.find("xberg_registry_is_empty")
            .or(() -> LIB.find("_xberg_registry_is_empty"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_registry_is_empty"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_registry_is_empty"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_registry_is_empty" +
                " (tried: xberg_registry_is_empty, _xberg_registry_is_empty). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGISTRY_SAMPLE_BYTES = LINKER.downcallHandle(
        LIB.find("xberg_registry_sample_bytes")
            .or(() -> LIB.find("_xberg_registry_sample_bytes"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_registry_sample_bytes"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_registry_sample_bytes"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_registry_sample_bytes" +
                " (tried: xberg_registry_sample_bytes, _xberg_registry_sample_bytes). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGISTRY_EXTEND_FROM_DIR = LINKER.downcallHandle(
        LIB.find("xberg_registry_extend_from_dir")
            .or(() -> LIB.find("_xberg_registry_extend_from_dir"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_registry_extend_from_dir"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_registry_extend_from_dir"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_registry_extend_from_dir" +
                " (tried: xberg_registry_extend_from_dir, _xberg_registry_extend_from_dir). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LAST_ERROR_CODE = LINKER.downcallHandle(
        LIB.find("xberg_last_error_code").orElse(null),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG)
    );

    static final MethodHandle XBERG_LAST_ERROR_CONTEXT = LINKER.downcallHandle(
        LIB.find("xberg_last_error_context").orElse(null),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CACHE_STATS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_cache_stats_from_json")
            .or(() -> LIB.find("_xberg_cache_stats_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_cache_stats_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_cache_stats_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CACHE_STATS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_cache_stats_free")
            .or(() -> LIB.find("_xberg_cache_stats_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_cache_stats_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_cache_stats_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ACCELERATION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_acceleration_config_from_json")
            .or(() -> LIB.find("_xberg_acceleration_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_acceleration_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_acceleration_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ACCELERATION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_acceleration_config_free")
            .or(() -> LIB.find("_xberg_acceleration_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_acceleration_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_acceleration_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CAPTIONING_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_captioning_config_from_json")
            .or(() -> LIB.find("_xberg_captioning_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_captioning_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_captioning_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CAPTIONING_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_captioning_config_free")
            .or(() -> LIB.find("_xberg_captioning_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_captioning_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_captioning_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNK_CLASSIFICATION_DEFINITION_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_chunk_classification_definition_from_json")
            .or(() -> LIB.find("_xberg_chunk_classification_definition_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunk_classification_definition_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunk_classification_definition_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNK_CLASSIFICATION_DEFINITION_FREE = LINKER.downcallHandle(
        LIB.find("xberg_chunk_classification_definition_free")
            .or(() -> LIB.find("_xberg_chunk_classification_definition_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunk_classification_definition_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunk_classification_definition_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNK_CLASSIFICATION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_chunk_classification_config_from_json")
            .or(() -> LIB.find("_xberg_chunk_classification_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunk_classification_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunk_classification_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNK_CLASSIFICATION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_chunk_classification_config_free")
            .or(() -> LIB.find("_xberg_chunk_classification_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunk_classification_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunk_classification_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_CLASSIFICATION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_page_classification_config_from_json")
            .or(() -> LIB.find("_xberg_page_classification_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_classification_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_classification_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_CLASSIFICATION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_page_classification_config_free")
            .or(() -> LIB.find("_xberg_page_classification_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_classification_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_classification_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CONTENT_FILTER_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_content_filter_config_from_json")
            .or(() -> LIB.find("_xberg_content_filter_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_content_filter_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_content_filter_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CONTENT_FILTER_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_content_filter_config_free")
            .or(() -> LIB.find("_xberg_content_filter_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_content_filter_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_content_filter_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CSV_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_csv_config_from_json")
            .or(() -> LIB.find("_xberg_csv_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_csv_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_csv_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CSV_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_csv_config_free")
            .or(() -> LIB.find("_xberg_csv_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_csv_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_csv_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMAIL_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_email_config_from_json")
            .or(() -> LIB.find("_xberg_email_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_email_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_email_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMAIL_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_email_config_free")
            .or(() -> LIB.find("_xberg_email_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_email_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_email_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_extraction_config_from_json")
            .or(() -> LIB.find("_xberg_extraction_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_extraction_config_free")
            .or(() -> LIB.find("_xberg_extraction_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FILE_EXTRACTION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_file_extraction_config_from_json")
            .or(() -> LIB.find("_xberg_file_extraction_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_file_extraction_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_file_extraction_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FILE_EXTRACTION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_file_extraction_config_free")
            .or(() -> LIB.find("_xberg_file_extraction_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_file_extraction_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_file_extraction_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SVG_OPTIONS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_svg_options_from_json")
            .or(() -> LIB.find("_xberg_svg_options_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_svg_options_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_svg_options_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SVG_OPTIONS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_svg_options_free")
            .or(() -> LIB.find("_xberg_svg_options_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_svg_options_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_svg_options_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACT_INPUT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_extract_input_from_json")
            .or(() -> LIB.find("_xberg_extract_input_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extract_input_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extract_input_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACT_INPUT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_extract_input_free")
            .or(() -> LIB.find("_xberg_extract_input_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extract_input_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extract_input_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_ERROR_ITEM_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_extraction_error_item_from_json")
            .or(() -> LIB.find("_xberg_extraction_error_item_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_error_item_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_error_item_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_ERROR_ITEM_FREE = LINKER.downcallHandle(
        LIB.find("xberg_extraction_error_item_free")
            .or(() -> LIB.find("_xberg_extraction_error_item_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_error_item_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_error_item_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_SUMMARY_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_extraction_summary_from_json")
            .or(() -> LIB.find("_xberg_extraction_summary_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_summary_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_summary_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_SUMMARY_FREE = LINKER.downcallHandle(
        LIB.find("xberg_extraction_summary_free")
            .or(() -> LIB.find("_xberg_extraction_summary_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_summary_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_summary_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_RESULT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_extraction_result_from_json")
            .or(() -> LIB.find("_xberg_extraction_result_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_result_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_result_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_extraction_result_free")
            .or(() -> LIB.find("_xberg_extraction_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_URL_EXTRACTION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_url_extraction_config_from_json")
            .or(() -> LIB.find("_xberg_url_extraction_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_url_extraction_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_url_extraction_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_URL_EXTRACTION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_url_extraction_config_free")
            .or(() -> LIB.find("_xberg_url_extraction_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_url_extraction_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_url_extraction_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_IMAGE_EXTRACTION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_image_extraction_config_from_json")
            .or(() -> LIB.find("_xberg_image_extraction_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_image_extraction_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_image_extraction_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_IMAGE_EXTRACTION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_image_extraction_config_free")
            .or(() -> LIB.find("_xberg_image_extraction_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_image_extraction_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_image_extraction_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TOKEN_REDUCTION_OPTIONS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_token_reduction_options_from_json")
            .or(() -> LIB.find("_xberg_token_reduction_options_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_token_reduction_options_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_token_reduction_options_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TOKEN_REDUCTION_OPTIONS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_token_reduction_options_free")
            .or(() -> LIB.find("_xberg_token_reduction_options_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_token_reduction_options_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_token_reduction_options_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LANGUAGE_DETECTION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_language_detection_config_from_json")
            .or(() -> LIB.find("_xberg_language_detection_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_language_detection_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_language_detection_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LANGUAGE_DETECTION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_language_detection_config_free")
            .or(() -> LIB.find("_xberg_language_detection_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_language_detection_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_language_detection_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HTML_OUTPUT_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_html_output_config_from_json")
            .or(() -> LIB.find("_xberg_html_output_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_html_output_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_html_output_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HTML_OUTPUT_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_html_output_config_free")
            .or(() -> LIB.find("_xberg_html_output_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_html_output_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_html_output_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LATE_INTERACTION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_late_interaction_config_from_json")
            .or(() -> LIB.find("_xberg_late_interaction_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_late_interaction_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_late_interaction_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LATE_INTERACTION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_late_interaction_config_free")
            .or(() -> LIB.find("_xberg_late_interaction_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_late_interaction_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_late_interaction_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LAYOUT_DETECTION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_layout_detection_config_from_json")
            .or(() -> LIB.find("_xberg_layout_detection_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_layout_detection_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_layout_detection_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LAYOUT_DETECTION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_layout_detection_config_free")
            .or(() -> LIB.find("_xberg_layout_detection_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_layout_detection_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_layout_detection_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LLM_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_llm_config_from_json")
            .or(() -> LIB.find("_xberg_llm_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LLM_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_llm_config_free")
            .or(() -> LIB.find("_xberg_llm_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LLM_PROVIDER_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_llm_provider_config_from_json")
            .or(() -> LIB.find("_xberg_llm_provider_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_provider_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_provider_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LLM_PROVIDER_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_llm_provider_config_free")
            .or(() -> LIB.find("_xberg_llm_provider_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_provider_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_provider_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LLM_CACHE_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_llm_cache_config_from_json")
            .or(() -> LIB.find("_xberg_llm_cache_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_cache_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_cache_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LLM_CACHE_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_llm_cache_config_free")
            .or(() -> LIB.find("_xberg_llm_cache_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_cache_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_cache_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LLM_BUDGET_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_llm_budget_config_from_json")
            .or(() -> LIB.find("_xberg_llm_budget_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_budget_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_budget_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LLM_BUDGET_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_llm_budget_config_free")
            .or(() -> LIB.find("_xberg_llm_budget_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_budget_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_budget_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LLM_RATE_LIMIT_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_llm_rate_limit_config_from_json")
            .or(() -> LIB.find("_xberg_llm_rate_limit_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_rate_limit_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_rate_limit_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LLM_RATE_LIMIT_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_llm_rate_limit_config_free")
            .or(() -> LIB.find("_xberg_llm_rate_limit_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_rate_limit_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_rate_limit_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_BEDROCK_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_bedrock_config_from_json")
            .or(() -> LIB.find("_xberg_bedrock_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_bedrock_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_bedrock_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_BEDROCK_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_bedrock_config_free")
            .or(() -> LIB.find("_xberg_bedrock_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_bedrock_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_bedrock_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_STRUCTURED_EXTRACTION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_structured_extraction_config_from_json")
            .or(() -> LIB.find("_xberg_structured_extraction_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_structured_extraction_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_structured_extraction_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_STRUCTURED_EXTRACTION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_structured_extraction_config_free")
            .or(() -> LIB.find("_xberg_structured_extraction_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_structured_extraction_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_structured_extraction_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_NER_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_ner_config_from_json")
            .or(() -> LIB.find("_xberg_ner_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ner_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ner_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_NER_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_ner_config_free")
            .or(() -> LIB.find("_xberg_ner_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ner_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ner_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_QUALITY_THRESHOLDS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_ocr_quality_thresholds_from_json")
            .or(() -> LIB.find("_xberg_ocr_quality_thresholds_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_quality_thresholds_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_quality_thresholds_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_QUALITY_THRESHOLDS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_ocr_quality_thresholds_free")
            .or(() -> LIB.find("_xberg_ocr_quality_thresholds_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_quality_thresholds_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_quality_thresholds_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_PIPELINE_STAGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_ocr_pipeline_stage_from_json")
            .or(() -> LIB.find("_xberg_ocr_pipeline_stage_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_pipeline_stage_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_pipeline_stage_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_PIPELINE_STAGE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_ocr_pipeline_stage_free")
            .or(() -> LIB.find("_xberg_ocr_pipeline_stage_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_pipeline_stage_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_pipeline_stage_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_PIPELINE_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_ocr_pipeline_config_from_json")
            .or(() -> LIB.find("_xberg_ocr_pipeline_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_pipeline_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_pipeline_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_PIPELINE_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_ocr_pipeline_config_free")
            .or(() -> LIB.find("_xberg_ocr_pipeline_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_pipeline_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_pipeline_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_ocr_config_from_json")
            .or(() -> LIB.find("_xberg_ocr_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_ocr_config_free")
            .or(() -> LIB.find("_xberg_ocr_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_page_config_from_json")
            .or(() -> LIB.find("_xberg_page_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_page_config_free")
            .or(() -> LIB.find("_xberg_page_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PDF_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_pdf_config_from_json")
            .or(() -> LIB.find("_xberg_pdf_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pdf_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pdf_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PDF_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_pdf_config_free")
            .or(() -> LIB.find("_xberg_pdf_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pdf_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pdf_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HIERARCHY_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_hierarchy_config_from_json")
            .or(() -> LIB.find("_xberg_hierarchy_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_hierarchy_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_hierarchy_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HIERARCHY_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_hierarchy_config_free")
            .or(() -> LIB.find("_xberg_hierarchy_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_hierarchy_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_hierarchy_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_POST_PROCESSOR_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_post_processor_config_from_json")
            .or(() -> LIB.find("_xberg_post_processor_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_post_processor_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_post_processor_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_POST_PROCESSOR_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_post_processor_config_free")
            .or(() -> LIB.find("_xberg_post_processor_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_post_processor_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_post_processor_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNKING_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_chunking_config_from_json")
            .or(() -> LIB.find("_xberg_chunking_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunking_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunking_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNKING_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_chunking_config_free")
            .or(() -> LIB.find("_xberg_chunking_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunking_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunking_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMBEDDING_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_embedding_config_from_json")
            .or(() -> LIB.find("_xberg_embedding_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_embedding_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_embedding_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMBEDDING_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_embedding_config_free")
            .or(() -> LIB.find("_xberg_embedding_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_embedding_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_embedding_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REDACTION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_redaction_config_from_json")
            .or(() -> LIB.find("_xberg_redaction_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_redaction_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_redaction_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REDACTION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_redaction_config_free")
            .or(() -> LIB.find("_xberg_redaction_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_redaction_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_redaction_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REDACTION_TERM_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_redaction_term_from_json")
            .or(() -> LIB.find("_xberg_redaction_term_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_redaction_term_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_redaction_term_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REDACTION_TERM_FREE = LINKER.downcallHandle(
        LIB.find("xberg_redaction_term_free")
            .or(() -> LIB.find("_xberg_redaction_term_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_redaction_term_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_redaction_term_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REDACTION_PATTERN_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_redaction_pattern_from_json")
            .or(() -> LIB.find("_xberg_redaction_pattern_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_redaction_pattern_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_redaction_pattern_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REDACTION_PATTERN_FREE = LINKER.downcallHandle(
        LIB.find("xberg_redaction_pattern_free")
            .or(() -> LIB.find("_xberg_redaction_pattern_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_redaction_pattern_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_redaction_pattern_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_RERANKER_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_reranker_config_from_json")
            .or(() -> LIB.find("_xberg_reranker_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_reranker_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_reranker_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_RERANKER_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_reranker_config_free")
            .or(() -> LIB.find("_xberg_reranker_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_reranker_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_reranker_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SPARSE_EMBEDDING_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_sparse_embedding_config_from_json")
            .or(() -> LIB.find("_xberg_sparse_embedding_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_sparse_embedding_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_sparse_embedding_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SPARSE_EMBEDDING_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_sparse_embedding_config_free")
            .or(() -> LIB.find("_xberg_sparse_embedding_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_sparse_embedding_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_sparse_embedding_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SUMMARIZATION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_summarization_config_from_json")
            .or(() -> LIB.find("_xberg_summarization_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_summarization_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_summarization_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SUMMARIZATION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_summarization_config_free")
            .or(() -> LIB.find("_xberg_summarization_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_summarization_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_summarization_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TRANSCRIPTION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_transcription_config_from_json")
            .or(() -> LIB.find("_xberg_transcription_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_transcription_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_transcription_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TRANSCRIPTION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_transcription_config_free")
            .or(() -> LIB.find("_xberg_transcription_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_transcription_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_transcription_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TRANSLATION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_translation_config_from_json")
            .or(() -> LIB.find("_xberg_translation_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_translation_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_translation_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TRANSLATION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_translation_config_free")
            .or(() -> LIB.find("_xberg_translation_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_translation_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_translation_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TREE_SITTER_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_tree_sitter_config_from_json")
            .or(() -> LIB.find("_xberg_tree_sitter_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_tree_sitter_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_tree_sitter_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TREE_SITTER_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_tree_sitter_config_free")
            .or(() -> LIB.find("_xberg_tree_sitter_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_tree_sitter_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_tree_sitter_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TREE_SITTER_PROCESS_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_tree_sitter_process_config_from_json")
            .or(() -> LIB.find("_xberg_tree_sitter_process_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_tree_sitter_process_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_tree_sitter_process_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TREE_SITTER_PROCESS_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_tree_sitter_process_config_free")
            .or(() -> LIB.find("_xberg_tree_sitter_process_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_tree_sitter_process_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_tree_sitter_process_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SUPPORTED_FORMAT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_supported_format_from_json")
            .or(() -> LIB.find("_xberg_supported_format_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_supported_format_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_supported_format_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SUPPORTED_FORMAT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_supported_format_free")
            .or(() -> LIB.find("_xberg_supported_format_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_supported_format_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_supported_format_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SERVER_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_server_config_from_json")
            .or(() -> LIB.find("_xberg_server_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_server_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_server_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SERVER_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_server_config_free")
            .or(() -> LIB.find("_xberg_server_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_server_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_server_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_STRUCTURED_DATA_RESULT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_structured_data_result_from_json")
            .or(() -> LIB.find("_xberg_structured_data_result_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_structured_data_result_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_structured_data_result_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_STRUCTURED_DATA_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_structured_data_result_free")
            .or(() -> LIB.find("_xberg_structured_data_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_structured_data_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_structured_data_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCX_APP_PROPERTIES_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_docx_app_properties_from_json")
            .or(() -> LIB.find("_xberg_docx_app_properties_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_docx_app_properties_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_docx_app_properties_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCX_APP_PROPERTIES_FREE = LINKER.downcallHandle(
        LIB.find("xberg_docx_app_properties_free")
            .or(() -> LIB.find("_xberg_docx_app_properties_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_docx_app_properties_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_docx_app_properties_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_XLSX_APP_PROPERTIES_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_xlsx_app_properties_from_json")
            .or(() -> LIB.find("_xberg_xlsx_app_properties_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_xlsx_app_properties_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_xlsx_app_properties_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_XLSX_APP_PROPERTIES_FREE = LINKER.downcallHandle(
        LIB.find("xberg_xlsx_app_properties_free")
            .or(() -> LIB.find("_xberg_xlsx_app_properties_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_xlsx_app_properties_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_xlsx_app_properties_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PPTX_APP_PROPERTIES_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_pptx_app_properties_from_json")
            .or(() -> LIB.find("_xberg_pptx_app_properties_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pptx_app_properties_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pptx_app_properties_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PPTX_APP_PROPERTIES_FREE = LINKER.downcallHandle(
        LIB.find("xberg_pptx_app_properties_free")
            .or(() -> LIB.find("_xberg_pptx_app_properties_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pptx_app_properties_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pptx_app_properties_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CORE_PROPERTIES_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_core_properties_from_json")
            .or(() -> LIB.find("_xberg_core_properties_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_core_properties_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_core_properties_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CORE_PROPERTIES_FREE = LINKER.downcallHandle(
        LIB.find("xberg_core_properties_free")
            .or(() -> LIB.find("_xberg_core_properties_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_core_properties_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_core_properties_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SECURITY_LIMITS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_security_limits_from_json")
            .or(() -> LIB.find("_xberg_security_limits_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_security_limits_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_security_limits_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SECURITY_LIMITS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_security_limits_free")
            .or(() -> LIB.find("_xberg_security_limits_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_security_limits_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_security_limits_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TOKEN_REDUCTION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_token_reduction_config_from_json")
            .or(() -> LIB.find("_xberg_token_reduction_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_token_reduction_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_token_reduction_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TOKEN_REDUCTION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_token_reduction_config_free")
            .or(() -> LIB.find("_xberg_token_reduction_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_token_reduction_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_token_reduction_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PATTERN_MATCH_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_pattern_match_from_json")
            .or(() -> LIB.find("_xberg_pattern_match_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pattern_match_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pattern_match_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PATTERN_MATCH_FREE = LINKER.downcallHandle(
        LIB.find("xberg_pattern_match_free")
            .or(() -> LIB.find("_xberg_pattern_match_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pattern_match_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pattern_match_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TOKEN_COUNTER_FREE = LINKER.downcallHandle(
        LIB.find("xberg_token_counter_free")
            .or(() -> LIB.find("_xberg_token_counter_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_token_counter_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_token_counter_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FOOTNOTE_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_footnote_config_from_json")
            .or(() -> LIB.find("_xberg_footnote_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_footnote_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_footnote_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FOOTNOTE_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_footnote_config_free")
            .or(() -> LIB.find("_xberg_footnote_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_footnote_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_footnote_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FOOTNOTE_ANCHOR_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_footnote_anchor_from_json")
            .or(() -> LIB.find("_xberg_footnote_anchor_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_footnote_anchor_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_footnote_anchor_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FOOTNOTE_ANCHOR_FREE = LINKER.downcallHandle(
        LIB.find("xberg_footnote_anchor_free")
            .or(() -> LIB.find("_xberg_footnote_anchor_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_footnote_anchor_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_footnote_anchor_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FOOTNOTE_DEFINITION_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_footnote_definition_from_json")
            .or(() -> LIB.find("_xberg_footnote_definition_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_footnote_definition_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_footnote_definition_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FOOTNOTE_DEFINITION_FREE = LINKER.downcallHandle(
        LIB.find("xberg_footnote_definition_free")
            .or(() -> LIB.find("_xberg_footnote_definition_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_footnote_definition_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_footnote_definition_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CITATION_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_citation_from_json")
            .or(() -> LIB.find("_xberg_citation_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_citation_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_citation_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CITATION_FREE = LINKER.downcallHandle(
        LIB.find("xberg_citation_free")
            .or(() -> LIB.find("_xberg_citation_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_citation_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_citation_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PDF_ANNOTATION_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_pdf_annotation_from_json")
            .or(() -> LIB.find("_xberg_pdf_annotation_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pdf_annotation_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pdf_annotation_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PDF_ANNOTATION_FREE = LINKER.downcallHandle(
        LIB.find("xberg_pdf_annotation_free")
            .or(() -> LIB.find("_xberg_pdf_annotation_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pdf_annotation_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pdf_annotation_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_CLASSIFICATION_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_page_classification_from_json")
            .or(() -> LIB.find("_xberg_page_classification_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_classification_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_classification_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_CLASSIFICATION_FREE = LINKER.downcallHandle(
        LIB.find("xberg_page_classification_free")
            .or(() -> LIB.find("_xberg_page_classification_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_classification_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_classification_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CLASSIFICATION_LABEL_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_classification_label_from_json")
            .or(() -> LIB.find("_xberg_classification_label_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_classification_label_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_classification_label_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CLASSIFICATION_LABEL_FREE = LINKER.downcallHandle(
        LIB.find("xberg_classification_label_free")
            .or(() -> LIB.find("_xberg_classification_label_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_classification_label_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_classification_label_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DJOT_CONTENT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_djot_content_from_json")
            .or(() -> LIB.find("_xberg_djot_content_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_djot_content_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_djot_content_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DJOT_CONTENT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_djot_content_free")
            .or(() -> LIB.find("_xberg_djot_content_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_djot_content_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_djot_content_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FORMATTED_BLOCK_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_formatted_block_from_json")
            .or(() -> LIB.find("_xberg_formatted_block_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_formatted_block_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_formatted_block_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FORMATTED_BLOCK_FREE = LINKER.downcallHandle(
        LIB.find("xberg_formatted_block_free")
            .or(() -> LIB.find("_xberg_formatted_block_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_formatted_block_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_formatted_block_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_INLINE_ELEMENT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_inline_element_from_json")
            .or(() -> LIB.find("_xberg_inline_element_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_inline_element_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_inline_element_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_INLINE_ELEMENT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_inline_element_free")
            .or(() -> LIB.find("_xberg_inline_element_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_inline_element_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_inline_element_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DJOT_IMAGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_djot_image_from_json")
            .or(() -> LIB.find("_xberg_djot_image_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_djot_image_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_djot_image_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DJOT_IMAGE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_djot_image_free")
            .or(() -> LIB.find("_xberg_djot_image_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_djot_image_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_djot_image_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DJOT_LINK_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_djot_link_from_json")
            .or(() -> LIB.find("_xberg_djot_link_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_djot_link_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_djot_link_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DJOT_LINK_FREE = LINKER.downcallHandle(
        LIB.find("xberg_djot_link_free")
            .or(() -> LIB.find("_xberg_djot_link_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_djot_link_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_djot_link_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FOOTNOTE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_footnote_from_json")
            .or(() -> LIB.find("_xberg_footnote_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_footnote_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_footnote_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FOOTNOTE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_footnote_free")
            .or(() -> LIB.find("_xberg_footnote_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_footnote_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_footnote_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_STRUCTURE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_document_structure_from_json")
            .or(() -> LIB.find("_xberg_document_structure_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_structure_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_structure_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_STRUCTURE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_document_structure_free")
            .or(() -> LIB.find("_xberg_document_structure_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_structure_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_structure_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_RELATIONSHIP_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_document_relationship_from_json")
            .or(() -> LIB.find("_xberg_document_relationship_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_relationship_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_relationship_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_RELATIONSHIP_FREE = LINKER.downcallHandle(
        LIB.find("xberg_document_relationship_free")
            .or(() -> LIB.find("_xberg_document_relationship_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_relationship_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_relationship_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_NODE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_document_node_from_json")
            .or(() -> LIB.find("_xberg_document_node_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_node_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_node_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_NODE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_document_node_free")
            .or(() -> LIB.find("_xberg_document_node_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_node_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_node_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TABLE_GRID_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_table_grid_from_json")
            .or(() -> LIB.find("_xberg_table_grid_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_table_grid_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_table_grid_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TABLE_GRID_FREE = LINKER.downcallHandle(
        LIB.find("xberg_table_grid_free")
            .or(() -> LIB.find("_xberg_table_grid_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_table_grid_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_table_grid_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_GRID_CELL_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_grid_cell_from_json")
            .or(() -> LIB.find("_xberg_grid_cell_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_grid_cell_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_grid_cell_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_GRID_CELL_FREE = LINKER.downcallHandle(
        LIB.find("xberg_grid_cell_free")
            .or(() -> LIB.find("_xberg_grid_cell_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_grid_cell_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_grid_cell_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TEXT_ANNOTATION_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_text_annotation_from_json")
            .or(() -> LIB.find("_xberg_text_annotation_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_text_annotation_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_text_annotation_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TEXT_ANNOTATION_FREE = LINKER.downcallHandle(
        LIB.find("xberg_text_annotation_free")
            .or(() -> LIB.find("_xberg_text_annotation_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_text_annotation_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_text_annotation_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ENTITY_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_entity_from_json")
            .or(() -> LIB.find("_xberg_entity_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_entity_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_entity_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ENTITY_FREE = LINKER.downcallHandle(
        LIB.find("xberg_entity_free")
            .or(() -> LIB.find("_xberg_entity_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_entity_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_entity_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_COUNTS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_document_counts_from_json")
            .or(() -> LIB.find("_xberg_document_counts_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_counts_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_counts_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_COUNTS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_document_counts_free")
            .or(() -> LIB.find("_xberg_document_counts_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_counts_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_counts_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LANGUAGE_CONFIDENCE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_language_confidence_from_json")
            .or(() -> LIB.find("_xberg_language_confidence_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_language_confidence_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_language_confidence_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LANGUAGE_CONFIDENCE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_language_confidence_free")
            .or(() -> LIB.find("_xberg_language_confidence_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_language_confidence_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_language_confidence_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTED_DOCUMENT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_extracted_document_from_json")
            .or(() -> LIB.find("_xberg_extracted_document_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extracted_document_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extracted_document_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTED_DOCUMENT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_extracted_document_free")
            .or(() -> LIB.find("_xberg_extracted_document_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extracted_document_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extracted_document_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ARCHIVE_ENTRY_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_archive_entry_from_json")
            .or(() -> LIB.find("_xberg_archive_entry_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_archive_entry_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_archive_entry_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ARCHIVE_ENTRY_FREE = LINKER.downcallHandle(
        LIB.find("xberg_archive_entry_free")
            .or(() -> LIB.find("_xberg_archive_entry_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_archive_entry_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_archive_entry_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PROCESSING_WARNING_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_processing_warning_from_json")
            .or(() -> LIB.find("_xberg_processing_warning_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_processing_warning_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_processing_warning_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PROCESSING_WARNING_FREE = LINKER.downcallHandle(
        LIB.find("xberg_processing_warning_free")
            .or(() -> LIB.find("_xberg_processing_warning_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_processing_warning_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_processing_warning_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LLM_USAGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_llm_usage_from_json")
            .or(() -> LIB.find("_xberg_llm_usage_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_usage_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_usage_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LLM_USAGE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_llm_usage_free")
            .or(() -> LIB.find("_xberg_llm_usage_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_usage_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_usage_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNK_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_chunk_from_json")
            .or(() -> LIB.find("_xberg_chunk_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunk_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunk_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNK_FREE = LINKER.downcallHandle(
        LIB.find("xberg_chunk_free")
            .or(() -> LIB.find("_xberg_chunk_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunk_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunk_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HEADING_CONTEXT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_heading_context_from_json")
            .or(() -> LIB.find("_xberg_heading_context_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_heading_context_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_heading_context_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HEADING_CONTEXT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_heading_context_free")
            .or(() -> LIB.find("_xberg_heading_context_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_heading_context_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_heading_context_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HEADING_LEVEL_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_heading_level_from_json")
            .or(() -> LIB.find("_xberg_heading_level_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_heading_level_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_heading_level_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HEADING_LEVEL_FREE = LINKER.downcallHandle(
        LIB.find("xberg_heading_level_free")
            .or(() -> LIB.find("_xberg_heading_level_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_heading_level_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_heading_level_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNK_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_chunk_metadata_from_json")
            .or(() -> LIB.find("_xberg_chunk_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunk_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunk_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNK_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_chunk_metadata_free")
            .or(() -> LIB.find("_xberg_chunk_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunk_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunk_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_SPAN_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_page_span_from_json")
            .or(() -> LIB.find("_xberg_page_span_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_span_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_span_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_SPAN_FREE = LINKER.downcallHandle(
        LIB.find("xberg_page_span_free")
            .or(() -> LIB.find("_xberg_page_span_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_span_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_span_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTED_IMAGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_extracted_image_from_json")
            .or(() -> LIB.find("_xberg_extracted_image_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extracted_image_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extracted_image_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTED_IMAGE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_extracted_image_free")
            .or(() -> LIB.find("_xberg_extracted_image_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extracted_image_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extracted_image_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_BOUNDING_BOX_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_bounding_box_from_json")
            .or(() -> LIB.find("_xberg_bounding_box_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_bounding_box_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_bounding_box_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_BOUNDING_BOX_FREE = LINKER.downcallHandle(
        LIB.find("xberg_bounding_box_free")
            .or(() -> LIB.find("_xberg_bounding_box_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_bounding_box_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_bounding_box_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ELEMENT_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_element_metadata_from_json")
            .or(() -> LIB.find("_xberg_element_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_element_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_element_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ELEMENT_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_element_metadata_free")
            .or(() -> LIB.find("_xberg_element_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_element_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_element_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ELEMENT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_element_from_json")
            .or(() -> LIB.find("_xberg_element_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_element_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_element_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ELEMENT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_element_free")
            .or(() -> LIB.find("_xberg_element_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_element_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_element_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PDF_FORM_FIELD_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_pdf_form_field_from_json")
            .or(() -> LIB.find("_xberg_pdf_form_field_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pdf_form_field_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pdf_form_field_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PDF_FORM_FIELD_FREE = LINKER.downcallHandle(
        LIB.find("xberg_pdf_form_field_free")
            .or(() -> LIB.find("_xberg_pdf_form_field_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pdf_form_field_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pdf_form_field_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXCEL_WORKBOOK_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_excel_workbook_from_json")
            .or(() -> LIB.find("_xberg_excel_workbook_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_excel_workbook_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_excel_workbook_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXCEL_WORKBOOK_FREE = LINKER.downcallHandle(
        LIB.find("xberg_excel_workbook_free")
            .or(() -> LIB.find("_xberg_excel_workbook_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_excel_workbook_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_excel_workbook_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXCEL_SHEET_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_excel_sheet_from_json")
            .or(() -> LIB.find("_xberg_excel_sheet_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_excel_sheet_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_excel_sheet_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXCEL_SHEET_FREE = LINKER.downcallHandle(
        LIB.find("xberg_excel_sheet_free")
            .or(() -> LIB.find("_xberg_excel_sheet_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_excel_sheet_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_excel_sheet_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_XML_EXTRACTION_RESULT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_xml_extraction_result_from_json")
            .or(() -> LIB.find("_xberg_xml_extraction_result_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_xml_extraction_result_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_xml_extraction_result_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_XML_EXTRACTION_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_xml_extraction_result_free")
            .or(() -> LIB.find("_xberg_xml_extraction_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_xml_extraction_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_xml_extraction_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TEXT_EXTRACTION_RESULT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_text_extraction_result_from_json")
            .or(() -> LIB.find("_xberg_text_extraction_result_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_text_extraction_result_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_text_extraction_result_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TEXT_EXTRACTION_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_text_extraction_result_free")
            .or(() -> LIB.find("_xberg_text_extraction_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_text_extraction_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_text_extraction_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PPTX_EXTRACTION_RESULT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_pptx_extraction_result_from_json")
            .or(() -> LIB.find("_xberg_pptx_extraction_result_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pptx_extraction_result_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pptx_extraction_result_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PPTX_EXTRACTION_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_pptx_extraction_result_free")
            .or(() -> LIB.find("_xberg_pptx_extraction_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pptx_extraction_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pptx_extraction_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMAIL_EXTRACTION_RESULT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_email_extraction_result_from_json")
            .or(() -> LIB.find("_xberg_email_extraction_result_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_email_extraction_result_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_email_extraction_result_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMAIL_EXTRACTION_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_email_extraction_result_free")
            .or(() -> LIB.find("_xberg_email_extraction_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_email_extraction_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_email_extraction_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMAIL_ATTACHMENT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_email_attachment_from_json")
            .or(() -> LIB.find("_xberg_email_attachment_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_email_attachment_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_email_attachment_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMAIL_ATTACHMENT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_email_attachment_free")
            .or(() -> LIB.find("_xberg_email_attachment_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_email_attachment_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_email_attachment_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_EXTRACTION_RESULT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_ocr_extraction_result_from_json")
            .or(() -> LIB.find("_xberg_ocr_extraction_result_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_extraction_result_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_extraction_result_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_EXTRACTION_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_ocr_extraction_result_free")
            .or(() -> LIB.find("_xberg_ocr_extraction_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_extraction_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_extraction_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_TABLE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_ocr_table_from_json")
            .or(() -> LIB.find("_xberg_ocr_table_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_table_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_table_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_TABLE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_ocr_table_free")
            .or(() -> LIB.find("_xberg_ocr_table_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_table_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_table_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_TABLE_BOUNDING_BOX_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_ocr_table_bounding_box_from_json")
            .or(() -> LIB.find("_xberg_ocr_table_bounding_box_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_table_bounding_box_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_table_bounding_box_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_TABLE_BOUNDING_BOX_FREE = LINKER.downcallHandle(
        LIB.find("xberg_ocr_table_bounding_box_free")
            .or(() -> LIB.find("_xberg_ocr_table_bounding_box_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_table_bounding_box_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_table_bounding_box_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_IMAGE_PREPROCESSING_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_image_preprocessing_config_from_json")
            .or(() -> LIB.find("_xberg_image_preprocessing_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_image_preprocessing_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_image_preprocessing_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_IMAGE_PREPROCESSING_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_image_preprocessing_config_free")
            .or(() -> LIB.find("_xberg_image_preprocessing_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_image_preprocessing_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_image_preprocessing_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TESSERACT_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_tesseract_config_from_json")
            .or(() -> LIB.find("_xberg_tesseract_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_tesseract_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_tesseract_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TESSERACT_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_tesseract_config_free")
            .or(() -> LIB.find("_xberg_tesseract_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_tesseract_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_tesseract_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_IMAGE_PREPROCESSING_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_image_preprocessing_metadata_from_json")
            .or(() -> LIB.find("_xberg_image_preprocessing_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_image_preprocessing_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_image_preprocessing_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_IMAGE_PREPROCESSING_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_image_preprocessing_metadata_free")
            .or(() -> LIB.find("_xberg_image_preprocessing_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_image_preprocessing_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_image_preprocessing_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FORMULA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_formula_from_json")
            .or(() -> LIB.find("_xberg_formula_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_formula_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_formula_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FORMULA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_formula_free")
            .or(() -> LIB.find("_xberg_formula_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_formula_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_formula_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CODE_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_code_metadata_from_json")
            .or(() -> LIB.find("_xberg_code_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_code_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_code_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CODE_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_code_metadata_free")
            .or(() -> LIB.find("_xberg_code_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_code_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_code_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CODE_CHUNK_INFO_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_code_chunk_info_from_json")
            .or(() -> LIB.find("_xberg_code_chunk_info_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_code_chunk_info_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_code_chunk_info_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CODE_CHUNK_INFO_FREE = LINKER.downcallHandle(
        LIB.find("xberg_code_chunk_info_free")
            .or(() -> LIB.find("_xberg_code_chunk_info_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_code_chunk_info_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_code_chunk_info_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CODE_DATA_ATTRIBUTE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_code_data_attribute_from_json")
            .or(() -> LIB.find("_xberg_code_data_attribute_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_code_data_attribute_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_code_data_attribute_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CODE_DATA_ATTRIBUTE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_code_data_attribute_free")
            .or(() -> LIB.find("_xberg_code_data_attribute_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_code_data_attribute_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_code_data_attribute_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CODE_DATA_NODE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_code_data_node_from_json")
            .or(() -> LIB.find("_xberg_code_data_node_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_code_data_node_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_code_data_node_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CODE_DATA_NODE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_code_data_node_free")
            .or(() -> LIB.find("_xberg_code_data_node_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_code_data_node_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_code_data_node_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_metadata_from_json")
            .or(() -> LIB.find("_xberg_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_metadata_free")
            .or(() -> LIB.find("_xberg_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXCEL_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_excel_metadata_from_json")
            .or(() -> LIB.find("_xberg_excel_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_excel_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_excel_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXCEL_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_excel_metadata_free")
            .or(() -> LIB.find("_xberg_excel_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_excel_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_excel_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMAIL_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_email_metadata_from_json")
            .or(() -> LIB.find("_xberg_email_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_email_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_email_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMAIL_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_email_metadata_free")
            .or(() -> LIB.find("_xberg_email_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_email_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_email_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ARCHIVE_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_archive_metadata_from_json")
            .or(() -> LIB.find("_xberg_archive_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_archive_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_archive_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ARCHIVE_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_archive_metadata_free")
            .or(() -> LIB.find("_xberg_archive_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_archive_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_archive_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_IMAGE_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_image_metadata_from_json")
            .or(() -> LIB.find("_xberg_image_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_image_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_image_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_IMAGE_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_image_metadata_free")
            .or(() -> LIB.find("_xberg_image_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_image_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_image_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_XML_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_xml_metadata_from_json")
            .or(() -> LIB.find("_xberg_xml_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_xml_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_xml_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_XML_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_xml_metadata_free")
            .or(() -> LIB.find("_xberg_xml_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_xml_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_xml_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TEXT_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_text_metadata_from_json")
            .or(() -> LIB.find("_xberg_text_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_text_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_text_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TEXT_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_text_metadata_free")
            .or(() -> LIB.find("_xberg_text_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_text_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_text_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HEADER_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_header_metadata_from_json")
            .or(() -> LIB.find("_xberg_header_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_header_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_header_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HEADER_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_header_metadata_free")
            .or(() -> LIB.find("_xberg_header_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_header_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_header_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LINK_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_link_metadata_from_json")
            .or(() -> LIB.find("_xberg_link_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_link_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_link_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LINK_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_link_metadata_free")
            .or(() -> LIB.find("_xberg_link_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_link_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_link_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_IMAGE_METADATA_TYPE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_image_metadata_type_from_json")
            .or(() -> LIB.find("_xberg_image_metadata_type_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_image_metadata_type_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_image_metadata_type_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_IMAGE_METADATA_TYPE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_image_metadata_type_free")
            .or(() -> LIB.find("_xberg_image_metadata_type_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_image_metadata_type_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_image_metadata_type_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_STRUCTURED_DATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_structured_data_from_json")
            .or(() -> LIB.find("_xberg_structured_data_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_structured_data_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_structured_data_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_STRUCTURED_DATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_structured_data_free")
            .or(() -> LIB.find("_xberg_structured_data_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_structured_data_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_structured_data_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HTML_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_html_metadata_from_json")
            .or(() -> LIB.find("_xberg_html_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_html_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_html_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HTML_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_html_metadata_free")
            .or(() -> LIB.find("_xberg_html_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_html_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_html_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_ocr_metadata_from_json")
            .or(() -> LIB.find("_xberg_ocr_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_ocr_metadata_free")
            .or(() -> LIB.find("_xberg_ocr_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ERROR_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_error_metadata_from_json")
            .or(() -> LIB.find("_xberg_error_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_error_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_error_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ERROR_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_error_metadata_free")
            .or(() -> LIB.find("_xberg_error_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_error_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_error_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PPTX_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_pptx_metadata_from_json")
            .or(() -> LIB.find("_xberg_pptx_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pptx_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pptx_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PPTX_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_pptx_metadata_free")
            .or(() -> LIB.find("_xberg_pptx_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pptx_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pptx_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCX_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_docx_metadata_from_json")
            .or(() -> LIB.find("_xberg_docx_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_docx_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_docx_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCX_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_docx_metadata_free")
            .or(() -> LIB.find("_xberg_docx_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_docx_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_docx_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CSV_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_csv_metadata_from_json")
            .or(() -> LIB.find("_xberg_csv_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_csv_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_csv_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CSV_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_csv_metadata_free")
            .or(() -> LIB.find("_xberg_csv_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_csv_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_csv_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_BIBTEX_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_bibtex_metadata_from_json")
            .or(() -> LIB.find("_xberg_bibtex_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_bibtex_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_bibtex_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_BIBTEX_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_bibtex_metadata_free")
            .or(() -> LIB.find("_xberg_bibtex_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_bibtex_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_bibtex_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CITATION_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_citation_metadata_from_json")
            .or(() -> LIB.find("_xberg_citation_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_citation_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_citation_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CITATION_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_citation_metadata_free")
            .or(() -> LIB.find("_xberg_citation_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_citation_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_citation_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_YEAR_RANGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_year_range_from_json")
            .or(() -> LIB.find("_xberg_year_range_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_year_range_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_year_range_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_YEAR_RANGE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_year_range_free")
            .or(() -> LIB.find("_xberg_year_range_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_year_range_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_year_range_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FICTION_BOOK_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_fiction_book_metadata_from_json")
            .or(() -> LIB.find("_xberg_fiction_book_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_fiction_book_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_fiction_book_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FICTION_BOOK_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_fiction_book_metadata_free")
            .or(() -> LIB.find("_xberg_fiction_book_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_fiction_book_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_fiction_book_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DBF_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_dbf_metadata_from_json")
            .or(() -> LIB.find("_xberg_dbf_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_dbf_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_dbf_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DBF_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_dbf_metadata_free")
            .or(() -> LIB.find("_xberg_dbf_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_dbf_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_dbf_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DBF_FIELD_INFO_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_dbf_field_info_from_json")
            .or(() -> LIB.find("_xberg_dbf_field_info_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_dbf_field_info_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_dbf_field_info_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DBF_FIELD_INFO_FREE = LINKER.downcallHandle(
        LIB.find("xberg_dbf_field_info_free")
            .or(() -> LIB.find("_xberg_dbf_field_info_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_dbf_field_info_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_dbf_field_info_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_JATS_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_jats_metadata_from_json")
            .or(() -> LIB.find("_xberg_jats_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_jats_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_jats_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_JATS_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_jats_metadata_free")
            .or(() -> LIB.find("_xberg_jats_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_jats_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_jats_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CONTRIBUTOR_ROLE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_contributor_role_from_json")
            .or(() -> LIB.find("_xberg_contributor_role_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_contributor_role_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_contributor_role_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CONTRIBUTOR_ROLE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_contributor_role_free")
            .or(() -> LIB.find("_xberg_contributor_role_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_contributor_role_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_contributor_role_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EPUB_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_epub_metadata_from_json")
            .or(() -> LIB.find("_xberg_epub_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_epub_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_epub_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EPUB_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_epub_metadata_free")
            .or(() -> LIB.find("_xberg_epub_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_epub_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_epub_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PST_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_pst_metadata_from_json")
            .or(() -> LIB.find("_xberg_pst_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pst_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pst_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PST_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_pst_metadata_free")
            .or(() -> LIB.find("_xberg_pst_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pst_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pst_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_AUDIO_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_audio_metadata_from_json")
            .or(() -> LIB.find("_xberg_audio_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_audio_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_audio_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_AUDIO_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_audio_metadata_free")
            .or(() -> LIB.find("_xberg_audio_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_audio_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_audio_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_CONFIDENCE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_ocr_confidence_from_json")
            .or(() -> LIB.find("_xberg_ocr_confidence_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_confidence_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_confidence_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_CONFIDENCE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_ocr_confidence_free")
            .or(() -> LIB.find("_xberg_ocr_confidence_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_confidence_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_confidence_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_ROTATION_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_ocr_rotation_from_json")
            .or(() -> LIB.find("_xberg_ocr_rotation_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_rotation_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_rotation_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_ROTATION_FREE = LINKER.downcallHandle(
        LIB.find("xberg_ocr_rotation_free")
            .or(() -> LIB.find("_xberg_ocr_rotation_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_rotation_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_rotation_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_ELEMENT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_ocr_element_from_json")
            .or(() -> LIB.find("_xberg_ocr_element_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_element_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_element_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_ELEMENT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_ocr_element_free")
            .or(() -> LIB.find("_xberg_ocr_element_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_element_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_element_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_ELEMENT_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_ocr_element_config_from_json")
            .or(() -> LIB.find("_xberg_ocr_element_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_element_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_element_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_OCR_ELEMENT_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_ocr_element_config_free")
            .or(() -> LIB.find("_xberg_ocr_element_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ocr_element_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ocr_element_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_STRUCTURE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_page_structure_from_json")
            .or(() -> LIB.find("_xberg_page_structure_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_structure_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_structure_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_STRUCTURE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_page_structure_free")
            .or(() -> LIB.find("_xberg_page_structure_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_structure_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_structure_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_BOUNDARY_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_page_boundary_from_json")
            .or(() -> LIB.find("_xberg_page_boundary_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_boundary_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_boundary_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_BOUNDARY_FREE = LINKER.downcallHandle(
        LIB.find("xberg_page_boundary_free")
            .or(() -> LIB.find("_xberg_page_boundary_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_boundary_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_boundary_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_INFO_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_page_info_from_json")
            .or(() -> LIB.find("_xberg_page_info_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_info_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_info_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_INFO_FREE = LINKER.downcallHandle(
        LIB.find("xberg_page_info_free")
            .or(() -> LIB.find("_xberg_page_info_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_info_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_info_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_CONTENT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_page_content_from_json")
            .or(() -> LIB.find("_xberg_page_content_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_content_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_content_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_CONTENT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_page_content_free")
            .or(() -> LIB.find("_xberg_page_content_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_content_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_content_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LAYOUT_REGION_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_layout_region_from_json")
            .or(() -> LIB.find("_xberg_layout_region_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_layout_region_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_layout_region_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LAYOUT_REGION_FREE = LINKER.downcallHandle(
        LIB.find("xberg_layout_region_free")
            .or(() -> LIB.find("_xberg_layout_region_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_layout_region_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_layout_region_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_HIERARCHY_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_page_hierarchy_from_json")
            .or(() -> LIB.find("_xberg_page_hierarchy_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_hierarchy_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_hierarchy_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_HIERARCHY_FREE = LINKER.downcallHandle(
        LIB.find("xberg_page_hierarchy_free")
            .or(() -> LIB.find("_xberg_page_hierarchy_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_hierarchy_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_hierarchy_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HIERARCHICAL_BLOCK_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_hierarchical_block_from_json")
            .or(() -> LIB.find("_xberg_hierarchical_block_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_hierarchical_block_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_hierarchical_block_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HIERARCHICAL_BLOCK_FREE = LINKER.downcallHandle(
        LIB.find("xberg_hierarchical_block_free")
            .or(() -> LIB.find("_xberg_hierarchical_block_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_hierarchical_block_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_hierarchical_block_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_QR_CODE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_qr_code_from_json")
            .or(() -> LIB.find("_xberg_qr_code_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_qr_code_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_qr_code_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_QR_CODE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_qr_code_free")
            .or(() -> LIB.find("_xberg_qr_code_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_qr_code_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_qr_code_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_QR_BOUNDING_BOX_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_qr_bounding_box_from_json")
            .or(() -> LIB.find("_xberg_qr_bounding_box_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_qr_bounding_box_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_qr_bounding_box_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_QR_BOUNDING_BOX_FREE = LINKER.downcallHandle(
        LIB.find("xberg_qr_bounding_box_free")
            .or(() -> LIB.find("_xberg_qr_bounding_box_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_qr_bounding_box_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_qr_bounding_box_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REDACTION_REPORT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_redaction_report_from_json")
            .or(() -> LIB.find("_xberg_redaction_report_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_redaction_report_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_redaction_report_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REDACTION_REPORT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_redaction_report_free")
            .or(() -> LIB.find("_xberg_redaction_report_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_redaction_report_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_redaction_report_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REDACTION_FINDING_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_redaction_finding_from_json")
            .or(() -> LIB.find("_xberg_redaction_finding_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_redaction_finding_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_redaction_finding_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REDACTION_FINDING_FREE = LINKER.downcallHandle(
        LIB.find("xberg_redaction_finding_free")
            .or(() -> LIB.find("_xberg_redaction_finding_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_redaction_finding_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_redaction_finding_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CELL_CHANGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_cell_change_from_json")
            .or(() -> LIB.find("_xberg_cell_change_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_cell_change_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_cell_change_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CELL_CHANGE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_cell_change_free")
            .or(() -> LIB.find("_xberg_cell_change_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_cell_change_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_cell_change_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PROPERTY_CHANGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_property_change_from_json")
            .or(() -> LIB.find("_xberg_property_change_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_property_change_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_property_change_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PROPERTY_CHANGE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_property_change_free")
            .or(() -> LIB.find("_xberg_property_change_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_property_change_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_property_change_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_REVISION_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_document_revision_from_json")
            .or(() -> LIB.find("_xberg_document_revision_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_revision_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_revision_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_REVISION_FREE = LINKER.downcallHandle(
        LIB.find("xberg_document_revision_free")
            .or(() -> LIB.find("_xberg_document_revision_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_revision_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_revision_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REVISION_DELTA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_revision_delta_from_json")
            .or(() -> LIB.find("_xberg_revision_delta_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_revision_delta_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_revision_delta_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REVISION_DELTA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_revision_delta_free")
            .or(() -> LIB.find("_xberg_revision_delta_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_revision_delta_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_revision_delta_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_SUMMARY_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_document_summary_from_json")
            .or(() -> LIB.find("_xberg_document_summary_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_summary_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_summary_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_SUMMARY_FREE = LINKER.downcallHandle(
        LIB.find("xberg_document_summary_free")
            .or(() -> LIB.find("_xberg_document_summary_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_summary_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_summary_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TABLE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_table_from_json")
            .or(() -> LIB.find("_xberg_table_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_table_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_table_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TABLE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_table_free")
            .or(() -> LIB.find("_xberg_table_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_table_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_table_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TABLE_CELL_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_table_cell_from_json")
            .or(() -> LIB.find("_xberg_table_cell_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_table_cell_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_table_cell_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TABLE_CELL_FREE = LINKER.downcallHandle(
        LIB.find("xberg_table_cell_free")
            .or(() -> LIB.find("_xberg_table_cell_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_table_cell_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_table_cell_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TRANSLATION_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_translation_from_json")
            .or(() -> LIB.find("_xberg_translation_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_translation_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_translation_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TRANSLATION_FREE = LINKER.downcallHandle(
        LIB.find("xberg_translation_free")
            .or(() -> LIB.find("_xberg_translation_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_translation_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_translation_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTED_URI_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_extracted_uri_from_json")
            .or(() -> LIB.find("_xberg_extracted_uri_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extracted_uri_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extracted_uri_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTED_URI_FREE = LINKER.downcallHandle(
        LIB.find("xberg_extracted_uri_free")
            .or(() -> LIB.find("_xberg_extracted_uri_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extracted_uri_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extracted_uri_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DETECT_RESPONSE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_detect_response_from_json")
            .or(() -> LIB.find("_xberg_detect_response_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_detect_response_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_detect_response_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DETECT_RESPONSE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_detect_response_free")
            .or(() -> LIB.find("_xberg_detect_response_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_detect_response_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_detect_response_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DIFF_OPTIONS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_diff_options_from_json")
            .or(() -> LIB.find("_xberg_diff_options_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_diff_options_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_diff_options_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DIFF_OPTIONS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_diff_options_free")
            .or(() -> LIB.find("_xberg_diff_options_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_diff_options_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_diff_options_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_DIFF_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_extraction_diff_from_json")
            .or(() -> LIB.find("_xberg_extraction_diff_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_diff_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_diff_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_DIFF_FREE = LINKER.downcallHandle(
        LIB.find("xberg_extraction_diff_free")
            .or(() -> LIB.find("_xberg_extraction_diff_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_diff_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_diff_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DIFF_HUNK_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_diff_hunk_from_json")
            .or(() -> LIB.find("_xberg_diff_hunk_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_diff_hunk_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_diff_hunk_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DIFF_HUNK_FREE = LINKER.downcallHandle(
        LIB.find("xberg_diff_hunk_free")
            .or(() -> LIB.find("_xberg_diff_hunk_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_diff_hunk_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_diff_hunk_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TABLE_DIFF_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_table_diff_from_json")
            .or(() -> LIB.find("_xberg_table_diff_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_table_diff_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_table_diff_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TABLE_DIFF_FREE = LINKER.downcallHandle(
        LIB.find("xberg_table_diff_free")
            .or(() -> LIB.find("_xberg_table_diff_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_table_diff_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_table_diff_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMBEDDED_CHANGES_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_embedded_changes_from_json")
            .or(() -> LIB.find("_xberg_embedded_changes_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_embedded_changes_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_embedded_changes_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMBEDDED_CHANGES_FREE = LINKER.downcallHandle(
        LIB.find("xberg_embedded_changes_free")
            .or(() -> LIB.find("_xberg_embedded_changes_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_embedded_changes_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_embedded_changes_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMBEDDED_DIFF_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_embedded_diff_from_json")
            .or(() -> LIB.find("_xberg_embedded_diff_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_embedded_diff_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_embedded_diff_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMBEDDED_DIFF_FREE = LINKER.downcallHandle(
        LIB.find("xberg_embedded_diff_free")
            .or(() -> LIB.find("_xberg_embedded_diff_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_embedded_diff_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_embedded_diff_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_RERANKED_DOCUMENT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_reranked_document_from_json")
            .or(() -> LIB.find("_xberg_reranked_document_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_reranked_document_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_reranked_document_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_RERANKED_DOCUMENT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_reranked_document_free")
            .or(() -> LIB.find("_xberg_reranked_document_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_reranked_document_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_reranked_document_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SPARSE_EMBEDDING_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_sparse_embedding_from_json")
            .or(() -> LIB.find("_xberg_sparse_embedding_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_sparse_embedding_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_sparse_embedding_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SPARSE_EMBEDDING_FREE = LINKER.downcallHandle(
        LIB.find("xberg_sparse_embedding_free")
            .or(() -> LIB.find("_xberg_sparse_embedding_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_sparse_embedding_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_sparse_embedding_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SPARSE_EMBEDDING_PRESET_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_sparse_embedding_preset_from_json")
            .or(() -> LIB.find("_xberg_sparse_embedding_preset_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_sparse_embedding_preset_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_sparse_embedding_preset_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SPARSE_EMBEDDING_PRESET_FREE = LINKER.downcallHandle(
        LIB.find("xberg_sparse_embedding_preset_free")
            .or(() -> LIB.find("_xberg_sparse_embedding_preset_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_sparse_embedding_preset_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_sparse_embedding_preset_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_MULTI_VECTOR_EMBEDDING_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_multi_vector_embedding_from_json")
            .or(() -> LIB.find("_xberg_multi_vector_embedding_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_multi_vector_embedding_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_multi_vector_embedding_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_MULTI_VECTOR_EMBEDDING_FREE = LINKER.downcallHandle(
        LIB.find("xberg_multi_vector_embedding_free")
            .or(() -> LIB.find("_xberg_multi_vector_embedding_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_multi_vector_embedding_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_multi_vector_embedding_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LATE_INTERACTION_PRESET_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_late_interaction_preset_from_json")
            .or(() -> LIB.find("_xberg_late_interaction_preset_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_late_interaction_preset_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_late_interaction_preset_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LATE_INTERACTION_PRESET_FREE = LINKER.downcallHandle(
        LIB.find("xberg_late_interaction_preset_free")
            .or(() -> LIB.find("_xberg_late_interaction_preset_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_late_interaction_preset_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_late_interaction_preset_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LATE_INTERACTION_MATCH_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_late_interaction_match_from_json")
            .or(() -> LIB.find("_xberg_late_interaction_match_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_late_interaction_match_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_late_interaction_match_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LATE_INTERACTION_MATCH_FREE = LINKER.downcallHandle(
        LIB.find("xberg_late_interaction_match_free")
            .or(() -> LIB.find("_xberg_late_interaction_match_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_late_interaction_match_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_late_interaction_match_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_YAKE_PARAMS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_yake_params_from_json")
            .or(() -> LIB.find("_xberg_yake_params_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_yake_params_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_yake_params_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_YAKE_PARAMS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_yake_params_free")
            .or(() -> LIB.find("_xberg_yake_params_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_yake_params_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_yake_params_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_RAKE_PARAMS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_rake_params_from_json")
            .or(() -> LIB.find("_xberg_rake_params_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_rake_params_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_rake_params_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_RAKE_PARAMS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_rake_params_free")
            .or(() -> LIB.find("_xberg_rake_params_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_rake_params_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_rake_params_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_KEYWORD_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_keyword_config_from_json")
            .or(() -> LIB.find("_xberg_keyword_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_keyword_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_keyword_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_KEYWORD_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_keyword_config_free")
            .or(() -> LIB.find("_xberg_keyword_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_keyword_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_keyword_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_KEYWORD_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_keyword_from_json")
            .or(() -> LIB.find("_xberg_keyword_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_keyword_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_keyword_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_KEYWORD_FREE = LINKER.downcallHandle(
        LIB.find("xberg_keyword_free")
            .or(() -> LIB.find("_xberg_keyword_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_keyword_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_keyword_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_document_metadata_from_json")
            .or(() -> LIB.find("_xberg_document_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_document_metadata_free")
            .or(() -> LIB.find("_xberg_document_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_USER_CHUNK_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_user_chunk_config_from_json")
            .or(() -> LIB.find("_xberg_user_chunk_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_user_chunk_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_user_chunk_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_USER_CHUNK_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_user_chunk_config_free")
            .or(() -> LIB.find("_xberg_user_chunk_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_user_chunk_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_user_chunk_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_CONFIDENCE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_extraction_confidence_from_json")
            .or(() -> LIB.find("_xberg_extraction_confidence_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_confidence_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_confidence_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_CONFIDENCE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_extraction_confidence_free")
            .or(() -> LIB.find("_xberg_extraction_confidence_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_confidence_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_confidence_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HEURISTICS_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_heuristics_config_from_json")
            .or(() -> LIB.find("_xberg_heuristics_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_heuristics_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_heuristics_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HEURISTICS_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_heuristics_config_free")
            .or(() -> LIB.find("_xberg_heuristics_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_heuristics_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_heuristics_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNK_INFO_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_chunk_info_from_json")
            .or(() -> LIB.find("_xberg_chunk_info_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunk_info_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunk_info_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNK_INFO_FREE = LINKER.downcallHandle(
        LIB.find("xberg_chunk_info_free")
            .or(() -> LIB.find("_xberg_chunk_info_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunk_info_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunk_info_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_RANGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_page_range_from_json")
            .or(() -> LIB.find("_xberg_page_range_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_range_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_range_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_RANGE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_page_range_free")
            .or(() -> LIB.find("_xberg_page_range_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_range_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_range_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_MULTIDOC_INPUT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_multidoc_input_from_json")
            .or(() -> LIB.find("_xberg_multidoc_input_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_multidoc_input_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_multidoc_input_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_MULTIDOC_INPUT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_multidoc_input_free")
            .or(() -> LIB.find("_xberg_multidoc_input_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_multidoc_input_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_multidoc_input_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_SIGNALS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_page_signals_from_json")
            .or(() -> LIB.find("_xberg_page_signals_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_signals_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_signals_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_SIGNALS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_page_signals_free")
            .or(() -> LIB.find("_xberg_page_signals_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_signals_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_signals_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_BOUNDARY_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_document_boundary_from_json")
            .or(() -> LIB.find("_xberg_document_boundary_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_boundary_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_boundary_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_BOUNDARY_FREE = LINKER.downcallHandle(
        LIB.find("xberg_document_boundary_free")
            .or(() -> LIB.find("_xberg_document_boundary_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_boundary_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_boundary_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_MULTIDOC_THRESHOLDS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_multidoc_thresholds_from_json")
            .or(() -> LIB.find("_xberg_multidoc_thresholds_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_multidoc_thresholds_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_multidoc_thresholds_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_MULTIDOC_THRESHOLDS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_multidoc_thresholds_free")
            .or(() -> LIB.find("_xberg_multidoc_thresholds_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_multidoc_thresholds_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_multidoc_thresholds_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_META_SCHEMA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_meta_schema_free")
            .or(() -> LIB.find("_xberg_meta_schema_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_meta_schema_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_meta_schema_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGISTRY_FREE = LINKER.downcallHandle(
        LIB.find("xberg_registry_free")
            .or(() -> LIB.find("_xberg_registry_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_registry_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_registry_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_RESOLVED_PRESET_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_resolved_preset_from_json")
            .or(() -> LIB.find("_xberg_resolved_preset_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_resolved_preset_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_resolved_preset_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_RESOLVED_PRESET_FREE = LINKER.downcallHandle(
        LIB.find("xberg_resolved_preset_free")
            .or(() -> LIB.find("_xberg_resolved_preset_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_resolved_preset_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_resolved_preset_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PRESET_SAMPLE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_preset_sample_from_json")
            .or(() -> LIB.find("_xberg_preset_sample_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_preset_sample_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_preset_sample_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PRESET_SAMPLE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_preset_sample_free")
            .or(() -> LIB.find("_xberg_preset_sample_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_preset_sample_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_preset_sample_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PRESET_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_preset_from_json")
            .or(() -> LIB.find("_xberg_preset_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_preset_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_preset_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PRESET_FREE = LINKER.downcallHandle(
        LIB.find("xberg_preset_free")
            .or(() -> LIB.find("_xberg_preset_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_preset_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_preset_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PRESET_SUMMARY_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_preset_summary_from_json")
            .or(() -> LIB.find("_xberg_preset_summary_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_preset_summary_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_preset_summary_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PRESET_SUMMARY_FREE = LINKER.downcallHandle(
        LIB.find("xberg_preset_summary_free")
            .or(() -> LIB.find("_xberg_preset_summary_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_preset_summary_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_preset_summary_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCTOR_CHECK_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_doctor_check_from_json")
            .or(() -> LIB.find("_xberg_doctor_check_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_doctor_check_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_doctor_check_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCTOR_CHECK_FREE = LINKER.downcallHandle(
        LIB.find("xberg_doctor_check_free")
            .or(() -> LIB.find("_xberg_doctor_check_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_doctor_check_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_doctor_check_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCTOR_REPORT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_doctor_report_from_json")
            .or(() -> LIB.find("_xberg_doctor_report_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_doctor_report_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_doctor_report_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCTOR_REPORT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_doctor_report_free")
            .or(() -> LIB.find("_xberg_doctor_report_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_doctor_report_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_doctor_report_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PADDLE_OCR_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_paddle_ocr_config_from_json")
            .or(() -> LIB.find("_xberg_paddle_ocr_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_paddle_ocr_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_paddle_ocr_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PADDLE_OCR_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_paddle_ocr_config_free")
            .or(() -> LIB.find("_xberg_paddle_ocr_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_paddle_ocr_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_paddle_ocr_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_MODEL_PATHS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_model_paths_from_json")
            .or(() -> LIB.find("_xberg_model_paths_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_model_paths_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_model_paths_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_MODEL_PATHS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_model_paths_free")
            .or(() -> LIB.find("_xberg_model_paths_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_model_paths_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_model_paths_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ORIENTATION_RESULT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_orientation_result_from_json")
            .or(() -> LIB.find("_xberg_orientation_result_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_orientation_result_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_orientation_result_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ORIENTATION_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_orientation_result_free")
            .or(() -> LIB.find("_xberg_orientation_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_orientation_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_orientation_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_B_BOX_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_b_box_from_json")
            .or(() -> LIB.find("_xberg_b_box_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_b_box_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_b_box_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_B_BOX_FREE = LINKER.downcallHandle(
        LIB.find("xberg_b_box_free")
            .or(() -> LIB.find("_xberg_b_box_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_b_box_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_b_box_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LAYOUT_DETECTION_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_layout_detection_from_json")
            .or(() -> LIB.find("_xberg_layout_detection_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_layout_detection_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_layout_detection_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LAYOUT_DETECTION_FREE = LINKER.downcallHandle(
        LIB.find("xberg_layout_detection_free")
            .or(() -> LIB.find("_xberg_layout_detection_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_layout_detection_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_layout_detection_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_RECOGNIZED_TABLE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_recognized_table_from_json")
            .or(() -> LIB.find("_xberg_recognized_table_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_recognized_table_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_recognized_table_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_RECOGNIZED_TABLE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_recognized_table_free")
            .or(() -> LIB.find("_xberg_recognized_table_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_recognized_table_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_recognized_table_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DETECTION_RESULT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_detection_result_from_json")
            .or(() -> LIB.find("_xberg_detection_result_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_detection_result_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_detection_result_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DETECTION_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_detection_result_free")
            .or(() -> LIB.find("_xberg_detection_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_detection_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_detection_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMBEDDED_FILE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_embedded_file_from_json")
            .or(() -> LIB.find("_xberg_embedded_file_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_embedded_file_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_embedded_file_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMBEDDED_FILE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_embedded_file_free")
            .or(() -> LIB.find("_xberg_embedded_file_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_embedded_file_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_embedded_file_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PDF_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_pdf_metadata_from_json")
            .or(() -> LIB.find("_xberg_pdf_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pdf_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pdf_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PDF_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_pdf_metadata_free")
            .or(() -> LIB.find("_xberg_pdf_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pdf_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pdf_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNK_CLASSIFICATION_ENRICHMENT_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_chunk_classification_enrichment_config_free")
            .or(() -> LIB.find("_xberg_chunk_classification_enrichment_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunk_classification_enrichment_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunk_classification_enrichment_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PROXY_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_proxy_config_from_json")
            .or(() -> LIB.find("_xberg_proxy_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_proxy_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_proxy_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PROXY_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_proxy_config_free")
            .or(() -> LIB.find("_xberg_proxy_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_proxy_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_proxy_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CONTENT_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_content_config_from_json")
            .or(() -> LIB.find("_xberg_content_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_content_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_content_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CONTENT_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_content_config_free")
            .or(() -> LIB.find("_xberg_content_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_content_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_content_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_BROWSER_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_browser_config_from_json")
            .or(() -> LIB.find("_xberg_browser_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_browser_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_browser_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_BROWSER_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_browser_config_free")
            .or(() -> LIB.find("_xberg_browser_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_browser_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_browser_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CRAWL_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_crawl_config_from_json")
            .or(() -> LIB.find("_xberg_crawl_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_crawl_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_crawl_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CRAWL_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_crawl_config_free")
            .or(() -> LIB.find("_xberg_crawl_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_crawl_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_crawl_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SITEMAP_URL_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_sitemap_url_from_json")
            .or(() -> LIB.find("_xberg_sitemap_url_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_sitemap_url_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_sitemap_url_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SITEMAP_URL_FREE = LINKER.downcallHandle(
        LIB.find("xberg_sitemap_url_free")
            .or(() -> LIB.find("_xberg_sitemap_url_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_sitemap_url_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_sitemap_url_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_MAP_RESULT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_map_result_from_json")
            .or(() -> LIB.find("_xberg_map_result_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_map_result_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_map_result_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_MAP_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_map_result_free")
            .or(() -> LIB.find("_xberg_map_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_map_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_map_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SSRF_POLICY_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_ssrf_policy_from_json")
            .or(() -> LIB.find("_xberg_ssrf_policy_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ssrf_policy_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ssrf_policy_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SSRF_POLICY_FREE = LINKER.downcallHandle(
        LIB.find("xberg_ssrf_policy_free")
            .or(() -> LIB.find("_xberg_ssrf_policy_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_ssrf_policy_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_ssrf_policy_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CONVERSION_OPTIONS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_conversion_options_from_json")
            .or(() -> LIB.find("_xberg_conversion_options_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_conversion_options_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_conversion_options_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CONVERSION_OPTIONS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_conversion_options_free")
            .or(() -> LIB.find("_xberg_conversion_options_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_conversion_options_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_conversion_options_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PREPROCESSING_OPTIONS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_preprocessing_options_from_json")
            .or(() -> LIB.find("_xberg_preprocessing_options_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_preprocessing_options_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_preprocessing_options_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PREPROCESSING_OPTIONS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_preprocessing_options_free")
            .or(() -> LIB.find("_xberg_preprocessing_options_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_preprocessing_options_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_preprocessing_options_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_RESULT_TO_JSON = LIB.find("xberg_extraction_result_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_MAP_RESULT_TO_JSON = LIB.find("xberg_map_result_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_DOCTOR_REPORT_TO_JSON = LIB.find("xberg_doctor_report_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_PRESET_TO_JSON = LIB.find("xberg_preset_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_REGISTER_OCR_BACKEND = LIB
        .find("xberg_register_ocr_backend")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_UNREGISTER_OCR_BACKEND = LIB
        .find("xberg_unregister_ocr_backend")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_CLEAR_OCR_BACKEND = LIB
        .find("xberg_clear_ocr_backend")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_REGISTER_POST_PROCESSOR = LIB
        .find("xberg_register_post_processor")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_UNREGISTER_POST_PROCESSOR = LIB
        .find("xberg_unregister_post_processor")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_CLEAR_POST_PROCESSOR = LIB
        .find("xberg_clear_post_processor")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_REGISTER_VALIDATOR = LIB
        .find("xberg_register_validator")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_UNREGISTER_VALIDATOR = LIB
        .find("xberg_unregister_validator")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_CLEAR_VALIDATOR = LIB
        .find("xberg_clear_validator")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_REGISTER_DOCUMENT_EXTRACTOR = LIB
        .find("xberg_register_document_extractor")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_UNREGISTER_DOCUMENT_EXTRACTOR = LIB
        .find("xberg_unregister_document_extractor")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_CLEAR_DOCUMENT_EXTRACTOR = LIB
        .find("xberg_clear_document_extractor")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_REGISTER_EMBEDDING_BACKEND = LIB
        .find("xberg_register_embedding_backend")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_UNREGISTER_EMBEDDING_BACKEND = LIB
        .find("xberg_unregister_embedding_backend")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_CLEAR_EMBEDDING_BACKEND = LIB
        .find("xberg_clear_embedding_backend")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_REGISTER_RENDERER = LIB
        .find("xberg_register_renderer")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_UNREGISTER_RENDERER = LIB
        .find("xberg_unregister_renderer")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_CLEAR_RENDERER = LIB
        .find("xberg_clear_renderer")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_REGISTER_RERANKER_BACKEND = LIB
        .find("xberg_register_reranker_backend")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_UNREGISTER_RERANKER_BACKEND = LIB
        .find("xberg_unregister_reranker_backend")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_CLEAR_RERANKER_BACKEND = LIB
        .find("xberg_clear_reranker_backend")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_REGISTER_TOKENIZER_BACKEND = LIB
        .find("xberg_register_tokenizer_backend")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_UNREGISTER_TOKENIZER_BACKEND = LIB
        .find("xberg_unregister_tokenizer_backend")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_CLEAR_TOKENIZER_BACKEND = LIB
        .find("xberg_clear_tokenizer_backend")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)))
        .orElse(null);


}
