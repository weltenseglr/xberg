// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// Configuration for tree-sitter language pack integration.
///
/// Controls grammar download behavior and code analysis options.
///
/// # Example (TOML)
///
/// ```toml
/// [tree_sitter]
/// languages = ["python", "rust"]
/// groups = ["web"]
///
/// [tree_sitter.process]
/// structure = true
/// comments = true
/// docstrings = true
/// ```
/// </summary>
public sealed record TreeSitterConfig
{
    /// <summary>
    /// Enable code intelligence processing (default: true).
    ///
    /// When `false`, tree-sitter analysis is completely skipped even if
    /// the config section is present.
    /// </summary>
    [JsonPropertyName("enabled")]
    public bool Enabled { get; init; } = true;

    /// <summary>
    /// Custom cache directory for downloaded grammars.
    ///
    /// When `None`, uses the default: `~/.cache/tree-sitter-language-pack/v{version}/libs/`.
    ///
    /// Consumed both by the CLI (`tree-sitter download --from-config`,
    /// `cache warm`) and by `CodeExtractor` at
    /// extraction time, so that a configured cache directory is honoured
    /// wherever grammars are looked up or downloaded, not only during an
    /// explicit CLI download.
    /// </summary>
    [JsonPropertyName("cache_dir")]
    public string? CacheDir { get; init; } = null;

    /// <summary>
    /// Languages to pre-download on init (e.g., `["python", "rust"]`).
    ///
    /// Consumed only by the CLI's `tree-sitter download --from-config` and
    /// `cache warm` commands as a pre-download hint. Extraction itself does
    /// not read this field: a given source file always processes with a
    /// single, already auto-detected language, so there is nothing for a
    /// language allowlist to gate at extraction time.
    /// </summary>
    [JsonPropertyName("languages")]
    public List<string>? Languages { get; init; } = null;

    /// <summary>
    /// Language groups to pre-download (e.g., `["web", "systems", "scripting"]`).
    ///
    /// Consumed only by the CLI's `tree-sitter download --from-config` and
    /// `cache warm` commands, for the same reason as `languages` above.
    /// </summary>
    [JsonPropertyName("groups")]
    public List<string>? Groups { get; init; } = null;

    /// <summary>
    /// Processing options for code analysis.
    /// </summary>
    [JsonPropertyName("process")]
    public TreeSitterProcessConfig Process { get; init; } = default!;


    /// <summary>
    /// Parse a <see cref="TreeSitterConfig"/> from a JSON string.
    /// </summary>
    /// <exception cref="XbergException">When the JSON cannot be deserialised.</exception>
    public static TreeSitterConfig FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<TreeSitterConfig>(json, JsonOptions)
                ?? throw new XbergException($"Failed to parse TreeSitterConfig from JSON: deserializer returned null");
        }
        catch (XbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new XbergException($"Failed to parse TreeSitterConfig from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };
}
