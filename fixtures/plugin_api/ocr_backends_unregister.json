{
	"id": "ocr_backends_unregister",
	"category": "ocr_backend_management",
	"description": "Unregister nonexistent OCR backend gracefully",
	"docs": {
		"topic": "plugin_api",
		"title": "Unregister nonexistent OCR backend gracefully",
		"description": "Unregister nonexistent OCR backend gracefully",
		"side_effects": "safe"
	},
	"tags": ["ocr", "plugin_management", "unregister", "trait-bridge"],
	"skip": {
		"languages": ["c"],
		"reason": "The plugin registries take a host-language callback, so the C API does not expose registration or the clear and unregister calls that pair with it. With no C function to name, the built-in c snippet recipe has no function identity and the fixture cannot be documented for c. Every other language keeps this fixture."
	},
	"call": "unregister_ocr_backend",
	"input": {
		"name": "nonexistent-backend-xyz"
	},
	"assertions": [
		{
			"type": "not_error"
		}
	]
}
