name: Benchmarks

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Optional branch or commit (defaults to the dispatched revision)"
        required: false
        default: ""
        type: string
      timeout:
        description: "Timeout per document in seconds"
        required: false
        default: "900"
        type: string

env:
  ITERATIONS: "3"
  TIKA_VERSION: "3.3.1"
  ORT_VERSION: "1.24.2"
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  CARGO_PROFILE_DEV_DEBUG: 0
  RUST_BACKTRACE: short
  RUST_MIN_STACK: 16777216
  RUSTFLAGS: "-C strip=symbols"
  MEASURE_QUALITY: "true"
  OCR_ENABLED: "true"
  RUN_OCR_BENCHMARKS: "true"
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  GROUND_TRUTH_DIR: "test_documents/ground_truth"

concurrency:
  group: benchmarks-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  setup:
    name: Build harness + native libs
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 360
    permissions:
      contents: read
      id-token: write
    outputs:
      artifact-name: benchmarks-target
      source-sha: ${{ steps.source-revision.outputs.sha }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.inputs.branch || github.sha }}

          submodules: recursive
      - name: Pin benchmark source revision
        id: source-revision
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Ensure benchmark harness exists
        run: scripts/benchmarks/ensure-benchmark-harness-exists.sh

      - name: Install system dependencies
        uses: ./.github/actions/install-system-deps

      - name: Setup Rust
        uses: xberg-io/actions/setup-rust@v1
        with:
          cache-key-prefix: benchmarks-setup
          use-sccache: "false"
          disable-cache: "false"
          report-stats: "true"

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Setup Layout Models
        uses: ./.github/actions/setup-layout-models

      - name: Install Task
        uses: xberg-io/actions/install-task@v1

      - name: Validate benchmark scripts
        run: task benchmark:validate:scripts

      - name: Cache benchmark harness
        uses: ./.github/actions/cache-benchmark-harness
        with:
          build-profile: release

      - name: Build xberg-cli (release, all features + Sceptre tract diagnostic)
        run: cargo build --locked --release -p xberg-cli --features all,xberg/sceptre-ocr-tract

      # Bench cohorts span pdf/office/markup/ebook/email/data/images across the
      # xberg + 6 competitor jobs -- no glob subset is safe here. ~keep
      - name: Fetch test_documents fixtures
        uses: xberg-io/actions/fetch-test-documents@v1

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_BENCHMARK_SA }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Install zstd (corpus cache decompression)
        run: sudo apt-get install -y zstd

      - name: Restore reference corpus cache
        env:
          GCP_BENCHMARK_BUCKET: ${{ secrets.GCP_BENCHMARK_BUCKET }}
        run: scripts/benchmarks/restore-corpus-cache.sh

      - name: Validate ground truth
        run: >-
          cargo run --locked --release -p benchmark-harness -- validate-gt
          --fixtures tools/benchmark-harness/fixtures --strict

      - name: Validate benchmark cohorts
        run: cargo test --locked --release -p benchmark-harness 'cohort::tests::'

      - name: Validate benchmark harness contracts
        # --no-fail-fast: cargo stops after the first failing test BINARY, so a
        # single broken contract hides the other targets entirely. Run 31632507453
        # reported results for only two of these four because fixture_validation
        # failed; whether the remaining two also failed was unknowable from the log.
        run: >-
          cargo test --locked --release -p benchmark-harness --no-fail-fast
          --test validate_artifacts --test lossless_aggregation
          --test fixture_validation --test aggregate_schema

      - name: Validate workflow matrix against harness contracts
        run: scripts/ci/benchmarks/validate-workflow-matrix.py

      - name: Package reference corpus cache
        run: tar -C test_documents -cf corpus-cache.tar .corpus-cache/pdf .corpus-cache/ground_truth

      - name: Upload reference corpus cache
        uses: actions/upload-artifact@v7
        with:
          name: benchmark-corpus-cache
          path: corpus-cache.tar
          retention-days: 7

      - name: Log disk space before artifact upload
        run: scripts/ci/validate/show-disk-space.sh "Disk space before artifact upload"

      - name: Upload build artifacts (harness binary + xberg-cli)
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-target
          path: |
            target/release/benchmark-harness
            target/release/xberg
          retention-days: 7
          if-no-files-found: warn

      - name: Upload benchmark harness binary (for third-party jobs)
        uses: actions/upload-artifact@v7
        with:
          name: benchmark-harness-binary
          path: |
            target/release/benchmark-harness
          retention-days: 7

      - name: Log disk space after artifact upload
        run: scripts/ci/validate/show-disk-space.sh "Disk space after artifact upload"

  bench-rust:
    name: bench-rust (${{ matrix.pipeline }}, ${{ matrix.output_format }}, ${{ matrix.mode }}, ${{ matrix.cohort }})
    needs: setup
    # Previously split heavy pipelines (layout/paddle/sceptre) onto runner-large-arm64 and
    # the rest onto runner-medium-arm64. Both self-hosted arm64 pools were retired, so every
    # leg now runs on the same hosted arm64 machine — figures need re-baselining before they
    # are compared against numbers published from the old runners.
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 360
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        pipeline:
          - baseline
          - layout
          - baseline-paddle
          - layout-paddle
          - sceptre-ort
          - sceptre-ort-layout
          - sceptre-ort-autorotate
        output_format: [markdown, plaintext]
        mode: [single-file, batch]
        cohort:
          - native-pdf-fast-b8
          - ocr-pdf-fast-b4
          - native-office-fast
          - native-markup-fast
          - native-ebook-fast
          - native-email-fast
          - native-data-fast
          - ocr-images-fast
        # The layout pipeline only benefits rendered-page formats (PDF, images); for the other
        # families a layout run would merely duplicate baseline, so drop those cells.
        # The PaddleOCR-engine variants (baseline-paddle, layout-paddle) force OCR, so they run ONLY
        # on the OCR cohorts (ocr-pdf-fast-b4, ocr-images-fast); drop them from every non-OCR cohort.
        # NOTE: keep this block a clean list of `- {...}` entries — the validate-workflow-matrix.py
        # exclude parser stops at the first non-entry line, so do not intersperse comments here.
        exclude:
          - { pipeline: layout, cohort: native-office-fast }
          - { pipeline: layout, cohort: native-markup-fast }
          - { pipeline: layout, cohort: native-ebook-fast }
          - { pipeline: layout, cohort: native-email-fast }
          - { pipeline: layout, cohort: native-data-fast }
          - { pipeline: baseline-paddle, cohort: native-pdf-fast-b8 }
          - { pipeline: baseline-paddle, cohort: native-office-fast }
          - { pipeline: baseline-paddle, cohort: native-markup-fast }
          - { pipeline: baseline-paddle, cohort: native-ebook-fast }
          - { pipeline: baseline-paddle, cohort: native-email-fast }
          - { pipeline: baseline-paddle, cohort: native-data-fast }
          - { pipeline: layout-paddle, cohort: native-pdf-fast-b8 }
          - { pipeline: layout-paddle, cohort: native-office-fast }
          - { pipeline: layout-paddle, cohort: native-markup-fast }
          - { pipeline: layout-paddle, cohort: native-ebook-fast }
          - { pipeline: layout-paddle, cohort: native-email-fast }
          - { pipeline: layout-paddle, cohort: native-data-fast }
          - { pipeline: sceptre-ort, cohort: native-pdf-fast-b8 }
          - { pipeline: sceptre-ort, cohort: native-office-fast }
          - { pipeline: sceptre-ort, cohort: native-markup-fast }
          - { pipeline: sceptre-ort, cohort: native-ebook-fast }
          - { pipeline: sceptre-ort, cohort: native-email-fast }
          - { pipeline: sceptre-ort, cohort: native-data-fast }
          - { pipeline: sceptre-ort-layout, cohort: native-pdf-fast-b8 }
          - { pipeline: sceptre-ort-layout, cohort: native-office-fast }
          - { pipeline: sceptre-ort-layout, cohort: native-markup-fast }
          - { pipeline: sceptre-ort-layout, cohort: native-ebook-fast }
          - { pipeline: sceptre-ort-layout, cohort: native-email-fast }
          - { pipeline: sceptre-ort-layout, cohort: native-data-fast }
          - { pipeline: sceptre-ort-autorotate, cohort: native-pdf-fast-b8 }
          - { pipeline: sceptre-ort-autorotate, cohort: native-office-fast }
          - { pipeline: sceptre-ort-autorotate, cohort: native-markup-fast }
          - { pipeline: sceptre-ort-autorotate, cohort: native-ebook-fast }
          - { pipeline: sceptre-ort-autorotate, cohort: native-email-fast }
          - { pipeline: sceptre-ort-autorotate, cohort: native-data-fast }
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}

          submodules: recursive
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Install system dependencies
        uses: ./.github/actions/install-system-deps

      - name: Setup Rust toolchain
        uses: xberg-io/actions/setup-rust@v1
        with:
          cache-key-prefix: benchmarks-rust-${{ matrix.pipeline }}
          use-sccache: "true"
          report-stats: "true"

      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          name: ${{ needs.setup.outputs.artifact-name }}
          path: target/release

      - name: Restore benchmark binary permissions
        run: scripts/benchmarks/restore-binary-permissions.sh

      # Bench cohorts span pdf/office/markup/ebook/email/data/images across the
      # xberg + 6 competitor jobs -- no glob subset is safe here. ~keep
      - name: Fetch test_documents fixtures
        uses: xberg-io/actions/fetch-test-documents@v1

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Setup Layout Models
        if: contains(matrix.pipeline, 'layout')
        uses: ./.github/actions/setup-layout-models

      # The bench-rust runner ships only `python3` (no `python` alias, and its system
      # python3 is externally-managed without pip), so the PaddleOCR model action's
      # `python -m pip install huggingface_hub` fails with exit 127. Provision a managed
      # Python with pip (matching the competitor jobs) before the model download.
      - name: Setup Python (PaddleOCR model download)
        if: contains(matrix.pipeline, 'paddle')
        uses: xberg-io/actions/setup-python-env@v1
        with:
          python-version: "3.11"
          cache-prefix: benchmark-paddle-hf
          install-command: "python -m pip install --disable-pip-version-check 'huggingface_hub>=0.34,<2'"

      - name: Setup PaddleOCR Models
        if: contains(matrix.pipeline, 'paddle')
        uses: ./.github/actions/setup-paddle-ocr-models

      - name: Pre-warm Sceptre ORT models
        if: contains(matrix.pipeline, 'sceptre-ort')
        env:
          PIPELINE: ${{ matrix.pipeline }}
        run: |
          set -euo pipefail
          for language in eng deu jpn; do
            args=(
              extract test_documents/images/test_hello_world.png
              --ocr true
              --force-ocr true
              --ocr-backend sceptre
              --ocr-backend-options '{"model":{"backend":"ort"}}'
              --ocr-language "$language"
              --no-cache true
              --format json
            )
            if [[ "$PIPELINE" == "sceptre-ort-autorotate" ]]; then
              args+=(--ocr-auto-rotate true)
            fi
            ./target/release/xberg "${args[@]}" >/dev/null
          done

      - name: Run benchmark
        env:
          FRAMEWORK: xberg-${{ matrix.output_format }}-${{ matrix.pipeline }}
          MODE: ${{ matrix.mode }}
          OUTPUT_FORMAT: ${{ matrix.output_format }}
          ITERATIONS: ${{ env.ITERATIONS }}
          TIMEOUT: ${{ github.event.inputs.timeout }}
          COHORT: tools/benchmark-harness/cohorts/${{ matrix.cohort }}.json
        run: scripts/benchmarks/run-benchmark.sh

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-rust-${{ matrix.pipeline }}-${{ matrix.output_format }}-${{ matrix.mode }}-${{ matrix.cohort }}-${{ github.run_id }}
          path: benchmark-results/
          retention-days: 30

  sceptre-tract-diagnostic:
    name: sceptre-tract diagnostic (${{ matrix.mode }})
    needs: setup
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 360
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        mode: [single-file, batch]
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}
          submodules: recursive

      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Install system dependencies
        uses: ./.github/actions/install-system-deps

      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          name: ${{ needs.setup.outputs.artifact-name }}
          path: target/release

      - name: Restore benchmark binary permissions
        run: scripts/benchmarks/restore-binary-permissions.sh

      # Bench cohorts span pdf/office/markup/ebook/email/data/images across the
      # xberg + 6 competitor jobs -- no glob subset is safe here. ~keep
      - name: Fetch test_documents fixtures
        uses: xberg-io/actions/fetch-test-documents@v1

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Pre-warm Sceptre tract models
        run: >-
          ./target/release/xberg extract test_documents/images/test_hello_world.png
          --ocr true --force-ocr true --ocr-backend sceptre
          --ocr-backend-options '{"model":{"backend":"tract"}}'
          --ocr-language eng --no-cache true --format json >/dev/null

      - name: Run bounded diagnostic benchmark
        env:
          FRAMEWORK: xberg-markdown-sceptre-tract
          MODE: ${{ matrix.mode }}
          OUTPUT_FORMAT: markdown
          ITERATIONS: ${{ env.ITERATIONS }}
          TIMEOUT: ${{ github.event.inputs.timeout }}
          COHORT: tools/benchmark-harness/cohorts/ocr-images-structured.json
        run: scripts/benchmarks/run-benchmark.sh

      - name: Upload diagnostic artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: diagnostic-sceptre-tract-markdown-${{ matrix.mode }}-ocr-images-structured-${{ github.run_id }}
          path: benchmark-results/
          retention-days: 30

  bench-docling:
    name: docling (${{ matrix.output_format }}, ${{ matrix.mode }}, ${{ matrix.cohort }})
    needs: [setup, bench-rust]
    if: ${{ needs.setup.result == 'success' && !cancelled() }}
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 360
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        mode: [single-file, batch]
        output_format: [markdown, plaintext]
        # Docling supports the multi-format families it has native readers for (office,
        # markup, data) plus images; ebook/email are xberg-only. Cells for formats Docling
        # cannot read are best-effort in the release contract, so partial coverage is fine.
        cohort:
          - native-pdf-fast-b8
          - ocr-pdf-fast-b4
          - native-office-fast
          - native-markup-fast
          - native-data-fast
          - ocr-images-fast
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}

          submodules: recursive
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Download benchmark harness binary
        uses: actions/download-artifact@v8
        with:
          name: benchmark-harness-binary
          path: target/release

      - name: Restore benchmark binary permissions
        run: scripts/benchmarks/restore-binary-permissions.sh

      # Bench cohorts span pdf/office/markup/ebook/email/data/images across the
      # xberg + 6 competitor jobs -- no glob subset is safe here. ~keep
      - name: Fetch test_documents fixtures
        uses: xberg-io/actions/fetch-test-documents@v1

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      - name: Install system dependencies (libheif + native libs for harness binary)
        uses: ./.github/actions/install-system-deps

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Setup Python
        uses: xberg-io/actions/setup-python-env@v1
        with:
          python-version: "3.11"
          cache-prefix: benchmark-docling
          install-command: "uv sync --frozen --no-install-project --no-install-workspace --group dev --group bench-docling"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1 libglib2.0-0 tesseract-ocr tesseract-ocr-eng

      - name: Restore EasyOCR model cache (docling OCR)
        id: easyocr-cache
        uses: actions/cache@v5
        with:
          path: ~/.EasyOCR
          key: docling-easyocr-en-ko-ja-v1-${{ runner.os }}-${{ runner.arch }}

      - name: Prefetch EasyOCR language models (en, ko, ja)
        if: steps.easyocr-cache.outputs.cache-hit != 'true'
        env:
          UV_NO_SYNC: "1"
        run: |
          set -euo pipefail
          # Docling's default OCR engine is EasyOCR; the image OCR cohort forwards
          # Korean/Japanese, so prefetch those recognition models (plus the shared
          # detection model) into ~/.EasyOCR to keep the download out of the timed run.
          uv run --no-sync python - <<'PY'
          import easyocr

          for langs in (["en", "ko"], ["ja", "en"]):
              easyocr.Reader(langs, gpu=False, verbose=False)
          PY

      - name: Run benchmark
        env:
          FRAMEWORK: docling
          MODE: ${{ matrix.mode }}
          OUTPUT_FORMAT: ${{ matrix.output_format }}
          ITERATIONS: ${{ env.ITERATIONS }}
          TIMEOUT: ${{ github.event.inputs.timeout }}
          COHORT: tools/benchmark-harness/cohorts/${{ matrix.cohort }}.json
          UV_NO_SYNC: "1"
        run: scripts/benchmarks/run-benchmark.sh

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-docling-${{ matrix.output_format }}-${{ matrix.mode }}-${{ matrix.cohort }}-${{ github.run_id }}
          path: benchmark-results/
          retention-days: 30

  bench-markitdown:
    name: markitdown (${{ matrix.output_format }}, ${{ matrix.mode }}, ${{ matrix.cohort }})
    needs: [setup, bench-rust]
    if: ${{ needs.setup.result == 'success' && !cancelled() }}
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 360
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        mode: [single-file]
        output_format: [markdown, plaintext]
        # MarkItDown emits markdown; run it over PDF plus the office/markup/data families it supports.
        cohort: [native-pdf-fast-b8, native-office-fast, native-markup-fast, native-data-fast]
        exclude:
          - output_format: plaintext
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}

          submodules: recursive
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Download benchmark harness binary
        uses: actions/download-artifact@v8
        with:
          name: benchmark-harness-binary
          path: target/release

      - name: Restore benchmark binary permissions
        run: scripts/benchmarks/restore-binary-permissions.sh

      # Bench cohorts span pdf/office/markup/ebook/email/data/images across the
      # xberg + 6 competitor jobs -- no glob subset is safe here. ~keep
      - name: Fetch test_documents fixtures
        uses: xberg-io/actions/fetch-test-documents@v1

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      - name: Install system dependencies (libheif + native libs for harness binary)
        uses: ./.github/actions/install-system-deps

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Setup Python
        uses: xberg-io/actions/setup-python-env@v1
        with:
          python-version: "3.11"
          cache-prefix: benchmark-markitdown
          install-command: "uv sync --frozen --no-install-project --no-install-workspace --group dev --group bench-markitdown --prerelease=allow"

      - name: Run benchmark
        env:
          FRAMEWORK: markitdown
          MODE: ${{ matrix.mode }}
          OUTPUT_FORMAT: ${{ matrix.output_format }}
          ITERATIONS: ${{ env.ITERATIONS }}
          TIMEOUT: ${{ github.event.inputs.timeout }}
          COHORT: tools/benchmark-harness/cohorts/${{ matrix.cohort }}.json
          UV_NO_SYNC: "1"
        run: scripts/benchmarks/run-benchmark.sh

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-markitdown-${{ matrix.output_format }}-${{ matrix.mode }}-${{ matrix.cohort }}-${{ github.run_id }}
          path: benchmark-results/
          retention-days: 30

  bench-unstructured:
    name: unstructured (${{ matrix.output_format }}, ${{ matrix.mode }}, ${{ matrix.cohort }})
    needs: [setup, bench-rust]
    if: ${{ needs.setup.result == 'success' && !cancelled() }}
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 360
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        mode: [single-file]
        output_format: [plaintext]
        # Unstructured emits plaintext; run it over PDF plus the office/markup/email/data families.
        cohort: [
          native-pdf-fast-b8,
          native-office-fast,
          native-markup-fast,
          native-email-fast,
          native-data-fast,
          ocr-pdf-fast-b4,
          ocr-images-fast,
        ]
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}

          submodules: recursive
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Download benchmark harness binary
        uses: actions/download-artifact@v8
        with:
          name: benchmark-harness-binary
          path: target/release

      - name: Restore benchmark binary permissions
        run: scripts/benchmarks/restore-binary-permissions.sh

      # Bench cohorts span pdf/office/markup/ebook/email/data/images across the
      # xberg + 6 competitor jobs -- no glob subset is safe here. ~keep
      - name: Fetch test_documents fixtures
        uses: xberg-io/actions/fetch-test-documents@v1

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      - name: Install system dependencies (libheif + native libs for harness binary)
        uses: ./.github/actions/install-system-deps

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y tesseract-ocr tesseract-ocr-eng libmagic-dev poppler-utils libreoffice pandoc

      - name: Setup Python
        uses: xberg-io/actions/setup-python-env@v1
        with:
          python-version: "3.11"
          cache-prefix: benchmark-unstructured
          install-command: "uv sync --frozen --no-install-project --no-install-workspace --group dev --group bench-unstructured"

      - name: Run benchmark
        env:
          FRAMEWORK: unstructured
          MODE: ${{ matrix.mode }}
          OUTPUT_FORMAT: ${{ matrix.output_format }}
          ITERATIONS: ${{ env.ITERATIONS }}
          TIMEOUT: ${{ github.event.inputs.timeout }}
          COHORT: tools/benchmark-harness/cohorts/${{ matrix.cohort }}.json
          UV_NO_SYNC: "1"
        run: scripts/benchmarks/run-benchmark.sh

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-unstructured-${{ matrix.output_format }}-${{ matrix.mode }}-${{ matrix.cohort }}-${{ github.run_id }}
          path: benchmark-results/
          retention-days: 30

  bench-tika:
    name: tika (${{ matrix.output_format }}, ${{ matrix.mode }}, ${{ matrix.cohort }})
    needs: [setup, bench-rust]
    if: ${{ needs.setup.result == 'success' && !cancelled() }}
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 360
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        mode: [single-file]
        output_format: [markdown, plaintext]
        # Tika is the broadest competitor: PDF plus office/markup/ebook/email/data families.
        cohort:
          - native-pdf-fast-b8
          - native-office-fast
          - native-markup-fast
          - native-ebook-fast
          - native-email-fast
          - native-data-fast
          - ocr-pdf-fast-b4
          - ocr-images-fast
        exclude:
          - output_format: markdown
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}

          submodules: recursive
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Download benchmark harness binary
        uses: actions/download-artifact@v8
        with:
          name: benchmark-harness-binary
          path: target/release

      - name: Restore benchmark binary permissions
        run: scripts/benchmarks/restore-binary-permissions.sh

      # Bench cohorts span pdf/office/markup/ebook/email/data/images across the
      # xberg + 6 competitor jobs -- no glob subset is safe here. ~keep
      - name: Fetch test_documents fixtures
        uses: xberg-io/actions/fetch-test-documents@v1

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      - name: Install system dependencies (libheif + native libs for harness binary)
        uses: ./.github/actions/install-system-deps

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Setup Java
        uses: actions/setup-java@v5
        id: setup-java
        with:
          distribution: "temurin"
          java-version: "25"

      - name: Download Apache Tika
        run: |
          mkdir -p tools/benchmark-harness/libs
          curl -fsSL --retry 5 --retry-delay 5 -o "tools/benchmark-harness/libs/tika-app-${{ env.TIKA_VERSION }}.jar" \
            "https://repo1.maven.org/maven2/org/apache/tika/tika-app/${{ env.TIKA_VERSION }}/tika-app-${{ env.TIKA_VERSION }}.jar"

      - name: Run benchmark
        env:
          FRAMEWORK: tika
          MODE: ${{ matrix.mode }}
          OUTPUT_FORMAT: ${{ matrix.output_format }}
          ITERATIONS: ${{ env.ITERATIONS }}
          TIMEOUT: ${{ github.event.inputs.timeout }}
          COHORT: tools/benchmark-harness/cohorts/${{ matrix.cohort }}.json
        run: scripts/benchmarks/run-benchmark.sh

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-tika-${{ matrix.output_format }}-${{ matrix.mode }}-${{ matrix.cohort }}-${{ github.run_id }}
          path: benchmark-results/
          retention-days: 30

  bench-pymupdf4llm:
    name: pymupdf4llm (${{ matrix.output_format }}, ${{ matrix.mode }}, ${{ matrix.cohort }})
    needs: [setup, bench-rust]
    if: ${{ needs.setup.result == 'success' && !cancelled() }}
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 360
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        mode: [single-file]
        output_format: [markdown, plaintext]
        # PyMuPDF4LLM emits markdown and has no OCR path, so it runs the PDF and ebook families
        # (which have text layers) but NOT the scanned-image cohort, where it would always be empty.
        cohort: [native-pdf-fast-b8, native-ebook-fast]
        exclude:
          - output_format: plaintext
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}

          submodules: recursive
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Download benchmark harness binary
        uses: actions/download-artifact@v8
        with:
          name: benchmark-harness-binary
          path: target/release

      - name: Restore benchmark binary permissions
        run: scripts/benchmarks/restore-binary-permissions.sh

      # Bench cohorts span pdf/office/markup/ebook/email/data/images across the
      # xberg + 6 competitor jobs -- no glob subset is safe here. ~keep
      - name: Fetch test_documents fixtures
        uses: xberg-io/actions/fetch-test-documents@v1

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      - name: Install system dependencies (libheif + native libs for harness binary)
        uses: ./.github/actions/install-system-deps

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y tesseract-ocr tesseract-ocr-eng libjpeg-dev libpng-dev libtiff-dev

      - name: Setup Python
        uses: xberg-io/actions/setup-python-env@v1
        with:
          python-version: "3.11"
          cache-prefix: benchmark-pymupdf4llm
          install-command: "uv sync --frozen --no-install-project --no-install-workspace --group dev --group bench-pymupdf4llm"

      - name: Run benchmark
        env:
          FRAMEWORK: pymupdf4llm
          MODE: ${{ matrix.mode }}
          OUTPUT_FORMAT: ${{ matrix.output_format }}
          ITERATIONS: ${{ env.ITERATIONS }}
          TIMEOUT: ${{ github.event.inputs.timeout }}
          TESSDATA_PREFIX: /usr/share/tesseract-ocr/5/tessdata
          COHORT: tools/benchmark-harness/cohorts/${{ matrix.cohort }}.json
          UV_NO_SYNC: "1"
        run: scripts/benchmarks/run-benchmark.sh

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-pymupdf4llm-${{ matrix.output_format }}-${{ matrix.mode }}-${{ matrix.cohort }}-${{ github.run_id }}
          path: benchmark-results/
          retention-days: 30

  prefetch-mineru-models:
    name: Prefetch MinerU model weights
    # Intentionally NOT `needs: setup`: prefetching MinerU weights only needs the
    # repo (scripts + fixtures), not the built harness, so it runs concurrently
    # with the FFI/harness build from the first stage instead of waiting for it.
    # Checks out the same ref setup resolves (its later source-sha pin is only
    # needed by jobs that must match the benchmarked binary's exact revision).
    if: ${{ !cancelled() }}
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 120
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.inputs.branch || github.sha }}
          submodules: recursive

      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      # Bench cohorts span pdf/office/markup/ebook/email/data/images across the
      # xberg + 6 competitor jobs -- no glob subset is safe here. ~keep
      - name: Fetch test_documents fixtures
        uses: xberg-io/actions/fetch-test-documents@v1

      - name: Setup Python
        uses: xberg-io/actions/setup-python-env@v1
        with:
          python-version: "3.11"
          cache-prefix: benchmark-mineru
          install-command: "uv sync --frozen --no-install-project --no-install-workspace --group dev --group bench-mineru"

      - name: Install system dependencies (libgl1 + libglib2.0-0 for mineru)
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1 libglib2.0-0 tesseract-ocr tesseract-ocr-eng

      - name: Configure Hugging Face cache
        run: |
          mkdir -p "$HOME/.cache/huggingface/hub"
          {
            echo "HF_HOME=$HOME/.cache/huggingface"
            echo "HF_HUB_CACHE=$HOME/.cache/huggingface/hub"
            echo "MINERU_MODEL_SOURCE=huggingface"
          } >> "$GITHUB_ENV"

      - name: Restore MinerU model cache
        id: mineru-model-cache
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/huggingface/hub
            ~/mineru.json
          key: mineru-models-3.4.4-cfg-v5-kor-jpn-${{ runner.os }}-${{ runner.arch }}

      - name: Warm MinerU model weights (native + OCR pipelines)
        if: steps.mineru-model-cache.outputs.cache-hit != 'true'
        env:
          UV_NO_SYNC: "1"
        run: |
          set -euo pipefail
          txt_fixture="test_documents/pdf/fake_memo.pdf"
          ocr_fixture="test_documents/pdf/non_searchable.pdf"
          echo "Warming MinerU pipeline weights (txt path) on ${txt_fixture}..."
          uv run --no-sync python tools/benchmark-harness/scripts/mineru_extract.py \
            --no-ocr --format=markdown sync "$txt_fixture" > /dev/null
          echo "Warming MinerU OCR weights (ocr path) on ${ocr_fixture}..."
          uv run --no-sync python tools/benchmark-harness/scripts/mineru_extract.py \
            --ocr --format=markdown sync "$ocr_fixture" > /dev/null
          echo "Warming MinerU Korean + Japanese PaddleOCR weights..."
          # The image OCR cohort forwards non-English languages; PaddleOCR downloads the
          # per-language model on first use, so warm korean/japan here (content is
          # irrelevant to which model is fetched) to keep it out of the timed run.
          uv run --no-sync python tools/benchmark-harness/scripts/mineru_extract.py \
            --ocr --ocr-lang=eng+kor --format=markdown sync "$ocr_fixture" > /dev/null
          uv run --no-sync python tools/benchmark-harness/scripts/mineru_extract.py \
            --ocr --ocr-lang=jpn_vert --format=markdown sync "$ocr_fixture" > /dev/null
          echo "MinerU model cache populated at $HF_HUB_CACHE"
          du -sh "$HF_HUB_CACHE" || true

      - name: Ensure MinerU config (mineru.json) present
        env:
          UV_NO_SYNC: "1"
        run: |
          set -euo pipefail
          # MinerU's persist_downloaded_model_config fetches a config template from
          # gcore.jsdelivr.net (bare requests.get, no timeout) on the first model
          # resolution whenever ~/mineru.json is absent -- ungoverned by HF_HUB_OFFLINE,
          # so on the egress-restricted bench runner it hangs the full timeout. Packaging
          # a valid ~/mineru.json makes get_existing_configured_model_root short-circuit
          # the download entirely. The warm step above writes it as a side effect; on a
          # cache hit it is restored by actions/cache. Regenerate online if still missing.
          if [ ! -f "$HOME/mineru.json" ]; then
            echo "mineru.json missing after warm/cache-restore; generating online..."
            uv run --no-sync python tools/benchmark-harness/scripts/mineru_extract.py \
              --no-ocr --format=markdown sync test_documents/pdf/fake_memo.pdf > /dev/null
          fi
          uv run --no-sync python - <<'PY'
          import json, os
          cfg = os.path.expanduser("~/mineru.json")
          data = json.load(open(cfg))
          models_dir = data.get("models-dir") or {}
          assert models_dir.get("pipeline"), f"mineru.json missing models-dir.pipeline: {data}"
          print(f"mineru.json OK: config_version={data.get('config_version')} models-dir={models_dir}")
          PY

      - name: Package MinerU model cache
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/mineru-model-cache"
          tar -C "$HOME" -cf "$RUNNER_TEMP/mineru-model-cache/mineru-models.tar" \
            .cache/huggingface/hub mineru.json
          ls -lh "$RUNNER_TEMP/mineru-model-cache/mineru-models.tar"

      - name: Upload MinerU model cache
        uses: actions/upload-artifact@v7
        with:
          name: mineru-model-cache-${{ github.run_id }}
          path: ${{ runner.temp }}/mineru-model-cache/mineru-models.tar
          retention-days: 1
          if-no-files-found: error

  bench-mineru:
    name: mineru (${{ matrix.output_format }}, ${{ matrix.mode }}, ${{ matrix.cohort }})
    needs: [setup, bench-rust, prefetch-mineru-models]
    if: ${{ needs.setup.result == 'success' && needs.prefetch-mineru-models.result == 'success' && !cancelled() }}
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 360
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        mode: [single-file]
        output_format: [markdown, plaintext]
        # MinerU handles PDF (native + OCR) and document images.
        cohort: [native-pdf-fast-b8, ocr-pdf-fast-b4, ocr-images-fast]
        exclude:
          - output_format: plaintext
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}

          submodules: recursive
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Download benchmark harness binary
        uses: actions/download-artifact@v8
        with:
          name: benchmark-harness-binary
          path: target/release

      - name: Restore benchmark binary permissions
        run: scripts/benchmarks/restore-binary-permissions.sh

      # Bench cohorts span pdf/office/markup/ebook/email/data/images across the
      # xberg + 6 competitor jobs -- no glob subset is safe here. ~keep
      - name: Fetch test_documents fixtures
        uses: xberg-io/actions/fetch-test-documents@v1

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      - name: Install system dependencies (libheif + native libs for harness binary)
        uses: ./.github/actions/install-system-deps

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Setup Python
        uses: xberg-io/actions/setup-python-env@v1
        with:
          python-version: "3.11"
          cache-prefix: benchmark-mineru
          install-command: "uv sync --frozen --no-install-project --no-install-workspace --group dev --group bench-mineru"

      - name: Install system dependencies (libgl1 + libglib2.0-0 for mineru)
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1 libglib2.0-0 tesseract-ocr tesseract-ocr-eng

      - name: Configure Hugging Face cache
        run: |
          mkdir -p "$HOME/.cache/huggingface/hub"
          {
            echo "HF_HOME=$HOME/.cache/huggingface"
            echo "HF_HUB_CACHE=$HOME/.cache/huggingface/hub"
            echo "MINERU_MODEL_SOURCE=huggingface"
            echo "HF_HUB_OFFLINE=1"
            echo "TRANSFORMERS_OFFLINE=1"
          } >> "$GITHUB_ENV"

      - name: Download MinerU model cache
        uses: actions/download-artifact@v8
        with:
          name: mineru-model-cache-${{ github.run_id }}
          path: ${{ runner.temp }}/mineru-model-cache

      - name: Unpack MinerU model cache
        run: |
          set -euo pipefail
          mkdir -p "$HOME/.cache/huggingface"
          tar -C "$HOME" -xf "$RUNNER_TEMP/mineru-model-cache/mineru-models.tar"
          test -f "$HOME/mineru.json" || { echo "mineru.json not restored from cache artifact"; exit 1; }
          du -sh "$HOME/.cache/huggingface/hub" || true

      - name: Log MinerU offline env and restored cache state
        if: always()
        run: |
          set -uo pipefail
          echo "=== MinerU offline env ==="
          echo "HF_HOME=${HF_HOME:-<unset>}"
          echo "HF_HUB_CACHE=${HF_HUB_CACHE:-<unset>}"
          echo "HF_HUB_OFFLINE=${HF_HUB_OFFLINE:-<unset>}"
          echo "TRANSFORMERS_OFFLINE=${TRANSFORMERS_OFFLINE:-<unset>}"
          echo "MINERU_MODEL_SOURCE=${MINERU_MODEL_SOURCE:-<unset>}"
          echo "=== ~/mineru.json (drives the offline model short-circuit; absence => gcore fetch hang) ==="
          cat "$HOME/mineru.json" || echo "!! mineru.json absent"
          echo "=== HF hub cache contents ==="
          ls -1 "$HOME/.cache/huggingface/hub" 2>/dev/null || echo "!! hub dir absent"
          du -sh "$HOME/.cache/huggingface/hub" 2>/dev/null || true

      - name: Run benchmark
        env:
          FRAMEWORK: mineru
          MODE: ${{ matrix.mode }}
          OUTPUT_FORMAT: ${{ matrix.output_format }}
          ITERATIONS: ${{ env.ITERATIONS }}
          TIMEOUT: ${{ github.event.inputs.timeout }}
          COHORT: tools/benchmark-harness/cohorts/${{ matrix.cohort }}.json
          UV_NO_SYNC: "1"
          # Belt-and-suspenders: cap any stray unbounded network call (offline) so it
          # fails fast and loud instead of hanging the full timeout. Bench-only, so the
          # prefetch job's large online model downloads are unaffected.
          MINERU_NET_TIMEOUT: "60"
        run: scripts/benchmarks/run-benchmark.sh

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-mineru-${{ matrix.output_format }}-${{ matrix.mode }}-${{ matrix.cohort }}-${{ github.run_id }}
          path: benchmark-results/
          retention-days: 30

  bench-liteparse:
    name: liteparse (${{ matrix.output_format }}, ${{ matrix.mode }}, ${{ matrix.cohort }})
    needs: [setup, bench-rust]
    if: ${{ needs.setup.result == 'success' && !cancelled() }}
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 360
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        mode: [single-file, batch]
        output_format: [markdown, plaintext]
        cohort: [native-pdf-fast-b8, ocr-pdf-fast-b4]
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}

          submodules: recursive
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Download benchmark harness binary
        uses: actions/download-artifact@v8
        with:
          name: benchmark-harness-binary
          path: target/release

      - name: Restore benchmark binary permissions
        run: scripts/benchmarks/restore-binary-permissions.sh

      # Bench cohorts span pdf/office/markup/ebook/email/data/images across the
      # xberg + 6 competitor jobs -- no glob subset is safe here. ~keep
      - name: Fetch test_documents fixtures
        uses: xberg-io/actions/fetch-test-documents@v1

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      - name: Install system dependencies (libheif + native libs for harness binary)
        uses: ./.github/actions/install-system-deps

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Setup Rust toolchain
        uses: xberg-io/actions/setup-rust@v1
        with:
          cache-key-prefix: benchmarks-liteparse
          use-sccache: "true"

      - name: Install system tesseract (liteparse default feature)
        run: |
          sudo apt-get update
          sudo apt-get install -y tesseract-ocr tesseract-ocr-eng libtesseract-dev libleptonica-dev

      - name: Install liteparse CLI (lit)
        run: |
          cargo install liteparse --version 2.9.0 --locked
          lit --version

      - name: Run benchmark
        env:
          FRAMEWORK: liteparse
          MODE: ${{ matrix.mode }}
          OUTPUT_FORMAT: ${{ matrix.output_format }}
          ITERATIONS: ${{ env.ITERATIONS }}
          TIMEOUT: ${{ github.event.inputs.timeout }}
          COHORT: tools/benchmark-harness/cohorts/${{ matrix.cohort }}.json
        run: scripts/benchmarks/run-benchmark.sh

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-liteparse-${{ matrix.output_format }}-${{ matrix.mode }}-${{ matrix.cohort }}-${{ github.run_id }}
          path: benchmark-results/
          retention-days: 30

  aggregate-and-publish:
    name: Aggregate & Release Results
    needs:
      [
        setup,
        prefetch-mineru-models,
        bench-rust,
        bench-docling,
        bench-markitdown,
        bench-unstructured,
        bench-tika,
        bench-pymupdf4llm,
        bench-mineru,
        bench-liteparse,
      ]
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: write
    # MinerU prefetch is a HARD gate: if it fails, do not publish a partial
    # baseline -- skip aggregation so the whole run fails. Other frameworks stay
    # soft (always() tolerates their failures); only prefetch is mandatory.
    if: ${{ always() && !cancelled() && needs.prefetch-mineru-models.result == 'success' && (github.ref == 'refs/heads/main' || github.event_name == 'workflow_dispatch') }}
    steps:
      - name: Create GitHub App token (if credentials available)
        id: app-token
        # Optional: BOT_APP_ID/BOT_APP_PRIVATE_KEY may be unset (e.g. forked runs), and the
        # token is only used to widen downstream write permissions, not required for the
        # aggregation itself. Losing it degrades to default GITHUB_TOKEN, it does not mask a
        # benchmark result. ~keep
        continue-on-error: true
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.BOT_APP_ID }}
          private-key: ${{ secrets.BOT_APP_PRIVATE_KEY }}

      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha || github.sha }}
          submodules: recursive

      # validate-artifacts/consolidate resolve each fixture's ground_truth_file, which
      # points into the vendor test_documents corpus (quality scoring is on: MEASURE_QUALITY).
      # Cohorts span nearly every format family, so no glob subset is safe here. ~keep
      - name: Fetch test_documents fixtures
        uses: xberg-io/actions/fetch-test-documents@v1

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      # Reuse the harness binary + xberg-cli that `setup` already built and
      # uploaded (benchmarks-target) instead of cold-rebuilding them here -- the
      # xberg-cli is only needed so its on-disk size can be measured, and the
      # harness binary runs validate-artifacts/consolidate. Same approach as
      # bench-rust. The runtime still needs the native libs + ORT to execute the
      # downloaded binary, but not a Rust toolchain or a compile.
      - name: Download build artifacts (harness binary + xberg-cli)
        if: needs.setup.result == 'success'
        uses: actions/download-artifact@v8
        with:
          name: ${{ needs.setup.outputs.artifact-name }}
          path: target/release

      - name: Restore benchmark binary permissions
        if: needs.setup.result == 'success'
        run: scripts/benchmarks/restore-binary-permissions.sh

      - name: Install system dependencies (libheif + native libs for harness binary)
        if: needs.setup.result == 'success'
        uses: ./.github/actions/install-system-deps

      - name: Setup ONNX Runtime
        if: needs.setup.result == 'success'
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Download all benchmark artifacts
        # Some cohort jobs are allowed to fail independently (this job's own `if: always()`
        # gate above tolerates that) and produce no benchmarks-*-<run_id> artifact, so the
        # wildcard pattern can legitimately match nothing for a subset of cohorts.
        # "Partition artifacts", "Log downloaded benchmark artifact inventory", and
        # "Validate benchmark artifacts" below all explicitly handle missing per-cohort
        # directories and fail the job if a cohort that should have produced results didn't. ~keep
        continue-on-error: true
        uses: actions/download-artifact@v8
        with:
          pattern: "benchmarks-*-${{ github.run_id }}"
          path: benchmark-artifacts/_all/
          merge-multiple: false

      - name: Partition artifacts into per-cohort directories
        if: always()
        run: |
          set -euo pipefail
          RUN_ID="${{ github.run_id }}"
          # CliCohort -> cohort-manifest basename. No basename is a suffix of another, so a
          # single trailing match assigns each artifact directory to exactly one cohort.
          declare -A COHORT_BASENAME=(
            [native]=native-pdf-fast-b8
            [ocr]=ocr-pdf-fast-b4
            [office]=native-office-fast
            [markup]=native-markup-fast
            [ebook]=native-ebook-fast
            [email]=native-email-fast
            [data]=native-data-fast
            [images]=ocr-images-fast
          )
          shopt -s nullglob
          for dir in benchmark-artifacts/_all/*/; do
            name="$(basename "$dir")"
            stem="${name%-"$RUN_ID"}"
            for clicohort in "${!COHORT_BASENAME[@]}"; do
              base="${COHORT_BASENAME[$clicohort]}"
              if [[ "$stem" == *-"$base" ]]; then
                mkdir -p "benchmark-artifacts/$clicohort"
                mv "$dir" "benchmark-artifacts/$clicohort/$name"
                break
              fi
            done
          done

      - name: Log downloaded benchmark artifact inventory
        if: always()
        run: |
          set -uo pipefail
          for cohort in native ocr office markup ebook email data images; do
            root="benchmark-artifacts/${cohort}"
            echo "=== ${cohort} artifacts (${root}) ==="
            if [ ! -d "$root" ]; then
              echo "!! no artifact directory (all ${cohort} jobs missing?)"
              continue
            fi
            find "$root" -mindepth 1 -maxdepth 1 -type d | sort | while read -r dir; do
              n_results=$(find "$dir" -name results.json | wc -l | tr -d ' ')
              n_prov=$(find "$dir" -name provenance.json | wc -l | tr -d ' ')
              echo "  $(basename "$dir"): results.json=${n_results} provenance.json=${n_prov}"
            done
          done

      - name: Validate benchmark artifacts (all cohorts)
        id: validate-artifacts
        if: always()
        run: |
          set -euo pipefail
          declare -A COHORT_BASENAME=(
            [native]=native-pdf-fast-b8
            [ocr]=ocr-pdf-fast-b4
            [office]=native-office-fast
            [markup]=native-markup-fast
            [ebook]=native-ebook-fast
            [email]=native-email-fast
            [data]=native-data-fast
            [images]=ocr-images-fast
          )
          for clicohort in native ocr office markup ebook email data images; do
            base="${COHORT_BASENAME[$clicohort]}"
            # mkdir so a cohort whose required producer emitted nothing yields the validator's
            # "missing artifacts" contract error rather than an opaque read_dir failure.
            mkdir -p "benchmark-artifacts/$clicohort"
            echo "=== validate-artifacts: $clicohort ($base) ==="
            ./target/release/benchmark-harness validate-artifacts \
              --cohort "$clicohort" \
              --artifacts-dir "benchmark-artifacts/$clicohort" \
              --cohort-manifest "tools/benchmark-harness/cohorts/$base.json" \
              --fixtures-root tools/benchmark-harness/fixtures \
              --source-sha '${{ needs.setup.outputs.source-sha }}' \
              --run-id '${{ github.run_id }}' \
              --iterations '${{ env.ITERATIONS }}'
          done

      - name: Consolidate results (all cohorts)
        if: ${{ always() && steps.validate-artifacts.outcome == 'success' }}
        run: |
          set -euo pipefail
          for clicohort in native ocr office markup ebook email data images; do
            INPUTS=$(find "benchmark-artifacts/$clicohort" -mindepth 1 -maxdepth 1 -type d | sort | paste -sd, -)
            ./target/release/benchmark-harness consolidate \
              --inputs "$INPUTS" \
              --output "consolidated-output/$clicohort/"
          done

      - name: Validate aggregates (all cohorts)
        id: validate-aggregates
        if: ${{ always() && steps.validate-artifacts.outcome == 'success' }}
        run: |
          set -euo pipefail
          for clicohort in native ocr office markup ebook email data images; do
            # Pass --fixtures-root explicitly (as the raw-artifact step above does): without it the
            # binary falls back to a compile-time env!("CARGO_MANIFEST_DIR") path baked on the
            # bench-rust build runner (/home/runner/_work/...), which does not exist on this runner.
            ./target/release/benchmark-harness validate-artifacts \
              --cohort "$clicohort" \
              --fixtures-root tools/benchmark-harness/fixtures \
              --aggregated-file "consolidated-output/$clicohort/aggregated.json"
          done

      - name: Prepare cohort release assets
        id: prepare-release-assets
        if: ${{ always() && steps.validate-aggregates.outcome == 'success' }}
        run: |
          set -euo pipefail
          mkdir -p release-assets
          SOURCE_SHA="${{ needs.setup.outputs.source-sha }}"
          UPDATED_AT="$(date -u +"%Y-%m-%dT%H:%M:%SZ")"
          RUN_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          RUN_ID="${{ github.run_id }}"
          # Per-cohort manifest name, BLAKE3, and required/optional matrix key counts come
          # straight from the pinned bench_matrix contract via `cohort-contract`, so the release
          # metadata can never drift from the validator (no hand-copied hashes/counts here).
          index_cohorts='{}'
          for clicohort in native ocr office markup ebook email data images; do
            cp "consolidated-output/$clicohort/aggregated.json" "release-assets/benchmarks-$clicohort-aggregated.json"
            contract="$(./target/release/benchmark-harness cohort-contract --cohort "$clicohort")"
            jq -n \
              --arg cohort "$(jq -r '.manifest_name' <<<"$contract")" \
              --arg cohort_hash "$(jq -r '.manifest_blake3' <<<"$contract")" \
              --argjson expected "$(jq -r '.expected_matrix_keys' <<<"$contract")" \
              --argjson optional "$(jq -r '.optional_matrix_keys' <<<"$contract")" \
              --arg commit "$SOURCE_SHA" \
              --arg run_id "$RUN_ID" \
              --arg run_url "$RUN_URL" \
              --arg updated_at "$UPDATED_AT" \
              '{cohort: $cohort, cohort_manifest_blake3: $cohort_hash, expected_matrix_keys: $expected, optional_matrix_keys: $optional, commit: $commit, run_id: $run_id, run_url: $run_url, updated_at: $updated_at}' \
              > "release-assets/benchmarks-$clicohort-metadata.json"
            index_cohorts="$(jq \
              --arg k "$clicohort" \
              --arg agg "benchmarks-$clicohort-aggregated.json" \
              --arg meta "benchmarks-$clicohort-metadata.json" \
              '.[$k] = {aggregated: $agg, metadata: $meta}' <<<"$index_cohorts")"
          done
          jq -n \
            --arg commit "$SOURCE_SHA" \
            --arg run_id "$RUN_ID" \
            --arg run_url "$RUN_URL" \
            --arg updated_at "$UPDATED_AT" \
            --argjson cohorts "$index_cohorts" \
            '{schema_version: 1, commit: $commit, run_id: $run_id, run_url: $run_url, updated_at: $updated_at, cohorts: $cohorts}' \
            > release-assets/benchmarks-index.json

      - name: Log aggregation gate status
        if: always()
        run: |
          echo "=== Upstream job results ==="
          echo "setup=${{ needs.setup.result }}"
          echo "bench-rust=${{ needs.bench-rust.result }}"
          echo "bench-docling=${{ needs.bench-docling.result }}"
          echo "bench-markitdown=${{ needs.bench-markitdown.result }}"
          echo "bench-unstructured=${{ needs.bench-unstructured.result }}"
          echo "bench-tika=${{ needs.bench-tika.result }}"
          echo "bench-pymupdf4llm=${{ needs.bench-pymupdf4llm.result }}"
          echo "bench-mineru=${{ needs.bench-mineru.result }} (optional; NOT a release gate)"
          echo "bench-liteparse=${{ needs.bench-liteparse.result }}"
          echo "=== Aggregation step outcomes ==="
          echo "validate-artifacts=${{ steps.validate-artifacts.outcome }}"
          echo "validate-aggregates=${{ steps.validate-aggregates.outcome }}"
          echo "prepare-release-assets=${{ steps.prepare-release-assets.outcome }}"

      - name: Create GitHub Release with benchmark results
        # Publication gates on the required producer jobs (setup + bench-rust; xberg is required in
        # every cohort) AND on validate-artifacts, which enforces the full contract. The validator
        # is the real gate for competitors: the PDF cohorts' competitor cells (docling, liteparse,
        # tika, markitdown, unstructured, pymupdf4llm) are REQUIRED, so a missing one fails
        # validate-artifacts and blocks the release even though we dropped the per-competitor
        # `needs.*.result` gates. Only the competitor cells on the heterogeneous family cohorts are
        # best-effort (optional in the contract): a framework that cannot read one member format of
        # a family fails its own job without blocking the baseline, and its partial results still
        # flow into the aggregate as non-gating data.
        if: >-
          ${{
            always() &&
            steps.validate-artifacts.outcome == 'success' &&
            steps.validate-aggregates.outcome == 'success' &&
            steps.prepare-release-assets.outcome == 'success' &&
            needs.setup.result == 'success' &&
            needs.bench-rust.result == 'success'
          }}
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token || secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          echo "=== Creating GitHub Release with benchmark results ==="

          SOURCE_SHA="${{ needs.setup.outputs.source-sha }}"
          SHORT_SHA="${SOURCE_SHA}"
          SHORT_SHA="${SHORT_SHA:0:7}"
          TAG="benchmark-run-${{ github.run_id }}"
          DATE=$(date -u +"%Y-%m-%d")

          gh release create "$TAG" \
            --prerelease \
            --target "${SOURCE_SHA}" \
            --title "Benchmark Results ${DATE} (${SHORT_SHA})" \
            --notes "Comparative benchmark results from workflow run [${{ github.run_id }}](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}).

          **Commit:** ${SOURCE_SHA}
          **Date:** ${DATE}" \
            release-assets/*.json
