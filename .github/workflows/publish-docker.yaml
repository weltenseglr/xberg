name: Publish Docker Images

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to build (e.g., v4.3.6)"
        required: true
        type: string
      dry_run:
        description: "Prepare artifacts without publishing"
        required: false
        type: boolean
        default: false
      ref:
        description: "Git ref (branch, tag, or commit) to build; defaults to the tag"
        required: false
        type: string
      force_republish:
        description: "Force re-publish even if artifacts already exist"
        required: false
        type: boolean
        default: false
  # Fire the instant the GitHub Release is created (publish.yaml creates it
  # non-draft, so `created` fires up front) — this workflow then builds and
  # publishes images in parallel with, and independent of, publish.yaml. Do NOT
  # use `published`: that only fires when a draft is un-drafted at finalize,
  # which would serialize Docker behind the entire publish run. ~keep
  release:
    types: [created]
  repository_dispatch:
    types: [publish-docker]

concurrency:
  group: ${{ github.workflow }}-${{ (github.event_name == 'workflow_dispatch' && (github.event.inputs.ref || github.event.inputs.tag)) || github.ref || github.run_id }}
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always
  ORT_VERSION: "1.24.2"
  MACOSX_DEPLOYMENT_TARGET: "14.0"
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

permissions:
  contents: read

jobs:
  prepare:
    name: Prepare metadata
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
      version: ${{ steps.meta.outputs.version }}
      ref: ${{ steps.meta.outputs.ref }}
      dry_run: ${{ steps.meta.outputs.dry_run }}
      force_republish: ${{ steps.meta.outputs.force_republish }}
      checkout_ref: ${{ steps.meta.outputs.checkout_ref }}
      target_sha: ${{ steps.meta.outputs.target_sha }}
      is_tag: ${{ steps.meta.outputs.is_tag }}
      is_prerelease: ${{ steps.meta.outputs.is_prerelease }}
      release_docker: ${{ steps.meta.outputs.release_docker }}
    steps:
      - name: Checkout code (default)
        uses: actions/checkout@v6

      - name: Resolve release metadata
        id: meta
        uses: xberg-io/actions/prepare-release-metadata@v1
        with:
          tag: ${{ inputs.tag || github.event.client_payload.tag || github.event.release.tag_name }}
          ref: ${{ inputs.ref || github.event.client_payload.ref }}
          available-targets: docker
          dry-run: ${{ inputs.dry_run }}
          force-republish: ${{ inputs.force_republish }}

      - name: Re-checkout at target ref
        if: ${{ steps.meta.outputs.checkout_ref != '' }}
        uses: actions/checkout@v6
        with:
          ref: ${{ steps.meta.outputs.checkout_ref }}
          fetch-depth: 0
          submodules: recursive

      - name: Show metadata
        env:
          META_TAG: ${{ steps.meta.outputs.tag }}
          META_VERSION: ${{ steps.meta.outputs.version }}
          META_REF: ${{ steps.meta.outputs.ref }}
          META_DRY_RUN: ${{ steps.meta.outputs.dry_run }}
          META_FORCE_REPUBLISH: ${{ steps.meta.outputs.force_republish }}
          META_CHECKOUT_REF: ${{ steps.meta.outputs.checkout_ref }}
          META_TARGET_SHA: ${{ steps.meta.outputs.target_sha }}
          META_IS_TAG: ${{ steps.meta.outputs.is_tag }}
          META_RELEASE_DOCKER: ${{ steps.meta.outputs.release_docker }}
        run: |
          {
            echo "## Release Metadata"
            echo "- **Tag**: \`$META_TAG\`"
            echo "- **Version**: \`$META_VERSION\`"
            echo "- **Ref**: \`$META_REF\`"
            echo "- **Dry Run**: \`$META_DRY_RUN\`"
            echo "- **Force Republish**: \`$META_FORCE_REPUBLISH\`"
            echo "- **Checkout Ref**: \`$META_CHECKOUT_REF\`"
            echo "- **Target SHA**: \`$META_TARGET_SHA\`"
            echo "- **Is Tag**: \`$META_IS_TAG\`"
            echo "- **Release Docker**: \`$META_RELEASE_DOCKER\`"
          } >> "$GITHUB_STEP_SUMMARY"

  check-docker:
    name: Check if Docker image tag exists
    needs: prepare
    if: ${{ needs.prepare.outputs.release_docker == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    outputs:
      core_exists: ${{ steps.core.outputs.exists }}
      full_exists: ${{ steps.full.outputs.exists }}
      omni_exists: ${{ steps.omni.outputs.exists }}
      cli_exists: ${{ steps.cli.outputs.exists }}
      builder_exists: ${{ steps.builder.outputs.exists }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.prepare.outputs.tag }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Check core image tag
        id: core
        env:
          DOCKER_TAG: ghcr.io/xberg-io/xberg:${{ needs.prepare.outputs.version }}-core
          SUMMARY_LABEL: core
        run: scripts/publish/check-docker-tag.sh

      - name: Check full image tag
        id: full
        env:
          DOCKER_TAG: ghcr.io/xberg-io/xberg:${{ needs.prepare.outputs.version }}
          SUMMARY_LABEL: full
        run: scripts/publish/check-docker-tag.sh

      - name: Check CLI image tag
        id: cli
        env:
          DOCKER_TAG: ghcr.io/xberg-io/xberg-cli:${{ needs.prepare.outputs.version }}
          SUMMARY_LABEL: cli
        run: scripts/publish/check-docker-tag.sh

      - name: Check omni image tag
        id: omni
        env:
          DOCKER_TAG: ghcr.io/xberg-io/xberg:${{ needs.prepare.outputs.version }}-omni
          SUMMARY_LABEL: omni
        run: scripts/publish/check-docker-tag.sh

      - name: Check builder image tag
        id: builder
        env:
          DOCKER_TAG: ghcr.io/xberg-io/xberg:${{ needs.prepare.outputs.version }}-builder
          SUMMARY_LABEL: builder
        run: scripts/publish/check-docker-tag.sh

  build-docker:
    name: Build ${{ matrix.variant }} (${{ matrix.platform.name }})
    needs:
      - prepare
      - check-docker
    runs-on: ${{ matrix.platform.runner }}
    # cli/amd64 (cross-compiled Rust+ONNX from source) hit the 90m ceiling in rc.36 and was
    # CANCELLED; give the from-source builds headroom. merge-docker is decoupled below so even
    # a timed-out leg no longer suppresses the core manifest publish (#1299). ~keep
    timeout-minutes: 120
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        variant: [core, full, omni, cli, builder]
        platform:
          - { name: amd64, docker: linux/amd64, runner: ubuntu-latest }
          - { name: arm64, docker: linux/arm64, runner: ubuntu-24.04-arm }
        include:
          - { variant: core, dockerfile: "docker/Dockerfile", target: core, image: ghcr.io/xberg-io/xberg, tag_suffix: "-core" }
          - { variant: full, dockerfile: "docker/Dockerfile", target: full, image: ghcr.io/xberg-io/xberg, tag_suffix: "" }
          - { variant: omni, dockerfile: "docker/Dockerfile", target: omni, image: ghcr.io/xberg-io/xberg, tag_suffix: "-omni" }
          - { variant: cli, dockerfile: "docker/Dockerfile.cli", target: "", image: ghcr.io/xberg-io/xberg-cli, tag_suffix: "" }
          - { variant: builder, dockerfile: "docker/Dockerfile", target: builder, image: ghcr.io/xberg-io/xberg, tag_suffix: "-builder" }
    if: ${{ needs.prepare.outputs.release_docker == 'true' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}
          fetch-depth: 0
          submodules: recursive

      - name: Free up disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Ensure target commit
        if: ${{ needs.prepare.outputs.target_sha != '' }}
        run: git checkout --progress --force ${{ needs.prepare.outputs.target_sha }}

      # scripts/ci/docker/test_docker.py bind-mounts test_documents at /data and exercises
      # /data/{pdf,docx,images,doc}/* fixtures across the core/full/cli container tests. ~keep
      - name: Fetch test_documents fixtures
        uses: xberg-io/actions/fetch-test-documents@v1
        with:
          include: |
            pdf/**
            docx/**
            images/**
            doc/**

      - name: Decide whether to build
        id: gate
        env:
          FORCE: ${{ needs.prepare.outputs.force_republish }}
          VARIANT: ${{ matrix.variant }}
          CORE_EXISTS: ${{ needs.check-docker.outputs.core_exists }}
          FULL_EXISTS: ${{ needs.check-docker.outputs.full_exists }}
          OMNI_EXISTS: ${{ needs.check-docker.outputs.omni_exists }}
          CLI_EXISTS: ${{ needs.check-docker.outputs.cli_exists }}
          BUILDER_EXISTS: ${{ needs.check-docker.outputs.builder_exists }}
        run: |
          go=false
          if [ "$FORCE" = "true" ]; then
            go=true
          else
            case "$VARIANT" in
              core) if [ "$CORE_EXISTS" != "true" ]; then go=true; fi ;;
              full) if [ "$FULL_EXISTS" != "true" ]; then go=true; fi ;;
              omni) if [ "$OMNI_EXISTS" != "true" ]; then go=true; fi ;;
              cli)  if [ "$CLI_EXISTS"  != "true" ]; then go=true; fi ;;
              builder) if [ "$BUILDER_EXISTS" != "true" ]; then go=true; fi ;;
            esac
          fi
          echo "go=$go" >> "$GITHUB_OUTPUT"
          if [ "$go" != "true" ]; then
            echo "Docker tag already exists for variant $VARIANT; skipping build." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Set up Docker Buildx
        if: ${{ steps.gate.outputs.go == 'true' }}
        uses: docker/setup-buildx-action@v4

      - name: Build test image
        if: ${{ steps.gate.outputs.go == 'true' && matrix.variant != 'builder' }}
        run: |
          TARGET_ARG=""
          if [ -n "${{ matrix.target }}" ]; then TARGET_ARG="--target ${{ matrix.target }}"; fi
          docker build $TARGET_ARG -f ${{ matrix.dockerfile }} --platform ${{ matrix.platform.docker }} --build-arg ONNXRUNTIME_VERSION=${{ env.ORT_VERSION }} -t xberg-publish:${{ matrix.variant }}-test .

      - name: Run Docker tests
        if: ${{ steps.gate.outputs.go == 'true' && matrix.variant != 'builder' }}
        run: python3 scripts/ci/docker/test_docker.py --image xberg-publish:${{ matrix.variant }}-test --variant ${{ matrix.variant }} --verbose

      - name: Log in to GitHub Container Registry
        if: ${{ steps.gate.outputs.go == 'true' && needs.prepare.outputs.dry_run != 'true' }}
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push by digest
        id: build
        if: ${{ steps.gate.outputs.go == 'true' && needs.prepare.outputs.dry_run != 'true' }}
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          target: ${{ matrix.target }}
          platforms: ${{ matrix.platform.docker }}
          build-args: |
            ONNXRUNTIME_VERSION=${{ env.ORT_VERSION }}
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.description=Xberg document intelligence - ${{ matrix.variant }} variant
            org.opencontainers.image.licenses=MIT
          outputs: type=image,name=${{ matrix.image }},push-by-digest=true,name-canonical=true,push=true
          cache-from: type=gha,scope=publish-docker-${{ matrix.variant }}-${{ matrix.platform.name }}
          # ignore-error: the image is already pushed by digest above; a transient
          # GitHub Actions cache-export failure (buildx `not_found`) must not fail the
          # whole build/publish. ~keep
          cache-to: type=gha,mode=max,ignore-error=true,scope=publish-docker-${{ matrix.variant }}-${{ matrix.platform.name }}

      - name: Export digest
        if: ${{ steps.gate.outputs.go == 'true' && needs.prepare.outputs.dry_run != 'true' }}
        run: |
          mkdir -p /tmp/digests
          digest="${{ steps.build.outputs.digest }}"
          touch "/tmp/digests/${digest#sha256:}"

      - name: Upload digest
        if: ${{ steps.gate.outputs.go == 'true' && needs.prepare.outputs.dry_run != 'true' }}
        uses: actions/upload-artifact@v7
        with:
          name: digest-${{ matrix.variant }}-${{ matrix.platform.name }}
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1

      - name: Docker dry-run summary
        if: ${{ steps.gate.outputs.go == 'true' && needs.prepare.outputs.dry_run == 'true' && matrix.platform.name == 'amd64' }}
        env:
          IMAGE: ${{ matrix.image }}
          VERSION: ${{ needs.prepare.outputs.version }}
          TAG_SUFFIX: ${{ matrix.tag_suffix }}
        run: scripts/publish/docker/dry-run-summary.sh

      - name: Clean up local Docker images
        if: ${{ always() }}
        run: docker rmi xberg-publish:${{ matrix.variant }}-test || true

  merge-docker:
    name: Publish ${{ matrix.variant }} manifest
    needs:
      - prepare
      - check-docker
      - build-docker
    # always(): a cancelled/failed sibling build leg (e.g. cli hitting the timeout) must NOT
    # skip the whole merge — the core/full digests that DID build still need their manifests
    # pushed (rc.36 #1299: one cancelled cli/amd64 leg suppressed the -core image entirely).
    # A per-variant leg with no digests skips cleanly below, so this matrix stays green and
    # publish-helm still runs. ~keep
    if: ${{ always() && needs.prepare.outputs.release_docker == 'true' && needs.prepare.outputs.dry_run != 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - { variant: core, image: ghcr.io/xberg-io/xberg, tag_suffix: "-core", extra_tag: "core" }
          - { variant: full, image: ghcr.io/xberg-io/xberg, tag_suffix: "", extra_tag: "latest" }
          - { variant: omni, image: ghcr.io/xberg-io/xberg, tag_suffix: "-omni", extra_tag: "omni" }
          - { variant: cli, image: ghcr.io/xberg-io/xberg-cli, tag_suffix: "", extra_tag: "latest" }
          - { variant: builder, image: ghcr.io/xberg-io/xberg, tag_suffix: "-builder", extra_tag: "builder" }
    steps:
      - name: Decide whether to publish
        id: gate
        env:
          FORCE: ${{ needs.prepare.outputs.force_republish }}
          VARIANT: ${{ matrix.variant }}
          CORE_EXISTS: ${{ needs.check-docker.outputs.core_exists }}
          FULL_EXISTS: ${{ needs.check-docker.outputs.full_exists }}
          OMNI_EXISTS: ${{ needs.check-docker.outputs.omni_exists }}
          CLI_EXISTS: ${{ needs.check-docker.outputs.cli_exists }}
          BUILDER_EXISTS: ${{ needs.check-docker.outputs.builder_exists }}
        run: |
          go=false
          if [ "$FORCE" = "true" ]; then
            go=true
          else
            case "$VARIANT" in
              core) if [ "$CORE_EXISTS" != "true" ]; then go=true; fi ;;
              full) if [ "$FULL_EXISTS" != "true" ]; then go=true; fi ;;
              omni) if [ "$OMNI_EXISTS" != "true" ]; then go=true; fi ;;
              cli)  if [ "$CLI_EXISTS"  != "true" ]; then go=true; fi ;;
              builder) if [ "$BUILDER_EXISTS" != "true" ]; then go=true; fi ;;
            esac
          fi
          echo "go=$go" >> "$GITHUB_OUTPUT"

      - name: Download digests
        if: ${{ steps.gate.outputs.go == 'true' }}
        # A variant whose build legs were all cancelled/failed (rc.36 #1299) has zero
        # matching digest-* artifacts, which makes download-artifact error even though that
        # is a legitimate "skip this variant" outcome. The very next step (Verify digests
        # present) is the real gate: it counts what came down and fails loudly if the count
        # is nonzero-but-incomplete, so a genuine partial failure still fails the job. ~keep
        continue-on-error: true
        uses: actions/download-artifact@v8
        with:
          path: /tmp/digests
          pattern: digest-${{ matrix.variant }}-*
          merge-multiple: true

      # Only push this variant's manifest if its build legs actually produced digests. A
      # variant whose build was cancelled/failed (e.g. cli on timeout) has none — skip it
      # cleanly (leg still succeeds) so the core/full manifests publish and the matrix stays
      # green for publish-helm. ~keep
      - name: Verify digests present
        id: check
        if: ${{ steps.gate.outputs.go == 'true' }}
        # A multi-arch manifest needs BOTH platform digests (amd64 + arm64). If only one
        # leg produced a digest, publishing would silently ship a single-arch manifest
        # (#1315: -core alternated amd64-only / arm64-only whenever one leg's gha cache
        # export failed and took the whole build leg down). Fail loudly instead. Zero
        # digests means the entire variant build was cancelled (rc.36 #1299) — skip it
        # cleanly so the other variants still publish. ~keep
        run: |
          count=$(find /tmp/digests -type f 2>/dev/null | wc -l | tr -d ' ')
          if [ "$count" -ge 2 ]; then
            echo "push=true" >> "$GITHUB_OUTPUT"
          elif [ "$count" -eq 1 ]; then
            echo "::error::variant '${{ matrix.variant }}' produced only 1 platform digest (expected 2: amd64+arm64) — refusing to publish a single-arch manifest (#1315). A build-docker leg failed; inspect the build jobs."
            exit 1
          else
            echo "::warning::no digests for variant '${{ matrix.variant }}' — its build leg did not complete; skipping its manifest publish (other variants unaffected)"
            echo "push=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Log in to GitHub Container Registry
        if: ${{ steps.check.outputs.push == 'true' }}
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        if: ${{ steps.check.outputs.push == 'true' }}
        uses: docker/setup-buildx-action@v4

      - name: Extract Docker metadata
        id: docker_meta
        if: ${{ steps.check.outputs.push == 'true' }}
        uses: docker/metadata-action@v6
        with:
          images: ${{ matrix.image }}
          tags: |
            type=raw,value=${{ needs.prepare.outputs.version }}${{ matrix.tag_suffix }}
            type=raw,value=${{ matrix.extra_tag }},enable=${{ needs.prepare.outputs.is_prerelease != 'true' }}

      - name: Create and push manifest
        if: ${{ steps.check.outputs.push == 'true' }}
        working-directory: /tmp/digests
        run: |
          docker buildx imagetools create \
            $(jq -cr '.tags | map("-t " + .) | join(" ")' <<< "$DOCKER_METADATA_OUTPUT_JSON") \
            $(printf '${{ matrix.image }}@sha256:%s ' *)

      - name: Inspect published manifest
        if: ${{ steps.check.outputs.push == 'true' }}
        run: docker buildx imagetools inspect ${{ matrix.image }}:${{ needs.prepare.outputs.version }}${{ matrix.tag_suffix }}

  # Publish the Helm chart only after the image manifests are pushed, so the
  # chart's image reference (ghcr.io/xberg-io/xberg:<version>) already resolves.
  # Delegates to the reusable publish-helm.yaml (sign + Artifact Hub metadata). ~keep
  publish-helm:
    name: Publish Helm chart
    needs:
      - prepare
      - merge-docker
    if: ${{ needs.prepare.outputs.release_docker == 'true' && needs.prepare.outputs.dry_run != 'true' }}
    permissions:
      contents: read
      packages: write
      id-token: write
    uses: ./.github/workflows/publish-helm.yaml
    with:
      version: ${{ needs.prepare.outputs.version }}
      dry_run: false
    secrets: inherit
