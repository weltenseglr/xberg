// This file is auto-generated by alef. DO NOT EDIT.
// Re-generate with: alef generate
#![allow(dead_code, unused_imports, unused_variables, unused_mut, noop_method_call)]
#![allow(missing_docs)]
#![allow(unsafe_op_in_unsafe_fn, unsafe_attr_outside_unsafe)]
#![allow(
    clippy::too_many_arguments,
    clippy::let_unit_value,
    clippy::needless_borrow,
    clippy::redundant_locals,
    dropping_references,
    clippy::unnecessary_cast,
    clippy::unused_unit,
    clippy::unwrap_or_default,
    clippy::derivable_impls,
    clippy::needless_borrows_for_generic_args,
    clippy::unnecessary_fallible_conversions,
    clippy::useless_conversion,
    clippy::type_complexity,
    clippy::clone_on_copy
)]
#![allow(
    clippy::missing_safety_doc,
    clippy::doc_lazy_continuation,
    clippy::doc_overindented_list_items
)]

use std::cell::RefCell;
use std::ffi::c_void;
use std::ffi::{CStr, CString, c_char};
use std::sync::Arc;
use xberg::Validator;
use xberg::engine::seams::PresetResolver;

thread_local! {
    static LAST_ERROR_CODE: RefCell<i32> = const { RefCell::new(0) };
    static LAST_ERROR_CONTEXT: RefCell<Option<CString>> = const { RefCell::new(None) };
    static LAST_RETURN_LENGTHS: RefCell<std::collections::BTreeMap<&'static str, usize>> =
        const { RefCell::new(std::collections::BTreeMap::new()) };
}

const ALEF_FFI_CONVERSION_ERROR: i32 = 1;
const ALEF_FFI_CORE_ERROR: i32 = 2;
const ALEF_FFI_PANIC_ERROR: i32 = 3;

fn alef_ffi_error_code(error: &dyn std::any::Any) -> i32 {
    if let Some(error) = error.downcast_ref::<xberg::error::XbergError>() {
        #[allow(unreachable_patterns)]
        return match error {
            xberg::error::XbergError::Io(..) => 2080723056,
            xberg::error::XbergError::Parsing { .. } => 191686562,
            xberg::error::XbergError::Ocr { .. } => 1744734248,
            xberg::error::XbergError::Validation { .. } => 2093146707,
            xberg::error::XbergError::Cache { .. } => 1740547354,
            xberg::error::XbergError::ImageProcessing { .. } => 1703658036,
            xberg::error::XbergError::Serialization { .. } => 486450251,
            xberg::error::XbergError::MissingDependency(..) => 1024192803,
            xberg::error::XbergError::Plugin { .. } => 1609738224,
            xberg::error::XbergError::LockPoisoned(..) => 1092832257,
            xberg::error::XbergError::UnsupportedFormat(..) => 299497109,
            xberg::error::XbergError::Embedding { .. } => 1958776963,
            xberg::error::XbergError::Reranking { .. } => 1326275648,
            xberg::error::XbergError::Transcription { .. } => 552614954,
            xberg::error::XbergError::Timeout { .. } => 1036077712,
            xberg::error::XbergError::Cancelled => 524666238,
            xberg::error::XbergError::Security { .. } => 670246576,
            xberg::error::XbergError::Other(..) => 1318675240,
            _ => ALEF_FFI_CORE_ERROR,
        };
    }
    if let Some(error) = error.downcast_ref::<xberg::heuristics::error::HeuristicsError>() {
        #[allow(unreachable_patterns)]
        return match error {
            xberg::heuristics::error::HeuristicsError::ConfigError(..) => 91451556,
            xberg::heuristics::error::HeuristicsError::PdfAnalysisError(..) => 1713417319,
            _ => ALEF_FFI_CORE_ERROR,
        };
    }
    if let Some(error) = error.downcast_ref::<xberg::presets::loader::LoadError>() {
        #[allow(unreachable_patterns)]
        return match error {
            xberg::presets::loader::LoadError::Parse { .. } => 949858217,
            xberg::presets::loader::LoadError::SchemaValidation { .. } => 380113526,
            xberg::presets::loader::LoadError::Deserialize { .. } => 2093946153,
            xberg::presets::loader::LoadError::IdMismatch { .. } => 2137750517,
            xberg::presets::loader::LoadError::BadMetaSchema(..) => 1285484759,
            xberg::presets::loader::LoadError::Io(..) => 973217713,
            _ => ALEF_FFI_CORE_ERROR,
        };
    }
    if let Some(error) = error.downcast_ref::<xberg::presets::resolve::ResolveError>() {
        #[allow(unreachable_patterns)]
        return match error {
            xberg::presets::resolve::ResolveError::SchemaNotObject => 1772672798,
            _ => ALEF_FFI_CORE_ERROR,
        };
    }
    ALEF_FFI_CORE_ERROR
}

fn set_last_error(code: i32, message: &str) {
    LAST_ERROR_CODE.with_borrow_mut(|c| *c = code);
    LAST_ERROR_CONTEXT.with_borrow_mut(|c| *c = CString::new(message).ok());
}

fn clear_last_error() {
    LAST_ERROR_CODE.with_borrow_mut(|c| *c = 0);
    LAST_ERROR_CONTEXT.with_borrow_mut(|c| *c = None);
}

fn set_panic_error() {
    set_last_error(ALEF_FFI_PANIC_ERROR, "Rust panic contained at FFI boundary");
}

fn catch_ffi_panic<T>(fallback: T, body: impl FnOnce() -> T) -> T {
    clear_last_error();
    catch_ffi_panic_preserving_error(fallback, body)
}

fn catch_ffi_panic_preserving_error<T>(fallback: T, body: impl FnOnce() -> T) -> T {
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(body)) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            fallback
        }
    }
}

fn set_last_return_len(function: &'static str, len: usize) {
    LAST_RETURN_LENGTHS.with_borrow_mut(|lengths| {
        lengths.insert(function, len);
    });
}

fn last_return_len(function: &'static str) -> usize {
    LAST_RETURN_LENGTHS.with_borrow(|lengths| lengths.get(function).copied().unwrap_or(0))
}

/// Return the last error code (0 means no error).
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// This function does not allocate and returns no owned pointer.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_last_error_code() -> i32 {
    catch_ffi_panic_preserving_error(ALEF_FFI_PANIC_ERROR, || LAST_ERROR_CODE.with_borrow(|c| *c))
}

/// Return the last error message. The pointer is borrowed and valid until the next FFI call on this thread.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// The returned pointer is borrowed from thread-local storage and must NOT be freed.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_last_error_context() -> *const c_char {
    catch_ffi_panic_preserving_error(std::ptr::null(), || {
        LAST_ERROR_CONTEXT.with_borrow(|ctx| ctx.as_ref().map_or(std::ptr::null(), |c| c.as_ptr()))
    })
}

type AlefHandle = u64;
const ALEF_INVALID_HANDLE_ERROR: i32 = 4;
const HANDLE_INDEX_MASK: u64 = u32::MAX as u64;

#[derive(Debug)]
enum HandleError {
    InvalidZero,
    UnknownSlot,
    StaleGeneration,
    WrongType,
    RegistryPoisoned,
    HandleBusy,
    AliasedHandle,
}

impl std::fmt::Display for HandleError {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let message = match self {
            Self::InvalidZero => "zero is not a valid handle",
            Self::UnknownSlot => "handle refers to an unknown slot",
            Self::StaleGeneration => "handle generation is stale",
            Self::WrongType => "handle has the wrong type",
            Self::RegistryPoisoned => "handle registry lock is poisoned",
            Self::HandleBusy => "handle is currently in use",
            Self::AliasedHandle => "the same handle was passed more than once",
        };
        formatter.write_str(message)
    }
}

type ErasedHandleValue = std::sync::Arc<std::sync::Mutex<Box<dyn std::any::Any + Send>>>;

struct HandleSlot {
    generation: u32,
    value: Option<ErasedHandleValue>,
}

#[derive(Default)]
struct HandleRegistry {
    slots: Vec<HandleSlot>,
}

impl HandleRegistry {
    fn insert<T: std::any::Any + Send>(&mut self, value: T) -> AlefHandle {
        let entry = std::sync::Arc::new(std::sync::Mutex::new(Box::new(value) as Box<dyn std::any::Any + Send>));
        if let Some((index, slot)) = self.slots.iter_mut().enumerate().find(|(_, slot)| slot.value.is_none()) {
            slot.value = Some(entry);
            return encode_handle(index, slot.generation);
        }
        self.slots.push(HandleSlot {
            generation: 1,
            value: Some(entry),
        });
        encode_handle(self.slots.len() - 1, 1)
    }

    fn get(&self, handle: AlefHandle) -> Result<ErasedHandleValue, HandleError> {
        let (index, generation) = decode_handle(handle)?;
        let slot = self.slots.get(index).ok_or(HandleError::UnknownSlot)?;
        if slot.generation != generation {
            return Err(HandleError::StaleGeneration);
        }
        slot.value.clone().ok_or(HandleError::StaleGeneration)
    }

    fn remove<T: std::any::Any + Send>(&mut self, handle: AlefHandle) -> Result<(), HandleError> {
        drop(self.take::<T>(handle)?);
        Ok(())
    }

    fn take<T: std::any::Any + Send>(&mut self, handle: AlefHandle) -> Result<T, HandleError> {
        let (index, generation) = decode_handle(handle)?;
        let slot = self.slots.get_mut(index).ok_or(HandleError::UnknownSlot)?;
        if slot.generation != generation {
            return Err(HandleError::StaleGeneration);
        }
        let value = slot.value.as_ref().ok_or(HandleError::StaleGeneration)?;
        let guard = value.lock().map_err(|_| HandleError::RegistryPoisoned)?;
        if !guard.is::<T>() {
            return Err(HandleError::WrongType);
        }
        drop(guard);
        let value = slot.value.take().ok_or(HandleError::StaleGeneration)?;
        let mutex = std::sync::Arc::try_unwrap(value).map_err(|value| {
            slot.value = Some(value);
            HandleError::HandleBusy
        })?;
        slot.generation = next_generation(slot.generation);
        let boxed = mutex.into_inner().map_err(|_| HandleError::RegistryPoisoned)?;
        boxed
            .downcast::<T>()
            .map(|value| *value)
            .map_err(|_| HandleError::WrongType)
    }
}

static HANDLE_REGISTRY: std::sync::OnceLock<std::sync::Mutex<HandleRegistry>> = std::sync::OnceLock::new();

fn handle_registry() -> &'static std::sync::Mutex<HandleRegistry> {
    HANDLE_REGISTRY.get_or_init(|| std::sync::Mutex::new(HandleRegistry::default()))
}

fn insert_handle<T: std::any::Any + Send>(value: T) -> Result<AlefHandle, HandleError> {
    let mut registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
    Ok(registry.insert(value))
}

fn with_handle<T: std::any::Any + Send, R>(
    handle: AlefHandle,
    operation: impl FnOnce(&T) -> R,
) -> Result<R, HandleError> {
    let value = {
        let registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
        registry.get(handle)?
    };
    let guard = value.lock().map_err(|_| HandleError::RegistryPoisoned)?;
    let typed = guard.downcast_ref::<T>().ok_or(HandleError::WrongType)?;
    Ok(operation(typed))
}

struct HandleRequest {
    handle: AlefHandle,
    expected_type: std::any::TypeId,
}

fn acquire_handles(requests: &[HandleRequest]) -> Result<Vec<(AlefHandle, ErasedHandleValue)>, HandleError> {
    let mut ordered = requests.iter().collect::<Vec<_>>();
    ordered.sort_by_key(|request| request.handle);
    if ordered.windows(2).any(|pair| pair[0].handle == pair[1].handle) {
        return Err(HandleError::AliasedHandle);
    }
    let registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
    let mut values = Vec::with_capacity(ordered.len());
    for request in ordered {
        let value = registry.get(request.handle)?;
        let guard = value.lock().map_err(|_| HandleError::RegistryPoisoned)?;
        if guard.as_ref().type_id() != request.expected_type {
            return Err(HandleError::WrongType);
        }
        drop(guard);
        values.push((request.handle, value));
    }
    Ok(values)
}

fn locked_handle_ptr<T: std::any::Any + Send>(
    guards: &mut [(AlefHandle, std::sync::MutexGuard<'_, Box<dyn std::any::Any + Send>>)],
    handle: AlefHandle,
) -> Result<*mut T, HandleError> {
    let (_, guard) = guards
        .iter_mut()
        .find(|(candidate, _)| *candidate == handle)
        .ok_or(HandleError::UnknownSlot)?;
    guard
        .downcast_mut::<T>()
        .map(|value| value as *mut T)
        .ok_or(HandleError::WrongType)
}

fn with_handle_mut<T: std::any::Any + Send, R>(
    handle: AlefHandle,
    operation: impl FnOnce(&mut T) -> R,
) -> Result<R, HandleError> {
    let value = {
        let registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
        registry.get(handle)?
    };
    let mut guard = value.lock().map_err(|_| HandleError::RegistryPoisoned)?;
    let typed = guard.downcast_mut::<T>().ok_or(HandleError::WrongType)?;
    Ok(operation(typed))
}

fn remove_handle<T: std::any::Any + Send>(handle: AlefHandle) -> Result<(), HandleError> {
    let mut registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
    registry.remove::<T>(handle)
}

fn take_handle<T: std::any::Any + Send>(handle: AlefHandle) -> Result<T, HandleError> {
    let mut registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
    registry.take::<T>(handle)
}

fn encode_handle(index: usize, generation: u32) -> AlefHandle {
    ((generation as u64) << 32) | ((index as u64) + 1)
}

fn decode_handle(handle: AlefHandle) -> Result<(usize, u32), HandleError> {
    if handle == 0 {
        return Err(HandleError::InvalidZero);
    }
    let encoded_index = handle & HANDLE_INDEX_MASK;
    if encoded_index == 0 {
        return Err(HandleError::UnknownSlot);
    }
    Ok(((encoded_index - 1) as usize, (handle >> 32) as u32))
}

fn next_generation(generation: u32) -> u32 {
    let next = generation.wrapping_add(1);
    if next == 0 { 1 } else { next }
}

fn set_handle_error(error: &HandleError) {
    set_last_error(ALEF_INVALID_HANDLE_ERROR, &error.to_string());
}

/// Free a string previously returned by this library.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_free_string(ptr: *mut c_char) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by CString::into_raw; caller ensures no aliases.
            unsafe {
                drop(CString::from_raw(ptr));
            }
        }
    })
}

/// Return the library version string. The pointer is static and must NOT be freed.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_version() -> *const c_char {
    catch_ffi_panic(std::ptr::null(), || {
        static VERSION: &str = concat!(env!("CARGO_PKG_VERSION"), "\0");
        VERSION.as_ptr() as *const c_char
    })
}

#[allow(dead_code)]
fn execution_provider_type_from_i32_rs(v: i32) -> Option<xberg::ExecutionProviderType> {
    match v {
        0 => Some(xberg::ExecutionProviderType::Auto),
        1 => Some(xberg::ExecutionProviderType::Cpu),
        2 => Some(xberg::ExecutionProviderType::CoreMl),
        3 => Some(xberg::ExecutionProviderType::Cuda),
        4 => Some(xberg::ExecutionProviderType::TensorRt),
        _ => None,
    }
}

#[allow(dead_code)]
fn extract_input_kind_from_i32_rs(v: i32) -> Option<xberg::ExtractInputKind> {
    match v {
        0 => Some(xberg::ExtractInputKind::Bytes),
        1 => Some(xberg::ExtractInputKind::Uri),
        _ => None,
    }
}

#[allow(dead_code)]
fn url_extraction_mode_from_i32_rs(v: i32) -> Option<xberg::UrlExtractionMode> {
    match v {
        0 => Some(xberg::UrlExtractionMode::Auto),
        1 => Some(xberg::UrlExtractionMode::Document),
        2 => Some(xberg::UrlExtractionMode::Crawl),
        _ => None,
    }
}

#[allow(dead_code)]
fn breadcrumb_target_from_i32_rs(v: i32) -> Option<xberg::BreadcrumbTarget> {
    match v {
        0 => Some(xberg::BreadcrumbTarget::Content),
        1 => Some(xberg::BreadcrumbTarget::Metadata),
        _ => None,
    }
}

#[allow(dead_code)]
fn jupyter_cell_rendering_from_i32_rs(v: i32) -> Option<xberg::JupyterCellRendering> {
    match v {
        0 => Some(xberg::JupyterCellRendering::Source),
        1 => Some(xberg::JupyterCellRendering::Outputs),
        2 => Some(xberg::JupyterCellRendering::Both),
        _ => None,
    }
}

#[allow(dead_code)]
fn html_theme_from_i32_rs(v: i32) -> Option<xberg::HtmlTheme> {
    match v {
        0 => Some(xberg::HtmlTheme::Default),
        1 => Some(xberg::HtmlTheme::GitHub),
        2 => Some(xberg::HtmlTheme::Dark),
        3 => Some(xberg::HtmlTheme::Light),
        4 => Some(xberg::HtmlTheme::Unstyled),
        _ => None,
    }
}

#[allow(dead_code)]
fn table_model_from_i32_rs(v: i32) -> Option<xberg::TableModel> {
    match v {
        0 => Some(xberg::TableModel::Tatr),
        1 => Some(xberg::TableModel::SlanetWired),
        2 => Some(xberg::TableModel::SlanetWireless),
        3 => Some(xberg::TableModel::SlanetPlus),
        4 => Some(xberg::TableModel::SlanetAuto),
        5 => Some(xberg::TableModel::Disabled),
        _ => None,
    }
}

#[allow(dead_code)]
fn table_overlap_preference_from_i32_rs(v: i32) -> Option<xberg::core::config::layout::TableOverlapPreference> {
    match v {
        0 => Some(xberg::core::config::layout::TableOverlapPreference::Content),
        1 => Some(xberg::core::config::layout::TableOverlapPreference::Native),
        2 => Some(xberg::core::config::layout::TableOverlapPreference::Layout),
        _ => None,
    }
}

#[allow(dead_code)]
fn layout_strategy_from_i32_rs(v: i32) -> Option<xberg::LayoutStrategy> {
    match v {
        0 => Some(xberg::LayoutStrategy::Always),
        1 => Some(xberg::LayoutStrategy::Auto),
        _ => None,
    }
}

#[allow(dead_code)]
fn call_mode_from_i32_rs(v: i32) -> Option<xberg::CallMode> {
    match v {
        0 => Some(xberg::CallMode::TextOnly),
        1 => Some(xberg::CallMode::VisionOnly),
        2 => Some(xberg::CallMode::TextPlusVision),
        _ => None,
    }
}

#[allow(dead_code)]
fn merge_mode_from_i32_rs(v: i32) -> Option<xberg::MergeMode> {
    match v {
        0 => Some(xberg::MergeMode::ObjectMerge),
        1 => Some(xberg::MergeMode::ArrayConcat),
        2 => Some(xberg::MergeMode::ObjectFirst),
        _ => None,
    }
}

#[allow(dead_code)]
fn ner_backend_kind_from_i32_rs(v: i32) -> Option<xberg::NerBackendKind> {
    match v {
        0 => Some(xberg::NerBackendKind::Onnx),
        1 => Some(xberg::NerBackendKind::Llm),
        _ => None,
    }
}

#[allow(dead_code)]
fn table_chunking_mode_from_i32_rs(v: i32) -> Option<xberg::TableChunkingMode> {
    match v {
        0 => Some(xberg::TableChunkingMode::Split),
        1 => Some(xberg::TableChunkingMode::RepeatHeader),
        _ => None,
    }
}

#[allow(dead_code)]
fn chunker_type_from_i32_rs(v: i32) -> Option<xberg::ChunkerType> {
    match v {
        0 => Some(xberg::ChunkerType::Text),
        1 => Some(xberg::ChunkerType::Markdown),
        2 => Some(xberg::ChunkerType::Yaml),
        3 => Some(xberg::ChunkerType::Semantic),
        _ => None,
    }
}

#[allow(dead_code)]
fn reranker_head_from_i32_rs(v: i32) -> Option<xberg::RerankerHead> {
    match v {
        0 => Some(xberg::RerankerHead::CrossEncoder),
        1 => Some(xberg::RerankerHead::Qwen3Generative),
        _ => None,
    }
}

#[allow(dead_code)]
fn whisper_model_from_i32_rs(v: i32) -> Option<xberg::WhisperModel> {
    match v {
        0 => Some(xberg::WhisperModel::Tiny),
        1 => Some(xberg::WhisperModel::Base),
        2 => Some(xberg::WhisperModel::Small),
        3 => Some(xberg::WhisperModel::Medium),
        4 => Some(xberg::WhisperModel::LargeV3),
        _ => None,
    }
}

#[allow(dead_code)]
fn code_content_mode_from_i32_rs(v: i32) -> Option<xberg::CodeContentMode> {
    match v {
        0 => Some(xberg::CodeContentMode::Chunks),
        1 => Some(xberg::CodeContentMode::Raw),
        2 => Some(xberg::CodeContentMode::Structure),
        _ => None,
    }
}

#[allow(dead_code)]
fn list_type_from_i32_rs(v: i32) -> Option<xberg::extraction::transform::ListType> {
    match v {
        0 => Some(xberg::extraction::transform::ListType::Bullet),
        1 => Some(xberg::extraction::transform::ListType::Numbered),
        2 => Some(xberg::extraction::transform::ListType::Lettered),
        3 => Some(xberg::extraction::transform::ListType::Indented),
        _ => None,
    }
}

#[allow(dead_code)]
fn ocr_backend_type_from_i32_rs(v: i32) -> Option<xberg::OcrBackendType> {
    match v {
        0 => Some(xberg::OcrBackendType::Tesseract),
        1 => Some(xberg::OcrBackendType::PaddleOCR),
        2 => Some(xberg::OcrBackendType::Candle),
        3 => Some(xberg::OcrBackendType::Custom),
        _ => None,
    }
}

#[allow(dead_code)]
fn processing_stage_from_i32_rs(v: i32) -> Option<xberg::ProcessingStage> {
    match v {
        0 => Some(xberg::ProcessingStage::Early),
        1 => Some(xberg::ProcessingStage::Middle),
        2 => Some(xberg::ProcessingStage::Late),
        _ => None,
    }
}

#[allow(dead_code)]
fn reduction_level_from_i32_rs(v: i32) -> Option<xberg::ReductionLevel> {
    match v {
        0 => Some(xberg::ReductionLevel::Off),
        1 => Some(xberg::ReductionLevel::Light),
        2 => Some(xberg::ReductionLevel::Moderate),
        3 => Some(xberg::ReductionLevel::Aggressive),
        4 => Some(xberg::ReductionLevel::Maximum),
        _ => None,
    }
}

#[allow(dead_code)]
fn pdf_annotation_type_from_i32_rs(v: i32) -> Option<xberg::PdfAnnotationType> {
    match v {
        0 => Some(xberg::PdfAnnotationType::Text),
        1 => Some(xberg::PdfAnnotationType::Highlight),
        2 => Some(xberg::PdfAnnotationType::Link),
        3 => Some(xberg::PdfAnnotationType::Stamp),
        4 => Some(xberg::PdfAnnotationType::Underline),
        5 => Some(xberg::PdfAnnotationType::StrikeOut),
        6 => Some(xberg::PdfAnnotationType::Squiggly),
        7 => Some(xberg::PdfAnnotationType::Ink),
        8 => Some(xberg::PdfAnnotationType::Square),
        9 => Some(xberg::PdfAnnotationType::Circle),
        10 => Some(xberg::PdfAnnotationType::Polygon),
        11 => Some(xberg::PdfAnnotationType::PolyLine),
        12 => Some(xberg::PdfAnnotationType::Line),
        13 => Some(xberg::PdfAnnotationType::Caret),
        14 => Some(xberg::PdfAnnotationType::FileAttachment),
        15 => Some(xberg::PdfAnnotationType::Sound),
        16 => Some(xberg::PdfAnnotationType::Movie),
        17 => Some(xberg::PdfAnnotationType::Other),
        _ => None,
    }
}

#[allow(dead_code)]
fn block_type_from_i32_rs(v: i32) -> Option<xberg::BlockType> {
    match v {
        0 => Some(xberg::BlockType::Paragraph),
        1 => Some(xberg::BlockType::Heading),
        2 => Some(xberg::BlockType::Blockquote),
        3 => Some(xberg::BlockType::CodeBlock),
        4 => Some(xberg::BlockType::ListItem),
        5 => Some(xberg::BlockType::OrderedList),
        6 => Some(xberg::BlockType::BulletList),
        7 => Some(xberg::BlockType::TaskList),
        8 => Some(xberg::BlockType::DefinitionList),
        9 => Some(xberg::BlockType::DefinitionTerm),
        10 => Some(xberg::BlockType::DefinitionDescription),
        11 => Some(xberg::BlockType::Div),
        12 => Some(xberg::BlockType::Section),
        13 => Some(xberg::BlockType::ThematicBreak),
        14 => Some(xberg::BlockType::RawBlock),
        15 => Some(xberg::BlockType::MathDisplay),
        _ => None,
    }
}

#[allow(dead_code)]
fn inline_type_from_i32_rs(v: i32) -> Option<xberg::InlineType> {
    match v {
        0 => Some(xberg::InlineType::Text),
        1 => Some(xberg::InlineType::Strong),
        2 => Some(xberg::InlineType::Emphasis),
        3 => Some(xberg::InlineType::Highlight),
        4 => Some(xberg::InlineType::Subscript),
        5 => Some(xberg::InlineType::Superscript),
        6 => Some(xberg::InlineType::Insert),
        7 => Some(xberg::InlineType::Delete),
        8 => Some(xberg::InlineType::Code),
        9 => Some(xberg::InlineType::Link),
        10 => Some(xberg::InlineType::Image),
        11 => Some(xberg::InlineType::Span),
        12 => Some(xberg::InlineType::Math),
        13 => Some(xberg::InlineType::RawInline),
        14 => Some(xberg::InlineType::FootnoteRef),
        15 => Some(xberg::InlineType::Symbol),
        _ => None,
    }
}

#[allow(dead_code)]
fn relationship_kind_from_i32_rs(v: i32) -> Option<xberg::RelationshipKind> {
    match v {
        0 => Some(xberg::RelationshipKind::FootnoteReference),
        1 => Some(xberg::RelationshipKind::CitationReference),
        2 => Some(xberg::RelationshipKind::InternalLink),
        3 => Some(xberg::RelationshipKind::Caption),
        4 => Some(xberg::RelationshipKind::Label),
        5 => Some(xberg::RelationshipKind::TocEntry),
        6 => Some(xberg::RelationshipKind::CrossReference),
        _ => None,
    }
}

#[allow(dead_code)]
fn content_layer_from_i32_rs(v: i32) -> Option<xberg::ContentLayer> {
    match v {
        0 => Some(xberg::ContentLayer::Body),
        1 => Some(xberg::ContentLayer::Header),
        2 => Some(xberg::ContentLayer::Footer),
        3 => Some(xberg::ContentLayer::Footnote),
        _ => None,
    }
}

#[allow(dead_code)]
fn extraction_method_from_i32_rs(v: i32) -> Option<xberg::ExtractionMethod> {
    match v {
        0 => Some(xberg::ExtractionMethod::Native),
        1 => Some(xberg::ExtractionMethod::Ocr),
        2 => Some(xberg::ExtractionMethod::Mixed),
        _ => None,
    }
}

#[allow(dead_code)]
fn chunk_type_from_i32_rs(v: i32) -> Option<xberg::ChunkType> {
    match v {
        0 => Some(xberg::ChunkType::Heading),
        1 => Some(xberg::ChunkType::PartyList),
        2 => Some(xberg::ChunkType::Definitions),
        3 => Some(xberg::ChunkType::OperativeClause),
        4 => Some(xberg::ChunkType::SignatureBlock),
        5 => Some(xberg::ChunkType::Schedule),
        6 => Some(xberg::ChunkType::TableLike),
        7 => Some(xberg::ChunkType::Formula),
        8 => Some(xberg::ChunkType::CodeBlock),
        9 => Some(xberg::ChunkType::Function),
        10 => Some(xberg::ChunkType::Class),
        11 => Some(xberg::ChunkType::Module),
        12 => Some(xberg::ChunkType::Image),
        13 => Some(xberg::ChunkType::OrgChart),
        14 => Some(xberg::ChunkType::Diagram),
        15 => Some(xberg::ChunkType::Unknown),
        _ => None,
    }
}

#[allow(dead_code)]
fn image_kind_from_i32_rs(v: i32) -> Option<xberg::ImageKind> {
    match v {
        0 => Some(xberg::ImageKind::Photograph),
        1 => Some(xberg::ImageKind::Diagram),
        2 => Some(xberg::ImageKind::Chart),
        3 => Some(xberg::ImageKind::Drawing),
        4 => Some(xberg::ImageKind::TextBlock),
        5 => Some(xberg::ImageKind::Decoration),
        6 => Some(xberg::ImageKind::Logo),
        7 => Some(xberg::ImageKind::Icon),
        8 => Some(xberg::ImageKind::TileFragment),
        9 => Some(xberg::ImageKind::Mask),
        10 => Some(xberg::ImageKind::PageRaster),
        11 => Some(xberg::ImageKind::Unknown),
        _ => None,
    }
}

#[allow(dead_code)]
fn result_format_from_i32_rs(v: i32) -> Option<xberg::ResultFormat> {
    match v {
        0 => Some(xberg::ResultFormat::Unified),
        1 => Some(xberg::ResultFormat::ElementBased),
        _ => None,
    }
}

#[allow(dead_code)]
fn element_type_from_i32_rs(v: i32) -> Option<xberg::ElementType> {
    match v {
        0 => Some(xberg::ElementType::Title),
        1 => Some(xberg::ElementType::NarrativeText),
        2 => Some(xberg::ElementType::Heading),
        3 => Some(xberg::ElementType::ListItem),
        4 => Some(xberg::ElementType::Table),
        5 => Some(xberg::ElementType::Image),
        6 => Some(xberg::ElementType::PageBreak),
        7 => Some(xberg::ElementType::CodeBlock),
        8 => Some(xberg::ElementType::BlockQuote),
        9 => Some(xberg::ElementType::Footer),
        10 => Some(xberg::ElementType::Header),
        _ => None,
    }
}

#[allow(dead_code)]
fn form_field_type_from_i32_rs(v: i32) -> Option<xberg::FormFieldType> {
    match v {
        0 => Some(xberg::FormFieldType::Text),
        1 => Some(xberg::FormFieldType::Checkbox),
        2 => Some(xberg::FormFieldType::Radio),
        3 => Some(xberg::FormFieldType::Choice),
        4 => Some(xberg::FormFieldType::Signature),
        5 => Some(xberg::FormFieldType::Button),
        6 => Some(xberg::FormFieldType::Unknown),
        _ => None,
    }
}

#[allow(dead_code)]
fn code_data_node_kind_from_i32_rs(v: i32) -> Option<xberg::CodeDataNodeKind> {
    match v {
        0 => Some(xberg::CodeDataNodeKind::KeyValue),
        1 => Some(xberg::CodeDataNodeKind::Element),
        2 => Some(xberg::CodeDataNodeKind::Sequence),
        _ => None,
    }
}

#[allow(dead_code)]
fn text_direction_from_i32_rs(v: i32) -> Option<xberg::TextDirection> {
    match v {
        0 => Some(xberg::TextDirection::LeftToRight),
        1 => Some(xberg::TextDirection::RightToLeft),
        2 => Some(xberg::TextDirection::Auto),
        _ => None,
    }
}

#[allow(dead_code)]
fn link_type_from_i32_rs(v: i32) -> Option<xberg::LinkType> {
    match v {
        0 => Some(xberg::LinkType::Anchor),
        1 => Some(xberg::LinkType::Internal),
        2 => Some(xberg::LinkType::External),
        3 => Some(xberg::LinkType::Email),
        4 => Some(xberg::LinkType::Phone),
        5 => Some(xberg::LinkType::Other),
        _ => None,
    }
}

#[allow(dead_code)]
fn image_type_from_i32_rs(v: i32) -> Option<xberg::ImageType> {
    match v {
        0 => Some(xberg::ImageType::DataUri),
        1 => Some(xberg::ImageType::InlineSvg),
        2 => Some(xberg::ImageType::External),
        3 => Some(xberg::ImageType::Relative),
        _ => None,
    }
}

#[allow(dead_code)]
fn structured_data_type_from_i32_rs(v: i32) -> Option<xberg::StructuredDataType> {
    match v {
        0 => Some(xberg::StructuredDataType::JsonLd),
        1 => Some(xberg::StructuredDataType::Microdata),
        2 => Some(xberg::StructuredDataType::RDFa),
        _ => None,
    }
}

#[allow(dead_code)]
fn ocr_element_level_from_i32_rs(v: i32) -> Option<xberg::OcrElementLevel> {
    match v {
        0 => Some(xberg::OcrElementLevel::Word),
        1 => Some(xberg::OcrElementLevel::Line),
        2 => Some(xberg::OcrElementLevel::Block),
        3 => Some(xberg::OcrElementLevel::Page),
        _ => None,
    }
}

#[allow(dead_code)]
fn page_unit_type_from_i32_rs(v: i32) -> Option<xberg::PageUnitType> {
    match v {
        0 => Some(xberg::PageUnitType::Page),
        1 => Some(xberg::PageUnitType::Slide),
        2 => Some(xberg::PageUnitType::Sheet),
        _ => None,
    }
}

#[allow(dead_code)]
fn redaction_strategy_from_i32_rs(v: i32) -> Option<xberg::RedactionStrategy> {
    match v {
        0 => Some(xberg::RedactionStrategy::Mask),
        1 => Some(xberg::RedactionStrategy::Hash),
        2 => Some(xberg::RedactionStrategy::TokenReplace),
        3 => Some(xberg::RedactionStrategy::Drop),
        _ => None,
    }
}

#[allow(dead_code)]
fn revision_kind_from_i32_rs(v: i32) -> Option<xberg::RevisionKind> {
    match v {
        0 => Some(xberg::RevisionKind::Insertion),
        1 => Some(xberg::RevisionKind::Deletion),
        2 => Some(xberg::RevisionKind::FormatChange),
        3 => Some(xberg::RevisionKind::Comment),
        _ => None,
    }
}

#[allow(dead_code)]
fn summary_strategy_from_i32_rs(v: i32) -> Option<xberg::SummaryStrategy> {
    match v {
        0 => Some(xberg::SummaryStrategy::Extractive),
        1 => Some(xberg::SummaryStrategy::Abstractive),
        _ => None,
    }
}

#[allow(dead_code)]
fn uri_kind_from_i32_rs(v: i32) -> Option<xberg::UriKind> {
    match v {
        0 => Some(xberg::UriKind::Hyperlink),
        1 => Some(xberg::UriKind::Image),
        2 => Some(xberg::UriKind::Anchor),
        3 => Some(xberg::UriKind::Citation),
        4 => Some(xberg::UriKind::Reference),
        5 => Some(xberg::UriKind::Email),
        _ => None,
    }
}

#[allow(dead_code)]
fn region_kind_from_i32_rs(v: i32) -> Option<xberg::RegionKind> {
    match v {
        0 => Some(xberg::RegionKind::Figure),
        1 => Some(xberg::RegionKind::DenseTable),
        2 => Some(xberg::RegionKind::ComplexLayout),
        3 => Some(xberg::RegionKind::Caption),
        _ => None,
    }
}

#[allow(dead_code)]
fn embeddings_embedding_backend_from_i32_rs(v: i32) -> Option<xberg::embeddings::EmbeddingBackend> {
    match v {
        0 => Some(xberg::embeddings::EmbeddingBackend::Onnx),
        1 => Some(xberg::embeddings::EmbeddingBackend::Static),
        _ => None,
    }
}

#[allow(dead_code)]
fn keyword_algorithm_from_i32_rs(v: i32) -> Option<xberg::KeywordAlgorithm> {
    match v {
        0 => Some(xberg::KeywordAlgorithm::Yake),
        1 => Some(xberg::KeywordAlgorithm::Rake),
        _ => None,
    }
}

#[allow(dead_code)]
fn schema_compliance_from_i32_rs(v: i32) -> Option<xberg::SchemaCompliance> {
    match v {
        0 => Some(xberg::SchemaCompliance::AllValid),
        1 => Some(xberg::SchemaCompliance::PartialValid),
        2 => Some(xberg::SchemaCompliance::AllInvalid),
        _ => None,
    }
}

#[allow(dead_code)]
fn boundary_reason_from_i32_rs(v: i32) -> Option<xberg::BoundaryReason> {
    match v {
        0 => Some(xberg::BoundaryReason::Start),
        1 => Some(xberg::BoundaryReason::PageOneMarker),
        2 => Some(xberg::BoundaryReason::LetterheadReset),
        3 => Some(xberg::BoundaryReason::DensityShift),
        4 => Some(xberg::BoundaryReason::End),
        _ => None,
    }
}

#[allow(dead_code)]
fn preset_category_from_i32_rs(v: i32) -> Option<xberg::PresetCategory> {
    match v {
        0 => Some(xberg::PresetCategory::Finance),
        1 => Some(xberg::PresetCategory::Identity),
        2 => Some(xberg::PresetCategory::Legal),
        3 => Some(xberg::PresetCategory::Logistics),
        4 => Some(xberg::PresetCategory::Medical),
        5 => Some(xberg::PresetCategory::Hr),
        6 => Some(xberg::PresetCategory::Other),
        _ => None,
    }
}

#[allow(dead_code)]
fn psm_mode_from_i32_rs(v: i32) -> Option<xberg::PSMMode> {
    match v {
        0 => Some(xberg::PSMMode::OsdOnly),
        1 => Some(xberg::PSMMode::AutoOsd),
        2 => Some(xberg::PSMMode::AutoOnly),
        3 => Some(xberg::PSMMode::Auto),
        4 => Some(xberg::PSMMode::SingleColumn),
        5 => Some(xberg::PSMMode::SingleBlockVertical),
        6 => Some(xberg::PSMMode::SingleBlock),
        7 => Some(xberg::PSMMode::SingleLine),
        8 => Some(xberg::PSMMode::SingleWord),
        9 => Some(xberg::PSMMode::CircleWord),
        10 => Some(xberg::PSMMode::SingleChar),
        _ => None,
    }
}

#[allow(dead_code)]
fn probe_status_from_i32_rs(v: i32) -> Option<xberg::ProbeStatus> {
    match v {
        0 => Some(xberg::ProbeStatus::Pass),
        1 => Some(xberg::ProbeStatus::Warn),
        2 => Some(xberg::ProbeStatus::Fail),
        3 => Some(xberg::ProbeStatus::Skip),
        _ => None,
    }
}

#[allow(dead_code)]
fn paddle_inference_backend_from_i32_rs(v: i32) -> Option<xberg::PaddleInferenceBackend> {
    match v {
        0 => Some(xberg::PaddleInferenceBackend::Ort),
        1 => Some(xberg::PaddleInferenceBackend::Tract),
        _ => None,
    }
}

#[allow(dead_code)]
fn paddle_language_from_i32_rs(v: i32) -> Option<xberg::PaddleLanguage> {
    match v {
        0 => Some(xberg::PaddleLanguage::English),
        1 => Some(xberg::PaddleLanguage::Chinese),
        2 => Some(xberg::PaddleLanguage::Japanese),
        3 => Some(xberg::PaddleLanguage::Korean),
        4 => Some(xberg::PaddleLanguage::German),
        5 => Some(xberg::PaddleLanguage::French),
        6 => Some(xberg::PaddleLanguage::Latin),
        7 => Some(xberg::PaddleLanguage::Cyrillic),
        8 => Some(xberg::PaddleLanguage::TraditionalChinese),
        9 => Some(xberg::PaddleLanguage::Thai),
        10 => Some(xberg::PaddleLanguage::Greek),
        11 => Some(xberg::PaddleLanguage::EastSlavic),
        12 => Some(xberg::PaddleLanguage::Arabic),
        13 => Some(xberg::PaddleLanguage::Devanagari),
        14 => Some(xberg::PaddleLanguage::Tamil),
        15 => Some(xberg::PaddleLanguage::Telugu),
        _ => None,
    }
}

#[allow(dead_code)]
fn layout_class_from_i32_rs(v: i32) -> Option<xberg::LayoutClass> {
    match v {
        0 => Some(xberg::LayoutClass::Caption),
        1 => Some(xberg::LayoutClass::Chart),
        2 => Some(xberg::LayoutClass::Footnote),
        3 => Some(xberg::LayoutClass::Formula),
        4 => Some(xberg::LayoutClass::ListItem),
        5 => Some(xberg::LayoutClass::PageFooter),
        6 => Some(xberg::LayoutClass::PageHeader),
        7 => Some(xberg::LayoutClass::Picture),
        8 => Some(xberg::LayoutClass::SectionHeader),
        9 => Some(xberg::LayoutClass::Table),
        10 => Some(xberg::LayoutClass::Text),
        11 => Some(xberg::LayoutClass::Title),
        12 => Some(xberg::LayoutClass::DocumentIndex),
        13 => Some(xberg::LayoutClass::Code),
        14 => Some(xberg::LayoutClass::CheckboxSelected),
        15 => Some(xberg::LayoutClass::CheckboxUnselected),
        16 => Some(xberg::LayoutClass::Form),
        17 => Some(xberg::LayoutClass::KeyValueRegion),
        _ => None,
    }
}

#[allow(dead_code)]
fn browser_mode_from_i32_rs(v: i32) -> Option<crawlberg::BrowserMode> {
    match v {
        0 => Some(crawlberg::BrowserMode::Auto),
        1 => Some(crawlberg::BrowserMode::Always),
        2 => Some(crawlberg::BrowserMode::Never),
        3 => Some(crawlberg::BrowserMode::Stealth),
        _ => None,
    }
}

#[allow(dead_code)]
fn browser_wait_from_i32_rs(v: i32) -> Option<crawlberg::BrowserWait> {
    match v {
        0 => Some(crawlberg::BrowserWait::NetworkIdle),
        1 => Some(crawlberg::BrowserWait::Selector),
        2 => Some(crawlberg::BrowserWait::Fixed),
        _ => None,
    }
}

#[allow(dead_code)]
fn browser_backend_from_i32_rs(v: i32) -> Option<crawlberg::BrowserBackend> {
    match v {
        0 => Some(crawlberg::BrowserBackend::Chromiumoxide),
        1 => Some(crawlberg::BrowserBackend::Native),
        _ => None,
    }
}

#[allow(dead_code)]
fn document_content_encoding_from_i32_rs(v: i32) -> Option<crawlberg::DocumentContentEncoding> {
    match v {
        0 => Some(crawlberg::DocumentContentEncoding::Base64),
        _ => None,
    }
}

#[allow(dead_code)]
fn asset_category_from_i32_rs(v: i32) -> Option<crawlberg::AssetCategory> {
    match v {
        0 => Some(crawlberg::AssetCategory::Document),
        1 => Some(crawlberg::AssetCategory::Image),
        2 => Some(crawlberg::AssetCategory::Audio),
        3 => Some(crawlberg::AssetCategory::Video),
        4 => Some(crawlberg::AssetCategory::Font),
        5 => Some(crawlberg::AssetCategory::Stylesheet),
        6 => Some(crawlberg::AssetCategory::Script),
        7 => Some(crawlberg::AssetCategory::Archive),
        8 => Some(crawlberg::AssetCategory::Data),
        9 => Some(crawlberg::AssetCategory::Other),
        _ => None,
    }
}

#[allow(dead_code)]
fn preprocessing_preset_from_i32_rs(v: i32) -> Option<html_to_markdown_rs::PreprocessingPreset> {
    match v {
        0 => Some(html_to_markdown_rs::PreprocessingPreset::Minimal),
        1 => Some(html_to_markdown_rs::PreprocessingPreset::Standard),
        2 => Some(html_to_markdown_rs::PreprocessingPreset::Aggressive),
        _ => None,
    }
}

#[allow(dead_code)]
fn heading_style_from_i32_rs(v: i32) -> Option<html_to_markdown_rs::HeadingStyle> {
    match v {
        0 => Some(html_to_markdown_rs::HeadingStyle::Underlined),
        1 => Some(html_to_markdown_rs::HeadingStyle::Atx),
        2 => Some(html_to_markdown_rs::HeadingStyle::AtxClosed),
        _ => None,
    }
}

#[allow(dead_code)]
fn list_indent_type_from_i32_rs(v: i32) -> Option<html_to_markdown_rs::ListIndentType> {
    match v {
        0 => Some(html_to_markdown_rs::ListIndentType::Spaces),
        1 => Some(html_to_markdown_rs::ListIndentType::Tabs),
        _ => None,
    }
}

#[allow(dead_code)]
fn whitespace_mode_from_i32_rs(v: i32) -> Option<html_to_markdown_rs::WhitespaceMode> {
    match v {
        0 => Some(html_to_markdown_rs::WhitespaceMode::Normalized),
        1 => Some(html_to_markdown_rs::WhitespaceMode::Strict),
        _ => None,
    }
}

#[allow(dead_code)]
fn newline_style_from_i32_rs(v: i32) -> Option<html_to_markdown_rs::NewlineStyle> {
    match v {
        0 => Some(html_to_markdown_rs::NewlineStyle::Spaces),
        1 => Some(html_to_markdown_rs::NewlineStyle::Backslash),
        _ => None,
    }
}

#[allow(dead_code)]
fn code_block_style_from_i32_rs(v: i32) -> Option<html_to_markdown_rs::CodeBlockStyle> {
    match v {
        0 => Some(html_to_markdown_rs::CodeBlockStyle::Indented),
        1 => Some(html_to_markdown_rs::CodeBlockStyle::Backticks),
        2 => Some(html_to_markdown_rs::CodeBlockStyle::Tildes),
        _ => None,
    }
}

#[allow(dead_code)]
fn highlight_style_from_i32_rs(v: i32) -> Option<html_to_markdown_rs::HighlightStyle> {
    match v {
        0 => Some(html_to_markdown_rs::HighlightStyle::DoubleEqual),
        1 => Some(html_to_markdown_rs::HighlightStyle::Html),
        2 => Some(html_to_markdown_rs::HighlightStyle::Bold),
        3 => Some(html_to_markdown_rs::HighlightStyle::None),
        _ => None,
    }
}

#[allow(dead_code)]
fn link_style_from_i32_rs(v: i32) -> Option<html_to_markdown_rs::LinkStyle> {
    match v {
        0 => Some(html_to_markdown_rs::LinkStyle::Inline),
        1 => Some(html_to_markdown_rs::LinkStyle::Reference),
        _ => None,
    }
}

#[allow(dead_code)]
fn url_escape_style_from_i32_rs(v: i32) -> Option<html_to_markdown_rs::UrlEscapeStyle> {
    match v {
        0 => Some(html_to_markdown_rs::UrlEscapeStyle::Angle),
        1 => Some(html_to_markdown_rs::UrlEscapeStyle::Percent),
        _ => None,
    }
}

/// Create a `CacheStats` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_cache_stats_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_cache_stats_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::CacheStats>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CacheStats` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_cache_stats_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::CacheStats, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CacheStats` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_cache_stats_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::CacheStats>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `total_files` field from a `CacheStats`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_cache_stats_total_files(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CacheStats, _>(handle, |obj| obj.total_files);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `total_size_mb` field from a `CacheStats`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_cache_stats_total_size_mb(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::CacheStats, _>(handle, |obj| obj.total_size_mb);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `available_space_mb` field from a `CacheStats`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_cache_stats_available_space_mb(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::CacheStats, _>(handle, |obj| obj.available_space_mb);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `oldest_file_age_days` field from a `CacheStats`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_cache_stats_oldest_file_age_days(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::CacheStats, _>(handle, |obj| obj.oldest_file_age_days);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `newest_file_age_days` field from a `CacheStats`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_cache_stats_newest_file_age_days(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::CacheStats, _>(handle, |obj| obj.newest_file_age_days);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Create a `AccelerationConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_acceleration_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_acceleration_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::AccelerationConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `AccelerationConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_acceleration_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::AccelerationConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `AccelerationConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_acceleration_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::AccelerationConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `provider` field from a `AccelerationConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_execution_provider_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_acceleration_config_provider(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::AccelerationConfig, _>(handle, |obj| match insert_handle(obj.provider.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `device_id` field from a `AccelerationConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_acceleration_config_device_id(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::AccelerationConfig, _>(handle, |obj| obj.device_id);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `CaptioningConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_captioning_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_captioning_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::CaptioningConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CaptioningConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_captioning_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::CaptioningConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CaptioningConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_captioning_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::CaptioningConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `llm` field from a `CaptioningConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_llm_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_captioning_config_llm(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CaptioningConfig, _>(handle, |obj| match insert_handle(obj.llm.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `prompt` field from a `CaptioningConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_captioning_config_prompt(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CaptioningConfig, _>(handle, |obj| match &obj.prompt {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `min_image_area` field from a `CaptioningConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_captioning_config_min_image_area(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CaptioningConfig, _>(handle, |obj| obj.min_image_area);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ChunkClassificationDefinition` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_chunk_classification_definition_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_classification_definition_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ChunkClassificationDefinition>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ChunkClassificationDefinition` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_classification_definition_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ChunkClassificationDefinition, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ChunkClassificationDefinition` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_classification_definition_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ChunkClassificationDefinition>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `label` field from a `ChunkClassificationDefinition`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_classification_definition_label(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ChunkClassificationDefinition, _>(handle, |obj| {
            match CString::new(obj.label.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `description` field from a `ChunkClassificationDefinition`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_classification_definition_description(
    handle: AlefHandle,
) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ChunkClassificationDefinition, _>(handle, |obj| {
            match CString::new(obj.description.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ChunkClassificationConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_chunk_classification_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_classification_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ChunkClassificationConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ChunkClassificationConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_classification_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ChunkClassificationConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ChunkClassificationConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_classification_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ChunkClassificationConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `prompt_template` field from a `ChunkClassificationConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_classification_config_prompt_template(
    handle: AlefHandle,
) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ChunkClassificationConfig, _>(handle, |obj| match &obj.prompt_template {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `definitions` field from a `ChunkClassificationConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_classification_config_definitions(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ChunkClassificationConfig, _>(handle, |obj| {
            match serde_json::to_string(&obj.definitions) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `llm` field from a `ChunkClassificationConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_llm_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_classification_config_llm(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::ChunkClassificationConfig, _>(handle, |obj| match insert_handle(obj.llm.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `batch_size` field from a `ChunkClassificationConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_classification_config_batch_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkClassificationConfig, _>(handle, |obj| obj.batch_size);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_concurrency` field from a `ChunkClassificationConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_classification_config_max_concurrency(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkClassificationConfig, _>(handle, |obj| obj.max_concurrency);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `PageClassificationConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_page_classification_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_classification_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PageClassificationConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PageClassificationConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_classification_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PageClassificationConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PageClassificationConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_classification_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PageClassificationConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `prompt_template` field from a `PageClassificationConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_classification_config_prompt_template(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PageClassificationConfig, _>(handle, |obj| match &obj.prompt_template {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `labels` field from a `PageClassificationConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_classification_config_labels(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PageClassificationConfig, _>(handle, |obj| match serde_json::to_string(&obj.labels) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `multi_label` field from a `PageClassificationConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_classification_config_multi_label(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageClassificationConfig, _>(handle, |obj| obj.multi_label as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `llm` field from a `PageClassificationConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_llm_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_classification_config_llm(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::PageClassificationConfig, _>(handle, |obj| match insert_handle(obj.llm.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ContentFilterConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_content_filter_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_filter_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ContentFilterConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ContentFilterConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_filter_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ContentFilterConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ContentFilterConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_filter_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ContentFilterConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `include_headers` field from a `ContentFilterConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_filter_config_include_headers(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ContentFilterConfig, _>(handle, |obj| obj.include_headers as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `include_footers` field from a `ContentFilterConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_filter_config_include_footers(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ContentFilterConfig, _>(handle, |obj| obj.include_footers as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `include_footnotes` field from a `ContentFilterConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_filter_config_include_footnotes(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ContentFilterConfig, _>(handle, |obj| obj.include_footnotes as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `strip_repeating_text` field from a `ContentFilterConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_filter_config_strip_repeating_text(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ContentFilterConfig, _>(handle, |obj| obj.strip_repeating_text as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `include_watermarks` field from a `ContentFilterConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_filter_config_include_watermarks(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ContentFilterConfig, _>(handle, |obj| obj.include_watermarks as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `CsvConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_csv_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_csv_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::CsvConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CsvConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_csv_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::CsvConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CsvConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_csv_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::CsvConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `delimiter` field from a `CsvConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_csv_config_delimiter(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CsvConfig, _>(handle, |obj| match &obj.delimiter {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `comment_prefixes` field from a `CsvConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_csv_config_comment_prefixes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::CsvConfig, _>(handle, |obj| match serde_json::to_string(&obj.comment_prefixes) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `EmailConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_email_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::EmailConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `EmailConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::EmailConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `EmailConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::EmailConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `msg_fallback_codepage` field from a `EmailConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_config_msg_fallback_codepage(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::EmailConfig, _>(handle, |obj| match &obj.msg_fallback_codepage {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ExtractionConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_extraction_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ExtractionConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ExtractionConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ExtractionConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ExtractionConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ExtractionConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `use_cache` field from a `ExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_use_cache(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| obj.use_cache as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `enable_quality_processing` field from a `ExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_enable_quality_processing(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| obj.enable_quality_processing as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `ocr` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ocr_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_ocr(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.ocr {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `force_ocr` field from a `ExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_force_ocr(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| obj.force_ocr as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `ocr_strategy` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ocr_strategy_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_ocr_strategy(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match insert_handle(obj.ocr_strategy.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `force_ocr_pages` field from a `ExtractionConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_force_ocr_pages(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.force_ocr_pages {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `disable_ocr` field from a `ExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_disable_ocr(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| obj.disable_ocr as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `chunking` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_chunking_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_chunking(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.chunking {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `content_filter` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_content_filter_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_content_filter(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.content_filter {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `images` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_image_extraction_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_images(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.images {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `pdf_options` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_pdf_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_pdf_options(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.pdf_options {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `token_reduction` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_token_reduction_options_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_token_reduction(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.token_reduction {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `language_detection` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_language_detection_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_language_detection(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.language_detection {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `pages` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_page_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_pages(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.pages {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `keywords` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_keyword_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_keywords(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.keywords {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `postprocessor` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_post_processor_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_postprocessor(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.postprocessor {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `html_options` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_conversion_options_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_html_options(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.html_options {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `html_output` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_html_output_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_html_output(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.html_output {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `extraction_timeout_secs` field from a `ExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_extraction_timeout_secs(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.extraction_timeout_secs {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_concurrent_extractions` field from a `ExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_max_concurrent_extractions(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.max_concurrent_extractions {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `result_format` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_result_format_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_result_format(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match insert_handle(obj.result_format) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `security_limits` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_security_limits_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_security_limits(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.security_limits {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_embedded_file_bytes` field from a `ExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_max_embedded_file_bytes(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.max_embedded_file_bytes {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `output_format` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_output_format_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_output_format(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match insert_handle(obj.output_format.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `escape_markdown` field from a `ExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_escape_markdown(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| obj.escape_markdown as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `table_anchors` field from a `ExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_table_anchors(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| obj.table_anchors as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `jupyter_cell_rendering` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_jupyter_cell_rendering_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_jupyter_cell_rendering(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match insert_handle(obj.jupyter_cell_rendering) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `layout` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_layout_detection_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_layout(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.layout {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `transcription` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_transcription_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_transcription(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.transcription {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `use_layout_for_markdown` field from a `ExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_use_layout_for_markdown(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| obj.use_layout_for_markdown as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `include_document_structure` field from a `ExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_include_document_structure(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| obj.include_document_structure as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `acceleration` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_acceleration_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_acceleration(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.acceleration {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `cache_namespace` field from a `ExtractionConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_cache_namespace(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.cache_namespace {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `cache_ttl_secs` field from a `ExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_cache_ttl_secs(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.cache_ttl_secs {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `email` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_email_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_email(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.email {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `csv` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_csv_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_csv(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.csv {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `url` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_url_extraction_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_url(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match insert_handle(obj.url.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_archive_depth` field from a `ExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_max_archive_depth(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| obj.max_archive_depth);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `tree_sitter` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_tree_sitter_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_tree_sitter(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.tree_sitter {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `structured_extraction` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_structured_extraction_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_structured_extraction(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.structured_extraction {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `ner` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ner_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_ner(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.ner {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `redaction` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_redaction_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_redaction(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.redaction {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `summarization` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_summarization_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_summarization(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.summarization {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `translation` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_translation_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_translation(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.translation {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `page_classification` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_page_classification_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_page_classification(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.page_classification {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `chunk_classification` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_chunk_classification_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_chunk_classification(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.chunk_classification {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `captioning` field from a `ExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_captioning_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_captioning(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.captioning {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `qr_codes` field from a `ExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_qr_codes(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionConfig, _>(handle, |obj| match &obj.qr_codes {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Validate the configuration, returning an error if any settings are invalid.
///
/// Checks:
/// - `ocr`: backend name, VLM backend/model requirements, language codes, and the
///   `vlm_fallback` quality threshold (see `OcrConfig.validate`).
/// - `chunking`: `max_characters` is non-zero and `overlap` is smaller than it.
/// - `token_reduction`: `mode` is one of the recognized reduction levels.
/// - `images`: `target_dpi`, `min_dpi`, and `max_dpi` are all positive and within the
///   supported range.
/// - `language_detection`: `min_confidence` is a `[0.0, 1.0]` value.
/// - `csv`: `delimiter`, when set, is exactly one ASCII character.
///
/// Called automatically when a config is loaded from a file
/// (`ExtractionConfig.from_file` and friends) or built from a JSON override
/// (`crate::core::config::merge::merge_config_json`). A config assembled directly through
/// the typed Rust API or an FFI builder is **not** automatically validated â call this
/// method explicitly before use in that case.
/// \note Returns `XbergError::Validation` describing the first invalid setting found.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_validate(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::ExtractionConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return -1;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return -1;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::ExtractionConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.validate();
        match result {
            Ok(()) => 0,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            -1
        }
    }
}

/// Check if image processing is needed by examining OCR and image extraction settings.
///
/// Returns `true` if either OCR is enabled or image extraction is configured,
/// indicating that image decompression and processing should occur.
/// Returns `false` if both are disabled, allowing optimization to skip unnecessary
/// image decompression for text-only extraction workflows.
///
/// # Optimization Impact
/// For text-only extractions (no OCR, no image extraction), skipping image
/// decompression can improve CPU utilization by 5-10% by avoiding wasteful
/// image I/O and processing when results won't be used.
/// Returns `true` when image binary data should be extracted.
///
/// True when `config.images.extract_images` is set, captioning is configured, or QR-code
/// detection is enabled. Captioning and QR-code detection both require image bytes
/// regardless of whether the caller also requested image extraction.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_needs_image_data(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::ExtractionConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::ExtractionConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.needs_image_data();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Returns `true` when any image processing is needed during extraction.
///
/// # Optimization Impact
///
/// For text-only extractions (no OCR, no image extraction, no captioning), skipping
/// image decompression can improve CPU utilization by 5-10% by avoiding wasteful
/// image I/O and processing when results won't be used.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_config_needs_image_processing(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::ExtractionConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::ExtractionConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.needs_image_processing();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Create a `FileExtractionConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_file_extraction_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::FileExtractionConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `FileExtractionConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::FileExtractionConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `FileExtractionConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::FileExtractionConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `enable_quality_processing` field from a `FileExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_enable_quality_processing(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.enable_quality_processing {
                Some(val) => *val as i32,
                None => 0,
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `ocr` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ocr_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_ocr(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.ocr {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `force_ocr` field from a `FileExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_force_ocr(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.force_ocr {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `ocr_strategy` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ocr_strategy_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_ocr_strategy(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.ocr_strategy {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `force_ocr_pages` field from a `FileExtractionConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_force_ocr_pages(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.force_ocr_pages {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `disable_ocr` field from a `FileExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_disable_ocr(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.disable_ocr {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `chunking` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_chunking_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_chunking(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.chunking {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `content_filter` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_content_filter_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_content_filter(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.content_filter {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `images` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_image_extraction_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_images(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.images {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `pdf_options` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_pdf_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_pdf_options(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.pdf_options {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `token_reduction` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_token_reduction_options_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_token_reduction(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.token_reduction {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `language_detection` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_language_detection_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_language_detection(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.language_detection {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `pages` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_page_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_pages(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.pages {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `keywords` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_keyword_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_keywords(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.keywords {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `postprocessor` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_post_processor_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_postprocessor(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.postprocessor {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `html_output` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_html_output_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_html_output(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.html_output {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `result_format` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_result_format_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_result_format(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.result_format {
            Some(val) => match insert_handle(*val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `output_format` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_output_format_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_output_format(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.output_format {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `include_document_structure` field from a `FileExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_include_document_structure(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.include_document_structure {
                Some(val) => *val as i32,
                None => 0,
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `layout` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_layout_detection_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_layout(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.layout {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `transcription` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_transcription_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_transcription(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.transcription {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `timeout_secs` field from a `FileExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_timeout_secs(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.timeout_secs {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `tree_sitter` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_tree_sitter_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_tree_sitter(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.tree_sitter {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `structured_extraction` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_structured_extraction_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_structured_extraction(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.structured_extraction {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `url` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_url_extraction_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_url(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.url {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `ner` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ner_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_ner(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.ner {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `redaction` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_redaction_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_redaction(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.redaction {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `summarization` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_summarization_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_summarization(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.summarization {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `translation` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_translation_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_translation(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.translation {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `page_classification` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_page_classification_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_page_classification(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.page_classification {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `chunk_classification` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_chunk_classification_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_chunk_classification(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.chunk_classification {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `captioning` field from a `FileExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_captioning_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_captioning(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.captioning {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `qr_codes` field from a `FileExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_file_extraction_config_qr_codes(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FileExtractionConfig, _>(handle, |obj| match &obj.qr_codes {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "svg")]
/// Create a `SvgOptions` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_svg_options_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_svg_options_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::core::config::extraction::SvgOptions>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "svg")]
/// Serialize a `SvgOptions` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_svg_options_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::core::config::extraction::SvgOptions, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "svg")]
/// Free a `SvgOptions` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_svg_options_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::core::config::extraction::SvgOptions>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "svg")]
/// Get the `sanitize` field from a `SvgOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_svg_options_sanitize(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::core::config::extraction::SvgOptions, _>(handle, |obj| obj.sanitize as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "svg")]
/// Get the `render_dpi` field from a `SvgOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_svg_options_render_dpi(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::core::config::extraction::SvgOptions, _>(handle, |obj| obj.render_dpi);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Create a `ExtractInput` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_extract_input_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract_input_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ExtractInput>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ExtractInput` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract_input_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ExtractInput, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ExtractInput` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract_input_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ExtractInput>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `kind` field from a `ExtractInput`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_extract_input_kind_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract_input_kind(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractInput, _>(handle, |obj| match insert_handle(obj.kind) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `bytes` field from a `ExtractInput`.
/// The returned byte pointer is borrowed from `ptr` and must not be freed.
/// It remains valid until `ptr` is destroyed or the field is mutated.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract_input_bytes(handle: AlefHandle, out_len: *mut usize) -> *mut u8 {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractInput, _>(handle, |obj| {
            match &obj.bytes {
                Some(val) => {
                    if !out_len.is_null() {
                        // SAFETY: null check above guarantees out_len is a valid pointer.
                        unsafe {
                            *out_len = val.len();
                        }
                    }
                    val.as_ptr() as *mut u8
                }
                None => {
                    if !out_len.is_null() {
                        // SAFETY: null check above guarantees out_len is a valid pointer.
                        unsafe {
                            *out_len = 0;
                        }
                    }
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `uri` field from a `ExtractInput`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract_input_uri(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractInput, _>(handle, |obj| match &obj.uri {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `mime_type` field from a `ExtractInput`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract_input_mime_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractInput, _>(handle, |obj| match &obj.mime_type {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `filename` field from a `ExtractInput`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract_input_filename(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractInput, _>(handle, |obj| match &obj.filename {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `config` field from a `ExtractInput`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_file_extraction_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract_input_config(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractInput, _>(handle, |obj| match &obj.config {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Build a bytes input with a MIME type and optional filename hint.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract_input_from_bytes(
    bytes: *const u8,
    bytes_len: usize,
    mime_type: *const std::ffi::c_char,
    filename: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if bytes.is_null() && bytes_len > 0 {
            set_last_error(1, "Null pointer passed for parameter 'bytes'");
            return 0;
        }
        // SAFETY: when bytes is null, bytes_len is 0 (checked above), so we use an empty slice; otherwise data is valid for len elements.
        let bytes_rs = if bytes.is_null() {
            Vec::new()
        } else {
            unsafe { std::slice::from_raw_parts(bytes, bytes_len) }.to_vec()
        };

        if mime_type.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'mime_type'");
            return 0;
        }
        // SAFETY: null check above guarantees mime_type is a valid pointer; string is valid UTF-8 from caller.
        let mime_type_rs = match unsafe { CStr::from_ptr(mime_type) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'mime_type'");
                return 0;
            }
        };

        let filename_rs = if filename.is_null() {
            None
        } else {
            // SAFETY: null check above guarantees filename is a valid pointer; string is valid UTF-8 from caller.
            match unsafe { CStr::from_ptr(filename) }.to_str() {
                Ok(s) => Some(s.to_string()),
                Err(_) => {
                    set_last_error(1, "Invalid UTF-8 in parameter 'filename'");
                    return 0;
                }
            }
        };

        let result = xberg::ExtractInput::from_bytes(bytes_rs, mime_type_rs, filename_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Build a URI input from a local path, `file://` URI, or HTTP(S) URL.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract_input_from_uri(uri: *const std::ffi::c_char) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if uri.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'uri'");
            return 0;
        }
        // SAFETY: null check above guarantees uri is a valid pointer; string is valid UTF-8 from caller.
        let uri_rs = match unsafe { CStr::from_ptr(uri) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'uri'");
                return 0;
            }
        };

        let result = xberg::ExtractInput::from_uri(uri_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Create a `ExtractionErrorItem` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_extraction_error_item_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_error_item_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ExtractionErrorItem>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ExtractionErrorItem` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_error_item_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ExtractionErrorItem, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ExtractionErrorItem` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_error_item_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ExtractionErrorItem>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `index` field from a `ExtractionErrorItem`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_error_item_index(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionErrorItem, _>(handle, |obj| obj.index);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `code` field from a `ExtractionErrorItem`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_error_item_code(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionErrorItem, _>(handle, |obj| obj.code);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `error_type` field from a `ExtractionErrorItem`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_error_item_error_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractionErrorItem, _>(handle, |obj| {
            match CString::new(obj.error_type.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `source` field from a `ExtractionErrorItem`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_error_item_source(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ExtractionErrorItem, _>(handle, |obj| match CString::new(obj.source.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `message` field from a `ExtractionErrorItem`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_error_item_message(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ExtractionErrorItem, _>(handle, |obj| match CString::new(obj.message.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ExtractionSummary` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_extraction_summary_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_summary_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ExtractionSummary>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ExtractionSummary` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_summary_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ExtractionSummary, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ExtractionSummary` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_summary_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ExtractionSummary>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `inputs` field from a `ExtractionSummary`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_summary_inputs(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionSummary, _>(handle, |obj| obj.inputs);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `results` field from a `ExtractionSummary`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_summary_results(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionSummary, _>(handle, |obj| obj.results);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `errors` field from a `ExtractionSummary`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_summary_errors(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionSummary, _>(handle, |obj| obj.errors);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `remote_urls` field from a `ExtractionSummary`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_summary_remote_urls(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionSummary, _>(handle, |obj| obj.remote_urls);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `pages_crawled` field from a `ExtractionSummary`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_summary_pages_crawled(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionSummary, _>(handle, |obj| obj.pages_crawled);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `documents_downloaded` field from a `ExtractionSummary`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_summary_documents_downloaded(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionSummary, _>(handle, |obj| obj.documents_downloaded);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ExtractionResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_extraction_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_result_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ExtractionResult>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ExtractionResult` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_result_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ExtractionResult, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ExtractionResult` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_result_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ExtractionResult>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `results` field from a `ExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_result_results(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ExtractionResult, _>(handle, |obj| match serde_json::to_string(&obj.results) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `errors` field from a `ExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_result_errors(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ExtractionResult, _>(handle, |obj| match serde_json::to_string(&obj.errors) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `summary` field from a `ExtractionResult`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_extraction_summary_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_result_summary(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::ExtractionResult, _>(handle, |obj| match insert_handle(obj.summary.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `crawl_final_urls` field from a `ExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_result_crawl_final_urls(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractionResult, _>(handle, |obj| {
            match serde_json::to_string(&obj.crawl_final_urls) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `crawl_redirect_count` field from a `ExtractionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_result_crawl_redirect_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractionResult, _>(handle, |obj| obj.crawl_redirect_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `crawl_unique_normalized_urls` field from a `ExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_result_crawl_unique_normalized_urls(
    handle: AlefHandle,
) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractionResult, _>(handle, |obj| {
            match serde_json::to_string(&obj.crawl_unique_normalized_urls) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Build an output containing one successful result.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_result_single(result: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: result,
            expected_type: std::any::TypeId::of::<xberg::ExtractedDocument>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if result == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'result'",
            );
            return 0;
        }
        let result_rs_ptr = match locked_handle_ptr::<xberg::ExtractedDocument>(&mut __alef_guards, result) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let result_rs = unsafe { &*result_rs_ptr }.clone();

        let result = xberg::ExtractionResult::single(result_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Create a `UrlExtractionConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_url_extraction_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_extraction_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::UrlExtractionConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `UrlExtractionConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_extraction_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::UrlExtractionConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `UrlExtractionConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_extraction_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::UrlExtractionConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `mode` field from a `UrlExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_url_extraction_mode_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_extraction_config_mode(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::UrlExtractionConfig, _>(handle, |obj| match insert_handle(obj.mode) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `crawl` field from a `UrlExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_crawl_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_extraction_config_crawl(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::UrlExtractionConfig, _>(handle, |obj| match insert_handle(obj.crawl.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `document_url_pattern` field from a `UrlExtractionConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_extraction_config_document_url_pattern(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::UrlExtractionConfig, _>(handle, |obj| match &obj.document_url_pattern {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `max_document_urls_per_result` field from a `UrlExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_extraction_config_max_document_urls_per_result(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::UrlExtractionConfig, _>(handle, |obj| match &obj.max_document_urls_per_result {
                Some(val) => *val,
                None => 0,
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_total_urls` field from a `UrlExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_extraction_config_max_total_urls(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::UrlExtractionConfig, _>(handle, |obj| match &obj.max_total_urls {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `allow_local_file_inputs` field from a `UrlExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_extraction_config_allow_local_file_inputs(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::UrlExtractionConfig, _>(handle, |obj| obj.allow_local_file_inputs as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `allow_file_uris` field from a `UrlExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_extraction_config_allow_file_uris(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::UrlExtractionConfig, _>(handle, |obj| obj.allow_file_uris as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ImageExtractionConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_image_extraction_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ImageExtractionConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ImageExtractionConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ImageExtractionConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ImageExtractionConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ImageExtractionConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `extract_images` field from a `ImageExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_extract_images(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImageExtractionConfig, _>(handle, |obj| obj.extract_images as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `target_dpi` field from a `ImageExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_target_dpi(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImageExtractionConfig, _>(handle, |obj| obj.target_dpi);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_image_dimension` field from a `ImageExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_max_image_dimension(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImageExtractionConfig, _>(handle, |obj| obj.max_image_dimension);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `inject_placeholders` field from a `ImageExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_inject_placeholders(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImageExtractionConfig, _>(handle, |obj| obj.inject_placeholders as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `auto_adjust_dpi` field from a `ImageExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_auto_adjust_dpi(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImageExtractionConfig, _>(handle, |obj| obj.auto_adjust_dpi as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `min_dpi` field from a `ImageExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_min_dpi(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImageExtractionConfig, _>(handle, |obj| obj.min_dpi);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_dpi` field from a `ImageExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_max_dpi(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImageExtractionConfig, _>(handle, |obj| obj.max_dpi);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_images_per_page` field from a `ImageExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_max_images_per_page(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImageExtractionConfig, _>(handle, |obj| match &obj.max_images_per_page {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `classify` field from a `ImageExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_classify(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImageExtractionConfig, _>(handle, |obj| obj.classify as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `include_page_rasters` field from a `ImageExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_include_page_rasters(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImageExtractionConfig, _>(handle, |obj| obj.include_page_rasters as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `run_ocr_on_images` field from a `ImageExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_run_ocr_on_images(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImageExtractionConfig, _>(handle, |obj| obj.run_ocr_on_images as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `ocr_text_only` field from a `ImageExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_ocr_text_only(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImageExtractionConfig, _>(handle, |obj| obj.ocr_text_only as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `append_ocr_text` field from a `ImageExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_append_ocr_text(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImageExtractionConfig, _>(handle, |obj| obj.append_ocr_text as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `output_format` field from a `ImageExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_image_output_format_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_output_format(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::ImageExtractionConfig, _>(handle, |obj| match insert_handle(obj.output_format) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `svg` field from a `ImageExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_svg_options_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_svg(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::ImageExtractionConfig, _>(handle, |obj| match insert_handle(obj.svg.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `include_data_base64` field from a `ImageExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_extraction_config_include_data_base64(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImageExtractionConfig, _>(handle, |obj| obj.include_data_base64 as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `TokenReductionOptions` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_token_reduction_options_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_options_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::TokenReductionOptions>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `TokenReductionOptions` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_options_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::TokenReductionOptions, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `TokenReductionOptions` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_options_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::TokenReductionOptions>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `mode` field from a `TokenReductionOptions`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_options_mode(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::TokenReductionOptions, _>(handle, |obj| match CString::new(obj.mode.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `preserve_important_words` field from a `TokenReductionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_options_preserve_important_words(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TokenReductionOptions, _>(handle, |obj| obj.preserve_important_words as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `LanguageDetectionConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_language_detection_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_language_detection_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::LanguageDetectionConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LanguageDetectionConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_language_detection_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::LanguageDetectionConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LanguageDetectionConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_language_detection_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::LanguageDetectionConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `enabled` field from a `LanguageDetectionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_language_detection_config_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LanguageDetectionConfig, _>(handle, |obj| obj.enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `min_confidence` field from a `LanguageDetectionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_language_detection_config_min_confidence(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::LanguageDetectionConfig, _>(handle, |obj| obj.min_confidence);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `detect_multiple` field from a `LanguageDetectionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_language_detection_config_detect_multiple(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LanguageDetectionConfig, _>(handle, |obj| obj.detect_multiple as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "html")]
/// Create a `HtmlOutputConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_html_output_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_output_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::HtmlOutputConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "html")]
/// Serialize a `HtmlOutputConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_output_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::HtmlOutputConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "html")]
/// Free a `HtmlOutputConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_output_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::HtmlOutputConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "html")]
/// Get the `css` field from a `HtmlOutputConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_output_config_css(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::HtmlOutputConfig, _>(handle, |obj| match &obj.css {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "html")]
/// Get the `css_file` field from a `HtmlOutputConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_output_config_css_file(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::HtmlOutputConfig, _>(handle, |obj| match &obj.css_file {
            Some(val) => match CString::new(val.to_string_lossy().to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI path value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "html")]
/// Get the `theme` field from a `HtmlOutputConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_html_theme_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_output_config_theme(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HtmlOutputConfig, _>(handle, |obj| match insert_handle(obj.theme.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "html")]
/// Get the `class_prefix` field from a `HtmlOutputConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_output_config_class_prefix(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::HtmlOutputConfig, _>(handle, |obj| match CString::new(obj.class_prefix.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "html")]
/// Get the `embed_css` field from a `HtmlOutputConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_output_config_embed_css(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HtmlOutputConfig, _>(handle, |obj| obj.embed_css as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `LateInteractionConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_late_interaction_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::LateInteractionConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LateInteractionConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::LateInteractionConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LateInteractionConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::LateInteractionConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `model` field from a `LateInteractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_late_interaction_model_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_config_model(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::LateInteractionConfig, _>(handle, |obj| match insert_handle(obj.model.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `batch_size` field from a `LateInteractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_config_batch_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LateInteractionConfig, _>(handle, |obj| obj.batch_size);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_length` field from a `LateInteractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_config_max_length(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LateInteractionConfig, _>(handle, |obj| obj.max_length);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `query_max_length` field from a `LateInteractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_config_query_max_length(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LateInteractionConfig, _>(handle, |obj| obj.query_max_length);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `show_download_progress` field from a `LateInteractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_config_show_download_progress(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LateInteractionConfig, _>(handle, |obj| obj.show_download_progress as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `cache_dir` field from a `LateInteractionConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_config_cache_dir(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LateInteractionConfig, _>(handle, |obj| match &obj.cache_dir {
            Some(val) => match CString::new(val.to_string_lossy().to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI path value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `acceleration` field from a `LateInteractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_acceleration_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_config_acceleration(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LateInteractionConfig, _>(handle, |obj| match &obj.acceleration {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_embed_duration_secs` field from a `LateInteractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_config_max_embed_duration_secs(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LateInteractionConfig, _>(handle, |obj| match &obj.max_embed_duration_secs {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Create a `LayoutDetectionConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_layout_detection_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_detection_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::LayoutDetectionConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Serialize a `LayoutDetectionConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_detection_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::LayoutDetectionConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "layout-types")]
/// Free a `LayoutDetectionConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_detection_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::LayoutDetectionConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `strategy` field from a `LayoutDetectionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_layout_strategy_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_detection_config_strategy(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LayoutDetectionConfig, _>(handle, |obj| match insert_handle(obj.strategy) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `confidence_threshold` field from a `LayoutDetectionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_detection_config_confidence_threshold(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::LayoutDetectionConfig, _>(handle, |obj| match &obj.confidence_threshold {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `apply_heuristics` field from a `LayoutDetectionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_detection_config_apply_heuristics(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LayoutDetectionConfig, _>(handle, |obj| obj.apply_heuristics as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `table_model` field from a `LayoutDetectionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_table_model_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_detection_config_table_model(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::LayoutDetectionConfig, _>(handle, |obj| match insert_handle(obj.table_model) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `table_overlap_preference` field from a `LayoutDetectionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_table_overlap_preference_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_detection_config_table_overlap_preference(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LayoutDetectionConfig, _>(handle, |obj| {
            match insert_handle(obj.table_overlap_preference) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `acceleration` field from a `LayoutDetectionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_acceleration_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_detection_config_acceleration(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LayoutDetectionConfig, _>(handle, |obj| match &obj.acceleration {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `enable_chart_understanding` field from a `LayoutDetectionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_detection_config_enable_chart_understanding(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::LayoutDetectionConfig, _>(handle, |obj| obj.enable_chart_understanding as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `LlmConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_llm_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::LlmConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LlmConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::LlmConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LlmConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::LlmConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `model` field from a `LlmConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match CString::new(obj.model.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `api_key` field from a `LlmConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_api_key(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.api_key {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `base_url` field from a `LlmConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_base_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.base_url {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `timeout_secs` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_timeout_secs(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.timeout_secs {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_retries` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_max_retries(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.max_retries {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `temperature` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_temperature(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.temperature {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `max_tokens` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_max_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.max_tokens {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `top_p` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_top_p(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.top_p {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `stop` field from a `LlmConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_stop(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.stop {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `seed` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_seed(handle: AlefHandle) -> i64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.seed {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `presence_penalty` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_presence_penalty(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.presence_penalty {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `frequency_penalty` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_frequency_penalty(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.frequency_penalty {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `reasoning_effort` field from a `LlmConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_reasoning_effort(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.reasoning_effort {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `extra_body` field from a `LlmConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_extra_body(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.extra_body {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `load_env` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_load_env(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.load_env {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `headers` field from a `LlmConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_headers(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.headers {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `providers` field from a `LlmConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_providers(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.providers {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `cache` field from a `LlmConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_llm_cache_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_cache(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.cache {
            Some(val) => match insert_handle((**val).clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `budget` field from a `LlmConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_llm_budget_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_budget(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.budget {
            Some(val) => match insert_handle((**val).clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `rate_limit` field from a `LlmConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_llm_rate_limit_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_rate_limit(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.rate_limit {
            Some(val) => match insert_handle((**val).clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `cost_tracking` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_cost_tracking(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.cost_tracking {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `tracing` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_tracing(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.tracing {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `cooldown_secs` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_cooldown_secs(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.cooldown_secs {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `health_check_secs` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_health_check_secs(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.health_check_secs {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `bedrock` field from a `LlmConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_bedrock_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_bedrock(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.bedrock {
            Some(val) => match insert_handle((**val).clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `credential_provider` field from a `LlmConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_credential_provider_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_credential_provider(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LlmConfig, _>(handle, |obj| match &obj.credential_provider {
            Some(val) => match insert_handle((**val).clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Validate the request-time sampling parameters that have a documented range:
/// `top_p` (`[0.0, 1.0]`), `presence_penalty`, and `frequency_penalty` (both
/// `[-2.0, 2.0]`, matching liter-llm's/OpenAI's semantics). An unset field is
/// always valid â silence in config should never be rejected.
///
/// Called from `build_client_config` before a liter-llm
/// client is built from this config, alongside the existing
/// `validate_cache_backend` check in that function.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_config_validate(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::LlmConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return -1;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return -1;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::LlmConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.validate();
        match result {
            Ok(()) => 0,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            -1
        }
    }
}

/// Create a `LlmProviderConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_llm_provider_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_provider_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::core::config::LlmProviderConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LlmProviderConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_provider_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::core::config::LlmProviderConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LlmProviderConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_provider_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::core::config::LlmProviderConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `name` field from a `LlmProviderConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_provider_config_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::core::config::LlmProviderConfig, _>(handle, |obj| {
            match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `base_url` field from a `LlmProviderConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_provider_config_base_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::core::config::LlmProviderConfig, _>(handle, |obj| {
            match CString::new(obj.base_url.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `auth_header` field from a `LlmProviderConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_provider_config_auth_header(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::core::config::LlmProviderConfig, _>(handle, |obj| match &obj.auth_header {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `model_prefixes` field from a `LlmProviderConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_provider_config_model_prefixes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::core::config::LlmProviderConfig, _>(handle, |obj| {
                match serde_json::to_string(&obj.model_prefixes) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `LlmCacheConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_llm_cache_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_cache_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::core::config::LlmCacheConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LlmCacheConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_cache_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::core::config::LlmCacheConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LlmCacheConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_cache_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::core::config::LlmCacheConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `max_entries` field from a `LlmCacheConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_cache_config_max_entries(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::core::config::LlmCacheConfig, _>(handle, |obj| match &obj.max_entries {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `ttl_seconds` field from a `LlmCacheConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_cache_config_ttl_seconds(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::core::config::LlmCacheConfig, _>(handle, |obj| match &obj.ttl_seconds {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `backend` field from a `LlmCacheConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_cache_config_backend(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::core::config::LlmCacheConfig, _>(handle, |obj| match &obj.backend {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `backend_config` field from a `LlmCacheConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_cache_config_backend_config(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::core::config::LlmCacheConfig, _>(handle, |obj| match &obj.backend_config {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `LlmBudgetConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_llm_budget_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_budget_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::core::config::LlmBudgetConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LlmBudgetConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_budget_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::core::config::LlmBudgetConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LlmBudgetConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_budget_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::core::config::LlmBudgetConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `global_limit` field from a `LlmBudgetConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_budget_config_global_limit(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::core::config::LlmBudgetConfig, _>(handle, |obj| match &obj.global_limit {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `model_limits` field from a `LlmBudgetConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_budget_config_model_limits(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::core::config::LlmBudgetConfig, _>(handle, |obj| match &obj.model_limits {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `enforcement` field from a `LlmBudgetConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_budget_config_enforcement(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::core::config::LlmBudgetConfig, _>(handle, |obj| match &obj.enforcement {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `LlmRateLimitConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_llm_rate_limit_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_rate_limit_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::core::config::LlmRateLimitConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LlmRateLimitConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_rate_limit_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::core::config::LlmRateLimitConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LlmRateLimitConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_rate_limit_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::core::config::LlmRateLimitConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `rpm` field from a `LlmRateLimitConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_rate_limit_config_rpm(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::core::config::LlmRateLimitConfig, _>(handle, |obj| match &obj.rpm {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `tpm` field from a `LlmRateLimitConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_rate_limit_config_tpm(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::core::config::LlmRateLimitConfig, _>(handle, |obj| match &obj.tpm {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `window_seconds` field from a `LlmRateLimitConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_rate_limit_config_window_seconds(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::core::config::LlmRateLimitConfig, _>(handle, |obj| match &obj.window_seconds {
                Some(val) => *val,
                None => 0,
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `BedrockConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_bedrock_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bedrock_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::BedrockConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `BedrockConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bedrock_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::BedrockConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `BedrockConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bedrock_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::BedrockConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `region` field from a `BedrockConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bedrock_config_region(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::BedrockConfig, _>(handle, |obj| match &obj.region {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `cross_region_prefix` field from a `BedrockConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bedrock_config_cross_region_prefix(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::BedrockConfig, _>(handle, |obj| match &obj.cross_region_prefix {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `access_key_id` field from a `BedrockConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bedrock_config_access_key_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::BedrockConfig, _>(handle, |obj| match &obj.access_key_id {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `secret_access_key` field from a `BedrockConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bedrock_config_secret_access_key(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::BedrockConfig, _>(handle, |obj| match &obj.secret_access_key {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `session_token` field from a `BedrockConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bedrock_config_session_token(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::BedrockConfig, _>(handle, |obj| match &obj.session_token {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `StructuredExtractionConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_structured_extraction_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_extraction_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::StructuredExtractionConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `StructuredExtractionConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_extraction_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::StructuredExtractionConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `StructuredExtractionConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_extraction_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::StructuredExtractionConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `schema` field from a `StructuredExtractionConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_extraction_config_schema(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::StructuredExtractionConfig, _>(handle, |obj| {
            match serde_json::to_string(&obj.schema) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `schema_name` field from a `StructuredExtractionConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_extraction_config_schema_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::StructuredExtractionConfig, _>(handle, |obj| {
            match CString::new(obj.schema_name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `schema_description` field from a `StructuredExtractionConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_extraction_config_schema_description(
    handle: AlefHandle,
) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::StructuredExtractionConfig, _>(handle, |obj| match &obj.schema_description {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `strict` field from a `StructuredExtractionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_extraction_config_strict(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::StructuredExtractionConfig, _>(handle, |obj| obj.strict as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `prompt` field from a `StructuredExtractionConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_extraction_config_prompt(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::StructuredExtractionConfig, _>(handle, |obj| match &obj.prompt {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `llm` field from a `StructuredExtractionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_llm_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_extraction_config_llm(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::StructuredExtractionConfig, _>(handle, |obj| match insert_handle(obj.llm.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `NerConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_ner_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ner_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::NerConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `NerConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ner_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::NerConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `NerConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ner_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::NerConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `backend` field from a `NerConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ner_backend_kind_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ner_config_backend(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::NerConfig, _>(handle, |obj| match insert_handle(obj.backend) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `categories` field from a `NerConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ner_config_categories(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::NerConfig, _>(handle, |obj| match serde_json::to_string(&obj.categories) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `model` field from a `NerConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ner_config_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::NerConfig, _>(handle, |obj| match &obj.model {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `llm` field from a `NerConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_llm_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ner_config_llm(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::NerConfig, _>(handle, |obj| match &obj.llm {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `custom_labels` field from a `NerConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ner_config_custom_labels(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::NerConfig, _>(handle, |obj| match serde_json::to_string(&obj.custom_labels) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `OcrQualityThresholds` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_ocr_quality_thresholds_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::OcrQualityThresholds>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `OcrQualityThresholds` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::OcrQualityThresholds, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OcrQualityThresholds` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::OcrQualityThresholds>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `min_total_non_whitespace` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_min_total_non_whitespace(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.min_total_non_whitespace);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `min_non_whitespace_per_page` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_min_non_whitespace_per_page(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.min_non_whitespace_per_page);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `min_meaningful_word_len` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_min_meaningful_word_len(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.min_meaningful_word_len);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `min_meaningful_words` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_min_meaningful_words(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.min_meaningful_words);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `min_alnum_ratio` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_min_alnum_ratio(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.min_alnum_ratio);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `min_garbage_chars` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_min_garbage_chars(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.min_garbage_chars);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_fragmented_word_ratio` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_max_fragmented_word_ratio(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.max_fragmented_word_ratio);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `critical_fragmented_word_ratio` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_critical_fragmented_word_ratio(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.critical_fragmented_word_ratio);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `min_avg_word_length` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_min_avg_word_length(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.min_avg_word_length);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `min_words_for_avg_length_check` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_min_words_for_avg_length_check(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.min_words_for_avg_length_check);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `min_consecutive_repeat_ratio` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_min_consecutive_repeat_ratio(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.min_consecutive_repeat_ratio);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `min_words_for_repeat_check` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_min_words_for_repeat_check(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.min_words_for_repeat_check);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `substantive_min_chars` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_substantive_min_chars(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.substantive_min_chars);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `non_text_min_chars` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_non_text_min_chars(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.non_text_min_chars);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `alnum_ws_ratio_threshold` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_alnum_ws_ratio_threshold(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.alnum_ws_ratio_threshold);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `pipeline_min_quality` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_pipeline_min_quality(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.pipeline_min_quality);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `min_undecodable_ratio` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_min_undecodable_ratio(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.min_undecodable_ratio);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `enable_provenance_ocr_routing` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_enable_provenance_ocr_routing(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.enable_provenance_ocr_routing as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `min_provenance_fallback_ratio` field from a `OcrQualityThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_quality_thresholds_min_provenance_fallback_ratio(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::OcrQualityThresholds, _>(handle, |obj| obj.min_provenance_fallback_ratio);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Create a `OcrPipelineStage` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_ocr_pipeline_stage_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_pipeline_stage_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::OcrPipelineStage>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `OcrPipelineStage` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_pipeline_stage_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::OcrPipelineStage, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OcrPipelineStage` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_pipeline_stage_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::OcrPipelineStage>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `backend` field from a `OcrPipelineStage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_pipeline_stage_backend(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::OcrPipelineStage, _>(handle, |obj| match CString::new(obj.backend.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `priority` field from a `OcrPipelineStage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_pipeline_stage_priority(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrPipelineStage, _>(handle, |obj| obj.priority);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `language` field from a `OcrPipelineStage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_pipeline_stage_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::OcrPipelineStage, _>(handle, |obj| match &obj.language {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `tesseract_config` field from a `OcrPipelineStage`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_tesseract_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_pipeline_stage_tesseract_config(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrPipelineStage, _>(handle, |obj| match &obj.tesseract_config {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `paddle_ocr_config` field from a `OcrPipelineStage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_pipeline_stage_paddle_ocr_config(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::OcrPipelineStage, _>(handle, |obj| match &obj.paddle_ocr_config {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `vlm_config` field from a `OcrPipelineStage`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_llm_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_pipeline_stage_vlm_config(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrPipelineStage, _>(handle, |obj| match &obj.vlm_config {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `backend_options` field from a `OcrPipelineStage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_pipeline_stage_backend_options(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::OcrPipelineStage, _>(handle, |obj| match &obj.backend_options {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `OcrPipelineConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_ocr_pipeline_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_pipeline_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::OcrPipelineConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `OcrPipelineConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_pipeline_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::OcrPipelineConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OcrPipelineConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_pipeline_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::OcrPipelineConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `stages` field from a `OcrPipelineConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_pipeline_config_stages(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::OcrPipelineConfig, _>(handle, |obj| match serde_json::to_string(&obj.stages) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `quality_thresholds` field from a `OcrPipelineConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ocr_quality_thresholds_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_pipeline_config_quality_thresholds(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrPipelineConfig, _>(handle, |obj| {
            match insert_handle(obj.quality_thresholds.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `OcrConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_ocr_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::OcrConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `OcrConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::OcrConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OcrConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::OcrConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `enabled` field from a `OcrConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrConfig, _>(handle, |obj| obj.enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `backend` field from a `OcrConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_backend(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::OcrConfig, _>(handle, |obj| match CString::new(obj.backend.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `language` field from a `OcrConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::OcrConfig, _>(handle, |obj| match serde_json::to_string(&obj.language) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `tesseract_config` field from a `OcrConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_tesseract_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_tesseract_config(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrConfig, _>(handle, |obj| match &obj.tesseract_config {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `output_format` field from a `OcrConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_output_format_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_output_format(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrConfig, _>(handle, |obj| match &obj.output_format {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `paddle_ocr_config` field from a `OcrConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_paddle_ocr_config(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::OcrConfig, _>(handle, |obj| match &obj.paddle_ocr_config {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `backend_options` field from a `OcrConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_backend_options(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::OcrConfig, _>(handle, |obj| match &obj.backend_options {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `element_config` field from a `OcrConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ocr_element_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_element_config(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrConfig, _>(handle, |obj| match &obj.element_config {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `quality_thresholds` field from a `OcrConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ocr_quality_thresholds_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_quality_thresholds(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrConfig, _>(handle, |obj| match &obj.quality_thresholds {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `pipeline` field from a `OcrConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ocr_pipeline_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_pipeline(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrConfig, _>(handle, |obj| match &obj.pipeline {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `auto_rotate` field from a `OcrConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_auto_rotate(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrConfig, _>(handle, |obj| obj.auto_rotate as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `vlm_fallback` field from a `OcrConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_vlm_fallback_policy_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_vlm_fallback(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrConfig, _>(handle, |obj| match insert_handle(obj.vlm_fallback.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `vlm_config` field from a `OcrConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_llm_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_vlm_config(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrConfig, _>(handle, |obj| match &obj.vlm_config {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `vlm_prompt` field from a `OcrConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_vlm_prompt(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::OcrConfig, _>(handle, |obj| match &obj.vlm_prompt {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `acceleration` field from a `OcrConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_acceleration_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_acceleration(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrConfig, _>(handle, |obj| match &obj.acceleration {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `tessdata_bytes` field from a `OcrConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_tessdata_bytes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::OcrConfig, _>(handle, |obj| match &obj.tessdata_bytes {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `tessdata_path` field from a `OcrConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_config_tessdata_path(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::OcrConfig, _>(handle, |obj| match &obj.tessdata_path {
            Some(val) => match CString::new(val.to_string_lossy().to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI path value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `PageConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_page_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PageConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PageConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PageConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PageConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PageConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `extract_pages` field from a `PageConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_config_extract_pages(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageConfig, _>(handle, |obj| obj.extract_pages as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `insert_page_markers` field from a `PageConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_config_insert_page_markers(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageConfig, _>(handle, |obj| obj.insert_page_markers as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `marker_format` field from a `PageConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_config_marker_format(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PageConfig, _>(handle, |obj| match CString::new(obj.marker_format.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Create a `PdfConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_pdf_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PdfConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Serialize a `PdfConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PdfConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "pdf")]
/// Free a `PdfConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PdfConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `extract_images` field from a `PdfConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_config_extract_images(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PdfConfig, _>(handle, |obj| obj.extract_images as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `extract_tables` field from a `PdfConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_config_extract_tables(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PdfConfig, _>(handle, |obj| obj.extract_tables as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `passwords` field from a `PdfConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_config_passwords(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PdfConfig, _>(handle, |obj| match &obj.passwords {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `extract_metadata` field from a `PdfConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_config_extract_metadata(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PdfConfig, _>(handle, |obj| obj.extract_metadata as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `hierarchy` field from a `PdfConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_hierarchy_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_config_hierarchy(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PdfConfig, _>(handle, |obj| match &obj.hierarchy {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `extract_annotations` field from a `PdfConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_config_extract_annotations(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PdfConfig, _>(handle, |obj| obj.extract_annotations as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `top_margin_fraction` field from a `PdfConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_config_top_margin_fraction(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::PdfConfig, _>(handle, |obj| match &obj.top_margin_fraction {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `bottom_margin_fraction` field from a `PdfConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_config_bottom_margin_fraction(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::PdfConfig, _>(handle, |obj| match &obj.bottom_margin_fraction {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `allow_single_column_tables` field from a `PdfConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_config_allow_single_column_tables(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PdfConfig, _>(handle, |obj| obj.allow_single_column_tables as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `ocr_inline_images` field from a `PdfConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_config_ocr_inline_images(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PdfConfig, _>(handle, |obj| obj.ocr_inline_images as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `extract_form_fields` field from a `PdfConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_config_extract_form_fields(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PdfConfig, _>(handle, |obj| obj.extract_form_fields as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `reading_order` field from a `PdfConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_config_reading_order(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PdfConfig, _>(handle, |obj| obj.reading_order as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Create a `HierarchyConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_hierarchy_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_hierarchy_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::HierarchyConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Serialize a `HierarchyConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_hierarchy_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::HierarchyConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "pdf")]
/// Free a `HierarchyConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_hierarchy_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::HierarchyConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `enabled` field from a `HierarchyConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_hierarchy_config_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HierarchyConfig, _>(handle, |obj| obj.enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `k_clusters` field from a `HierarchyConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_hierarchy_config_k_clusters(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HierarchyConfig, _>(handle, |obj| obj.k_clusters);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `include_bbox` field from a `HierarchyConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_hierarchy_config_include_bbox(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HierarchyConfig, _>(handle, |obj| obj.include_bbox as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `PostProcessorConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_post_processor_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_post_processor_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PostProcessorConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PostProcessorConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_post_processor_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PostProcessorConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PostProcessorConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_post_processor_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PostProcessorConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `enabled` field from a `PostProcessorConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_post_processor_config_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PostProcessorConfig, _>(handle, |obj| obj.enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `enabled_processors` field from a `PostProcessorConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_post_processor_config_enabled_processors(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PostProcessorConfig, _>(handle, |obj| match &obj.enabled_processors {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `disabled_processors` field from a `PostProcessorConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_post_processor_config_disabled_processors(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PostProcessorConfig, _>(handle, |obj| match &obj.disabled_processors {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `enabled_set` field from a `PostProcessorConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_post_processor_config_enabled_set(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PostProcessorConfig, _>(handle, |obj| match &obj.enabled_set {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `disabled_set` field from a `PostProcessorConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_post_processor_config_disabled_set(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PostProcessorConfig, _>(handle, |obj| match &obj.disabled_set {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ChunkingConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_chunking_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ChunkingConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ChunkingConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ChunkingConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ChunkingConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ChunkingConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `max_characters` field from a `ChunkingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_config_max_characters(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkingConfig, _>(handle, |obj| obj.max_characters);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `overlap` field from a `ChunkingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_config_overlap(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkingConfig, _>(handle, |obj| obj.overlap);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `trim` field from a `ChunkingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_config_trim(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkingConfig, _>(handle, |obj| obj.trim as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `chunker_type` field from a `ChunkingConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_chunker_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_config_chunker_type(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkingConfig, _>(handle, |obj| match insert_handle(obj.chunker_type) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `embedding` field from a `ChunkingConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_embedding_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_config_embedding(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkingConfig, _>(handle, |obj| match &obj.embedding {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `sparse_embedding` field from a `ChunkingConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_sparse_embedding_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_config_sparse_embedding(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkingConfig, _>(handle, |obj| match &obj.sparse_embedding {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `late_interaction` field from a `ChunkingConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_late_interaction_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_config_late_interaction(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkingConfig, _>(handle, |obj| match &obj.late_interaction {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `preset` field from a `ChunkingConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_config_preset(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ChunkingConfig, _>(handle, |obj| match &obj.preset {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `sizing` field from a `ChunkingConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_chunk_sizing_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_config_sizing(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkingConfig, _>(handle, |obj| match insert_handle(obj.sizing.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `prepend_heading_context` field from a `ChunkingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_config_prepend_heading_context(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkingConfig, _>(handle, |obj| obj.prepend_heading_context as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `topic_threshold` field from a `ChunkingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_config_topic_threshold(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::ChunkingConfig, _>(handle, |obj| match &obj.topic_threshold {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `table_chunking` field from a `ChunkingConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_table_chunking_mode_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_config_table_chunking(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkingConfig, _>(handle, |obj| match insert_handle(obj.table_chunking) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `breadcrumb_target` field from a `ChunkingConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_breadcrumb_target_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_config_breadcrumb_target(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::ChunkingConfig, _>(handle, |obj| match insert_handle(obj.breadcrumb_target) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `EmbeddingConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_embedding_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedding_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::EmbeddingConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `EmbeddingConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedding_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::EmbeddingConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `EmbeddingConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedding_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::EmbeddingConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `model` field from a `EmbeddingConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_embedding_model_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedding_config_model(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::EmbeddingConfig, _>(handle, |obj| match insert_handle(obj.model.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `normalize` field from a `EmbeddingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedding_config_normalize(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::EmbeddingConfig, _>(handle, |obj| obj.normalize as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `batch_size` field from a `EmbeddingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedding_config_batch_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::EmbeddingConfig, _>(handle, |obj| obj.batch_size);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `show_download_progress` field from a `EmbeddingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedding_config_show_download_progress(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::EmbeddingConfig, _>(handle, |obj| obj.show_download_progress as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `cache_dir` field from a `EmbeddingConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedding_config_cache_dir(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmbeddingConfig, _>(handle, |obj| match &obj.cache_dir {
            Some(val) => match CString::new(val.to_string_lossy().to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI path value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `acceleration` field from a `EmbeddingConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_acceleration_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedding_config_acceleration(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::EmbeddingConfig, _>(handle, |obj| match &obj.acceleration {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_embed_duration_secs` field from a `EmbeddingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedding_config_max_embed_duration_secs(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::EmbeddingConfig, _>(handle, |obj| match &obj.max_embed_duration_secs {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_sequence_length` field from a `EmbeddingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedding_config_max_sequence_length(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::EmbeddingConfig, _>(handle, |obj| match &obj.max_sequence_length {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `RedactionConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_redaction_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::RedactionConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `RedactionConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::RedactionConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `RedactionConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::RedactionConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `categories` field from a `RedactionConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_config_categories(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::RedactionConfig, _>(handle, |obj| match serde_json::to_string(&obj.categories) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `strategy` field from a `RedactionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_redaction_strategy_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_config_strategy(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RedactionConfig, _>(handle, |obj| match insert_handle(obj.strategy) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `ner` field from a `RedactionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ner_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_config_ner(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RedactionConfig, _>(handle, |obj| match &obj.ner {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `preserve_offsets` field from a `RedactionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_config_preserve_offsets(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RedactionConfig, _>(handle, |obj| obj.preserve_offsets as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `custom_terms` field from a `RedactionConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_config_custom_terms(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::RedactionConfig, _>(handle, |obj| match serde_json::to_string(&obj.custom_terms) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `custom_patterns` field from a `RedactionConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_config_custom_patterns(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::RedactionConfig, _>(handle, |obj| match serde_json::to_string(&obj.custom_patterns) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Validate user-supplied terms and patterns at config-construction time.
///
/// Compiles every `RedactionPattern.pattern` (with the case-insensitive
/// inline flag where applicable) and returns the first compilation error so
/// the caller can reject the config before the redaction pipeline runs.
/// Pure terms (regex-escaped) cannot fail to compile, but the function
/// still rejects empty values to avoid degenerate zero-length matches.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_config_validate(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::RedactionConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return -1;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return -1;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::RedactionConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.validate();
        match result {
            Ok(()) => 0,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            -1
        }
    }
}

/// Create a `RedactionTerm` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_redaction_term_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_term_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::RedactionTerm>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `RedactionTerm` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_term_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::RedactionTerm, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `RedactionTerm` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_term_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::RedactionTerm>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `label` field from a `RedactionTerm`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_term_label(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::RedactionTerm, _>(handle, |obj| match CString::new(obj.label.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `value` field from a `RedactionTerm`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_term_value(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::RedactionTerm, _>(handle, |obj| match CString::new(obj.value.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `case_sensitive` field from a `RedactionTerm`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_term_case_sensitive(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RedactionTerm, _>(handle, |obj| obj.case_sensitive as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Build a term whose label is the literal value itself (case-insensitive).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_term_literal(value: *const std::ffi::c_char) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if value.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'value'");
            return 0;
        }
        // SAFETY: null check above guarantees value is a valid pointer; string is valid UTF-8 from caller.
        let value_rs = match unsafe { CStr::from_ptr(value) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'value'");
                return 0;
            }
        };

        let result = xberg::RedactionTerm::literal(value_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Build a term with a custom label.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_term_labeled(
    label: *const std::ffi::c_char,
    value: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if label.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'label'");
            return 0;
        }
        // SAFETY: null check above guarantees label is a valid pointer; string is valid UTF-8 from caller.
        let label_rs = match unsafe { CStr::from_ptr(label) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'label'");
                return 0;
            }
        };

        if value.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'value'");
            return 0;
        }
        // SAFETY: null check above guarantees value is a valid pointer; string is valid UTF-8 from caller.
        let value_rs = match unsafe { CStr::from_ptr(value) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'value'");
                return 0;
            }
        };

        let result = xberg::RedactionTerm::labeled(label_rs, value_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Create a `RedactionPattern` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_redaction_pattern_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_pattern_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::RedactionPattern>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `RedactionPattern` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_pattern_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::RedactionPattern, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `RedactionPattern` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_pattern_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::RedactionPattern>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `label` field from a `RedactionPattern`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_pattern_label(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::RedactionPattern, _>(handle, |obj| match CString::new(obj.label.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `pattern` field from a `RedactionPattern`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_pattern_pattern(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::RedactionPattern, _>(handle, |obj| match CString::new(obj.pattern.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `case_sensitive` field from a `RedactionPattern`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_pattern_case_sensitive(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RedactionPattern, _>(handle, |obj| obj.case_sensitive as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Build a pattern with the given label (case-insensitive by default).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_pattern_labeled(
    label: *const std::ffi::c_char,
    pattern: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if label.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'label'");
            return 0;
        }
        // SAFETY: null check above guarantees label is a valid pointer; string is valid UTF-8 from caller.
        let label_rs = match unsafe { CStr::from_ptr(label) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'label'");
                return 0;
            }
        };

        if pattern.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'pattern'");
            return 0;
        }
        // SAFETY: null check above guarantees pattern is a valid pointer; string is valid UTF-8 from caller.
        let pattern_rs = match unsafe { CStr::from_ptr(pattern) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'pattern'");
                return 0;
            }
        };

        let result = xberg::RedactionPattern::labeled(label_rs, pattern_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Create a `RerankerConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_reranker_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranker_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::RerankerConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `RerankerConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranker_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::RerankerConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `RerankerConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranker_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::RerankerConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `model` field from a `RerankerConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_reranker_model_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranker_config_model(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RerankerConfig, _>(handle, |obj| match insert_handle(obj.model.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `top_k` field from a `RerankerConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranker_config_top_k(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RerankerConfig, _>(handle, |obj| match &obj.top_k {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `batch_size` field from a `RerankerConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranker_config_batch_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RerankerConfig, _>(handle, |obj| obj.batch_size);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `show_download_progress` field from a `RerankerConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranker_config_show_download_progress(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RerankerConfig, _>(handle, |obj| obj.show_download_progress as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `cache_dir` field from a `RerankerConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranker_config_cache_dir(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::RerankerConfig, _>(handle, |obj| match &obj.cache_dir {
            Some(val) => match CString::new(val.to_string_lossy().to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI path value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `acceleration` field from a `RerankerConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_acceleration_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranker_config_acceleration(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RerankerConfig, _>(handle, |obj| match &obj.acceleration {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_rerank_duration_secs` field from a `RerankerConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranker_config_max_rerank_duration_secs(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RerankerConfig, _>(handle, |obj| match &obj.max_rerank_duration_secs {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `SparseEmbeddingConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_sparse_embedding_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::SparseEmbeddingConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `SparseEmbeddingConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::SparseEmbeddingConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `SparseEmbeddingConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::SparseEmbeddingConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `model` field from a `SparseEmbeddingConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_sparse_embedding_model_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_config_model(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::SparseEmbeddingConfig, _>(handle, |obj| match insert_handle(obj.model.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `batch_size` field from a `SparseEmbeddingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_config_batch_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SparseEmbeddingConfig, _>(handle, |obj| obj.batch_size);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_length` field from a `SparseEmbeddingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_config_max_length(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SparseEmbeddingConfig, _>(handle, |obj| obj.max_length);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `show_download_progress` field from a `SparseEmbeddingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_config_show_download_progress(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SparseEmbeddingConfig, _>(handle, |obj| obj.show_download_progress as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `cache_dir` field from a `SparseEmbeddingConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_config_cache_dir(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::SparseEmbeddingConfig, _>(handle, |obj| match &obj.cache_dir {
            Some(val) => match CString::new(val.to_string_lossy().to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI path value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `acceleration` field from a `SparseEmbeddingConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_acceleration_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_config_acceleration(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SparseEmbeddingConfig, _>(handle, |obj| match &obj.acceleration {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_embed_duration_secs` field from a `SparseEmbeddingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_config_max_embed_duration_secs(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SparseEmbeddingConfig, _>(handle, |obj| match &obj.max_embed_duration_secs {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `SummarizationConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_summarization_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_summarization_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::SummarizationConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `SummarizationConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_summarization_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::SummarizationConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `SummarizationConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_summarization_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::SummarizationConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `strategy` field from a `SummarizationConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_summary_strategy_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_summarization_config_strategy(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SummarizationConfig, _>(handle, |obj| match insert_handle(obj.strategy) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_tokens` field from a `SummarizationConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_summarization_config_max_tokens(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SummarizationConfig, _>(handle, |obj| match &obj.max_tokens {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `llm` field from a `SummarizationConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_llm_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_summarization_config_llm(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SummarizationConfig, _>(handle, |obj| match &obj.llm {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Create a `TranscriptionConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_transcription_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_transcription_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::TranscriptionConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Serialize a `TranscriptionConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_transcription_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::TranscriptionConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Free a `TranscriptionConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_transcription_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::TranscriptionConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Get the `enabled` field from a `TranscriptionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_transcription_config_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TranscriptionConfig, _>(handle, |obj| obj.enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Get the `model` field from a `TranscriptionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_whisper_model_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_transcription_config_model(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TranscriptionConfig, _>(handle, |obj| match insert_handle(obj.model) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Get the `language` field from a `TranscriptionConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_transcription_config_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::TranscriptionConfig, _>(handle, |obj| match &obj.language {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Get the `timestamps` field from a `TranscriptionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_transcription_config_timestamps(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TranscriptionConfig, _>(handle, |obj| obj.timestamps as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Get the `max_duration_ms` field from a `TranscriptionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_transcription_config_max_duration_ms(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TranscriptionConfig, _>(handle, |obj| match &obj.max_duration_ms {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Get the `max_bytes` field from a `TranscriptionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_transcription_config_max_bytes(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TranscriptionConfig, _>(handle, |obj| match &obj.max_bytes {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Get the `timeout_ms` field from a `TranscriptionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_transcription_config_timeout_ms(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TranscriptionConfig, _>(handle, |obj| match &obj.timeout_ms {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Get the `model_cache_dir` field from a `TranscriptionConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_transcription_config_model_cache_dir(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::TranscriptionConfig, _>(handle, |obj| match &obj.model_cache_dir {
            Some(val) => match CString::new(val.to_string_lossy().to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI path value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Get the `allow_network` field from a `TranscriptionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_transcription_config_allow_network(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TranscriptionConfig, _>(handle, |obj| obj.allow_network as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Get the `verify_hash` field from a `TranscriptionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_transcription_config_verify_hash(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TranscriptionConfig, _>(handle, |obj| obj.verify_hash as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `TranslationConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_translation_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_translation_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::TranslationConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `TranslationConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_translation_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::TranslationConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `TranslationConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_translation_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::TranslationConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `target_lang` field from a `TranslationConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_translation_config_target_lang(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::TranslationConfig, _>(handle, |obj| match CString::new(obj.target_lang.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `source_lang` field from a `TranslationConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_translation_config_source_lang(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::TranslationConfig, _>(handle, |obj| match &obj.source_lang {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `preserve_markup` field from a `TranslationConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_translation_config_preserve_markup(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TranslationConfig, _>(handle, |obj| obj.preserve_markup as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `llm` field from a `TranslationConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_llm_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_translation_config_llm(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TranslationConfig, _>(handle, |obj| match insert_handle(obj.llm.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Create a `TreeSitterConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_tree_sitter_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::TreeSitterConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Serialize a `TreeSitterConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::TreeSitterConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Free a `TreeSitterConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::TreeSitterConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `enabled` field from a `TreeSitterConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_config_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TreeSitterConfig, _>(handle, |obj| obj.enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `cache_dir` field from a `TreeSitterConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_config_cache_dir(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::TreeSitterConfig, _>(handle, |obj| match &obj.cache_dir {
            Some(val) => match CString::new(val.to_string_lossy().to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI path value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `languages` field from a `TreeSitterConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_config_languages(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::TreeSitterConfig, _>(handle, |obj| match &obj.languages {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `groups` field from a `TreeSitterConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_config_groups(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::TreeSitterConfig, _>(handle, |obj| match &obj.groups {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `process` field from a `TreeSitterConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_tree_sitter_process_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_config_process(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::TreeSitterConfig, _>(handle, |obj| match insert_handle(obj.process.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Create a `TreeSitterProcessConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_tree_sitter_process_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_process_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::TreeSitterProcessConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Serialize a `TreeSitterProcessConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_process_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::TreeSitterProcessConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Free a `TreeSitterProcessConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_process_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::TreeSitterProcessConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `structure` field from a `TreeSitterProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_process_config_structure(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TreeSitterProcessConfig, _>(handle, |obj| obj.structure as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `imports` field from a `TreeSitterProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_process_config_imports(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TreeSitterProcessConfig, _>(handle, |obj| obj.imports as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `exports` field from a `TreeSitterProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_process_config_exports(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TreeSitterProcessConfig, _>(handle, |obj| obj.exports as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `comments` field from a `TreeSitterProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_process_config_comments(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TreeSitterProcessConfig, _>(handle, |obj| obj.comments as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `docstrings` field from a `TreeSitterProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_process_config_docstrings(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TreeSitterProcessConfig, _>(handle, |obj| obj.docstrings as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `symbols` field from a `TreeSitterProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_process_config_symbols(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TreeSitterProcessConfig, _>(handle, |obj| obj.symbols as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `diagnostics` field from a `TreeSitterProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_process_config_diagnostics(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TreeSitterProcessConfig, _>(handle, |obj| obj.diagnostics as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `data_extraction` field from a `TreeSitterProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_process_config_data_extraction(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TreeSitterProcessConfig, _>(handle, |obj| obj.data_extraction as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `chunk_max_size` field from a `TreeSitterProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_process_config_chunk_max_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TreeSitterProcessConfig, _>(handle, |obj| match &obj.chunk_max_size {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `content_mode` field from a `TreeSitterProcessConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_code_content_mode_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tree_sitter_process_config_content_mode(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::TreeSitterProcessConfig, _>(handle, |obj| match insert_handle(obj.content_mode) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `SupportedFormat` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_supported_format_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_supported_format_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::SupportedFormat>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `SupportedFormat` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_supported_format_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::SupportedFormat, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `SupportedFormat` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_supported_format_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::SupportedFormat>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `extension` field from a `SupportedFormat`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_supported_format_extension(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::SupportedFormat, _>(handle, |obj| match CString::new(obj.extension.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `mime_type` field from a `SupportedFormat`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_supported_format_mime_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::SupportedFormat, _>(handle, |obj| match CString::new(obj.mime_type.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "api-types")]
/// Create a `ServerConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_server_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_server_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ServerConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "api-types")]
/// Serialize a `ServerConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_server_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ServerConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "api-types")]
/// Free a `ServerConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_server_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ServerConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "api-types")]
/// Get the `host` field from a `ServerConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_server_config_host(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ServerConfig, _>(handle, |obj| match CString::new(obj.host.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "api-types")]
/// Get the `port` field from a `ServerConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_server_config_port(handle: AlefHandle) -> u16 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ServerConfig, _>(handle, |obj| obj.port);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "api-types")]
/// Get the `cors_origins` field from a `ServerConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_server_config_cors_origins(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ServerConfig, _>(handle, |obj| match serde_json::to_string(&obj.cors_origins) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "api-types")]
/// Get the `max_request_body_bytes` field from a `ServerConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_server_config_max_request_body_bytes(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ServerConfig, _>(handle, |obj| obj.max_request_body_bytes);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "api-types")]
/// Get the `max_multipart_field_bytes` field from a `ServerConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_server_config_max_multipart_field_bytes(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ServerConfig, _>(handle, |obj| obj.max_multipart_field_bytes);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the server listen address (host:port).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use xberg::core::ServerConfig;
///
/// let config = ServerConfig::default();
/// assert_eq!(config.listen_addr(), "127.0.0.1:8000");
/// \endcode
#[cfg(feature = "api-types")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_server_config_listen_addr(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::ServerConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<xberg::ServerConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.listen_addr();
        match CString::new(result.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Check if CORS allows all origins.
///
/// Returns `true` if the `cors_origins` vector is empty, meaning all origins
/// are allowed. Returns `false` if specific origins are configured.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use xberg::core::ServerConfig;
///
/// let mut config = ServerConfig::default();
/// assert!(config.cors_allows_all());
///
/// config.cors_origins.push("https://example.com".to_string());
/// assert!(!config.cors_allows_all());
/// \endcode
#[cfg(feature = "api-types")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_server_config_cors_allows_all(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::ServerConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::ServerConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.cors_allows_all();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Check if a given origin is allowed by CORS configuration.
///
/// Returns `true` if:
/// - CORS allows all origins (empty origins list), or
/// - The given origin is in the allowed origins list
/// \param origin The origin to check (e.g., "<https://example.com>")
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use xberg::core::ServerConfig;
///
/// let mut config = ServerConfig::default();
/// assert!(config.is_origin_allowed("https://example.com"));
///
/// config.cors_origins.push("https://allowed.com".to_string());
/// assert!(config.is_origin_allowed("https://allowed.com"));
/// assert!(!config.is_origin_allowed("https://denied.com"));
/// \endcode
#[cfg(feature = "api-types")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_server_config_is_origin_allowed(
    this: AlefHandle,
    origin: *const std::ffi::c_char,
) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::ServerConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::ServerConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if origin.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'origin'");
            return 0;
        }
        // SAFETY: null check above guarantees origin is a valid pointer; string is valid UTF-8 from caller.
        let origin_rs = match unsafe { CStr::from_ptr(origin) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'origin'");
                return 0;
            }
        };

        let result = obj.is_origin_allowed(&origin_rs);
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Get maximum request body size in megabytes (rounded up).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use xberg::core::ServerConfig;
///
/// let mut config = ServerConfig::default();
/// assert_eq!(config.max_request_body_mb(), 100);
/// \endcode
#[cfg(feature = "api-types")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_server_config_max_request_body_mb(this: AlefHandle) -> usize {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::ServerConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::ServerConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        obj.max_request_body_mb()
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Get maximum multipart field size in megabytes (rounded up).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use xberg::core::ServerConfig;
///
/// let mut config = ServerConfig::default();
/// assert_eq!(config.max_multipart_field_mb(), 100);
/// \endcode
#[cfg(feature = "api-types")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_server_config_max_multipart_field_mb(this: AlefHandle) -> usize {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::ServerConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::ServerConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        obj.max_multipart_field_mb()
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Create a `StructuredDataResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_structured_data_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_result_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::extraction::structured::StructuredDataResult>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `StructuredDataResult` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_result_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::extraction::structured::StructuredDataResult, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `StructuredDataResult` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_result_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::extraction::structured::StructuredDataResult>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `content` field from a `StructuredDataResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_result_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::extraction::structured::StructuredDataResult, _>(handle, |obj| {
            match CString::new(obj.content.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `format` field from a `StructuredDataResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_result_format(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::extraction::structured::StructuredDataResult, _>(handle, |obj| {
            match CString::new(obj.format.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `metadata` field from a `StructuredDataResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_result_metadata(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::extraction::structured::StructuredDataResult, _>(handle, |obj| {
            match serde_json::to_string(&obj.metadata) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `text_fields` field from a `StructuredDataResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_result_text_fields(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::extraction::structured::StructuredDataResult, _>(handle, |obj| {
            match serde_json::to_string(&obj.text_fields) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `value` field from a `StructuredDataResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_result_value(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::extraction::structured::StructuredDataResult, _>(handle, |obj| match &obj.value {
                Some(val) => match serde_json::to_string(&val) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                },
                None => std::ptr::null_mut(),
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `flattened` field from a `StructuredDataResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_result_flattened(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::extraction::structured::StructuredDataResult, _>(handle, |obj| {
            match serde_json::to_string(&obj.flattened) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Create a `DocxAppProperties` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_docx_app_properties_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DocxAppProperties>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "office")]
/// Serialize a `DocxAppProperties` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DocxAppProperties, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "office")]
/// Free a `DocxAppProperties` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DocxAppProperties>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `application` field from a `DocxAppProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_application(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DocxAppProperties, _>(handle, |obj| match &obj.application {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `app_version` field from a `DocxAppProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_app_version(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DocxAppProperties, _>(handle, |obj| match &obj.app_version {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `template` field from a `DocxAppProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_template(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DocxAppProperties, _>(handle, |obj| match &obj.template {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `total_time` field from a `DocxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_total_time(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocxAppProperties, _>(handle, |obj| match &obj.total_time {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `pages` field from a `DocxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_pages(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocxAppProperties, _>(handle, |obj| match &obj.pages {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `words` field from a `DocxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_words(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocxAppProperties, _>(handle, |obj| match &obj.words {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `characters` field from a `DocxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_characters(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocxAppProperties, _>(handle, |obj| match &obj.characters {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `characters_with_spaces` field from a `DocxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_characters_with_spaces(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocxAppProperties, _>(handle, |obj| match &obj.characters_with_spaces {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `lines` field from a `DocxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_lines(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocxAppProperties, _>(handle, |obj| match &obj.lines {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `paragraphs` field from a `DocxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_paragraphs(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocxAppProperties, _>(handle, |obj| match &obj.paragraphs {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `company` field from a `DocxAppProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_company(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DocxAppProperties, _>(handle, |obj| match &obj.company {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `doc_security` field from a `DocxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_doc_security(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocxAppProperties, _>(handle, |obj| match &obj.doc_security {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `scale_crop` field from a `DocxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_scale_crop(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocxAppProperties, _>(handle, |obj| match &obj.scale_crop {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `links_up_to_date` field from a `DocxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_links_up_to_date(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocxAppProperties, _>(handle, |obj| match &obj.links_up_to_date {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `shared_doc` field from a `DocxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_shared_doc(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocxAppProperties, _>(handle, |obj| match &obj.shared_doc {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `hyperlinks_changed` field from a `DocxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_app_properties_hyperlinks_changed(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocxAppProperties, _>(handle, |obj| match &obj.hyperlinks_changed {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `XlsxAppProperties` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_xlsx_app_properties_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xlsx_app_properties_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::extraction::office_metadata::app_properties::XlsxAppProperties>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `XlsxAppProperties` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xlsx_app_properties_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::extraction::office_metadata::app_properties::XlsxAppProperties, _>(
            handle,
            serde_json::to_string,
        ) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `XlsxAppProperties` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xlsx_app_properties_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) =
                remove_handle::<xberg::extraction::office_metadata::app_properties::XlsxAppProperties>(handle)
            {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `application` field from a `XlsxAppProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xlsx_app_properties_application(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::XlsxAppProperties, _>(handle, |obj| {
                match &obj.application {
                    Some(val) => match CString::new(val.to_string()) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI field value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    None => std::ptr::null_mut(),
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `app_version` field from a `XlsxAppProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xlsx_app_properties_app_version(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::XlsxAppProperties, _>(handle, |obj| {
                match &obj.app_version {
                    Some(val) => match CString::new(val.to_string()) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI field value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    None => std::ptr::null_mut(),
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `doc_security` field from a `XlsxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xlsx_app_properties_doc_security(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::XlsxAppProperties, _>(handle, |obj| {
                match &obj.doc_security {
                    Some(val) => *val,
                    None => 0,
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `scale_crop` field from a `XlsxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xlsx_app_properties_scale_crop(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::XlsxAppProperties, _>(handle, |obj| {
                match &obj.scale_crop {
                    Some(val) => *val as i32,
                    None => 0,
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `links_up_to_date` field from a `XlsxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xlsx_app_properties_links_up_to_date(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::XlsxAppProperties, _>(handle, |obj| {
                match &obj.links_up_to_date {
                    Some(val) => *val as i32,
                    None => 0,
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `shared_doc` field from a `XlsxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xlsx_app_properties_shared_doc(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::XlsxAppProperties, _>(handle, |obj| {
                match &obj.shared_doc {
                    Some(val) => *val as i32,
                    None => 0,
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `hyperlinks_changed` field from a `XlsxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xlsx_app_properties_hyperlinks_changed(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::XlsxAppProperties, _>(handle, |obj| {
                match &obj.hyperlinks_changed {
                    Some(val) => *val as i32,
                    None => 0,
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `company` field from a `XlsxAppProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xlsx_app_properties_company(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::XlsxAppProperties, _>(handle, |obj| {
                match &obj.company {
                    Some(val) => match CString::new(val.to_string()) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI field value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    None => std::ptr::null_mut(),
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `worksheet_names` field from a `XlsxAppProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xlsx_app_properties_worksheet_names(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::XlsxAppProperties, _>(handle, |obj| {
                match serde_json::to_string(&obj.worksheet_names) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `PptxAppProperties` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_pptx_app_properties_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::extraction::office_metadata::app_properties::PptxAppProperties>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PptxAppProperties` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::extraction::office_metadata::app_properties::PptxAppProperties, _>(
            handle,
            serde_json::to_string,
        ) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PptxAppProperties` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) =
                remove_handle::<xberg::extraction::office_metadata::app_properties::PptxAppProperties>(handle)
            {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `application` field from a `PptxAppProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_application(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::PptxAppProperties, _>(handle, |obj| {
                match &obj.application {
                    Some(val) => match CString::new(val.to_string()) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI field value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    None => std::ptr::null_mut(),
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `app_version` field from a `PptxAppProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_app_version(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::PptxAppProperties, _>(handle, |obj| {
                match &obj.app_version {
                    Some(val) => match CString::new(val.to_string()) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI field value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    None => std::ptr::null_mut(),
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `total_time` field from a `PptxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_total_time(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::PptxAppProperties, _>(handle, |obj| {
                match &obj.total_time {
                    Some(val) => *val,
                    None => 0,
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `company` field from a `PptxAppProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_company(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::PptxAppProperties, _>(handle, |obj| {
                match &obj.company {
                    Some(val) => match CString::new(val.to_string()) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI field value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    None => std::ptr::null_mut(),
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `doc_security` field from a `PptxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_doc_security(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::PptxAppProperties, _>(handle, |obj| {
                match &obj.doc_security {
                    Some(val) => *val,
                    None => 0,
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `scale_crop` field from a `PptxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_scale_crop(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::PptxAppProperties, _>(handle, |obj| {
                match &obj.scale_crop {
                    Some(val) => *val as i32,
                    None => 0,
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `links_up_to_date` field from a `PptxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_links_up_to_date(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::PptxAppProperties, _>(handle, |obj| {
                match &obj.links_up_to_date {
                    Some(val) => *val as i32,
                    None => 0,
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `shared_doc` field from a `PptxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_shared_doc(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::PptxAppProperties, _>(handle, |obj| {
                match &obj.shared_doc {
                    Some(val) => *val as i32,
                    None => 0,
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `hyperlinks_changed` field from a `PptxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_hyperlinks_changed(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::PptxAppProperties, _>(handle, |obj| {
                match &obj.hyperlinks_changed {
                    Some(val) => *val as i32,
                    None => 0,
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `slides` field from a `PptxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_slides(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::PptxAppProperties, _>(handle, |obj| {
                match &obj.slides {
                    Some(val) => *val,
                    None => 0,
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `notes` field from a `PptxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_notes(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::PptxAppProperties, _>(handle, |obj| {
                match &obj.notes {
                    Some(val) => *val,
                    None => 0,
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `hidden_slides` field from a `PptxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_hidden_slides(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::PptxAppProperties, _>(handle, |obj| {
                match &obj.hidden_slides {
                    Some(val) => *val,
                    None => 0,
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `multimedia_clips` field from a `PptxAppProperties`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_multimedia_clips(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::PptxAppProperties, _>(handle, |obj| {
                match &obj.multimedia_clips {
                    Some(val) => *val,
                    None => 0,
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `presentation_format` field from a `PptxAppProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_presentation_format(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::PptxAppProperties, _>(handle, |obj| {
                match &obj.presentation_format {
                    Some(val) => match CString::new(val.to_string()) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI field value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    None => std::ptr::null_mut(),
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `slide_titles` field from a `PptxAppProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_app_properties_slide_titles(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::extraction::office_metadata::app_properties::PptxAppProperties, _>(handle, |obj| {
                match serde_json::to_string(&obj.slide_titles) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Create a `CoreProperties` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_core_properties_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::CoreProperties>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "office")]
/// Serialize a `CoreProperties` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::CoreProperties, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "office")]
/// Free a `CoreProperties` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::CoreProperties>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `title` field from a `CoreProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_title(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CoreProperties, _>(handle, |obj| match &obj.title {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `subject` field from a `CoreProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_subject(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CoreProperties, _>(handle, |obj| match &obj.subject {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `creator` field from a `CoreProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_creator(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CoreProperties, _>(handle, |obj| match &obj.creator {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `keywords` field from a `CoreProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_keywords(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CoreProperties, _>(handle, |obj| match &obj.keywords {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `description` field from a `CoreProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_description(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CoreProperties, _>(handle, |obj| match &obj.description {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `last_modified_by` field from a `CoreProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_last_modified_by(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CoreProperties, _>(handle, |obj| match &obj.last_modified_by {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `revision` field from a `CoreProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_revision(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CoreProperties, _>(handle, |obj| match &obj.revision {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `created` field from a `CoreProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_created(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CoreProperties, _>(handle, |obj| match &obj.created {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `modified` field from a `CoreProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_modified(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CoreProperties, _>(handle, |obj| match &obj.modified {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `category` field from a `CoreProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_category(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CoreProperties, _>(handle, |obj| match &obj.category {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `content_status` field from a `CoreProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_content_status(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CoreProperties, _>(handle, |obj| match &obj.content_status {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `language` field from a `CoreProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CoreProperties, _>(handle, |obj| match &obj.language {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `identifier` field from a `CoreProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_identifier(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CoreProperties, _>(handle, |obj| match &obj.identifier {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `version` field from a `CoreProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_version(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CoreProperties, _>(handle, |obj| match &obj.version {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `last_printed` field from a `CoreProperties`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_core_properties_last_printed(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CoreProperties, _>(handle, |obj| match &obj.last_printed {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `SecurityLimits` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_security_limits_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_security_limits_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::SecurityLimits>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `SecurityLimits` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_security_limits_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::SecurityLimits, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `SecurityLimits` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_security_limits_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::SecurityLimits>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `max_archive_size` field from a `SecurityLimits`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_security_limits_max_archive_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SecurityLimits, _>(handle, |obj| obj.max_archive_size);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_compression_ratio` field from a `SecurityLimits`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_security_limits_max_compression_ratio(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SecurityLimits, _>(handle, |obj| obj.max_compression_ratio);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_files_in_archive` field from a `SecurityLimits`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_security_limits_max_files_in_archive(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SecurityLimits, _>(handle, |obj| obj.max_files_in_archive);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_nesting_depth` field from a `SecurityLimits`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_security_limits_max_nesting_depth(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SecurityLimits, _>(handle, |obj| obj.max_nesting_depth);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_entity_length` field from a `SecurityLimits`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_security_limits_max_entity_length(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SecurityLimits, _>(handle, |obj| obj.max_entity_length);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_content_size` field from a `SecurityLimits`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_security_limits_max_content_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SecurityLimits, _>(handle, |obj| obj.max_content_size);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_iterations` field from a `SecurityLimits`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_security_limits_max_iterations(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SecurityLimits, _>(handle, |obj| obj.max_iterations);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_xml_depth` field from a `SecurityLimits`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_security_limits_max_xml_depth(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SecurityLimits, _>(handle, |obj| obj.max_xml_depth);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_table_cells` field from a `SecurityLimits`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_security_limits_max_table_cells(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SecurityLimits, _>(handle, |obj| obj.max_table_cells);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "quality")]
/// Create a `TokenReductionConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_token_reduction_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::TokenReductionConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "quality")]
/// Serialize a `TokenReductionConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::TokenReductionConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "quality")]
/// Free a `TokenReductionConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::TokenReductionConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "quality")]
/// Get the `level` field from a `TokenReductionConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_reduction_level_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_config_level(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TokenReductionConfig, _>(handle, |obj| match insert_handle(obj.level) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "quality")]
/// Get the `language_hint` field from a `TokenReductionConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_config_language_hint(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::TokenReductionConfig, _>(handle, |obj| match &obj.language_hint {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "quality")]
/// Get the `preserve_markdown` field from a `TokenReductionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_config_preserve_markdown(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TokenReductionConfig, _>(handle, |obj| obj.preserve_markdown as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "quality")]
/// Get the `preserve_code` field from a `TokenReductionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_config_preserve_code(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TokenReductionConfig, _>(handle, |obj| obj.preserve_code as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "quality")]
/// Get the `semantic_threshold` field from a `TokenReductionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_config_semantic_threshold(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::TokenReductionConfig, _>(handle, |obj| obj.semantic_threshold);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "quality")]
/// Get the `enable_parallel` field from a `TokenReductionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_config_enable_parallel(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TokenReductionConfig, _>(handle, |obj| obj.enable_parallel as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "quality")]
/// Get the `use_simd` field from a `TokenReductionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_config_use_simd(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TokenReductionConfig, _>(handle, |obj| obj.use_simd as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "quality")]
/// Get the `custom_stopwords` field from a `TokenReductionConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_config_custom_stopwords(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::TokenReductionConfig, _>(handle, |obj| match &obj.custom_stopwords {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "quality")]
/// Get the `preserve_patterns` field from a `TokenReductionConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_config_preserve_patterns(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::TokenReductionConfig, _>(handle, |obj| {
            match serde_json::to_string(&obj.preserve_patterns) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "quality")]
/// Get the `target_reduction` field from a `TokenReductionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_config_target_reduction(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::TokenReductionConfig, _>(handle, |obj| match &obj.target_reduction {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "quality")]
/// Get the `enable_semantic_clustering` field from a `TokenReductionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_config_enable_semantic_clustering(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TokenReductionConfig, _>(handle, |obj| obj.enable_semantic_clustering as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "quality")]
/// Get the `preserve_important_words` field from a `TokenReductionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_reduction_config_preserve_important_words(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TokenReductionConfig, _>(handle, |obj| obj.preserve_important_words as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `PatternMatch` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_pattern_match_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pattern_match_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::text::redaction::patterns::PatternMatch>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PatternMatch` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pattern_match_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::text::redaction::patterns::PatternMatch, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PatternMatch` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pattern_match_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::text::redaction::patterns::PatternMatch>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `start` field from a `PatternMatch`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pattern_match_start(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::text::redaction::patterns::PatternMatch, _>(handle, |obj| obj.start);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `end` field from a `PatternMatch`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pattern_match_end(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::text::redaction::patterns::PatternMatch, _>(handle, |obj| obj.end);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `category` field from a `PatternMatch`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_pii_category_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pattern_match_category(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::text::redaction::patterns::PatternMatch, _>(handle, |obj| {
                match insert_handle(obj.category.clone()) {
                    Ok(handle) => handle,
                    Err(error) => {
                        set_handle_error(&error);
                        0
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `text` field from a `PatternMatch`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pattern_match_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::text::redaction::patterns::PatternMatch, _>(handle, |obj| {
                match CString::new(obj.text.to_string()) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI field value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "redaction")]
/// Free a `TokenCounter` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_counter_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::TokenCounter>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "redaction")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_token_counter_new() -> AlefHandle {
    catch_ffi_panic(0, || {
        let result = xberg::TokenCounter::new();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Create a `FootnoteConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_footnote_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::FootnoteConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Serialize a `FootnoteConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::FootnoteConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Free a `FootnoteConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::FootnoteConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Get the `parse_citations` field from a `FootnoteConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_config_parse_citations(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FootnoteConfig, _>(handle, |obj| obj.parse_citations as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Set whether to parse the citation block.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "markdown-footnotes")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_config_with_parse_citations(this: AlefHandle, enabled: i32) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();

        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<xberg::FootnoteConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let enabled_rs = enabled != 0;

        let result = obj.with_parse_citations(enabled_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

#[cfg(feature = "markdown-footnotes")]
/// Create a `FootnoteAnchor` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_footnote_anchor_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_anchor_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::FootnoteAnchor>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Serialize a `FootnoteAnchor` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_anchor_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::FootnoteAnchor, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Free a `FootnoteAnchor` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_anchor_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::FootnoteAnchor>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Get the `label` field from a `FootnoteAnchor`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_anchor_label(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::FootnoteAnchor, _>(handle, |obj| match CString::new(obj.label.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Get the `offset` field from a `FootnoteAnchor`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_anchor_offset(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FootnoteAnchor, _>(handle, |obj| obj.offset);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Create a `FootnoteDefinition` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_footnote_definition_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_definition_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::FootnoteDefinition>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Serialize a `FootnoteDefinition` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_definition_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::FootnoteDefinition, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Free a `FootnoteDefinition` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_definition_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::FootnoteDefinition>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Get the `label` field from a `FootnoteDefinition`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_definition_label(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::FootnoteDefinition, _>(handle, |obj| match CString::new(obj.label.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Get the `content` field from a `FootnoteDefinition`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_definition_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::FootnoteDefinition, _>(handle, |obj| match CString::new(obj.content.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Get the `offset` field from a `FootnoteDefinition`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_definition_offset(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FootnoteDefinition, _>(handle, |obj| obj.offset);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Create a `Citation` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_citation_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_citation_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::Citation>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Serialize a `Citation` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_citation_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::Citation, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Free a `Citation` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_citation_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::Citation>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Get the `label` field from a `Citation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_citation_label(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Citation, _>(handle, |obj| match CString::new(obj.label.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Get the `source` field from a `Citation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_citation_source(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Citation, _>(handle, |obj| match CString::new(obj.source.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Get the `locator` field from a `Citation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_citation_locator(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Citation, _>(handle, |obj| match &obj.locator {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "markdown-footnotes")]
/// Get the `excerpt` field from a `Citation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_citation_excerpt(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Citation, _>(handle, |obj| match &obj.excerpt {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `PdfAnnotation` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_pdf_annotation_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PdfAnnotation>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PdfAnnotation` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PdfAnnotation, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PdfAnnotation` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PdfAnnotation>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `annotation_type` field from a `PdfAnnotation`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_pdf_annotation_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_annotation_type(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PdfAnnotation, _>(handle, |obj| match insert_handle(obj.annotation_type) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `content` field from a `PdfAnnotation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PdfAnnotation, _>(handle, |obj| match &obj.content {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `page_number` field from a `PdfAnnotation`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_page_number(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PdfAnnotation, _>(handle, |obj| obj.page_number);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `bounding_box` field from a `PdfAnnotation`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_bounding_box_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_bounding_box(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PdfAnnotation, _>(handle, |obj| match &obj.bounding_box {
            Some(val) => match insert_handle(*val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `author` field from a `PdfAnnotation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_author(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PdfAnnotation, _>(handle, |obj| match &obj.author {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `modified` field from a `PdfAnnotation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_modified(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PdfAnnotation, _>(handle, |obj| match &obj.modified {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `color` field from a `PdfAnnotation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_color(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PdfAnnotation, _>(handle, |obj| match &obj.color {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `subject` field from a `PdfAnnotation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_subject(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PdfAnnotation, _>(handle, |obj| match &obj.subject {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `quad_points` field from a `PdfAnnotation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_quad_points(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PdfAnnotation, _>(handle, |obj| match &obj.quad_points {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `marked_text` field from a `PdfAnnotation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_marked_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PdfAnnotation, _>(handle, |obj| match &obj.marked_text {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `PageClassification` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_page_classification_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_classification_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PageClassification>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PageClassification` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_classification_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PageClassification, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PageClassification` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_classification_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PageClassification>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `page_number` field from a `PageClassification`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_classification_page_number(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageClassification, _>(handle, |obj| obj.page_number);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `labels` field from a `PageClassification`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_classification_labels(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PageClassification, _>(handle, |obj| match serde_json::to_string(&obj.labels) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ClassificationLabel` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_classification_label_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_classification_label_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ClassificationLabel>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ClassificationLabel` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_classification_label_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ClassificationLabel, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ClassificationLabel` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_classification_label_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ClassificationLabel>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `label` field from a `ClassificationLabel`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_classification_label_label(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ClassificationLabel, _>(handle, |obj| match CString::new(obj.label.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `confidence` field from a `ClassificationLabel`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_classification_label_confidence(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::ClassificationLabel, _>(handle, |obj| match &obj.confidence {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Create a `DjotContent` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_djot_content_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_content_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DjotContent>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DjotContent` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_content_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DjotContent, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DjotContent` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_content_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DjotContent>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `plain_text` field from a `DjotContent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_content_plain_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::DjotContent, _>(handle, |obj| match CString::new(obj.plain_text.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `blocks` field from a `DjotContent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_content_blocks(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DjotContent, _>(handle, |obj| match serde_json::to_string(&obj.blocks) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `metadata` field from a `DjotContent`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_metadata_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_content_metadata(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DjotContent, _>(handle, |obj| match insert_handle(obj.metadata.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `tables` field from a `DjotContent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_content_tables(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DjotContent, _>(handle, |obj| match serde_json::to_string(&obj.tables) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `images` field from a `DjotContent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_content_images(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DjotContent, _>(handle, |obj| match serde_json::to_string(&obj.images) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `links` field from a `DjotContent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_content_links(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DjotContent, _>(handle, |obj| match serde_json::to_string(&obj.links) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `footnotes` field from a `DjotContent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_content_footnotes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DjotContent, _>(handle, |obj| match serde_json::to_string(&obj.footnotes) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `FormattedBlock` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_formatted_block_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_formatted_block_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::FormattedBlock>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `FormattedBlock` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_formatted_block_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::FormattedBlock, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `FormattedBlock` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_formatted_block_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::FormattedBlock>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `block_type` field from a `FormattedBlock`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_block_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_formatted_block_block_type(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::FormattedBlock, _>(handle, |obj| match insert_handle(obj.block_type.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `level` field from a `FormattedBlock`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_formatted_block_level(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::FormattedBlock, _>(handle, |obj| match &obj.level {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `inline_content` field from a `FormattedBlock`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_formatted_block_inline_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::FormattedBlock, _>(handle, |obj| match serde_json::to_string(&obj.inline_content) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `language` field from a `FormattedBlock`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_formatted_block_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::FormattedBlock, _>(handle, |obj| match &obj.language {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `code` field from a `FormattedBlock`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_formatted_block_code(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::FormattedBlock, _>(handle, |obj| match &obj.code {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `children` field from a `FormattedBlock`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_formatted_block_children(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::FormattedBlock, _>(handle, |obj| match serde_json::to_string(&obj.children) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `InlineElement` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_inline_element_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_inline_element_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::InlineElement>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `InlineElement` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_inline_element_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::InlineElement, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `InlineElement` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_inline_element_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::InlineElement>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `element_type` field from a `InlineElement`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_inline_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_inline_element_element_type(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::InlineElement, _>(handle, |obj| match insert_handle(obj.element_type.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `content` field from a `InlineElement`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_inline_element_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::InlineElement, _>(handle, |obj| match CString::new(obj.content.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `metadata` field from a `InlineElement`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_inline_element_metadata(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::InlineElement, _>(handle, |obj| match &obj.metadata {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `DjotImage` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_djot_image_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_image_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DjotImage>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DjotImage` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_image_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DjotImage, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DjotImage` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_image_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DjotImage>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `src` field from a `DjotImage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_image_src(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DjotImage, _>(handle, |obj| match CString::new(obj.src.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `alt` field from a `DjotImage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_image_alt(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DjotImage, _>(handle, |obj| match CString::new(obj.alt.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `title` field from a `DjotImage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_image_title(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DjotImage, _>(handle, |obj| match &obj.title {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `DjotLink` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_djot_link_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_link_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DjotLink>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DjotLink` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_link_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DjotLink, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DjotLink` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_link_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DjotLink>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `url` field from a `DjotLink`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_link_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DjotLink, _>(handle, |obj| match CString::new(obj.url.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `text` field from a `DjotLink`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_link_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DjotLink, _>(handle, |obj| match CString::new(obj.text.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `title` field from a `DjotLink`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_djot_link_title(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DjotLink, _>(handle, |obj| match &obj.title {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `Footnote` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_footnote_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::Footnote>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `Footnote` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::Footnote, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `Footnote` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::Footnote>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `label` field from a `Footnote`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_label(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Footnote, _>(handle, |obj| match CString::new(obj.label.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `content` field from a `Footnote`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_footnote_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Footnote, _>(handle, |obj| match serde_json::to_string(&obj.content) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `DocumentStructure` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_document_structure_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_structure_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DocumentStructure>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DocumentStructure` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_structure_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DocumentStructure, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DocumentStructure` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_structure_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DocumentStructure>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `nodes` field from a `DocumentStructure`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_structure_nodes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::DocumentStructure, _>(handle, |obj| match serde_json::to_string(&obj.nodes) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `source_format` field from a `DocumentStructure`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_structure_source_format(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DocumentStructure, _>(handle, |obj| match &obj.source_format {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `relationships` field from a `DocumentStructure`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_structure_relationships(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::DocumentStructure, _>(handle, |obj| match serde_json::to_string(&obj.relationships) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `node_types` field from a `DocumentStructure`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_structure_node_types(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::DocumentStructure, _>(handle, |obj| match serde_json::to_string(&obj.node_types) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Compute and populate the `node_types` field from the current `nodes`.
///
/// Call this after all nodes have been added to the structure. Internal
/// construction paths (builder, derivation) call this automatically.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use xberg::types::document_structure::{DocumentStructure, DocumentNode, NodeContent};
///
/// let mut structure = DocumentStructure {
///     nodes: vec![DocumentNode {
///         id: String::new(),
///         content: NodeContent::Paragraph { text: "Hello".into() },
///         parent: None,
///         children: vec![],
///         content_layer: Default::default(),
///         page: None,
///         page_end: None,
///         bbox: None,
///         annotations: vec![],
///         attributes: None,
///     }],
///     source_format: None,
///     relationships: vec![],
///     node_types: vec![],
/// };
/// structure.finalize_node_types();
/// assert!(structure.node_types.contains(&"paragraph".to_string()));
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_structure_finalize_node_types(this: AlefHandle) {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::DocumentStructure>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::DocumentStructure>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &mut *obj_ptr };

        let result = obj.finalize_node_types();
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            ()
        }
    }
}

/// Check if the document structure is empty.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_structure_is_empty(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::DocumentStructure>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::DocumentStructure>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.is_empty();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Create a `DocumentRelationship` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_document_relationship_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_relationship_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DocumentRelationship>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DocumentRelationship` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_relationship_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DocumentRelationship, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DocumentRelationship` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_relationship_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DocumentRelationship>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `source` field from a `DocumentRelationship`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_relationship_source(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentRelationship, _>(handle, |obj| obj.source.0);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `target` field from a `DocumentRelationship`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_relationship_target(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentRelationship, _>(handle, |obj| obj.target.0);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `kind` field from a `DocumentRelationship`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_relationship_kind_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_relationship_kind(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentRelationship, _>(handle, |obj| match insert_handle(obj.kind) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `DocumentNode` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_document_node_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_node_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DocumentNode>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DocumentNode` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_node_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DocumentNode, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DocumentNode` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_node_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DocumentNode>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `id` field from a `DocumentNode`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_node_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DocumentNode, _>(handle, |obj| match CString::new(obj.id.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `content` field from a `DocumentNode`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_node_content_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_node_content(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentNode, _>(handle, |obj| match insert_handle(obj.content.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `parent` field from a `DocumentNode`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_node_parent(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentNode, _>(handle, |obj| match &obj.parent {
            Some(val) => val.0,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `children` field from a `DocumentNode`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_node_children(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DocumentNode, _>(handle, |obj| match serde_json::to_string(&obj.children) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `content_layer` field from a `DocumentNode`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_content_layer_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_node_content_layer(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentNode, _>(handle, |obj| match insert_handle(obj.content_layer) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `page` field from a `DocumentNode`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_node_page(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentNode, _>(handle, |obj| match &obj.page {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `page_end` field from a `DocumentNode`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_node_page_end(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentNode, _>(handle, |obj| match &obj.page_end {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `bbox` field from a `DocumentNode`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_bounding_box_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_node_bbox(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentNode, _>(handle, |obj| match &obj.bbox {
            Some(val) => match insert_handle(*val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `annotations` field from a `DocumentNode`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_node_annotations(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::DocumentNode, _>(handle, |obj| match serde_json::to_string(&obj.annotations) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `attributes` field from a `DocumentNode`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_node_attributes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DocumentNode, _>(handle, |obj| match &obj.attributes {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `TableGrid` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_table_grid_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_grid_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::TableGrid>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `TableGrid` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_grid_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::TableGrid, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `TableGrid` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_grid_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::TableGrid>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `rows` field from a `TableGrid`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_grid_rows(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TableGrid, _>(handle, |obj| obj.rows);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `cols` field from a `TableGrid`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_grid_cols(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TableGrid, _>(handle, |obj| obj.cols);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `cells` field from a `TableGrid`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_grid_cells(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::TableGrid, _>(handle, |obj| match serde_json::to_string(&obj.cells) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `GridCell` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_grid_cell_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_grid_cell_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::GridCell>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `GridCell` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_grid_cell_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::GridCell, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `GridCell` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_grid_cell_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::GridCell>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `content` field from a `GridCell`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_grid_cell_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::GridCell, _>(handle, |obj| match CString::new(obj.content.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `row` field from a `GridCell`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_grid_cell_row(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::GridCell, _>(handle, |obj| obj.row);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `col` field from a `GridCell`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_grid_cell_col(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::GridCell, _>(handle, |obj| obj.col);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `row_span` field from a `GridCell`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_grid_cell_row_span(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::GridCell, _>(handle, |obj| obj.row_span);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `col_span` field from a `GridCell`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_grid_cell_col_span(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::GridCell, _>(handle, |obj| obj.col_span);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `is_header` field from a `GridCell`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_grid_cell_is_header(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::GridCell, _>(handle, |obj| obj.is_header as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `bbox` field from a `GridCell`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_bounding_box_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_grid_cell_bbox(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::GridCell, _>(handle, |obj| match &obj.bbox {
            Some(val) => match insert_handle(*val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `TextAnnotation` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_text_annotation_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_annotation_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::TextAnnotation>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `TextAnnotation` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_annotation_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::TextAnnotation, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `TextAnnotation` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_annotation_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::TextAnnotation>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `start` field from a `TextAnnotation`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_annotation_start(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TextAnnotation, _>(handle, |obj| obj.start);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `end` field from a `TextAnnotation`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_annotation_end(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TextAnnotation, _>(handle, |obj| obj.end);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `kind` field from a `TextAnnotation`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_annotation_kind_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_annotation_kind(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TextAnnotation, _>(handle, |obj| match insert_handle(obj.kind.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `Entity` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_entity_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_entity_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::Entity>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `Entity` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_entity_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::Entity, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `Entity` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_entity_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::Entity>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `category` field from a `Entity`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_entity_category_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_entity_category(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Entity, _>(handle, |obj| match insert_handle(obj.category.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `text` field from a `Entity`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_entity_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Entity, _>(handle, |obj| match CString::new(obj.text.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `start` field from a `Entity`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_entity_start(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Entity, _>(handle, |obj| obj.start);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `end` field from a `Entity`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_entity_end(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Entity, _>(handle, |obj| obj.end);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `confidence` field from a `Entity`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_entity_confidence(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::Entity, _>(handle, |obj| match &obj.confidence {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Create a `DocumentCounts` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_document_counts_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_counts_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DocumentCounts>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DocumentCounts` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_counts_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DocumentCounts, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DocumentCounts` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_counts_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DocumentCounts>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `pages` field from a `DocumentCounts`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_counts_pages(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentCounts, _>(handle, |obj| obj.pages);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `tables` field from a `DocumentCounts`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_counts_tables(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentCounts, _>(handle, |obj| obj.tables);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `images` field from a `DocumentCounts`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_counts_images(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentCounts, _>(handle, |obj| obj.images);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `LanguageConfidence` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_language_confidence_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_language_confidence_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::LanguageConfidence>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LanguageConfidence` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_language_confidence_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::LanguageConfidence, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LanguageConfidence` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_language_confidence_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::LanguageConfidence>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `language` field from a `LanguageConfidence`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_language_confidence_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::LanguageConfidence, _>(handle, |obj| match CString::new(obj.language.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `confidence` field from a `LanguageConfidence`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_language_confidence_confidence(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::LanguageConfidence, _>(handle, |obj| obj.confidence);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `proportion` field from a `LanguageConfidence`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_language_confidence_proportion(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::LanguageConfidence, _>(handle, |obj| obj.proportion);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `script` field from a `LanguageConfidence`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_language_confidence_script(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::LanguageConfidence, _>(handle, |obj| match CString::new(obj.script.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `reliable` field from a `LanguageConfidence`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_language_confidence_reliable(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LanguageConfidence, _>(handle, |obj| obj.reliable as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ExtractedDocument` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_extracted_document_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ExtractedDocument>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ExtractedDocument` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ExtractedDocument, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ExtractedDocument` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ExtractedDocument>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `content` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match CString::new(obj.content.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `mime_type` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_mime_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match CString::new(obj.mime_type.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `metadata` field from a `ExtractedDocument`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_metadata_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_metadata(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match insert_handle(obj.metadata.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `extraction_method` field from a `ExtractedDocument`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_extraction_method_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_extraction_method(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.extraction_method {
            Some(val) => match insert_handle(*val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `tables` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_tables(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match serde_json::to_string(&obj.tables) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `counts` field from a `ExtractedDocument`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_document_counts_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_counts(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match insert_handle(obj.counts) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `detected_languages` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_detected_languages(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.detected_languages {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `detected_language_confidences` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_detected_language_confidences(
    handle: AlefHandle,
) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.detected_language_confidences {
                Some(val) => match serde_json::to_string(&val) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                },
                None => std::ptr::null_mut(),
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `chunks` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_chunks(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.chunks {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `images` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_images(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.images {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `pages` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_pages(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.pages {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `elements` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_elements(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.elements {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `djot_content` field from a `ExtractedDocument`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_djot_content_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_djot_content(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.djot_content {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `ocr_elements` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_ocr_elements(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.ocr_elements {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `document` field from a `ExtractedDocument`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_document_structure_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_document(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.document {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `extracted_keywords` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_extracted_keywords(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.extracted_keywords {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `quality_score` field from a `ExtractedDocument`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_quality_score(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.quality_score {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `processing_warnings` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_processing_warnings(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| {
            match serde_json::to_string(&obj.processing_warnings) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `annotations` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_annotations(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.annotations {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `children` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_children(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.children {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `uris` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_uris(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.uris {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `revisions` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_revisions(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.revisions {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `structured_output` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_structured_output(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.structured_output {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `code_intelligence` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_code_intelligence(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.code_intelligence {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `llm_usage` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_llm_usage(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.llm_usage {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `entities` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_entities(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.entities {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `summary` field from a `ExtractedDocument`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_document_summary_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_summary(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.summary {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `extraction_confidence` field from a `ExtractedDocument`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_extraction_confidence_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_extraction_confidence(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.extraction_confidence {
            Some(val) => match insert_handle(*val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `translation` field from a `ExtractedDocument`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_translation_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_translation(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.translation {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `page_classifications` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_page_classifications(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.page_classifications {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `redaction_report` field from a `ExtractedDocument`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_redaction_report_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_redaction_report(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match &obj.redaction_report {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `formulas` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_formulas(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match serde_json::to_string(&obj.formulas) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `form_fields` field from a `ExtractedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_document_form_fields(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ExtractedDocument, _>(handle, |obj| match serde_json::to_string(&obj.form_fields) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ArchiveEntry` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_archive_entry_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_archive_entry_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ArchiveEntry>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ArchiveEntry` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_archive_entry_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ArchiveEntry, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ArchiveEntry` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_archive_entry_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ArchiveEntry>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `path` field from a `ArchiveEntry`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_archive_entry_path(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ArchiveEntry, _>(handle, |obj| match CString::new(obj.path.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `mime_type` field from a `ArchiveEntry`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_archive_entry_mime_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ArchiveEntry, _>(handle, |obj| match CString::new(obj.mime_type.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `result` field from a `ArchiveEntry`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_extracted_document_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_archive_entry_result(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ArchiveEntry, _>(handle, |obj| match insert_handle((*obj.result).clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ProcessingWarning` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_processing_warning_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_processing_warning_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ProcessingWarning>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ProcessingWarning` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_processing_warning_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ProcessingWarning, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ProcessingWarning` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_processing_warning_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ProcessingWarning>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `source` field from a `ProcessingWarning`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_processing_warning_source(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ProcessingWarning, _>(handle, |obj| match CString::new(obj.source.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `message` field from a `ProcessingWarning`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_processing_warning_message(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ProcessingWarning, _>(handle, |obj| match CString::new(obj.message.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `LlmUsage` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_llm_usage_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_usage_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::LlmUsage>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LlmUsage` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_usage_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::LlmUsage, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LlmUsage` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_usage_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::LlmUsage>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `model` field from a `LlmUsage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_usage_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LlmUsage, _>(handle, |obj| match CString::new(obj.model.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `source` field from a `LlmUsage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_usage_source(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LlmUsage, _>(handle, |obj| match CString::new(obj.source.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `input_tokens` field from a `LlmUsage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_usage_input_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LlmUsage, _>(handle, |obj| match &obj.input_tokens {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `output_tokens` field from a `LlmUsage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_usage_output_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LlmUsage, _>(handle, |obj| match &obj.output_tokens {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `total_tokens` field from a `LlmUsage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_usage_total_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LlmUsage, _>(handle, |obj| match &obj.total_tokens {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `estimated_cost` field from a `LlmUsage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_usage_estimated_cost(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::LlmUsage, _>(handle, |obj| match &obj.estimated_cost {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `finish_reason` field from a `LlmUsage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_llm_usage_finish_reason(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LlmUsage, _>(handle, |obj| match &obj.finish_reason {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `Chunk` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_chunk_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::Chunk>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `Chunk` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::Chunk, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `Chunk` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::Chunk>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `content` field from a `Chunk`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Chunk, _>(handle, |obj| match CString::new(obj.content.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `chunk_type` field from a `Chunk`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_chunk_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_chunk_type(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Chunk, _>(handle, |obj| match insert_handle(obj.chunk_type) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `embedding` field from a `Chunk`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_embedding(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Chunk, _>(handle, |obj| match &obj.embedding {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `sparse_embedding` field from a `Chunk`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_sparse_embedding_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_sparse_embedding(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Chunk, _>(handle, |obj| match &obj.sparse_embedding {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `late_interaction` field from a `Chunk`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_multi_vector_embedding_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_late_interaction(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Chunk, _>(handle, |obj| match &obj.late_interaction {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `metadata` field from a `Chunk`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_chunk_metadata_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_metadata(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Chunk, _>(handle, |obj| match insert_handle(obj.metadata.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `HeadingContext` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_heading_context_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heading_context_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::HeadingContext>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `HeadingContext` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heading_context_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::HeadingContext, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `HeadingContext` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heading_context_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::HeadingContext>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `headings` field from a `HeadingContext`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heading_context_headings(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::HeadingContext, _>(handle, |obj| match serde_json::to_string(&obj.headings) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `HeadingLevel` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_heading_level_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heading_level_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::HeadingLevel>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `HeadingLevel` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heading_level_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::HeadingLevel, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `HeadingLevel` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heading_level_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::HeadingLevel>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `level` field from a `HeadingLevel`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heading_level_level(handle: AlefHandle) -> u8 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HeadingLevel, _>(handle, |obj| obj.level);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `text` field from a `HeadingLevel`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heading_level_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::HeadingLevel, _>(handle, |obj| match CString::new(obj.text.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ChunkMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_chunk_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ChunkMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ChunkMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ChunkMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ChunkMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ChunkMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `byte_start` field from a `ChunkMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_metadata_byte_start(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkMetadata, _>(handle, |obj| obj.byte_start);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `byte_end` field from a `ChunkMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_metadata_byte_end(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkMetadata, _>(handle, |obj| obj.byte_end);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `token_count` field from a `ChunkMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_metadata_token_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkMetadata, _>(handle, |obj| match &obj.token_count {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `chunk_index` field from a `ChunkMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_metadata_chunk_index(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkMetadata, _>(handle, |obj| obj.chunk_index);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `total_chunks` field from a `ChunkMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_metadata_total_chunks(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkMetadata, _>(handle, |obj| obj.total_chunks);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `first_page` field from a `ChunkMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_metadata_first_page(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkMetadata, _>(handle, |obj| match &obj.first_page {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `last_page` field from a `ChunkMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_metadata_last_page(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkMetadata, _>(handle, |obj| match &obj.last_page {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `heading_context` field from a `ChunkMetadata`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_heading_context_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_metadata_heading_context(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkMetadata, _>(handle, |obj| match &obj.heading_context {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `heading_path` field from a `ChunkMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_metadata_heading_path(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ChunkMetadata, _>(handle, |obj| match serde_json::to_string(&obj.heading_path) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `image_indices` field from a `ChunkMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_metadata_image_indices(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ChunkMetadata, _>(handle, |obj| match serde_json::to_string(&obj.image_indices) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `node_ids` field from a `ChunkMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_metadata_node_ids(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ChunkMetadata, _>(handle, |obj| match serde_json::to_string(&obj.node_ids) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `page_spans` field from a `ChunkMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_metadata_page_spans(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ChunkMetadata, _>(handle, |obj| match serde_json::to_string(&obj.page_spans) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `classifications` field from a `ChunkMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_metadata_classifications(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ChunkMetadata, _>(handle, |obj| match serde_json::to_string(&obj.classifications) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `PageSpan` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_page_span_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_span_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PageSpan>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PageSpan` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_span_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PageSpan, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PageSpan` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_span_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PageSpan>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `page` field from a `PageSpan`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_span_page(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageSpan, _>(handle, |obj| obj.page);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `bbox` field from a `PageSpan`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_bounding_box_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_span_bbox(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageSpan, _>(handle, |obj| match &obj.bbox {
            Some(val) => match insert_handle(*val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ExtractedImage` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_extracted_image_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ExtractedImage>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ExtractedImage` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ExtractedImage, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ExtractedImage` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ExtractedImage>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `data` field from a `ExtractedImage`.
/// The returned byte pointer is borrowed from `ptr` and must not be freed.
/// It remains valid until `ptr` is destroyed or the field is mutated.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_data(handle: AlefHandle, out_len: *mut usize) -> *mut u8 {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| {
            let data = &obj.data;
            if !out_len.is_null() {
                // SAFETY: null check above guarantees out_len is a valid pointer.
                unsafe {
                    *out_len = data.len();
                }
            }
            data.as_ptr() as *mut u8
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `format` field from a `ExtractedImage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_format(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ExtractedImage, _>(handle, |obj| match CString::new(obj.format.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `image_index` field from a `ExtractedImage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_image_index(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| obj.image_index);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `page_number` field from a `ExtractedImage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_page_number(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| match &obj.page_number {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `width` field from a `ExtractedImage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_width(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| match &obj.width {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `height` field from a `ExtractedImage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_height(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| match &obj.height {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `colorspace` field from a `ExtractedImage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_colorspace(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| match &obj.colorspace {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `bits_per_component` field from a `ExtractedImage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_bits_per_component(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| match &obj.bits_per_component {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `is_mask` field from a `ExtractedImage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_is_mask(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| obj.is_mask as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `description` field from a `ExtractedImage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_description(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| match &obj.description {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `ocr_result` field from a `ExtractedImage`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_extracted_document_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_ocr_result(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| match &obj.ocr_result {
            Some(val) => match insert_handle((**val).clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `bounding_box` field from a `ExtractedImage`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_bounding_box_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_bounding_box(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| match &obj.bounding_box {
            Some(val) => match insert_handle(*val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `source_path` field from a `ExtractedImage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_source_path(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| match &obj.source_path {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `image_kind` field from a `ExtractedImage`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_image_kind_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_image_kind(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| match &obj.image_kind {
            Some(val) => match insert_handle(*val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `kind_confidence` field from a `ExtractedImage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_kind_confidence(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| match &obj.kind_confidence {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `cluster_id` field from a `ExtractedImage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_cluster_id(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| match &obj.cluster_id {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `caption` field from a `ExtractedImage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_caption(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| match &obj.caption {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `qr_codes` field from a `ExtractedImage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_qr_codes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| match &obj.qr_codes {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `data_base64` field from a `ExtractedImage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_image_data_base64(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedImage, _>(handle, |obj| match &obj.data_base64 {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `BoundingBox` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_bounding_box_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bounding_box_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::BoundingBox>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `BoundingBox` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bounding_box_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::BoundingBox, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `BoundingBox` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bounding_box_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::BoundingBox>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `x0` field from a `BoundingBox`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bounding_box_x0(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::BoundingBox, _>(handle, |obj| obj.x0);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `y0` field from a `BoundingBox`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bounding_box_y0(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::BoundingBox, _>(handle, |obj| obj.y0);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `x1` field from a `BoundingBox`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bounding_box_x1(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::BoundingBox, _>(handle, |obj| obj.x1);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `y1` field from a `BoundingBox`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bounding_box_y1(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::BoundingBox, _>(handle, |obj| obj.y1);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Create a `ElementMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_element_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ElementMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ElementMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ElementMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ElementMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ElementMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `page_number` field from a `ElementMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_metadata_page_number(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ElementMetadata, _>(handle, |obj| match &obj.page_number {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `filename` field from a `ElementMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_metadata_filename(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ElementMetadata, _>(handle, |obj| match &obj.filename {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `coordinates` field from a `ElementMetadata`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_bounding_box_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_metadata_coordinates(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ElementMetadata, _>(handle, |obj| match &obj.coordinates {
            Some(val) => match insert_handle(*val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `element_index` field from a `ElementMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_metadata_element_index(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ElementMetadata, _>(handle, |obj| match &obj.element_index {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `additional` field from a `ElementMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_metadata_additional(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ElementMetadata, _>(handle, |obj| match serde_json::to_string(&obj.additional) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `Element` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_element_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::Element>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `Element` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::Element, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `Element` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::Element>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `element_type` field from a `Element`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_element_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_element_type(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Element, _>(handle, |obj| match insert_handle(obj.element_type) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `text` field from a `Element`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Element, _>(handle, |obj| match CString::new(obj.text.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `metadata` field from a `Element`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_element_metadata_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_metadata(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Element, _>(handle, |obj| match insert_handle(obj.metadata.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `PdfFormField` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_pdf_form_field_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_form_field_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PdfFormField>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PdfFormField` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_form_field_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PdfFormField, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PdfFormField` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_form_field_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PdfFormField>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `name` field from a `PdfFormField`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_form_field_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PdfFormField, _>(handle, |obj| match CString::new(obj.name.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `full_name` field from a `PdfFormField`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_form_field_full_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PdfFormField, _>(handle, |obj| match CString::new(obj.full_name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `field_type` field from a `PdfFormField`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_form_field_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_form_field_field_type(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PdfFormField, _>(handle, |obj| match insert_handle(obj.field_type) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `value` field from a `PdfFormField`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_form_field_value(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PdfFormField, _>(handle, |obj| match &obj.value {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `default_value` field from a `PdfFormField`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_form_field_default_value(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PdfFormField, _>(handle, |obj| match &obj.default_value {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `flags` field from a `PdfFormField`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_form_field_flags(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PdfFormField, _>(handle, |obj| obj.flags);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `page` field from a `PdfFormField`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_form_field_page(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PdfFormField, _>(handle, |obj| match &obj.page {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `bbox` field from a `PdfFormField`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_bounding_box_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_form_field_bbox(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PdfFormField, _>(handle, |obj| match &obj.bbox {
            Some(val) => match insert_handle(*val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_length` field from a `PdfFormField`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_form_field_max_length(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PdfFormField, _>(handle, |obj| match &obj.max_length {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `tooltip` field from a `PdfFormField`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_form_field_tooltip(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PdfFormField, _>(handle, |obj| match &obj.tooltip {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ExcelWorkbook` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_excel_workbook_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_workbook_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ExcelWorkbook>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ExcelWorkbook` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_workbook_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ExcelWorkbook, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ExcelWorkbook` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_workbook_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ExcelWorkbook>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `sheets` field from a `ExcelWorkbook`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_workbook_sheets(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExcelWorkbook, _>(handle, |obj| match serde_json::to_string(&obj.sheets) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `metadata` field from a `ExcelWorkbook`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_workbook_metadata(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExcelWorkbook, _>(handle, |obj| match serde_json::to_string(&obj.metadata) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `revisions` field from a `ExcelWorkbook`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_workbook_revisions(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExcelWorkbook, _>(handle, |obj| match &obj.revisions {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ExcelSheet` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_excel_sheet_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_sheet_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ExcelSheet>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ExcelSheet` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_sheet_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ExcelSheet, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ExcelSheet` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_sheet_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ExcelSheet>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `name` field from a `ExcelSheet`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_sheet_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExcelSheet, _>(handle, |obj| match CString::new(obj.name.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `markdown` field from a `ExcelSheet`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_sheet_markdown(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExcelSheet, _>(handle, |obj| match CString::new(obj.markdown.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `row_count` field from a `ExcelSheet`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_sheet_row_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExcelSheet, _>(handle, |obj| obj.row_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `col_count` field from a `ExcelSheet`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_sheet_col_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExcelSheet, _>(handle, |obj| obj.col_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `cell_count` field from a `ExcelSheet`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_sheet_cell_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExcelSheet, _>(handle, |obj| obj.cell_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `table_cells` field from a `ExcelSheet`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_sheet_table_cells(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExcelSheet, _>(handle, |obj| match &obj.table_cells {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `XmlExtractionResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_xml_extraction_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xml_extraction_result_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::XmlExtractionResult>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `XmlExtractionResult` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xml_extraction_result_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::XmlExtractionResult, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `XmlExtractionResult` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xml_extraction_result_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::XmlExtractionResult>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `content` field from a `XmlExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xml_extraction_result_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::XmlExtractionResult, _>(handle, |obj| match CString::new(obj.content.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `element_count` field from a `XmlExtractionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xml_extraction_result_element_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::XmlExtractionResult, _>(handle, |obj| obj.element_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `unique_elements` field from a `XmlExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xml_extraction_result_unique_elements(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::XmlExtractionResult, _>(handle, |obj| {
            match serde_json::to_string(&obj.unique_elements) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `TextExtractionResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_text_extraction_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_extraction_result_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::TextExtractionResult>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `TextExtractionResult` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_extraction_result_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::TextExtractionResult, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `TextExtractionResult` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_extraction_result_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::TextExtractionResult>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `content` field from a `TextExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_extraction_result_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::TextExtractionResult, _>(handle, |obj| match CString::new(obj.content.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `line_count` field from a `TextExtractionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_extraction_result_line_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TextExtractionResult, _>(handle, |obj| obj.line_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `word_count` field from a `TextExtractionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_extraction_result_word_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TextExtractionResult, _>(handle, |obj| obj.word_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `character_count` field from a `TextExtractionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_extraction_result_character_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TextExtractionResult, _>(handle, |obj| obj.character_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `headers` field from a `TextExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_extraction_result_headers(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::TextExtractionResult, _>(handle, |obj| match &obj.headers {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `PptxExtractionResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_pptx_extraction_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_extraction_result_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PptxExtractionResult>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PptxExtractionResult` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_extraction_result_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PptxExtractionResult, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PptxExtractionResult` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_extraction_result_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PptxExtractionResult>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `content` field from a `PptxExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_extraction_result_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PptxExtractionResult, _>(handle, |obj| match CString::new(obj.content.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `metadata` field from a `PptxExtractionResult`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_pptx_metadata_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_extraction_result_metadata(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::PptxExtractionResult, _>(handle, |obj| match insert_handle(obj.metadata.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `slide_count` field from a `PptxExtractionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_extraction_result_slide_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PptxExtractionResult, _>(handle, |obj| obj.slide_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `image_count` field from a `PptxExtractionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_extraction_result_image_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PptxExtractionResult, _>(handle, |obj| obj.image_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `table_count` field from a `PptxExtractionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_extraction_result_table_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PptxExtractionResult, _>(handle, |obj| obj.table_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `images` field from a `PptxExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_extraction_result_images(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PptxExtractionResult, _>(handle, |obj| match serde_json::to_string(&obj.images) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `page_structure` field from a `PptxExtractionResult`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_page_structure_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_extraction_result_page_structure(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PptxExtractionResult, _>(handle, |obj| match &obj.page_structure {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `page_contents` field from a `PptxExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_extraction_result_page_contents(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PptxExtractionResult, _>(handle, |obj| match &obj.page_contents {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `document` field from a `PptxExtractionResult`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_document_structure_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_extraction_result_document(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PptxExtractionResult, _>(handle, |obj| match &obj.document {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `office_metadata` field from a `PptxExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_extraction_result_office_metadata(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PptxExtractionResult, _>(handle, |obj| {
            match serde_json::to_string(&obj.office_metadata) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `revisions` field from a `PptxExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_extraction_result_revisions(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PptxExtractionResult, _>(handle, |obj| match &obj.revisions {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `EmailExtractionResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_email_extraction_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_extraction_result_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::EmailExtractionResult>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `EmailExtractionResult` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_extraction_result_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::EmailExtractionResult, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `EmailExtractionResult` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_extraction_result_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::EmailExtractionResult>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `subject` field from a `EmailExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_extraction_result_subject(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmailExtractionResult, _>(handle, |obj| match &obj.subject {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `from_email` field from a `EmailExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_extraction_result_from_email(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmailExtractionResult, _>(handle, |obj| match &obj.from_email {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `to_emails` field from a `EmailExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_extraction_result_to_emails(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::EmailExtractionResult, _>(handle, |obj| match serde_json::to_string(&obj.to_emails) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `cc_emails` field from a `EmailExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_extraction_result_cc_emails(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::EmailExtractionResult, _>(handle, |obj| match serde_json::to_string(&obj.cc_emails) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `bcc_emails` field from a `EmailExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_extraction_result_bcc_emails(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmailExtractionResult, _>(handle, |obj| {
            match serde_json::to_string(&obj.bcc_emails) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `date` field from a `EmailExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_extraction_result_date(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmailExtractionResult, _>(handle, |obj| match &obj.date {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `message_id` field from a `EmailExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_extraction_result_message_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmailExtractionResult, _>(handle, |obj| match &obj.message_id {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `plain_text` field from a `EmailExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_extraction_result_plain_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmailExtractionResult, _>(handle, |obj| match &obj.plain_text {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `html_content` field from a `EmailExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_extraction_result_html_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmailExtractionResult, _>(handle, |obj| match &obj.html_content {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `content` field from a `EmailExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_extraction_result_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::EmailExtractionResult, _>(handle, |obj| match CString::new(obj.content.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `attachments` field from a `EmailExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_extraction_result_attachments(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmailExtractionResult, _>(handle, |obj| {
            match serde_json::to_string(&obj.attachments) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `metadata` field from a `EmailExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_extraction_result_metadata(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::EmailExtractionResult, _>(handle, |obj| match serde_json::to_string(&obj.metadata) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `EmailAttachment` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_email_attachment_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_attachment_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::EmailAttachment>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `EmailAttachment` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_attachment_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::EmailAttachment, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `EmailAttachment` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_attachment_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::EmailAttachment>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `name` field from a `EmailAttachment`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_attachment_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmailAttachment, _>(handle, |obj| match &obj.name {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `filename` field from a `EmailAttachment`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_attachment_filename(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmailAttachment, _>(handle, |obj| match &obj.filename {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `mime_type` field from a `EmailAttachment`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_attachment_mime_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmailAttachment, _>(handle, |obj| match &obj.mime_type {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `size` field from a `EmailAttachment`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_attachment_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::EmailAttachment, _>(handle, |obj| match &obj.size {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `is_image` field from a `EmailAttachment`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_attachment_is_image(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::EmailAttachment, _>(handle, |obj| obj.is_image as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `data` field from a `EmailAttachment`.
/// The returned byte pointer is borrowed from `ptr` and must not be freed.
/// It remains valid until `ptr` is destroyed or the field is mutated.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_attachment_data(handle: AlefHandle, out_len: *mut usize) -> *mut u8 {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmailAttachment, _>(handle, |obj| {
            match &obj.data {
                Some(val) => {
                    if !out_len.is_null() {
                        // SAFETY: null check above guarantees out_len is a valid pointer.
                        unsafe {
                            *out_len = val.len();
                        }
                    }
                    val.as_ptr() as *mut u8
                }
                None => {
                    if !out_len.is_null() {
                        // SAFETY: null check above guarantees out_len is a valid pointer.
                        unsafe {
                            *out_len = 0;
                        }
                    }
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `OcrExtractionResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_ocr_extraction_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_extraction_result_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::OcrExtractionResult>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `OcrExtractionResult` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_extraction_result_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::OcrExtractionResult, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OcrExtractionResult` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_extraction_result_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::OcrExtractionResult>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `content` field from a `OcrExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_extraction_result_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::OcrExtractionResult, _>(handle, |obj| match CString::new(obj.content.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `mime_type` field from a `OcrExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_extraction_result_mime_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::OcrExtractionResult, _>(handle, |obj| match CString::new(obj.mime_type.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `metadata` field from a `OcrExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_extraction_result_metadata(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::OcrExtractionResult, _>(handle, |obj| match serde_json::to_string(&obj.metadata) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `tables` field from a `OcrExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_extraction_result_tables(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::OcrExtractionResult, _>(handle, |obj| match serde_json::to_string(&obj.tables) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `ocr_elements` field from a `OcrExtractionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_extraction_result_ocr_elements(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::OcrExtractionResult, _>(handle, |obj| match &obj.ocr_elements {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `OcrTable` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_ocr_table_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_table_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::OcrTable>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `OcrTable` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_table_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::OcrTable, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OcrTable` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_table_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::OcrTable>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `cells` field from a `OcrTable`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_table_cells(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::OcrTable, _>(handle, |obj| match serde_json::to_string(&obj.cells) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `markdown` field from a `OcrTable`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_table_markdown(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::OcrTable, _>(handle, |obj| match CString::new(obj.markdown.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `page_number` field from a `OcrTable`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_table_page_number(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrTable, _>(handle, |obj| obj.page_number);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `bounding_box` field from a `OcrTable`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ocr_table_bounding_box_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_table_bounding_box(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrTable, _>(handle, |obj| match &obj.bounding_box {
            Some(val) => match insert_handle(*val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `OcrTableBoundingBox` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_ocr_table_bounding_box_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_table_bounding_box_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::OcrTableBoundingBox>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `OcrTableBoundingBox` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_table_bounding_box_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::OcrTableBoundingBox, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OcrTableBoundingBox` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_table_bounding_box_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::OcrTableBoundingBox>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `left` field from a `OcrTableBoundingBox`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_table_bounding_box_left(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrTableBoundingBox, _>(handle, |obj| obj.left);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `top` field from a `OcrTableBoundingBox`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_table_bounding_box_top(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrTableBoundingBox, _>(handle, |obj| obj.top);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `right` field from a `OcrTableBoundingBox`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_table_bounding_box_right(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrTableBoundingBox, _>(handle, |obj| obj.right);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `bottom` field from a `OcrTableBoundingBox`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_table_bounding_box_bottom(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrTableBoundingBox, _>(handle, |obj| obj.bottom);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ImagePreprocessingConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_image_preprocessing_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ImagePreprocessingConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ImagePreprocessingConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ImagePreprocessingConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ImagePreprocessingConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ImagePreprocessingConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `target_dpi` field from a `ImagePreprocessingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_config_target_dpi(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImagePreprocessingConfig, _>(handle, |obj| obj.target_dpi);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `auto_rotate` field from a `ImagePreprocessingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_config_auto_rotate(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImagePreprocessingConfig, _>(handle, |obj| obj.auto_rotate as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `deskew` field from a `ImagePreprocessingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_config_deskew(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImagePreprocessingConfig, _>(handle, |obj| obj.deskew as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `denoise` field from a `ImagePreprocessingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_config_denoise(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImagePreprocessingConfig, _>(handle, |obj| obj.denoise as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `contrast_enhance` field from a `ImagePreprocessingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_config_contrast_enhance(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImagePreprocessingConfig, _>(handle, |obj| obj.contrast_enhance as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `binarization_method` field from a `ImagePreprocessingConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_config_binarization_method(
    handle: AlefHandle,
) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ImagePreprocessingConfig, _>(handle, |obj| {
            match CString::new(obj.binarization_method.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `invert_colors` field from a `ImagePreprocessingConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_config_invert_colors(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImagePreprocessingConfig, _>(handle, |obj| obj.invert_colors as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `TesseractConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_tesseract_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::TesseractConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `TesseractConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::TesseractConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `TesseractConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::TesseractConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `language` field from a `TesseractConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::TesseractConfig, _>(handle, |obj| match serde_json::to_string(&obj.language) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `psm` field from a `TesseractConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_psm(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TesseractConfig, _>(handle, |obj| obj.psm);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `output_format` field from a `TesseractConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_output_format(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::TesseractConfig, _>(handle, |obj| match CString::new(obj.output_format.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `oem` field from a `TesseractConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_oem(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TesseractConfig, _>(handle, |obj| obj.oem);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `min_confidence` field from a `TesseractConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_min_confidence(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::TesseractConfig, _>(handle, |obj| obj.min_confidence);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `preprocessing` field from a `TesseractConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_image_preprocessing_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_preprocessing(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TesseractConfig, _>(handle, |obj| match &obj.preprocessing {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `enable_table_detection` field from a `TesseractConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_enable_table_detection(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TesseractConfig, _>(handle, |obj| obj.enable_table_detection as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `table_min_confidence` field from a `TesseractConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_table_min_confidence(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::TesseractConfig, _>(handle, |obj| obj.table_min_confidence);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `table_column_threshold` field from a `TesseractConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_table_column_threshold(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TesseractConfig, _>(handle, |obj| obj.table_column_threshold);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `table_row_threshold_ratio` field from a `TesseractConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_table_row_threshold_ratio(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::TesseractConfig, _>(handle, |obj| obj.table_row_threshold_ratio);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `use_cache` field from a `TesseractConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_use_cache(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TesseractConfig, _>(handle, |obj| obj.use_cache as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `classify_use_pre_adapted_templates` field from a `TesseractConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_classify_use_pre_adapted_templates(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::TesseractConfig, _>(handle, |obj| obj.classify_use_pre_adapted_templates as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `language_model_ngram_on` field from a `TesseractConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_language_model_ngram_on(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TesseractConfig, _>(handle, |obj| obj.language_model_ngram_on as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `tessedit_dont_blkrej_good_wds` field from a `TesseractConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_tessedit_dont_blkrej_good_wds(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TesseractConfig, _>(handle, |obj| obj.tessedit_dont_blkrej_good_wds as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `tessedit_dont_rowrej_good_wds` field from a `TesseractConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_tessedit_dont_rowrej_good_wds(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TesseractConfig, _>(handle, |obj| obj.tessedit_dont_rowrej_good_wds as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `tessedit_enable_dict_correction` field from a `TesseractConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_tessedit_enable_dict_correction(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TesseractConfig, _>(handle, |obj| obj.tessedit_enable_dict_correction as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `tessedit_char_whitelist` field from a `TesseractConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_tessedit_char_whitelist(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::TesseractConfig, _>(handle, |obj| {
            match CString::new(obj.tessedit_char_whitelist.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `tessedit_char_blacklist` field from a `TesseractConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_tessedit_char_blacklist(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::TesseractConfig, _>(handle, |obj| {
            match CString::new(obj.tessedit_char_blacklist.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `tessedit_use_primary_params_model` field from a `TesseractConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_tessedit_use_primary_params_model(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::TesseractConfig, _>(handle, |obj| obj.tessedit_use_primary_params_model as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `textord_space_size_is_variable` field from a `TesseractConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_textord_space_size_is_variable(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TesseractConfig, _>(handle, |obj| obj.textord_space_size_is_variable as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `thresholding_method` field from a `TesseractConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_tesseract_config_thresholding_method(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TesseractConfig, _>(handle, |obj| obj.thresholding_method as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ImagePreprocessingMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_image_preprocessing_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ImagePreprocessingMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ImagePreprocessingMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ImagePreprocessingMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ImagePreprocessingMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ImagePreprocessingMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `target_dpi` field from a `ImagePreprocessingMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_metadata_target_dpi(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImagePreprocessingMetadata, _>(handle, |obj| obj.target_dpi);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `scale_factor` field from a `ImagePreprocessingMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_metadata_scale_factor(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::ImagePreprocessingMetadata, _>(handle, |obj| obj.scale_factor);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `auto_adjusted` field from a `ImagePreprocessingMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_metadata_auto_adjusted(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImagePreprocessingMetadata, _>(handle, |obj| obj.auto_adjusted as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `final_dpi` field from a `ImagePreprocessingMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_metadata_final_dpi(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImagePreprocessingMetadata, _>(handle, |obj| obj.final_dpi);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `resample_method` field from a `ImagePreprocessingMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_metadata_resample_method(
    handle: AlefHandle,
) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ImagePreprocessingMetadata, _>(handle, |obj| {
            match CString::new(obj.resample_method.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `dimension_clamped` field from a `ImagePreprocessingMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_metadata_dimension_clamped(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImagePreprocessingMetadata, _>(handle, |obj| obj.dimension_clamped as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `calculated_dpi` field from a `ImagePreprocessingMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_metadata_calculated_dpi(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImagePreprocessingMetadata, _>(handle, |obj| match &obj.calculated_dpi {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `skipped_resize` field from a `ImagePreprocessingMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_metadata_skipped_resize(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImagePreprocessingMetadata, _>(handle, |obj| obj.skipped_resize as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `resize_error` field from a `ImagePreprocessingMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_preprocessing_metadata_resize_error(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ImagePreprocessingMetadata, _>(handle, |obj| match &obj.resize_error {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `Formula` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_formula_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_formula_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::Formula>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `Formula` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_formula_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::Formula, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `Formula` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_formula_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::Formula>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `latex` field from a `Formula`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_formula_latex(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Formula, _>(handle, |obj| match CString::new(obj.latex.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `bbox` field from a `Formula`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_bounding_box_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_formula_bbox(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Formula, _>(handle, |obj| match insert_handle(obj.bbox) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `page` field from a `Formula`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_formula_page(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Formula, _>(handle, |obj| obj.page);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Create a `CodeMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_code_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::CodeMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Serialize a `CodeMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::CodeMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Free a `CodeMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::CodeMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `chunks` field from a `CodeMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_metadata_chunks(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CodeMetadata, _>(handle, |obj| match serde_json::to_string(&obj.chunks) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `data` field from a `CodeMetadata`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_code_data_node_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_metadata_data(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CodeMetadata, _>(handle, |obj| match &obj.data {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Create a `CodeChunkInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_code_chunk_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_chunk_info_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::CodeChunkInfo>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Serialize a `CodeChunkInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_chunk_info_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::CodeChunkInfo, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Free a `CodeChunkInfo` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_chunk_info_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::CodeChunkInfo>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `text` field from a `CodeChunkInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_chunk_info_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CodeChunkInfo, _>(handle, |obj| match CString::new(obj.text.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `context_path` field from a `CodeChunkInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_chunk_info_context_path(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::CodeChunkInfo, _>(handle, |obj| match serde_json::to_string(&obj.context_path) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `node_types` field from a `CodeChunkInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_chunk_info_node_types(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::CodeChunkInfo, _>(handle, |obj| match serde_json::to_string(&obj.node_types) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `byte_start` field from a `CodeChunkInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_chunk_info_byte_start(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CodeChunkInfo, _>(handle, |obj| obj.byte_start);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `byte_end` field from a `CodeChunkInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_chunk_info_byte_end(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CodeChunkInfo, _>(handle, |obj| obj.byte_end);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Create a `CodeDataAttribute` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_code_data_attribute_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_attribute_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::CodeDataAttribute>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Serialize a `CodeDataAttribute` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_attribute_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::CodeDataAttribute, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Free a `CodeDataAttribute` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_attribute_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::CodeDataAttribute>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `name` field from a `CodeDataAttribute`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_attribute_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::CodeDataAttribute, _>(handle, |obj| match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `value` field from a `CodeDataAttribute`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_attribute_value(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::CodeDataAttribute, _>(handle, |obj| match CString::new(obj.value.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `byte_start` field from a `CodeDataAttribute`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_attribute_byte_start(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CodeDataAttribute, _>(handle, |obj| obj.byte_start);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `byte_end` field from a `CodeDataAttribute`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_attribute_byte_end(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CodeDataAttribute, _>(handle, |obj| obj.byte_end);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Create a `CodeDataNode` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_code_data_node_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_node_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::CodeDataNode>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Serialize a `CodeDataNode` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_node_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::CodeDataNode, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Free a `CodeDataNode` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_node_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::CodeDataNode>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `kind` field from a `CodeDataNode`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_code_data_node_kind_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_node_kind(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CodeDataNode, _>(handle, |obj| match insert_handle(obj.kind) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `key` field from a `CodeDataNode`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_node_key(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CodeDataNode, _>(handle, |obj| match &obj.key {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `value` field from a `CodeDataNode`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_node_value(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CodeDataNode, _>(handle, |obj| match &obj.value {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `attributes` field from a `CodeDataNode`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_node_attributes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::CodeDataNode, _>(handle, |obj| match serde_json::to_string(&obj.attributes) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `children` field from a `CodeDataNode`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_node_children(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CodeDataNode, _>(handle, |obj| match serde_json::to_string(&obj.children) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `byte_start` field from a `CodeDataNode`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_node_byte_start(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CodeDataNode, _>(handle, |obj| obj.byte_start);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tree-sitter")]
/// Get the `byte_end` field from a `CodeDataNode`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_node_byte_end(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CodeDataNode, _>(handle, |obj| obj.byte_end);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `Metadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::Metadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `Metadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::Metadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `Metadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::Metadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `title` field from a `Metadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_title(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.title {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `subject` field from a `Metadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_subject(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.subject {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `authors` field from a `Metadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_authors(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.authors {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `keywords` field from a `Metadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_keywords(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.keywords {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `language` field from a `Metadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.language {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `created_at` field from a `Metadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_created_at(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.created_at {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `modified_at` field from a `Metadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_modified_at(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.modified_at {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `created_by` field from a `Metadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_created_by(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.created_by {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `modified_by` field from a `Metadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_modified_by(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.modified_by {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `pages` field from a `Metadata`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_page_structure_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_pages(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.pages {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `format` field from a `Metadata`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_format_metadata_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_format(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.format {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `image_preprocessing` field from a `Metadata`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_image_preprocessing_metadata_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_image_preprocessing(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.image_preprocessing {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `json_schema` field from a `Metadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_json_schema(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.json_schema {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `error` field from a `Metadata`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_error_metadata_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_error(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.error {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `extraction_duration_ms` field from a `Metadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_extraction_duration_ms(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.extraction_duration_ms {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `category` field from a `Metadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_category(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.category {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `tags` field from a `Metadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_tags(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.tags {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `document_version` field from a `Metadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_document_version(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.document_version {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `abstract_text` field from a `Metadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_abstract_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.abstract_text {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `output_format` field from a `Metadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_output_format(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match &obj.output_format {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `ocr_used` field from a `Metadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_ocr_used(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| obj.ocr_used as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `additional` field from a `Metadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_additional(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Metadata, _>(handle, |obj| match serde_json::to_string(&obj.additional) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Returns `true` when no metadata fields, format-specific metadata, or
/// additional postprocessor fields are populated.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_metadata_is_empty(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::Metadata>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::Metadata>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.is_empty();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Create a `ExcelMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_excel_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ExcelMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ExcelMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ExcelMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ExcelMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ExcelMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `sheet_count` field from a `ExcelMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_metadata_sheet_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExcelMetadata, _>(handle, |obj| match &obj.sheet_count {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `sheet_names` field from a `ExcelMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_excel_metadata_sheet_names(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExcelMetadata, _>(handle, |obj| match &obj.sheet_names {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `EmailMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_email_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::EmailMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `EmailMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::EmailMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `EmailMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::EmailMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `from_email` field from a `EmailMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_metadata_from_email(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmailMetadata, _>(handle, |obj| match &obj.from_email {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `from_name` field from a `EmailMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_metadata_from_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmailMetadata, _>(handle, |obj| match &obj.from_name {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `to_emails` field from a `EmailMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_metadata_to_emails(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::EmailMetadata, _>(handle, |obj| match serde_json::to_string(&obj.to_emails) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `cc_emails` field from a `EmailMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_metadata_cc_emails(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::EmailMetadata, _>(handle, |obj| match serde_json::to_string(&obj.cc_emails) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `bcc_emails` field from a `EmailMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_metadata_bcc_emails(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::EmailMetadata, _>(handle, |obj| match serde_json::to_string(&obj.bcc_emails) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `message_id` field from a `EmailMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_metadata_message_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmailMetadata, _>(handle, |obj| match &obj.message_id {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `attachments` field from a `EmailMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_email_metadata_attachments(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::EmailMetadata, _>(handle, |obj| match serde_json::to_string(&obj.attachments) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ArchiveMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_archive_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_archive_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ArchiveMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ArchiveMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_archive_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ArchiveMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ArchiveMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_archive_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ArchiveMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `format` field from a `ArchiveMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_archive_metadata_format(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ArchiveMetadata, _>(handle, |obj| match CString::new(obj.format.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `file_count` field from a `ArchiveMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_archive_metadata_file_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ArchiveMetadata, _>(handle, |obj| obj.file_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `file_list` field from a `ArchiveMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_archive_metadata_file_list(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ArchiveMetadata, _>(handle, |obj| match serde_json::to_string(&obj.file_list) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `total_size` field from a `ArchiveMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_archive_metadata_total_size(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ArchiveMetadata, _>(handle, |obj| obj.total_size);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `compressed_size` field from a `ArchiveMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_archive_metadata_compressed_size(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ArchiveMetadata, _>(handle, |obj| match &obj.compressed_size {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ImageMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_image_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ImageMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ImageMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ImageMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ImageMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ImageMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `width` field from a `ImageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_metadata_width(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImageMetadata, _>(handle, |obj| obj.width);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `height` field from a `ImageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_metadata_height(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImageMetadata, _>(handle, |obj| obj.height);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `format` field from a `ImageMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_metadata_format(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ImageMetadata, _>(handle, |obj| match CString::new(obj.format.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `exif` field from a `ImageMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_metadata_exif(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ImageMetadata, _>(handle, |obj| match serde_json::to_string(&obj.exif) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `XmlMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_xml_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xml_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::XmlMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `XmlMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xml_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::XmlMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `XmlMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xml_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::XmlMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `element_count` field from a `XmlMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xml_metadata_element_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::XmlMetadata, _>(handle, |obj| obj.element_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `unique_elements` field from a `XmlMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_xml_metadata_unique_elements(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::XmlMetadata, _>(handle, |obj| match serde_json::to_string(&obj.unique_elements) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `TextMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_text_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::TextMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `TextMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::TextMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `TextMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::TextMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `line_count` field from a `TextMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_metadata_line_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TextMetadata, _>(handle, |obj| obj.line_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `word_count` field from a `TextMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_metadata_word_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TextMetadata, _>(handle, |obj| obj.word_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `character_count` field from a `TextMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_metadata_character_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TextMetadata, _>(handle, |obj| obj.character_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `headers` field from a `TextMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_metadata_headers(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::TextMetadata, _>(handle, |obj| match &obj.headers {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `HeaderMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_header_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_header_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::HeaderMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `HeaderMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_header_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::HeaderMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `HeaderMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_header_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::HeaderMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `level` field from a `HeaderMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_header_metadata_level(handle: AlefHandle) -> u8 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HeaderMetadata, _>(handle, |obj| obj.level);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `text` field from a `HeaderMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_header_metadata_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::HeaderMetadata, _>(handle, |obj| match CString::new(obj.text.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `id` field from a `HeaderMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_header_metadata_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::HeaderMetadata, _>(handle, |obj| match &obj.id {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `depth` field from a `HeaderMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_header_metadata_depth(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HeaderMetadata, _>(handle, |obj| obj.depth);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `html_offset` field from a `HeaderMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_header_metadata_html_offset(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HeaderMetadata, _>(handle, |obj| obj.html_offset);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `LinkMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_link_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_link_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::LinkMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LinkMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_link_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::LinkMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LinkMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_link_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::LinkMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `href` field from a `LinkMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_link_metadata_href(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LinkMetadata, _>(handle, |obj| match CString::new(obj.href.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `text` field from a `LinkMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_link_metadata_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LinkMetadata, _>(handle, |obj| match CString::new(obj.text.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `title` field from a `LinkMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_link_metadata_title(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LinkMetadata, _>(handle, |obj| match &obj.title {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `link_type` field from a `LinkMetadata`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_link_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_link_metadata_link_type(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LinkMetadata, _>(handle, |obj| match insert_handle(obj.link_type) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `rel` field from a `LinkMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_link_metadata_rel(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LinkMetadata, _>(handle, |obj| match serde_json::to_string(&obj.rel) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ImageMetadataType` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_image_metadata_type_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_metadata_type_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ImageMetadataType>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ImageMetadataType` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_metadata_type_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ImageMetadataType, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ImageMetadataType` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_metadata_type_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ImageMetadataType>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `src` field from a `ImageMetadataType`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_metadata_type_src(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ImageMetadataType, _>(handle, |obj| match CString::new(obj.src.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `alt` field from a `ImageMetadataType`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_metadata_type_alt(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ImageMetadataType, _>(handle, |obj| match &obj.alt {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `title` field from a `ImageMetadataType`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_metadata_type_title(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ImageMetadataType, _>(handle, |obj| match &obj.title {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `image_type` field from a `ImageMetadataType`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_image_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_metadata_type_image_type(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ImageMetadataType, _>(handle, |obj| match insert_handle(obj.image_type) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `StructuredData` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_structured_data_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::StructuredData>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `StructuredData` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::StructuredData, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `StructuredData` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::StructuredData>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `data_type` field from a `StructuredData`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_structured_data_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_data_type(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::StructuredData, _>(handle, |obj| match insert_handle(obj.data_type) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `raw_json` field from a `StructuredData`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_raw_json(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::StructuredData, _>(handle, |obj| match CString::new(obj.raw_json.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `schema_type` field from a `StructuredData`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_schema_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::StructuredData, _>(handle, |obj| match &obj.schema_type {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `HtmlMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_html_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::HtmlMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `HtmlMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::HtmlMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `HtmlMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::HtmlMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `title` field from a `HtmlMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_title(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::HtmlMetadata, _>(handle, |obj| match &obj.title {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `description` field from a `HtmlMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_description(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::HtmlMetadata, _>(handle, |obj| match &obj.description {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `keywords` field from a `HtmlMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_keywords(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::HtmlMetadata, _>(handle, |obj| match serde_json::to_string(&obj.keywords) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `author` field from a `HtmlMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_author(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::HtmlMetadata, _>(handle, |obj| match &obj.author {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `canonical_url` field from a `HtmlMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_canonical_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::HtmlMetadata, _>(handle, |obj| match &obj.canonical_url {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `base_href` field from a `HtmlMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_base_href(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::HtmlMetadata, _>(handle, |obj| match &obj.base_href {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `language` field from a `HtmlMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::HtmlMetadata, _>(handle, |obj| match &obj.language {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `text_direction` field from a `HtmlMetadata`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_text_direction_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_text_direction(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HtmlMetadata, _>(handle, |obj| match &obj.text_direction {
            Some(val) => match insert_handle(*val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `open_graph` field from a `HtmlMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_open_graph(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::HtmlMetadata, _>(handle, |obj| match serde_json::to_string(&obj.open_graph) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `twitter_card` field from a `HtmlMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_twitter_card(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::HtmlMetadata, _>(handle, |obj| match serde_json::to_string(&obj.twitter_card) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `meta_tags` field from a `HtmlMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_meta_tags(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::HtmlMetadata, _>(handle, |obj| match serde_json::to_string(&obj.meta_tags) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `headers` field from a `HtmlMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_headers(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::HtmlMetadata, _>(handle, |obj| match serde_json::to_string(&obj.headers) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `links` field from a `HtmlMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_links(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::HtmlMetadata, _>(handle, |obj| match serde_json::to_string(&obj.links) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `images` field from a `HtmlMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_images(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::HtmlMetadata, _>(handle, |obj| match serde_json::to_string(&obj.images) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `structured_data` field from a `HtmlMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_metadata_structured_data(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::HtmlMetadata, _>(handle, |obj| match serde_json::to_string(&obj.structured_data) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `OcrMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_ocr_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::OcrMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `OcrMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::OcrMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OcrMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::OcrMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `language` field from a `OcrMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_metadata_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::OcrMetadata, _>(handle, |obj| match CString::new(obj.language.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `psm` field from a `OcrMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_metadata_psm(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrMetadata, _>(handle, |obj| obj.psm);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `output_format` field from a `OcrMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_metadata_output_format(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::OcrMetadata, _>(handle, |obj| match CString::new(obj.output_format.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `table_count` field from a `OcrMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_metadata_table_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrMetadata, _>(handle, |obj| obj.table_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `table_rows` field from a `OcrMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_metadata_table_rows(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrMetadata, _>(handle, |obj| match &obj.table_rows {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `table_cols` field from a `OcrMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_metadata_table_cols(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrMetadata, _>(handle, |obj| match &obj.table_cols {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ErrorMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_error_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_error_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ErrorMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ErrorMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_error_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ErrorMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ErrorMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_error_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ErrorMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `error_type` field from a `ErrorMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_error_metadata_error_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ErrorMetadata, _>(handle, |obj| match CString::new(obj.error_type.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `message` field from a `ErrorMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_error_metadata_message(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ErrorMetadata, _>(handle, |obj| match CString::new(obj.message.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `PptxMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_pptx_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PptxMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PptxMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PptxMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PptxMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PptxMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `slide_count` field from a `PptxMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_metadata_slide_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PptxMetadata, _>(handle, |obj| obj.slide_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `slide_names` field from a `PptxMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_metadata_slide_names(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PptxMetadata, _>(handle, |obj| match serde_json::to_string(&obj.slide_names) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `image_count` field from a `PptxMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_metadata_image_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PptxMetadata, _>(handle, |obj| match &obj.image_count {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `table_count` field from a `PptxMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pptx_metadata_table_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PptxMetadata, _>(handle, |obj| match &obj.table_count {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "office")]
/// Create a `DocxMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_docx_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DocxMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "office")]
/// Serialize a `DocxMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DocxMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "office")]
/// Free a `DocxMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DocxMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `core_properties` field from a `DocxMetadata`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_core_properties_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_metadata_core_properties(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocxMetadata, _>(handle, |obj| match &obj.core_properties {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `app_properties` field from a `DocxMetadata`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_docx_app_properties_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_metadata_app_properties(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocxMetadata, _>(handle, |obj| match &obj.app_properties {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "office")]
/// Get the `custom_properties` field from a `DocxMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_docx_metadata_custom_properties(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DocxMetadata, _>(handle, |obj| match &obj.custom_properties {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `CsvMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_csv_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_csv_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::CsvMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CsvMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_csv_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::CsvMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CsvMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_csv_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::CsvMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `row_count` field from a `CsvMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_csv_metadata_row_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CsvMetadata, _>(handle, |obj| obj.row_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `column_count` field from a `CsvMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_csv_metadata_column_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CsvMetadata, _>(handle, |obj| obj.column_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `delimiter` field from a `CsvMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_csv_metadata_delimiter(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CsvMetadata, _>(handle, |obj| match &obj.delimiter {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `has_header` field from a `CsvMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_csv_metadata_has_header(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CsvMetadata, _>(handle, |obj| obj.has_header as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `column_types` field from a `CsvMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_csv_metadata_column_types(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CsvMetadata, _>(handle, |obj| match &obj.column_types {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `BibtexMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_bibtex_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bibtex_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::BibtexMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `BibtexMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bibtex_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::BibtexMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `BibtexMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bibtex_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::BibtexMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `entry_count` field from a `BibtexMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bibtex_metadata_entry_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::BibtexMetadata, _>(handle, |obj| obj.entry_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `citation_keys` field from a `BibtexMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bibtex_metadata_citation_keys(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::BibtexMetadata, _>(handle, |obj| match serde_json::to_string(&obj.citation_keys) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `authors` field from a `BibtexMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bibtex_metadata_authors(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::BibtexMetadata, _>(handle, |obj| match serde_json::to_string(&obj.authors) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `year_range` field from a `BibtexMetadata`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_year_range_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bibtex_metadata_year_range(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::BibtexMetadata, _>(handle, |obj| match &obj.year_range {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `entry_types` field from a `BibtexMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_bibtex_metadata_entry_types(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::BibtexMetadata, _>(handle, |obj| match &obj.entry_types {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `CitationMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_citation_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_citation_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::CitationMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CitationMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_citation_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::CitationMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CitationMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_citation_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::CitationMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `citation_count` field from a `CitationMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_citation_metadata_citation_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CitationMetadata, _>(handle, |obj| obj.citation_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `format` field from a `CitationMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_citation_metadata_format(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CitationMetadata, _>(handle, |obj| match &obj.format {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `authors` field from a `CitationMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_citation_metadata_authors(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::CitationMetadata, _>(handle, |obj| match serde_json::to_string(&obj.authors) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `year_range` field from a `CitationMetadata`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_year_range_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_citation_metadata_year_range(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CitationMetadata, _>(handle, |obj| match &obj.year_range {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `dois` field from a `CitationMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_citation_metadata_dois(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CitationMetadata, _>(handle, |obj| match serde_json::to_string(&obj.dois) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `keywords` field from a `CitationMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_citation_metadata_keywords(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::CitationMetadata, _>(handle, |obj| match serde_json::to_string(&obj.keywords) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `YearRange` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_year_range_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_year_range_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::YearRange>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `YearRange` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_year_range_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::YearRange, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `YearRange` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_year_range_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::YearRange>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `min` field from a `YearRange`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_year_range_min(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::YearRange, _>(handle, |obj| match &obj.min {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max` field from a `YearRange`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_year_range_max(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::YearRange, _>(handle, |obj| match &obj.max {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `years` field from a `YearRange`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_year_range_years(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::YearRange, _>(handle, |obj| match serde_json::to_string(&obj.years) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `FictionBookMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_fiction_book_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_fiction_book_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::FictionBookMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `FictionBookMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_fiction_book_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::FictionBookMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `FictionBookMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_fiction_book_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::FictionBookMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `genres` field from a `FictionBookMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_fiction_book_metadata_genres(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::FictionBookMetadata, _>(handle, |obj| match serde_json::to_string(&obj.genres) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `sequences` field from a `FictionBookMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_fiction_book_metadata_sequences(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::FictionBookMetadata, _>(handle, |obj| match serde_json::to_string(&obj.sequences) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `annotation` field from a `FictionBookMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_fiction_book_metadata_annotation(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::FictionBookMetadata, _>(handle, |obj| match &obj.annotation {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `DbfMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_dbf_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_dbf_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DbfMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DbfMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_dbf_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DbfMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DbfMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_dbf_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DbfMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `record_count` field from a `DbfMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_dbf_metadata_record_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DbfMetadata, _>(handle, |obj| obj.record_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `field_count` field from a `DbfMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_dbf_metadata_field_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DbfMetadata, _>(handle, |obj| obj.field_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `fields` field from a `DbfMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_dbf_metadata_fields(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DbfMetadata, _>(handle, |obj| match serde_json::to_string(&obj.fields) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `DbfFieldInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_dbf_field_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_dbf_field_info_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DbfFieldInfo>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DbfFieldInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_dbf_field_info_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DbfFieldInfo, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DbfFieldInfo` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_dbf_field_info_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DbfFieldInfo>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `name` field from a `DbfFieldInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_dbf_field_info_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DbfFieldInfo, _>(handle, |obj| match CString::new(obj.name.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `field_type` field from a `DbfFieldInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_dbf_field_info_field_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::DbfFieldInfo, _>(handle, |obj| match CString::new(obj.field_type.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `JatsMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_jats_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_jats_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::JatsMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `JatsMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_jats_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::JatsMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `JatsMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_jats_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::JatsMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `copyright` field from a `JatsMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_jats_metadata_copyright(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::JatsMetadata, _>(handle, |obj| match &obj.copyright {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `license` field from a `JatsMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_jats_metadata_license(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::JatsMetadata, _>(handle, |obj| match &obj.license {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `history_dates` field from a `JatsMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_jats_metadata_history_dates(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::JatsMetadata, _>(handle, |obj| match serde_json::to_string(&obj.history_dates) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `contributor_roles` field from a `JatsMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_jats_metadata_contributor_roles(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::JatsMetadata, _>(handle, |obj| match serde_json::to_string(&obj.contributor_roles) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ContributorRole` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_contributor_role_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_contributor_role_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ContributorRole>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ContributorRole` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_contributor_role_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ContributorRole, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ContributorRole` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_contributor_role_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ContributorRole>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `name` field from a `ContributorRole`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_contributor_role_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ContributorRole, _>(handle, |obj| match CString::new(obj.name.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `role` field from a `ContributorRole`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_contributor_role_role(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ContributorRole, _>(handle, |obj| match &obj.role {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `EpubMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_epub_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_epub_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::EpubMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `EpubMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_epub_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::EpubMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `EpubMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_epub_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::EpubMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `coverage` field from a `EpubMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_epub_metadata_coverage(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EpubMetadata, _>(handle, |obj| match &obj.coverage {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `dc_format` field from a `EpubMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_epub_metadata_dc_format(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EpubMetadata, _>(handle, |obj| match &obj.dc_format {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `relation` field from a `EpubMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_epub_metadata_relation(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EpubMetadata, _>(handle, |obj| match &obj.relation {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `source` field from a `EpubMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_epub_metadata_source(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EpubMetadata, _>(handle, |obj| match &obj.source {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `dc_type` field from a `EpubMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_epub_metadata_dc_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EpubMetadata, _>(handle, |obj| match &obj.dc_type {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `cover_image` field from a `EpubMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_epub_metadata_cover_image(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EpubMetadata, _>(handle, |obj| match &obj.cover_image {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `PstMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_pst_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pst_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PstMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PstMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pst_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PstMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PstMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pst_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PstMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `message_count` field from a `PstMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pst_metadata_message_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PstMetadata, _>(handle, |obj| obj.message_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Create a `AudioMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_audio_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_audio_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::AudioMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Serialize a `AudioMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_audio_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::AudioMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Free a `AudioMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_audio_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::AudioMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Get the `duration_ms` field from a `AudioMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_audio_metadata_duration_ms(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::AudioMetadata, _>(handle, |obj| match &obj.duration_ms {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Get the `codec` field from a `AudioMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_audio_metadata_codec(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::AudioMetadata, _>(handle, |obj| match &obj.codec {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Get the `container` field from a `AudioMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_audio_metadata_container(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::AudioMetadata, _>(handle, |obj| match &obj.container {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Get the `sample_rate_hz` field from a `AudioMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_audio_metadata_sample_rate_hz(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::AudioMetadata, _>(handle, |obj| match &obj.sample_rate_hz {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Get the `channels` field from a `AudioMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_audio_metadata_channels(handle: AlefHandle) -> u16 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::AudioMetadata, _>(handle, |obj| match &obj.channels {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "transcription-types")]
/// Get the `bitrate` field from a `AudioMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_audio_metadata_bitrate(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::AudioMetadata, _>(handle, |obj| match &obj.bitrate {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `OcrConfidence` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_ocr_confidence_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_confidence_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::OcrConfidence>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `OcrConfidence` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_confidence_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::OcrConfidence, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OcrConfidence` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_confidence_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::OcrConfidence>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `detection` field from a `OcrConfidence`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_confidence_detection(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::OcrConfidence, _>(handle, |obj| match &obj.detection {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `recognition` field from a `OcrConfidence`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_confidence_recognition(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::OcrConfidence, _>(handle, |obj| obj.recognition);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Create a `OcrRotation` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_ocr_rotation_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_rotation_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::OcrRotation>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `OcrRotation` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_rotation_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::OcrRotation, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OcrRotation` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_rotation_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::OcrRotation>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `angle_degrees` field from a `OcrRotation`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_rotation_angle_degrees(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::OcrRotation, _>(handle, |obj| obj.angle_degrees);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `confidence` field from a `OcrRotation`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_rotation_confidence(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::OcrRotation, _>(handle, |obj| match &obj.confidence {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Create a `OcrElement` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_ocr_element_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::OcrElement>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `OcrElement` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::OcrElement, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OcrElement` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::OcrElement>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `text` field from a `OcrElement`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::OcrElement, _>(handle, |obj| match CString::new(obj.text.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `geometry` field from a `OcrElement`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ocr_bounding_geometry_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_geometry(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrElement, _>(handle, |obj| match insert_handle(obj.geometry.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `confidence` field from a `OcrElement`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ocr_confidence_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_confidence(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrElement, _>(handle, |obj| match insert_handle(obj.confidence.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `level` field from a `OcrElement`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ocr_element_level_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_level(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrElement, _>(handle, |obj| match insert_handle(obj.level) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `rotation` field from a `OcrElement`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ocr_rotation_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_rotation(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrElement, _>(handle, |obj| match &obj.rotation {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `page_number` field from a `OcrElement`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_page_number(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrElement, _>(handle, |obj| obj.page_number);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `parent_id` field from a `OcrElement`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_parent_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::OcrElement, _>(handle, |obj| match &obj.parent_id {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `backend_metadata` field from a `OcrElement`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_backend_metadata(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::OcrElement, _>(handle, |obj| match serde_json::to_string(&obj.backend_metadata) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `OcrElementConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_ocr_element_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::OcrElementConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `OcrElementConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::OcrElementConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OcrElementConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::OcrElementConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `include_elements` field from a `OcrElementConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_config_include_elements(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrElementConfig, _>(handle, |obj| obj.include_elements as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `min_level` field from a `OcrElementConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ocr_element_level_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_config_min_level(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrElementConfig, _>(handle, |obj| match insert_handle(obj.min_level) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `min_confidence` field from a `OcrElementConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_config_min_confidence(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::OcrElementConfig, _>(handle, |obj| obj.min_confidence);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `build_hierarchy` field from a `OcrElementConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_config_build_hierarchy(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OcrElementConfig, _>(handle, |obj| obj.build_hierarchy as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `PageStructure` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_page_structure_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_structure_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PageStructure>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PageStructure` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_structure_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PageStructure, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PageStructure` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_structure_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PageStructure>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `total_count` field from a `PageStructure`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_structure_total_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageStructure, _>(handle, |obj| obj.total_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `unit_type` field from a `PageStructure`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_page_unit_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_structure_unit_type(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageStructure, _>(handle, |obj| match insert_handle(obj.unit_type) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `boundaries` field from a `PageStructure`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_structure_boundaries(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PageStructure, _>(handle, |obj| match &obj.boundaries {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `pages` field from a `PageStructure`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_structure_pages(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PageStructure, _>(handle, |obj| match &obj.pages {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `PageBoundary` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_page_boundary_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_boundary_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PageBoundary>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PageBoundary` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_boundary_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PageBoundary, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PageBoundary` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_boundary_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PageBoundary>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `byte_start` field from a `PageBoundary`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_boundary_byte_start(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageBoundary, _>(handle, |obj| obj.byte_start);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `byte_end` field from a `PageBoundary`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_boundary_byte_end(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageBoundary, _>(handle, |obj| obj.byte_end);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `page_number` field from a `PageBoundary`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_boundary_page_number(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageBoundary, _>(handle, |obj| obj.page_number);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `PageInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_page_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_info_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PageInfo>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PageInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_info_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PageInfo, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PageInfo` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_info_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PageInfo>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `number` field from a `PageInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_info_number(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageInfo, _>(handle, |obj| obj.number);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `title` field from a `PageInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_info_title(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PageInfo, _>(handle, |obj| match &obj.title {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `image_count` field from a `PageInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_info_image_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageInfo, _>(handle, |obj| match &obj.image_count {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `table_count` field from a `PageInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_info_table_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageInfo, _>(handle, |obj| match &obj.table_count {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `hidden` field from a `PageInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_info_hidden(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageInfo, _>(handle, |obj| match &obj.hidden {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `is_blank` field from a `PageInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_info_is_blank(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageInfo, _>(handle, |obj| match &obj.is_blank {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `has_vector_graphics` field from a `PageInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_info_has_vector_graphics(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageInfo, _>(handle, |obj| obj.has_vector_graphics as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `PageContent` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_page_content_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_content_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PageContent>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PageContent` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_content_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PageContent, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PageContent` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_content_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PageContent>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `page_number` field from a `PageContent`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_content_page_number(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageContent, _>(handle, |obj| obj.page_number);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `content` field from a `PageContent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_content_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PageContent, _>(handle, |obj| match CString::new(obj.content.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `tables` field from a `PageContent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_content_tables(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PageContent, _>(handle, |obj| match serde_json::to_string(&obj.tables) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `image_indices` field from a `PageContent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_content_image_indices(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PageContent, _>(handle, |obj| match serde_json::to_string(&obj.image_indices) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `hierarchy` field from a `PageContent`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_page_hierarchy_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_content_hierarchy(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageContent, _>(handle, |obj| match &obj.hierarchy {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `is_blank` field from a `PageContent`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_content_is_blank(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageContent, _>(handle, |obj| match &obj.is_blank {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `layout_regions` field from a `PageContent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_content_layout_regions(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PageContent, _>(handle, |obj| match &obj.layout_regions {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `speaker_notes` field from a `PageContent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_content_speaker_notes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PageContent, _>(handle, |obj| match &obj.speaker_notes {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `section_name` field from a `PageContent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_content_section_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PageContent, _>(handle, |obj| match &obj.section_name {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `sheet_name` field from a `PageContent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_content_sheet_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PageContent, _>(handle, |obj| match &obj.sheet_name {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `LayoutRegion` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_layout_region_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_region_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::LayoutRegion>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LayoutRegion` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_region_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::LayoutRegion, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LayoutRegion` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_region_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::LayoutRegion>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `class_name` field from a `LayoutRegion`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_region_class_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::LayoutRegion, _>(handle, |obj| match CString::new(obj.class_name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `confidence` field from a `LayoutRegion`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_region_confidence(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::LayoutRegion, _>(handle, |obj| obj.confidence);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `bounding_box` field from a `LayoutRegion`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_bounding_box_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_region_bounding_box(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LayoutRegion, _>(handle, |obj| match insert_handle(obj.bounding_box) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `area_fraction` field from a `LayoutRegion`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_region_area_fraction(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::LayoutRegion, _>(handle, |obj| obj.area_fraction);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Create a `PageHierarchy` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_page_hierarchy_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_hierarchy_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PageHierarchy>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PageHierarchy` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_hierarchy_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PageHierarchy, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PageHierarchy` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_hierarchy_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PageHierarchy>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `block_count` field from a `PageHierarchy`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_hierarchy_block_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageHierarchy, _>(handle, |obj| obj.block_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `blocks` field from a `PageHierarchy`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_hierarchy_blocks(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PageHierarchy, _>(handle, |obj| match serde_json::to_string(&obj.blocks) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `HierarchicalBlock` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_hierarchical_block_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_hierarchical_block_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::HierarchicalBlock>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `HierarchicalBlock` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_hierarchical_block_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::HierarchicalBlock, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `HierarchicalBlock` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_hierarchical_block_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::HierarchicalBlock>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `text` field from a `HierarchicalBlock`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_hierarchical_block_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::HierarchicalBlock, _>(handle, |obj| match CString::new(obj.text.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `font_size` field from a `HierarchicalBlock`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_hierarchical_block_font_size(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::HierarchicalBlock, _>(handle, |obj| obj.font_size);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `level` field from a `HierarchicalBlock`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_hierarchical_block_level(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::HierarchicalBlock, _>(handle, |obj| match CString::new(obj.level.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `QrCode` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_qr_code_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_qr_code_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::QrCode>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `QrCode` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_qr_code_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::QrCode, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `QrCode` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_qr_code_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::QrCode>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `payload` field from a `QrCode`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_qr_code_payload(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::QrCode, _>(handle, |obj| match CString::new(obj.payload.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `confidence` field from a `QrCode`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_qr_code_confidence(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::QrCode, _>(handle, |obj| match &obj.confidence {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `bbox` field from a `QrCode`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_qr_bounding_box_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_qr_code_bbox(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::QrCode, _>(handle, |obj| match &obj.bbox {
            Some(val) => match insert_handle(*val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `QrBoundingBox` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_qr_bounding_box_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_qr_bounding_box_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::QrBoundingBox>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `QrBoundingBox` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_qr_bounding_box_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::QrBoundingBox, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `QrBoundingBox` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_qr_bounding_box_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::QrBoundingBox>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `x` field from a `QrBoundingBox`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_qr_bounding_box_x(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::QrBoundingBox, _>(handle, |obj| obj.x);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `y` field from a `QrBoundingBox`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_qr_bounding_box_y(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::QrBoundingBox, _>(handle, |obj| obj.y);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `width` field from a `QrBoundingBox`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_qr_bounding_box_width(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::QrBoundingBox, _>(handle, |obj| obj.width);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `height` field from a `QrBoundingBox`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_qr_bounding_box_height(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::QrBoundingBox, _>(handle, |obj| obj.height);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `RedactionReport` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_redaction_report_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_report_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::RedactionReport>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `RedactionReport` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_report_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::RedactionReport, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `RedactionReport` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_report_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::RedactionReport>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `findings` field from a `RedactionReport`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_report_findings(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::RedactionReport, _>(handle, |obj| match serde_json::to_string(&obj.findings) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `total_redacted` field from a `RedactionReport`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_report_total_redacted(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RedactionReport, _>(handle, |obj| obj.total_redacted);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `RedactionFinding` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_redaction_finding_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_finding_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::RedactionFinding>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `RedactionFinding` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_finding_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::RedactionFinding, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `RedactionFinding` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_finding_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::RedactionFinding>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `start` field from a `RedactionFinding`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_finding_start(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RedactionFinding, _>(handle, |obj| obj.start);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `end` field from a `RedactionFinding`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_finding_end(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RedactionFinding, _>(handle, |obj| obj.end);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `category` field from a `RedactionFinding`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_pii_category_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_finding_category(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::RedactionFinding, _>(handle, |obj| match insert_handle(obj.category.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `strategy` field from a `RedactionFinding`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_redaction_strategy_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_finding_strategy(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RedactionFinding, _>(handle, |obj| match insert_handle(obj.strategy) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `replacement_token` field from a `RedactionFinding`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_finding_replacement_token(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::RedactionFinding, _>(handle, |obj| {
            match CString::new(obj.replacement_token.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `CellChange` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_cell_change_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_cell_change_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::CellChange>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CellChange` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_cell_change_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::CellChange, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CellChange` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_cell_change_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::CellChange>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `row` field from a `CellChange`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_cell_change_row(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CellChange, _>(handle, |obj| obj.row);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `col` field from a `CellChange`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_cell_change_col(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::CellChange, _>(handle, |obj| obj.col);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `from` field from a `CellChange`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_cell_change_from(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CellChange, _>(handle, |obj| match CString::new(obj.from.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `to` field from a `CellChange`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_cell_change_to(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::CellChange, _>(handle, |obj| match CString::new(obj.to.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `PropertyChange` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_property_change_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_property_change_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PropertyChange>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PropertyChange` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_property_change_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PropertyChange, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PropertyChange` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_property_change_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PropertyChange>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `name` field from a `PropertyChange`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_property_change_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PropertyChange, _>(handle, |obj| match CString::new(obj.name.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `from` field from a `PropertyChange`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_property_change_from(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PropertyChange, _>(handle, |obj| match &obj.from {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `to` field from a `PropertyChange`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_property_change_to(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PropertyChange, _>(handle, |obj| match &obj.to {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `DocumentRevision` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_document_revision_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_revision_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DocumentRevision>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DocumentRevision` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_revision_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DocumentRevision, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DocumentRevision` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_revision_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DocumentRevision>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `revision_id` field from a `DocumentRevision`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_revision_revision_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::DocumentRevision, _>(handle, |obj| match CString::new(obj.revision_id.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `author` field from a `DocumentRevision`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_revision_author(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DocumentRevision, _>(handle, |obj| match &obj.author {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `timestamp` field from a `DocumentRevision`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_revision_timestamp(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DocumentRevision, _>(handle, |obj| match &obj.timestamp {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `kind` field from a `DocumentRevision`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_revision_kind_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_revision_kind(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentRevision, _>(handle, |obj| match insert_handle(obj.kind) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `anchor` field from a `DocumentRevision`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_revision_anchor_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_revision_anchor(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentRevision, _>(handle, |obj| match &obj.anchor {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `delta` field from a `DocumentRevision`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_revision_delta_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_revision_delta(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentRevision, _>(handle, |obj| match insert_handle(obj.delta.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `RevisionDelta` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_revision_delta_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_revision_delta_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::RevisionDelta>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `RevisionDelta` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_revision_delta_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::RevisionDelta, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `RevisionDelta` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_revision_delta_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::RevisionDelta>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `content` field from a `RevisionDelta`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_revision_delta_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::RevisionDelta, _>(handle, |obj| match serde_json::to_string(&obj.content) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `table_changes` field from a `RevisionDelta`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_revision_delta_table_changes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::RevisionDelta, _>(handle, |obj| match serde_json::to_string(&obj.table_changes) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `property_changes` field from a `RevisionDelta`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_revision_delta_property_changes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::RevisionDelta, _>(handle, |obj| match serde_json::to_string(&obj.property_changes) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `DocumentSummary` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_document_summary_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_summary_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DocumentSummary>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DocumentSummary` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_summary_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DocumentSummary, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DocumentSummary` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_summary_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DocumentSummary>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `text` field from a `DocumentSummary`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_summary_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DocumentSummary, _>(handle, |obj| match CString::new(obj.text.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `strategy` field from a `DocumentSummary`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_summary_strategy_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_summary_strategy(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentSummary, _>(handle, |obj| match insert_handle(obj.strategy) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `token_count` field from a `DocumentSummary`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_summary_token_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentSummary, _>(handle, |obj| match &obj.token_count {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `Table` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_table_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::Table>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `Table` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::Table, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `Table` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::Table>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `cells` field from a `Table`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_cells(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Table, _>(handle, |obj| match serde_json::to_string(&obj.cells) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `markdown` field from a `Table`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_markdown(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Table, _>(handle, |obj| match CString::new(obj.markdown.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `page_number` field from a `Table`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_page_number(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Table, _>(handle, |obj| obj.page_number);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `bounding_box` field from a `Table`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_bounding_box_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_bounding_box(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Table, _>(handle, |obj| match &obj.bounding_box {
            Some(val) => match insert_handle(*val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `table_id` field from a `Table`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_table_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Table, _>(handle, |obj| match &obj.table_id {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `columns` field from a `Table`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_columns(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Table, _>(handle, |obj| match &obj.columns {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `TableCell` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_table_cell_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_cell_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::TableCell>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `TableCell` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_cell_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::TableCell, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `TableCell` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_cell_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::TableCell>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `content` field from a `TableCell`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_cell_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::TableCell, _>(handle, |obj| match CString::new(obj.content.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `row_span` field from a `TableCell`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_cell_row_span(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TableCell, _>(handle, |obj| obj.row_span);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `col_span` field from a `TableCell`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_cell_col_span(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TableCell, _>(handle, |obj| obj.col_span);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `is_header` field from a `TableCell`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_cell_is_header(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TableCell, _>(handle, |obj| obj.is_header as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `Translation` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_translation_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_translation_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::Translation>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `Translation` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_translation_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::Translation, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `Translation` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_translation_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::Translation>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `target_lang` field from a `Translation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_translation_target_lang(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::Translation, _>(handle, |obj| match CString::new(obj.target_lang.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `source_lang` field from a `Translation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_translation_source_lang(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Translation, _>(handle, |obj| match &obj.source_lang {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `content` field from a `Translation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_translation_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Translation, _>(handle, |obj| match CString::new(obj.content.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `formatted_content` field from a `Translation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_translation_formatted_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Translation, _>(handle, |obj| match &obj.formatted_content {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ExtractedUri` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_extracted_uri_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_uri_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ExtractedUri>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ExtractedUri` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_uri_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ExtractedUri, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ExtractedUri` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_uri_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ExtractedUri>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `url` field from a `ExtractedUri`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_uri_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedUri, _>(handle, |obj| match CString::new(obj.url.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `label` field from a `ExtractedUri`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_uri_label(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ExtractedUri, _>(handle, |obj| match &obj.label {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `page` field from a `ExtractedUri`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_uri_page(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedUri, _>(handle, |obj| match &obj.page {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `kind` field from a `ExtractedUri`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_uri_kind_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extracted_uri_kind(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ExtractedUri, _>(handle, |obj| match insert_handle(obj.kind) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "api")]
/// Create a `DetectResponse` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_detect_response_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_detect_response_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::api::DetectResponse>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "api")]
/// Serialize a `DetectResponse` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_detect_response_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::api::DetectResponse, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "api")]
/// Free a `DetectResponse` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_detect_response_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::api::DetectResponse>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "api")]
/// Get the `mime_type` field from a `DetectResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_detect_response_mime_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::api::DetectResponse, _>(handle, |obj| match CString::new(obj.mime_type.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "api")]
/// Get the `filename` field from a `DetectResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_detect_response_filename(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::api::DetectResponse, _>(handle, |obj| match &obj.filename {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Create a `DiffOptions` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_diff_options_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_diff_options_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DiffOptions>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Serialize a `DiffOptions` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_diff_options_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DiffOptions, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "diff")]
/// Free a `DiffOptions` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_diff_options_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DiffOptions>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `include_metadata` field from a `DiffOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_diff_options_include_metadata(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DiffOptions, _>(handle, |obj| obj.include_metadata as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `include_embedded` field from a `DiffOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_diff_options_include_embedded(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DiffOptions, _>(handle, |obj| obj.include_embedded as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `max_content_chars` field from a `DiffOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_diff_options_max_content_chars(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DiffOptions, _>(handle, |obj| match &obj.max_content_chars {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Create a `ExtractionDiff` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_extraction_diff_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_diff_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ExtractionDiff>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Serialize a `ExtractionDiff` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_diff_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ExtractionDiff, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "diff")]
/// Free a `ExtractionDiff` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_diff_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ExtractionDiff>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `content_diff` field from a `ExtractionDiff`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_diff_content_diff(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ExtractionDiff, _>(handle, |obj| match serde_json::to_string(&obj.content_diff) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `tables_added` field from a `ExtractionDiff`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_diff_tables_added(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ExtractionDiff, _>(handle, |obj| match serde_json::to_string(&obj.tables_added) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `tables_removed` field from a `ExtractionDiff`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_diff_tables_removed(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ExtractionDiff, _>(handle, |obj| match serde_json::to_string(&obj.tables_removed) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `tables_changed` field from a `ExtractionDiff`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_diff_tables_changed(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ExtractionDiff, _>(handle, |obj| match serde_json::to_string(&obj.tables_changed) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `metadata_changed` field from a `ExtractionDiff`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_diff_metadata_changed(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ExtractionDiff, _>(handle, |obj| match serde_json::to_string(&obj.metadata_changed) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `embedded_changes` field from a `ExtractionDiff`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_embedded_changes_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_diff_embedded_changes(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::ExtractionDiff, _>(handle, |obj| match insert_handle(obj.embedded_changes.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Create a `DiffHunk` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_diff_hunk_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_diff_hunk_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DiffHunk>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Serialize a `DiffHunk` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_diff_hunk_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DiffHunk, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "diff")]
/// Free a `DiffHunk` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_diff_hunk_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DiffHunk>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `from_line` field from a `DiffHunk`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_diff_hunk_from_line(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DiffHunk, _>(handle, |obj| obj.from_line);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `from_count` field from a `DiffHunk`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_diff_hunk_from_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DiffHunk, _>(handle, |obj| obj.from_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `to_line` field from a `DiffHunk`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_diff_hunk_to_line(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DiffHunk, _>(handle, |obj| obj.to_line);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `to_count` field from a `DiffHunk`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_diff_hunk_to_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DiffHunk, _>(handle, |obj| obj.to_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `lines` field from a `DiffHunk`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_diff_hunk_lines(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DiffHunk, _>(handle, |obj| match serde_json::to_string(&obj.lines) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Create a `TableDiff` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_table_diff_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_diff_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::TableDiff>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Serialize a `TableDiff` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_diff_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::TableDiff, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "diff")]
/// Free a `TableDiff` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_diff_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::TableDiff>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `from_index` field from a `TableDiff`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_diff_from_index(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TableDiff, _>(handle, |obj| obj.from_index);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `to_index` field from a `TableDiff`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_diff_to_index(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::TableDiff, _>(handle, |obj| obj.to_index);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `cell_changes` field from a `TableDiff`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_diff_cell_changes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::TableDiff, _>(handle, |obj| match serde_json::to_string(&obj.cell_changes) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Create a `EmbeddedChanges` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_embedded_changes_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_changes_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::EmbeddedChanges>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Serialize a `EmbeddedChanges` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_changes_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::EmbeddedChanges, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "diff")]
/// Free a `EmbeddedChanges` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_changes_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::EmbeddedChanges>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `added` field from a `EmbeddedChanges`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_changes_added(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmbeddedChanges, _>(handle, |obj| match serde_json::to_string(&obj.added) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `removed` field from a `EmbeddedChanges`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_changes_removed(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::EmbeddedChanges, _>(handle, |obj| match serde_json::to_string(&obj.removed) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `changed` field from a `EmbeddedChanges`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_changes_changed(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::EmbeddedChanges, _>(handle, |obj| match serde_json::to_string(&obj.changed) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Create a `EmbeddedDiff` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_embedded_diff_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_diff_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::EmbeddedDiff>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Serialize a `EmbeddedDiff` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_diff_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::EmbeddedDiff, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "diff")]
/// Free a `EmbeddedDiff` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_diff_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::EmbeddedDiff>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `path` field from a `EmbeddedDiff`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_diff_path(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::EmbeddedDiff, _>(handle, |obj| match CString::new(obj.path.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "diff")]
/// Get the `diff` field from a `EmbeddedDiff`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_extraction_diff_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_diff_diff(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::EmbeddedDiff, _>(handle, |obj| match insert_handle((*obj.diff).clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(any(feature = "reranker-presets", feature = "reranker"))]
/// Create a `RerankedDocument` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_reranked_document_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranked_document_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::RerankedDocument>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(any(feature = "reranker-presets", feature = "reranker"))]
/// Serialize a `RerankedDocument` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranked_document_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::RerankedDocument, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(any(feature = "reranker-presets", feature = "reranker"))]
/// Free a `RerankedDocument` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranked_document_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::RerankedDocument>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(any(feature = "reranker-presets", feature = "reranker"))]
/// Get the `index` field from a `RerankedDocument`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranked_document_index(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RerankedDocument, _>(handle, |obj| obj.index);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(any(feature = "reranker-presets", feature = "reranker"))]
/// Get the `score` field from a `RerankedDocument`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranked_document_score(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::RerankedDocument, _>(handle, |obj| obj.score);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(any(feature = "reranker-presets", feature = "reranker"))]
/// Get the `document` field from a `RerankedDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranked_document_document(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::RerankedDocument, _>(handle, |obj| match CString::new(obj.document.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "sparse-embedding-presets")]
/// Create a `SparseEmbedding` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_sparse_embedding_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::SparseEmbedding>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "sparse-embedding-presets")]
/// Serialize a `SparseEmbedding` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::SparseEmbedding, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "sparse-embedding-presets")]
/// Free a `SparseEmbedding` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::SparseEmbedding>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "sparse-embedding-presets")]
/// Get the `indices` field from a `SparseEmbedding`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_indices(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::SparseEmbedding, _>(handle, |obj| match serde_json::to_string(&obj.indices) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "sparse-embedding-presets")]
/// Get the `values` field from a `SparseEmbedding`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_values(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::SparseEmbedding, _>(handle, |obj| match serde_json::to_string(&obj.values) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "sparse-embedding-presets")]
/// Create a `SparseEmbeddingPreset` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_sparse_embedding_preset_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_preset_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::SparseEmbeddingPreset>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "sparse-embedding-presets")]
/// Serialize a `SparseEmbeddingPreset` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_preset_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::SparseEmbeddingPreset, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "sparse-embedding-presets")]
/// Free a `SparseEmbeddingPreset` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_preset_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::SparseEmbeddingPreset>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "sparse-embedding-presets")]
/// Get the `name` field from a `SparseEmbeddingPreset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_preset_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::SparseEmbeddingPreset, _>(handle, |obj| match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "sparse-embedding-presets")]
/// Get the `model_repo` field from a `SparseEmbeddingPreset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_preset_model_repo(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::SparseEmbeddingPreset, _>(handle, |obj| {
            match CString::new(obj.model_repo.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "sparse-embedding-presets")]
/// Get the `model_file` field from a `SparseEmbeddingPreset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_preset_model_file(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::SparseEmbeddingPreset, _>(handle, |obj| {
            match CString::new(obj.model_file.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "sparse-embedding-presets")]
/// Get the `additional_files` field from a `SparseEmbeddingPreset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_preset_additional_files(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::SparseEmbeddingPreset, _>(handle, |obj| {
            match serde_json::to_string(&obj.additional_files) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "sparse-embedding-presets")]
/// Get the `max_length` field from a `SparseEmbeddingPreset`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_preset_max_length(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::SparseEmbeddingPreset, _>(handle, |obj| obj.max_length);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "sparse-embedding-presets")]
/// Get the `description` field from a `SparseEmbeddingPreset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_preset_description(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::SparseEmbeddingPreset, _>(handle, |obj| {
            match CString::new(obj.description.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Create a `MultiVectorEmbedding` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_multi_vector_embedding_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_multi_vector_embedding_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::MultiVectorEmbedding>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Serialize a `MultiVectorEmbedding` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_multi_vector_embedding_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::MultiVectorEmbedding, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Free a `MultiVectorEmbedding` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_multi_vector_embedding_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::MultiVectorEmbedding>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Get the `num_tokens` field from a `MultiVectorEmbedding`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_multi_vector_embedding_num_tokens(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::MultiVectorEmbedding, _>(handle, |obj| obj.num_tokens);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Get the `dim` field from a `MultiVectorEmbedding`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_multi_vector_embedding_dim(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::MultiVectorEmbedding, _>(handle, |obj| obj.dim);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Get the `data` field from a `MultiVectorEmbedding`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_multi_vector_embedding_data(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::MultiVectorEmbedding, _>(handle, |obj| match serde_json::to_string(&obj.data) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Returns `true` if `data` holds exactly `num_tokens * dim` values â i.e.
/// the flat buffer matches the declared shape.
///
/// All fields are `pub` and the type is `Deserialize`, so a value coming
/// from an untrusted source (FFI caller, JSON, a store row) may be
/// malformed. `max_sim_score` guards on this so a length-mismatched
/// buffer scores `0.0` rather than silently mis-scoring (a shorter `data`
/// would make `rows` (Self::rows)' `chunks_exact` drop a trailing partial
/// chunk). Uses `checked_mul` so an overflowing `num_tokens * dim` is
/// reported as malformed instead of wrapping.
///
/// Since v5.0.0.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "late-interaction-presets")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_multi_vector_embedding_is_well_formed(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::MultiVectorEmbedding>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::MultiVectorEmbedding>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.is_well_formed();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

#[cfg(feature = "late-interaction-presets")]
/// Create a `LateInteractionPreset` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_late_interaction_preset_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_preset_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::LateInteractionPreset>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Serialize a `LateInteractionPreset` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_preset_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::LateInteractionPreset, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Free a `LateInteractionPreset` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_preset_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::LateInteractionPreset>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Get the `name` field from a `LateInteractionPreset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_preset_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::LateInteractionPreset, _>(handle, |obj| match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Get the `model_repo` field from a `LateInteractionPreset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_preset_model_repo(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LateInteractionPreset, _>(handle, |obj| {
            match CString::new(obj.model_repo.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Get the `model_file` field from a `LateInteractionPreset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_preset_model_file(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LateInteractionPreset, _>(handle, |obj| {
            match CString::new(obj.model_file.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Get the `additional_files` field from a `LateInteractionPreset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_preset_additional_files(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LateInteractionPreset, _>(handle, |obj| {
            match serde_json::to_string(&obj.additional_files) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Get the `max_length` field from a `LateInteractionPreset`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_preset_max_length(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LateInteractionPreset, _>(handle, |obj| obj.max_length);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Get the `query_max_length` field from a `LateInteractionPreset`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_preset_query_max_length(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LateInteractionPreset, _>(handle, |obj| obj.query_max_length);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Get the `dim` field from a `LateInteractionPreset`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_preset_dim(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LateInteractionPreset, _>(handle, |obj| obj.dim);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Get the `description` field from a `LateInteractionPreset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_preset_description(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::LateInteractionPreset, _>(handle, |obj| {
            match CString::new(obj.description.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Create a `LateInteractionMatch` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_late_interaction_match_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_match_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::LateInteractionMatch>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Serialize a `LateInteractionMatch` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_match_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::LateInteractionMatch, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Free a `LateInteractionMatch` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_match_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::LateInteractionMatch>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Get the `index` field from a `LateInteractionMatch`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_match_index(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LateInteractionMatch, _>(handle, |obj| obj.index);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "late-interaction-presets")]
/// Get the `score` field from a `LateInteractionMatch`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_match_score(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::LateInteractionMatch, _>(handle, |obj| obj.score);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "keywords-yake")]
/// Create a `YakeParams` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_yake_params_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_yake_params_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::YakeParams>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "keywords-yake")]
/// Serialize a `YakeParams` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_yake_params_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::YakeParams, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "keywords-yake")]
/// Free a `YakeParams` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_yake_params_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::YakeParams>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "keywords-yake")]
/// Get the `window_size` field from a `YakeParams`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_yake_params_window_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::YakeParams, _>(handle, |obj| obj.window_size);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "keywords-rake")]
/// Create a `RakeParams` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_rake_params_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_rake_params_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::RakeParams>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "keywords-rake")]
/// Serialize a `RakeParams` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_rake_params_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::RakeParams, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "keywords-rake")]
/// Free a `RakeParams` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_rake_params_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::RakeParams>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "keywords-rake")]
/// Get the `min_word_length` field from a `RakeParams`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_rake_params_min_word_length(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RakeParams, _>(handle, |obj| obj.min_word_length);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "keywords-rake")]
/// Get the `max_words_per_phrase` field from a `RakeParams`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_rake_params_max_words_per_phrase(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RakeParams, _>(handle, |obj| obj.max_words_per_phrase);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
/// Create a `KeywordConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_keyword_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::KeywordConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
/// Serialize a `KeywordConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::KeywordConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
/// Free a `KeywordConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::KeywordConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
/// Get the `algorithm` field from a `KeywordConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_keyword_algorithm_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_config_algorithm(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::KeywordConfig, _>(handle, |obj| match insert_handle(obj.algorithm) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
/// Get the `max_keywords` field from a `KeywordConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_config_max_keywords(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::KeywordConfig, _>(handle, |obj| obj.max_keywords);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
/// Get the `min_score` field from a `KeywordConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_config_min_score(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::KeywordConfig, _>(handle, |obj| obj.min_score);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
/// Get the `language` field from a `KeywordConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_config_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::KeywordConfig, _>(handle, |obj| match &obj.language {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
/// Get the `yake_params` field from a `KeywordConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_yake_params_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_config_yake_params(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::KeywordConfig, _>(handle, |obj| match &obj.yake_params {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
/// Get the `rake_params` field from a `KeywordConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_rake_params_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_config_rake_params(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::KeywordConfig, _>(handle, |obj| match &obj.rake_params {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
/// Create a `Keyword` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_keyword_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::Keyword>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
/// Serialize a `Keyword` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::Keyword, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
/// Free a `Keyword` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::Keyword>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
/// Get the `text` field from a `Keyword`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Keyword, _>(handle, |obj| match CString::new(obj.text.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
/// Get the `score` field from a `Keyword`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_score(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::Keyword, _>(handle, |obj| obj.score);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
/// Get the `algorithm` field from a `Keyword`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_keyword_algorithm_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_algorithm(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Keyword, _>(handle, |obj| match insert_handle(obj.algorithm) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
/// Get the `positions` field from a `Keyword`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_positions(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Keyword, _>(handle, |obj| match &obj.positions {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Create a `DocumentMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_document_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DocumentMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Serialize a `DocumentMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DocumentMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "heuristics")]
/// Free a `DocumentMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DocumentMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `mime_type` field from a `DocumentMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_metadata_mime_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::DocumentMetadata, _>(handle, |obj| match CString::new(obj.mime_type.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `size_bytes` field from a `DocumentMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_metadata_size_bytes(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentMetadata, _>(handle, |obj| obj.size_bytes);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `page_count` field from a `DocumentMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_metadata_page_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentMetadata, _>(handle, |obj| match &obj.page_count {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `force_ocr` field from a `DocumentMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_metadata_force_ocr(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentMetadata, _>(handle, |obj| obj.force_ocr as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `user_chunk_config` field from a `DocumentMetadata`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_user_chunk_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_metadata_user_chunk_config(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentMetadata, _>(handle, |obj| match &obj.user_chunk_config {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `chunking_enabled` field from a `DocumentMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_metadata_chunking_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentMetadata, _>(handle, |obj| obj.chunking_enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Create a `UserChunkConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_user_chunk_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_user_chunk_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::UserChunkConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Serialize a `UserChunkConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_user_chunk_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::UserChunkConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "heuristics")]
/// Free a `UserChunkConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_user_chunk_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::UserChunkConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `page_ranges` field from a `UserChunkConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_user_chunk_config_page_ranges(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::UserChunkConfig, _>(handle, |obj| match &obj.page_ranges {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `pages_per_chunk` field from a `UserChunkConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_user_chunk_config_pages_per_chunk(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::UserChunkConfig, _>(handle, |obj| match &obj.pages_per_chunk {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `force_chunking` field from a `UserChunkConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_user_chunk_config_force_chunking(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::UserChunkConfig, _>(handle, |obj| obj.force_chunking as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `disable_chunking` field from a `UserChunkConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_user_chunk_config_disable_chunking(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::UserChunkConfig, _>(handle, |obj| obj.disable_chunking as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Create a `ExtractionConfidence` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_extraction_confidence_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_confidence_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ExtractionConfidence>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Serialize a `ExtractionConfidence` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_confidence_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ExtractionConfidence, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "heuristics")]
/// Free a `ExtractionConfidence` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_confidence_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ExtractionConfidence>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `text_coverage` field from a `ExtractionConfidence`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_confidence_text_coverage(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::ExtractionConfidence, _>(handle, |obj| obj.text_coverage);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `ocr_aggregate` field from a `ExtractionConfidence`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_confidence_ocr_aggregate(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::ExtractionConfidence, _>(handle, |obj| match &obj.ocr_aggregate {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `schema_compliance` field from a `ExtractionConfidence`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_schema_compliance_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_confidence_schema_compliance(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::ExtractionConfidence, _>(handle, |obj| match insert_handle(obj.schema_compliance) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `combined` field from a `ExtractionConfidence`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_confidence_combined(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::ExtractionConfidence, _>(handle, |obj| obj.combined);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Create a `HeuristicsConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_heuristics_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heuristics_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::HeuristicsConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Serialize a `HeuristicsConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heuristics_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::HeuristicsConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "heuristics")]
/// Free a `HeuristicsConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heuristics_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::HeuristicsConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `enable_pdf_text_heuristics` field from a `HeuristicsConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heuristics_config_enable_pdf_text_heuristics(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HeuristicsConfig, _>(handle, |obj| obj.enable_pdf_text_heuristics as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `text_layer_threshold` field from a `HeuristicsConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heuristics_config_text_layer_threshold(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::HeuristicsConfig, _>(handle, |obj| obj.text_layer_threshold);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `file_size_threshold_bytes` field from a `HeuristicsConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heuristics_config_file_size_threshold_bytes(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HeuristicsConfig, _>(handle, |obj| obj.file_size_threshold_bytes);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `page_count_threshold` field from a `HeuristicsConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heuristics_config_page_count_threshold(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HeuristicsConfig, _>(handle, |obj| obj.page_count_threshold);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `target_pages_per_chunk` field from a `HeuristicsConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heuristics_config_target_pages_per_chunk(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HeuristicsConfig, _>(handle, |obj| obj.target_pages_per_chunk);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `max_pages_per_chunk` field from a `HeuristicsConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heuristics_config_max_pages_per_chunk(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HeuristicsConfig, _>(handle, |obj| obj.max_pages_per_chunk);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `disk_processing_threshold_bytes` field from a `HeuristicsConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heuristics_config_disk_processing_threshold_bytes(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HeuristicsConfig, _>(handle, |obj| obj.disk_processing_threshold_bytes);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `min_chars_per_page` field from a `HeuristicsConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heuristics_config_min_chars_per_page(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HeuristicsConfig, _>(handle, |obj| obj.min_chars_per_page);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `max_xlsx_sheet_count` field from a `HeuristicsConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heuristics_config_max_xlsx_sheet_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HeuristicsConfig, _>(handle, |obj| obj.max_xlsx_sheet_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `max_xlsx_workbook_cells` field from a `HeuristicsConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heuristics_config_max_xlsx_workbook_cells(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HeuristicsConfig, _>(handle, |obj| obj.max_xlsx_workbook_cells);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `max_pptx_embedded_count` field from a `HeuristicsConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heuristics_config_max_pptx_embedded_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::HeuristicsConfig, _>(handle, |obj| obj.max_pptx_embedded_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Validate the configuration.
/// \note Returns `HeuristicsError.ConfigError` when:
/// - `target_pages_per_chunk` is 0
/// - `max_pages_per_chunk` < `target_pages_per_chunk`
/// - `file_size_threshold_bytes` is 0
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "heuristics")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heuristics_config_validate(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::HeuristicsConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return -1;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return -1;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::HeuristicsConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.validate();
        match result {
            Ok(()) => 0,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            -1
        }
    }
}

#[cfg(feature = "heuristics")]
/// Create a `ChunkInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_chunk_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_info_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ChunkInfo>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Serialize a `ChunkInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_info_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ChunkInfo, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "heuristics")]
/// Free a `ChunkInfo` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_info_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ChunkInfo>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `index` field from a `ChunkInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_info_index(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkInfo, _>(handle, |obj| obj.index);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `pages` field from a `ChunkInfo`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_page_range_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_info_pages(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkInfo, _>(handle, |obj| match insert_handle(obj.pages.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `estimated_time_ms` field from a `ChunkInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_info_estimated_time_ms(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ChunkInfo, _>(handle, |obj| obj.estimated_time_ms);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Create a `PageRange` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_page_range_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_range_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PageRange>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Serialize a `PageRange` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_range_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PageRange, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "heuristics")]
/// Free a `PageRange` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_range_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PageRange>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `start` field from a `PageRange`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_range_start(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageRange, _>(handle, |obj| obj.start);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `end` field from a `PageRange`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_range_end(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageRange, _>(handle, |obj| obj.end);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the number of pages in this range.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "heuristics")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_range_page_count(this: AlefHandle) -> u32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::PageRange>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::PageRange>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        obj.page_count()
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

#[cfg(feature = "heuristics")]
/// Create a `MultidocInput` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_multidoc_input_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_multidoc_input_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::MultidocInput>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Serialize a `MultidocInput` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_multidoc_input_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::MultidocInput, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "heuristics")]
/// Free a `MultidocInput` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_multidoc_input_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::MultidocInput>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `page_count` field from a `MultidocInput`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_multidoc_input_page_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::MultidocInput, _>(handle, |obj| obj.page_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `pages` field from a `MultidocInput`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_multidoc_input_pages(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::MultidocInput, _>(handle, |obj| match serde_json::to_string(&obj.pages) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Create a `PageSignals` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_page_signals_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_signals_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PageSignals>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Serialize a `PageSignals` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_signals_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PageSignals, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "heuristics")]
/// Free a `PageSignals` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_signals_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PageSignals>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `page_number` field from a `PageSignals`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_signals_page_number(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageSignals, _>(handle, |obj| obj.page_number);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `text_excerpt` field from a `PageSignals`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_signals_text_excerpt(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PageSignals, _>(handle, |obj| match CString::new(obj.text_excerpt.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `starts_with_letterhead_like` field from a `PageSignals`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_signals_starts_with_letterhead_like(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageSignals, _>(handle, |obj| obj.starts_with_letterhead_like as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `has_page_number_one_marker` field from a `PageSignals`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_signals_has_page_number_one_marker(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageSignals, _>(handle, |obj| obj.has_page_number_one_marker as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `has_signature_block` field from a `PageSignals`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_signals_has_signature_block(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PageSignals, _>(handle, |obj| obj.has_signature_block as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `layout_text_density` field from a `PageSignals`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_signals_layout_text_density(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::PageSignals, _>(handle, |obj| obj.layout_text_density);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Derive signals from raw page text.
///
/// Callers that already have structured per-page data (e.g. from a PDF extractor)
/// can set individual fields directly.  This constructor is for callers that only
/// have the plain-text content of a page (e.g. from `PageContent`).
/// \param page_number â 1-indexed page number.
/// \param text â Full extracted text for the page.
/// \param layout_text_density â Pre-computed text density in `[0.0, 1.0]`. Pass `0.0` when unknown
/// (disables density-shift detection for this page). # Heuristics All signal derivations are
/// *conservative starting points*. Each is documented inline. They err on the side of fewer false
/// positives; tune thresholds via `MultidocThresholds` rather than by changing these heuristics.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "heuristics")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_signals_from_page_text(
    page_number: u32,
    text: *const std::ffi::c_char,
    layout_text_density: f32,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let page_number_rs = page_number;

        if text.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'text'");
            return 0;
        }
        // SAFETY: null check above guarantees text is a valid pointer; string is valid UTF-8 from caller.
        let text_rs = match unsafe { CStr::from_ptr(text) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'text'");
                return 0;
            }
        };

        let layout_text_density_rs = layout_text_density;

        let result = xberg::PageSignals::from_page_text(page_number_rs, &text_rs, layout_text_density_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

#[cfg(feature = "heuristics")]
/// Create a `DocumentBoundary` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_document_boundary_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_boundary_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DocumentBoundary>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Serialize a `DocumentBoundary` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_boundary_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DocumentBoundary, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "heuristics")]
/// Free a `DocumentBoundary` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_boundary_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DocumentBoundary>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `start_page` field from a `DocumentBoundary`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_boundary_start_page(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentBoundary, _>(handle, |obj| obj.start_page);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `end_page` field from a `DocumentBoundary`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_boundary_end_page(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentBoundary, _>(handle, |obj| obj.end_page);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `confidence` field from a `DocumentBoundary`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_boundary_confidence(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::DocumentBoundary, _>(handle, |obj| obj.confidence);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `reason` field from a `DocumentBoundary`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_boundary_reason_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_boundary_reason(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DocumentBoundary, _>(handle, |obj| match insert_handle(obj.reason.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Create a `MultidocThresholds` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_multidoc_thresholds_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_multidoc_thresholds_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::MultidocThresholds>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Serialize a `MultidocThresholds` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_multidoc_thresholds_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::MultidocThresholds, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "heuristics")]
/// Free a `MultidocThresholds` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_multidoc_thresholds_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::MultidocThresholds>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `density_shift_threshold` field from a `MultidocThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_multidoc_thresholds_density_shift_threshold(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::MultidocThresholds, _>(handle, |obj| obj.density_shift_threshold);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "heuristics")]
/// Get the `bigram_overlap_min` field from a `MultidocThresholds`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_multidoc_thresholds_bigram_overlap_min(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::MultidocThresholds, _>(handle, |obj| obj.bigram_overlap_min);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Free a `MetaSchema` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_meta_schema_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::MetaSchema>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "presets")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_meta_schema_compile(meta_schema_json: *const std::ffi::c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        let meta_schema_json_rs = if meta_schema_json.is_null() {
            String::new()
        } else {
            // SAFETY: null check above guarantees meta_schema_json is a valid pointer.
            unsafe { std::ffi::CStr::from_ptr(meta_schema_json) }
                .to_string_lossy()
                .into_owned()
        };
        let result = xberg::MetaSchema::compile(&meta_schema_json_rs);
        match result {
            Ok(value) => match insert_handle(value) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(1, &e.to_string());
                0
            }
        }
    })
}

/// Validate `raw` against the meta-schema and deserialize into a `Preset`,
/// stamping the fingerprint over the canonical file bytes.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "presets")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_meta_schema_parse_preset(
    this: AlefHandle,
    path: *const std::ffi::c_char,
    raw: *const u8,
    raw_len: usize,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::MetaSchema>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::MetaSchema>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if path.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'path'");
            return 0;
        }
        // SAFETY: null check above guarantees path is a valid pointer; string is valid UTF-8 from caller.
        let path_rs = match unsafe { CStr::from_ptr(path) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'path'");
                return 0;
            }
        };

        if raw.is_null() && raw_len > 0 {
            set_last_error(1, "Null pointer passed for parameter 'raw'");
            return 0;
        }
        // SAFETY: when raw is null, raw_len is 0 (checked above), so we use an empty slice; otherwise data is valid for len elements.
        let raw_rs = if raw.is_null() {
            Vec::new()
        } else {
            unsafe { std::slice::from_raw_parts(raw, raw_len) }.to_vec()
        };

        let result = obj.parse_preset(&path_rs, &raw_rs);
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

#[cfg(feature = "presets")]
/// Free a `Registry` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_registry_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::Registry>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "presets")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_registry_load_embedded() -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        let result = xberg::Registry::load_embedded();
        match result {
            Ok(value) => match insert_handle(value) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(1, &e.to_string());
                0
            }
        }
    })
}

/// Look up a preset by its identifier.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "presets")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_registry_get(this: AlefHandle, id: *const std::ffi::c_char) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::Registry>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::Registry>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if id.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'id'");
            return 0;
        }
        // SAFETY: null check above guarantees id is a valid pointer; string is valid UTF-8 from caller.
        let id_rs = match unsafe { CStr::from_ptr(id) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'id'");
                return 0;
            }
        };

        let result = obj.get(&id_rs);
        let result = result.cloned();
        match result {
            Some(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Materialize a `PresetSummary` list for the public registry endpoint.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "presets")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_registry_summaries(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::Registry>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<xberg::Registry>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.summaries();
        match serde_json::to_string(&result) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Number of presets currently loaded.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "presets")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_registry_len(this: AlefHandle) -> usize {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::Registry>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::Registry>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        obj.len()
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Whether the registry contains zero presets.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "presets")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_registry_is_empty(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::Registry>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::Registry>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.is_empty();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Read raw sample bytes for `<preset_id>` from
/// `library/<id>/samples/<name>`. Returns `None` when the file is absent.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "presets")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_registry_sample_bytes(
    this: AlefHandle,
    preset_id: *const std::ffi::c_char,
    name: *const std::ffi::c_char,
) -> *mut u8 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::Registry>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<xberg::Registry>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if preset_id.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'preset_id'");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees preset_id is a valid pointer; string is valid UTF-8 from caller.
        let preset_id_rs = match unsafe { CStr::from_ptr(preset_id) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'preset_id'");
                return std::ptr::null_mut();
            }
        };

        if name.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'name'");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'name'");
                return std::ptr::null_mut();
            }
        };

        let result = obj.sample_bytes(&preset_id_rs, &name_rs);
        match result {
            Some(val) => val.as_ptr() as *mut u8,
            None => std::ptr::null_mut(),
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Load additional preset files from a runtime directory and insert them
/// into this registry.
///
/// Reads every `*.json` file directly under `dir` (non-recursive),
/// validates each against the meta-schema, and inserts it. Files that fail
/// validation are rejected â the error is returned immediately and the
/// registry is left in a partially-updated state. Existing entries with the
/// same id are overwritten.
///
/// Returns the number of presets successfully loaded from `dir`.
///
/// # Use case
///
/// This is the injection point for downstream catalogs that add curated
/// presets on top of the single embedded OSS preset.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "presets")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_registry_extend_from_dir(this: AlefHandle, dir: *const std::ffi::c_char) -> usize {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::Registry>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::Registry>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &mut *obj_ptr };

        if dir.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'dir'");
            return 0;
        }
        // SAFETY: null check above guarantees dir is a valid pointer; string is valid UTF-8 from caller.
        let dir_rs = match unsafe { CStr::from_ptr(dir) }.to_str() {
            Ok(s) => std::path::PathBuf::from(s),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'dir'");
                return 0;
            }
        };

        let result = obj.extend_from_dir(dir_rs.as_path());
        match result {
            Ok(val) => val,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

#[cfg(feature = "presets")]
/// Create a `ResolvedPreset` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_resolved_preset_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_resolved_preset_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ResolvedPreset>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Serialize a `ResolvedPreset` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_resolved_preset_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ResolvedPreset, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "presets")]
/// Free a `ResolvedPreset` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_resolved_preset_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ResolvedPreset>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `id` field from a `ResolvedPreset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_resolved_preset_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ResolvedPreset, _>(handle, |obj| match CString::new(obj.id.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `version` field from a `ResolvedPreset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_resolved_preset_version(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ResolvedPreset, _>(handle, |obj| match CString::new(obj.version.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `fingerprint` field from a `ResolvedPreset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_resolved_preset_fingerprint(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ResolvedPreset, _>(handle, |obj| match CString::new(obj.fingerprint.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `schema_name` field from a `ResolvedPreset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_resolved_preset_schema_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ResolvedPreset, _>(handle, |obj| match CString::new(obj.schema_name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `schema` field from a `ResolvedPreset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_resolved_preset_schema(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ResolvedPreset, _>(handle, |obj| match serde_json::to_string(&obj.schema) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `system_prompt` field from a `ResolvedPreset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_resolved_preset_system_prompt(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::ResolvedPreset, _>(handle, |obj| match CString::new(obj.system_prompt.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `merge_mode` field from a `ResolvedPreset`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_merge_mode_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_resolved_preset_merge_mode(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ResolvedPreset, _>(handle, |obj| match insert_handle(obj.merge_mode) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `preferred_call_mode` field from a `ResolvedPreset`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_call_mode_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_resolved_preset_preferred_call_mode(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::ResolvedPreset, _>(handle, |obj| match insert_handle(obj.preferred_call_mode) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `emit_citations` field from a `ResolvedPreset`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_resolved_preset_emit_citations(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::ResolvedPreset, _>(handle, |obj| obj.emit_citations as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Create a `PresetSample` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_preset_sample_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_sample_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PresetSample>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Serialize a `PresetSample` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_sample_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PresetSample, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "presets")]
/// Free a `PresetSample` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_sample_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PresetSample>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `input_path` field from a `PresetSample`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_sample_input_path(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PresetSample, _>(handle, |obj| match CString::new(obj.input_path.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `output_path` field from a `PresetSample`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_sample_output_path(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PresetSample, _>(handle, |obj| match CString::new(obj.output_path.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Create a `Preset` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_preset_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::Preset>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Serialize a `Preset` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::Preset, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "presets")]
/// Free a `Preset` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::Preset>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `id` field from a `Preset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Preset, _>(handle, |obj| match CString::new(obj.id.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `version` field from a `Preset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_version(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Preset, _>(handle, |obj| match CString::new(obj.version.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `schema_name` field from a `Preset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_schema_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Preset, _>(handle, |obj| match CString::new(obj.schema_name.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `description` field from a `Preset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_description(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Preset, _>(handle, |obj| match CString::new(obj.description.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `category` field from a `Preset`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_preset_category_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_category(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Preset, _>(handle, |obj| match insert_handle(obj.category) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `tags` field from a `Preset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_tags(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Preset, _>(handle, |obj| match serde_json::to_string(&obj.tags) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `schema` field from a `Preset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_schema(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Preset, _>(handle, |obj| match serde_json::to_string(&obj.schema) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `system_prompt` field from a `Preset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_system_prompt(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Preset, _>(handle, |obj| match CString::new(obj.system_prompt.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `context_template` field from a `Preset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_context_template(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Preset, _>(handle, |obj| match &obj.context_template {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `merge_mode` field from a `Preset`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_merge_mode_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_merge_mode(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Preset, _>(handle, |obj| match insert_handle(obj.merge_mode) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `preferred_call_mode` field from a `Preset`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_call_mode_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_preferred_call_mode(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Preset, _>(handle, |obj| match insert_handle(obj.preferred_call_mode) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `emit_citations` field from a `Preset`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_emit_citations(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Preset, _>(handle, |obj| obj.emit_citations as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `sample` field from a `Preset`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_preset_sample_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_sample(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::Preset, _>(handle, |obj| match &obj.sample {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `fingerprint` field from a `Preset`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_fingerprint(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::Preset, _>(handle, |obj| match CString::new(obj.fingerprint.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Create a `PresetSummary` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_preset_summary_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_summary_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PresetSummary>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Serialize a `PresetSummary` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_summary_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PresetSummary, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "presets")]
/// Free a `PresetSummary` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_summary_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PresetSummary>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `id` field from a `PresetSummary`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_summary_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PresetSummary, _>(handle, |obj| match CString::new(obj.id.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `version` field from a `PresetSummary`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_summary_version(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PresetSummary, _>(handle, |obj| match CString::new(obj.version.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `schema_name` field from a `PresetSummary`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_summary_schema_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PresetSummary, _>(handle, |obj| match CString::new(obj.schema_name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `description` field from a `PresetSummary`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_summary_description(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PresetSummary, _>(handle, |obj| match CString::new(obj.description.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `category` field from a `PresetSummary`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_preset_category_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_summary_category(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PresetSummary, _>(handle, |obj| match insert_handle(obj.category) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `tags` field from a `PresetSummary`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_summary_tags(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PresetSummary, _>(handle, |obj| match serde_json::to_string(&obj.tags) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `preferred_call_mode` field from a `PresetSummary`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_call_mode_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_summary_preferred_call_mode(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<xberg::PresetSummary, _>(handle, |obj| match insert_handle(obj.preferred_call_mode) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `emit_citations` field from a `PresetSummary`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_summary_emit_citations(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PresetSummary, _>(handle, |obj| obj.emit_citations as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "presets")]
/// Get the `fingerprint` field from a `PresetSummary`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_summary_fingerprint(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PresetSummary, _>(handle, |obj| match CString::new(obj.fingerprint.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `DoctorCheck` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_doctor_check_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_doctor_check_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DoctorCheck>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DoctorCheck` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_doctor_check_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DoctorCheck, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DoctorCheck` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_doctor_check_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DoctorCheck>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `name` field from a `DoctorCheck`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_doctor_check_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DoctorCheck, _>(handle, |obj| match CString::new(obj.name.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `status` field from a `DoctorCheck`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_probe_status_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_doctor_check_status(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DoctorCheck, _>(handle, |obj| match insert_handle(obj.status) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `message` field from a `DoctorCheck`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_doctor_check_message(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DoctorCheck, _>(handle, |obj| match CString::new(obj.message.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// A `ProbeStatus.Pass` verdict: the checked backend or setting will
/// work as configured on this host.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_doctor_check_pass(
    name: *const std::ffi::c_char,
    message: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'name'");
            return 0;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'name'");
                return 0;
            }
        };

        if message.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'message'");
            return 0;
        }
        // SAFETY: null check above guarantees message is a valid pointer; string is valid UTF-8 from caller.
        let message_rs = match unsafe { CStr::from_ptr(message) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'message'");
                return 0;
            }
        };

        let result = xberg::DoctorCheck::pass(name_rs, message_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// A `ProbeStatus.Warn` verdict: the check ran and found something
/// actionable, but nothing is broken. Never fails the report.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_doctor_check_warn(
    name: *const std::ffi::c_char,
    message: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'name'");
            return 0;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'name'");
                return 0;
            }
        };

        if message.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'message'");
            return 0;
        }
        // SAFETY: null check above guarantees message is a valid pointer; string is valid UTF-8 from caller.
        let message_rs = match unsafe { CStr::from_ptr(message) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'message'");
                return 0;
            }
        };

        let result = xberg::DoctorCheck::warn(name_rs, message_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// A `ProbeStatus.Fail` verdict: the configured setup will not work (or
/// will silently degrade) on this host.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_doctor_check_fail(
    name: *const std::ffi::c_char,
    message: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'name'");
            return 0;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'name'");
                return 0;
            }
        };

        if message.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'message'");
            return 0;
        }
        // SAFETY: null check above guarantees message is a valid pointer; string is valid UTF-8 from caller.
        let message_rs = match unsafe { CStr::from_ptr(message) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'message'");
                return 0;
            }
        };

        let result = xberg::DoctorCheck::fail(name_rs, message_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// A `ProbeStatus.Skip` verdict: the check cannot run locally; first
/// real use decides, possibly after a download.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_doctor_check_skip(
    name: *const std::ffi::c_char,
    message: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'name'");
            return 0;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'name'");
                return 0;
            }
        };

        if message.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'message'");
            return 0;
        }
        // SAFETY: null check above guarantees message is a valid pointer; string is valid UTF-8 from caller.
        let message_rs = match unsafe { CStr::from_ptr(message) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'message'");
                return 0;
            }
        };

        let result = xberg::DoctorCheck::skip(name_rs, message_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Create a `DoctorReport` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_doctor_report_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_doctor_report_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DoctorReport>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DoctorReport` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_doctor_report_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DoctorReport, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DoctorReport` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_doctor_report_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DoctorReport>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `checks` field from a `DoctorReport`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_doctor_report_checks(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::DoctorReport, _>(handle, |obj| match serde_json::to_string(&obj.checks) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Whether no check failed. Warnings and skips do not count as failures,
/// so the report stays usable as a scripting/CI gate.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_doctor_report_is_ok(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<xberg::DoctorReport>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<xberg::DoctorReport>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.is_ok();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

#[cfg(feature = "paddle-ocr-types")]
/// Create a `PaddleOcrConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_paddle_ocr_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::PaddleOcrConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Serialize a `PaddleOcrConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::PaddleOcrConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Free a `PaddleOcrConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::PaddleOcrConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `language` field from a `PaddleOcrConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PaddleOcrConfig, _>(handle, |obj| match CString::new(obj.language.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `cache_dir` field from a `PaddleOcrConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_cache_dir(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::PaddleOcrConfig, _>(handle, |obj| match &obj.cache_dir {
            Some(val) => match CString::new(val.to_string_lossy().to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI path value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `use_angle_cls` field from a `PaddleOcrConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_use_angle_cls(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PaddleOcrConfig, _>(handle, |obj| obj.use_angle_cls as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `enable_table_detection` field from a `PaddleOcrConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_enable_table_detection(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PaddleOcrConfig, _>(handle, |obj| obj.enable_table_detection as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `det_db_thresh` field from a `PaddleOcrConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_det_db_thresh(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::PaddleOcrConfig, _>(handle, |obj| obj.det_db_thresh);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `det_db_box_thresh` field from a `PaddleOcrConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_det_db_box_thresh(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::PaddleOcrConfig, _>(handle, |obj| obj.det_db_box_thresh);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `det_db_unclip_ratio` field from a `PaddleOcrConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_det_db_unclip_ratio(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::PaddleOcrConfig, _>(handle, |obj| obj.det_db_unclip_ratio);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `det_limit_side_len` field from a `PaddleOcrConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_det_limit_side_len(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PaddleOcrConfig, _>(handle, |obj| obj.det_limit_side_len);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `rec_batch_num` field from a `PaddleOcrConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_rec_batch_num(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PaddleOcrConfig, _>(handle, |obj| obj.rec_batch_num);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `padding` field from a `PaddleOcrConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_padding(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PaddleOcrConfig, _>(handle, |obj| obj.padding);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `drop_score` field from a `PaddleOcrConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_drop_score(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::PaddleOcrConfig, _>(handle, |obj| obj.drop_score);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `model_tier` field from a `PaddleOcrConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_model_tier(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PaddleOcrConfig, _>(handle, |obj| match CString::new(obj.model_tier.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `model_version` field from a `PaddleOcrConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_model_version(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::PaddleOcrConfig, _>(handle, |obj| match CString::new(obj.model_version.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `inference_backend` field from a `PaddleOcrConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_paddle_inference_backend_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_inference_backend(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::PaddleOcrConfig, _>(handle, |obj| match &obj.inference_backend {
            Some(val) => match insert_handle(*val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Sets a custom Hugging Face Hub cache root for model files.
/// \param path Path to cache directory
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use xberg::PaddleOcrConfig;
/// use std::path::PathBuf;
///
/// let config = PaddleOcrConfig::new("en")
///     .with_cache_dir(PathBuf::from("/tmp/paddle-cache"));
/// \endcode
#[cfg(feature = "paddle-ocr-types")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_with_cache_dir(
    this: AlefHandle,
    path: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();

        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<xberg::PaddleOcrConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if path.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'path'");
            return 0;
        }
        // SAFETY: null check above guarantees path is a valid pointer; string is valid UTF-8 from caller.
        let path_rs = match unsafe { CStr::from_ptr(path) }.to_str() {
            Ok(s) => std::path::PathBuf::from(s),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'path'");
                return 0;
            }
        };

        let result = obj.with_cache_dir(path_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Enables or disables table structure detection.
/// \param enable Whether to enable table detection
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use xberg::PaddleOcrConfig;
///
/// let config = PaddleOcrConfig::new("en")
///     .with_table_detection(true);
/// \endcode
#[cfg(feature = "paddle-ocr-types")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_with_table_detection(this: AlefHandle, enable: i32) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();

        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<xberg::PaddleOcrConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let enable_rs = enable != 0;

        let result = obj.with_table_detection(enable_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Enables or disables angle classification for rotated text.
/// \param enable Whether to enable angle classification
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "paddle-ocr-types")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_with_angle_cls(this: AlefHandle, enable: i32) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();

        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<xberg::PaddleOcrConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let enable_rs = enable != 0;

        let result = obj.with_angle_cls(enable_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Sets the database threshold for text detection.
/// \param threshold Detection threshold (0.0-1.0)
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "paddle-ocr-types")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_with_det_db_thresh(this: AlefHandle, threshold: f32) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();

        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<xberg::PaddleOcrConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let threshold_rs = threshold;

        let result = obj.with_det_db_thresh(threshold_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Sets the box threshold for text bounding box refinement.
/// \param threshold Box threshold (0.0-1.0)
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "paddle-ocr-types")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_with_det_db_box_thresh(
    this: AlefHandle,
    threshold: f32,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();

        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<xberg::PaddleOcrConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let threshold_rs = threshold;

        let result = obj.with_det_db_box_thresh(threshold_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Sets the unclip ratio for expanding text bounding boxes.
/// \param ratio Unclip ratio (typically 1.5-2.0)
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "paddle-ocr-types")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_with_det_db_unclip_ratio(this: AlefHandle, ratio: f32) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();

        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<xberg::PaddleOcrConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let ratio_rs = ratio;

        let result = obj.with_det_db_unclip_ratio(ratio_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Sets the maximum side length for detection images.
/// \param length Maximum side length in pixels
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "paddle-ocr-types")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_with_det_limit_side_len(this: AlefHandle, length: u32) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();

        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<xberg::PaddleOcrConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let length_rs = length;

        let result = obj.with_det_limit_side_len(length_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Sets the batch size for recognition inference.
/// \param batch_size Number of text regions to process simultaneously
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "paddle-ocr-types")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_with_rec_batch_num(this: AlefHandle, batch_size: u32) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();

        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<xberg::PaddleOcrConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let batch_size_rs = batch_size;

        let result = obj.with_rec_batch_num(batch_size_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Sets the minimum recognition confidence threshold.
/// \param score Minimum confidence (0.0-1.0), text below this is dropped
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "paddle-ocr-types")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_with_drop_score(this: AlefHandle, score: f32) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();

        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<xberg::PaddleOcrConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let score_rs = score;

        let result = obj.with_drop_score(score_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Sets padding in pixels added around images before detection.
/// \param padding Padding in pixels (0-100)
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "paddle-ocr-types")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_with_padding(this: AlefHandle, padding: u32) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();

        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<xberg::PaddleOcrConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let padding_rs = padding;

        let result = obj.with_padding(padding_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Sets the model tier controlling detection/recognition model size.
/// \param tier `"mobile"` (default, lightweight, faster) or `"server"` (high accuracy, GPU/complex
/// documents)
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "paddle-ocr-types")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_with_model_tier(
    this: AlefHandle,
    tier: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();

        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<xberg::PaddleOcrConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if tier.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'tier'");
            return 0;
        }
        // SAFETY: null check above guarantees tier is a valid pointer; string is valid UTF-8 from caller.
        let tier_rs = match unsafe { CStr::from_ptr(tier) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'tier'");
                return 0;
            }
        };

        let result = obj.with_model_tier(tier_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Sets the model generation.
/// \param version `"pp-ocrv6"` (default) or `"pp-ocrv5"`. Under `"pp-ocrv6"`, `model_tier` selects
/// among `"medium"`/`"small"`/`"tiny"`.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "paddle-ocr-types")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_ocr_config_with_model_version(
    this: AlefHandle,
    version: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();

        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<xberg::PaddleOcrConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if version.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'version'");
            return 0;
        }
        // SAFETY: null check above guarantees version is a valid pointer; string is valid UTF-8 from caller.
        let version_rs = match unsafe { CStr::from_ptr(version) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'version'");
                return 0;
            }
        };

        let result = obj.with_model_version(version_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

#[cfg(feature = "paddle-ocr-types")]
/// Create a `ModelPaths` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_model_paths_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_model_paths_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::ModelPaths>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Serialize a `ModelPaths` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_model_paths_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::ModelPaths, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Free a `ModelPaths` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_model_paths_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::ModelPaths>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `det_model` field from a `ModelPaths`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_model_paths_det_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ModelPaths, _>(handle, |obj| {
            match CString::new(obj.det_model.to_string_lossy().to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI path value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `cls_model` field from a `ModelPaths`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_model_paths_cls_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ModelPaths, _>(handle, |obj| {
            match CString::new(obj.cls_model.to_string_lossy().to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI path value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `rec_model` field from a `ModelPaths`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_model_paths_rec_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ModelPaths, _>(handle, |obj| {
            match CString::new(obj.rec_model.to_string_lossy().to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI path value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "paddle-ocr-types")]
/// Get the `dict_file` field from a `ModelPaths`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_model_paths_dict_file(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::ModelPaths, _>(handle, |obj| {
            match CString::new(obj.dict_file.to_string_lossy().to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI path value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "auto-rotate-types")]
/// Create a `OrientationResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_orientation_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_orientation_result_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::OrientationResult>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "auto-rotate-types")]
/// Serialize a `OrientationResult` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_orientation_result_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::OrientationResult, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "auto-rotate-types")]
/// Free a `OrientationResult` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_orientation_result_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::OrientationResult>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "auto-rotate-types")]
/// Get the `degrees` field from a `OrientationResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_orientation_result_degrees(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::OrientationResult, _>(handle, |obj| obj.degrees);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "auto-rotate-types")]
/// Get the `confidence` field from a `OrientationResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_orientation_result_confidence(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::OrientationResult, _>(handle, |obj| obj.confidence);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Create a `BBox` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_b_box_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_b_box_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::BBox>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Serialize a `BBox` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_b_box_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::BBox, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "layout-types")]
/// Free a `BBox` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_b_box_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::BBox>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `x1` field from a `BBox`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_b_box_x1(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::BBox, _>(handle, |obj| obj.x1);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `y1` field from a `BBox`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_b_box_y1(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::BBox, _>(handle, |obj| obj.y1);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `x2` field from a `BBox`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_b_box_x2(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::BBox, _>(handle, |obj| obj.x2);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `y2` field from a `BBox`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_b_box_y2(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::BBox, _>(handle, |obj| obj.y2);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Create a `LayoutDetection` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_layout_detection_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_detection_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::LayoutDetection>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Serialize a `LayoutDetection` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_detection_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::LayoutDetection, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "layout-types")]
/// Free a `LayoutDetection` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_detection_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::LayoutDetection>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `class_name` field from a `LayoutDetection`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_layout_class_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_detection_class_name(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LayoutDetection, _>(handle, |obj| match insert_handle(obj.class_name) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `confidence` field from a `LayoutDetection`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_detection_confidence(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::LayoutDetection, _>(handle, |obj| obj.confidence);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `bbox` field from a `LayoutDetection`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_b_box_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_detection_bbox(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::LayoutDetection, _>(handle, |obj| match insert_handle(obj.bbox) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Create a `RecognizedTable` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_recognized_table_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_recognized_table_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::RecognizedTable>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Serialize a `RecognizedTable` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_recognized_table_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::RecognizedTable, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "layout-types")]
/// Free a `RecognizedTable` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_recognized_table_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::RecognizedTable>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `detection_bbox` field from a `RecognizedTable`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_b_box_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_recognized_table_detection_bbox(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::RecognizedTable, _>(handle, |obj| match insert_handle(obj.detection_bbox) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `cells` field from a `RecognizedTable`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_recognized_table_cells(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::RecognizedTable, _>(handle, |obj| match serde_json::to_string(&obj.cells) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `markdown` field from a `RecognizedTable`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_recognized_table_markdown(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::RecognizedTable, _>(handle, |obj| match CString::new(obj.markdown.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Create a `DetectionResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_detection_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_detection_result_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::DetectionResult>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Serialize a `DetectionResult` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_detection_result_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::DetectionResult, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "layout-types")]
/// Free a `DetectionResult` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_detection_result_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::DetectionResult>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `page_width` field from a `DetectionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_detection_result_page_width(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DetectionResult, _>(handle, |obj| obj.page_width);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `page_height` field from a `DetectionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_detection_result_page_height(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::DetectionResult, _>(handle, |obj| obj.page_height);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "layout-types")]
/// Get the `detections` field from a `DetectionResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_detection_result_detections(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::DetectionResult, _>(handle, |obj| match serde_json::to_string(&obj.detections) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Create a `EmbeddedFile` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_embedded_file_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_file_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::pdf::embedded_files::EmbeddedFile>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Serialize a `EmbeddedFile` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_file_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::pdf::embedded_files::EmbeddedFile, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "pdf")]
/// Free a `EmbeddedFile` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_file_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::pdf::embedded_files::EmbeddedFile>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `name` field from a `EmbeddedFile`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_file_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::pdf::embedded_files::EmbeddedFile, _>(handle, |obj| {
            match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `data` field from a `EmbeddedFile`.
/// The returned byte pointer is borrowed from `ptr` and must not be freed.
/// It remains valid until `ptr` is destroyed or the field is mutated.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_file_data(handle: AlefHandle, out_len: *mut usize) -> *mut u8 {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::pdf::embedded_files::EmbeddedFile, _>(handle, |obj| {
            let data = &obj.data;
            if !out_len.is_null() {
                // SAFETY: null check above guarantees out_len is a valid pointer.
                unsafe {
                    *out_len = data.len();
                }
            }
            data.as_ptr() as *mut u8
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `compressed_size` field from a `EmbeddedFile`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_file_compressed_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::pdf::embedded_files::EmbeddedFile, _>(handle, |obj| obj.compressed_size);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `mime_type` field from a `EmbeddedFile`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedded_file_mime_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::pdf::embedded_files::EmbeddedFile, _>(handle, |obj| match &obj.mime_type {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Create a `PdfMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_pdf_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<xberg::pdf::metadata::PdfMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Serialize a `PdfMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<xberg::pdf::metadata::PdfMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "pdf")]
/// Free a `PdfMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::pdf::metadata::PdfMetadata>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `pdf_version` field from a `PdfMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_metadata_pdf_version(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::pdf::metadata::PdfMetadata, _>(handle, |obj| match &obj.pdf_version {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `producer` field from a `PdfMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_metadata_producer(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::pdf::metadata::PdfMetadata, _>(handle, |obj| match &obj.producer {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `is_encrypted` field from a `PdfMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_metadata_is_encrypted(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::pdf::metadata::PdfMetadata, _>(handle, |obj| match &obj.is_encrypted {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `width` field from a `PdfMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_metadata_width(handle: AlefHandle) -> i64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::pdf::metadata::PdfMetadata, _>(handle, |obj| match &obj.width {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `height` field from a `PdfMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_metadata_height(handle: AlefHandle) -> i64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::pdf::metadata::PdfMetadata, _>(handle, |obj| match &obj.height {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `page_count` field from a `PdfMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_metadata_page_count(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::pdf::metadata::PdfMetadata, _>(handle, |obj| match &obj.page_count {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `scanned_confidence` field from a `PdfMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_metadata_scanned_confidence(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<xberg::pdf::metadata::PdfMetadata, _>(handle, |obj| match &obj.scanned_confidence {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `scanned_pages` field from a `PdfMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_metadata_scanned_pages(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::pdf::metadata::PdfMetadata, _>(handle, |obj| match &obj.scanned_pages {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `layout_gated_pages` field from a `PdfMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_metadata_layout_gated_pages(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<xberg::pdf::metadata::PdfMetadata, _>(handle, |obj| match &obj.layout_gated_pages {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "pdf")]
/// Get the `layout_gate_reasons` field from a `PdfMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_metadata_layout_gate_reasons(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<xberg::pdf::metadata::PdfMetadata, _>(handle, |obj| match &obj.layout_gate_reasons {
                Some(val) => match serde_json::to_string(&val) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                },
                None => std::ptr::null_mut(),
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "classification")]
/// Free a `ChunkClassificationEnrichmentConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_classification_enrichment_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<xberg::enrich::ChunkClassificationEnrichmentConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "classification")]
/// Get the `config` field from a `ChunkClassificationEnrichmentConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_chunk_classification_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_classification_enrichment_config_config(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<xberg::enrich::ChunkClassificationEnrichmentConfig, _>(handle, |obj| {
            match insert_handle(obj.config.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ProxyConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_proxy_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_proxy_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<crawlberg::ProxyConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ProxyConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_proxy_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<crawlberg::ProxyConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ProxyConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_proxy_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<crawlberg::ProxyConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `url` field from a `ProxyConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_proxy_config_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::ProxyConfig, _>(handle, |obj| match CString::new(obj.url.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `username` field from a `ProxyConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_proxy_config_username(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::ProxyConfig, _>(handle, |obj| match &obj.username {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `password` field from a `ProxyConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_proxy_config_password(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::ProxyConfig, _>(handle, |obj| match &obj.password {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ContentConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_content_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<crawlberg::ContentConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ContentConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<crawlberg::ContentConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ContentConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<crawlberg::ContentConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `output_format` field from a `ContentConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_config_output_format(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::ContentConfig, _>(handle, |obj| {
            match CString::new(obj.output_format.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `preprocessing_preset` field from a `ContentConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_config_preprocessing_preset(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::ContentConfig, _>(handle, |obj| {
            match CString::new(obj.preprocessing_preset.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `remove_navigation` field from a `ContentConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_config_remove_navigation(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::ContentConfig, _>(handle, |obj| obj.remove_navigation as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `remove_forms` field from a `ContentConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_config_remove_forms(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::ContentConfig, _>(handle, |obj| obj.remove_forms as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `strip_tags` field from a `ContentConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_config_strip_tags(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<crawlberg::ContentConfig, _>(handle, |obj| match serde_json::to_string(&obj.strip_tags) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `preserve_tags` field from a `ContentConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_config_preserve_tags(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<crawlberg::ContentConfig, _>(handle, |obj| match serde_json::to_string(&obj.preserve_tags) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `exclude_selectors` field from a `ContentConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_config_exclude_selectors(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::ContentConfig, _>(handle, |obj| {
            match serde_json::to_string(&obj.exclude_selectors) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `skip_images` field from a `ContentConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_config_skip_images(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::ContentConfig, _>(handle, |obj| obj.skip_images as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_depth` field from a `ContentConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_config_max_depth(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::ContentConfig, _>(handle, |obj| match &obj.max_depth {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `wrap` field from a `ContentConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_config_wrap(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::ContentConfig, _>(handle, |obj| obj.wrap as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `wrap_width` field from a `ContentConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_config_wrap_width(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::ContentConfig, _>(handle, |obj| obj.wrap_width);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `include_document_structure` field from a `ContentConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_config_include_document_structure(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::ContentConfig, _>(handle, |obj| obj.include_document_structure as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `BrowserConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_browser_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<crawlberg::BrowserConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `BrowserConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<crawlberg::BrowserConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `BrowserConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<crawlberg::BrowserConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `mode` field from a `BrowserConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_browser_mode_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_config_mode(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::BrowserConfig, _>(handle, |obj| match insert_handle(obj.mode.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `backend` field from a `BrowserConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_browser_backend_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_config_backend(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<crawlberg::BrowserConfig, _>(handle, |obj| match insert_handle(obj.backend.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `endpoint` field from a `BrowserConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_config_endpoint(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::BrowserConfig, _>(handle, |obj| match &obj.endpoint {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `timeout` field from a `BrowserConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_config_timeout(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::BrowserConfig, _>(handle, |obj| obj.timeout.as_millis() as u64);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `wait` field from a `BrowserConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_browser_wait_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_config_wait(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::BrowserConfig, _>(handle, |obj| match insert_handle(obj.wait.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `wait_selector` field from a `BrowserConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_config_wait_selector(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::BrowserConfig, _>(handle, |obj| match &obj.wait_selector {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `extra_wait` field from a `BrowserConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_config_extra_wait(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::BrowserConfig, _>(handle, |obj| match &obj.extra_wait {
            Some(val) => val.as_millis() as u64,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `proxy` field from a `BrowserConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_proxy_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_config_proxy(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::BrowserConfig, _>(handle, |obj| match &obj.proxy {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `block_url_patterns` field from a `BrowserConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_config_block_url_patterns(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::BrowserConfig, _>(handle, |obj| {
            match serde_json::to_string(&obj.block_url_patterns) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `eval_script` field from a `BrowserConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_config_eval_script(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::BrowserConfig, _>(handle, |obj| match &obj.eval_script {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `robots_user_agent` field from a `BrowserConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_config_robots_user_agent(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::BrowserConfig, _>(handle, |obj| match &obj.robots_user_agent {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `capture_network_events` field from a `BrowserConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_config_capture_network_events(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::BrowserConfig, _>(handle, |obj| obj.capture_network_events as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `session_affinity` field from a `BrowserConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_config_session_affinity(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::BrowserConfig, _>(handle, |obj| obj.session_affinity as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `CrawlConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_crawl_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<crawlberg::CrawlConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CrawlConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<crawlberg::CrawlConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CrawlConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<crawlberg::CrawlConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `max_depth` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_max_depth(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.max_depth {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_pages` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_max_pages(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.max_pages {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_links_per_page` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_max_links_per_page(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.max_links_per_page {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_concurrent` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_max_concurrent(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.max_concurrent {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `respect_robots_txt` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_respect_robots_txt(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| obj.respect_robots_txt as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `soft_http_errors` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_soft_http_errors(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| obj.soft_http_errors as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `user_agent` field from a `CrawlConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_user_agent(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.user_agent {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `stay_on_domain` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_stay_on_domain(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| obj.stay_on_domain as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `allow_subdomains` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_allow_subdomains(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| obj.allow_subdomains as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `include_paths` field from a `CrawlConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_include_paths(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match serde_json::to_string(&obj.include_paths) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `exclude_paths` field from a `CrawlConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_exclude_paths(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match serde_json::to_string(&obj.exclude_paths) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `custom_headers` field from a `CrawlConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_custom_headers(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match serde_json::to_string(&obj.custom_headers) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `request_timeout` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_request_timeout(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| obj.request_timeout.as_millis() as u64);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `rate_limit_ms` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_rate_limit_ms(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.rate_limit_ms {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_redirects` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_max_redirects(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| obj.max_redirects);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `retry_count` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_retry_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| obj.retry_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `retry_codes` field from a `CrawlConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_retry_codes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match serde_json::to_string(&obj.retry_codes) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `cookies_enabled` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_cookies_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| obj.cookies_enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `auth` field from a `CrawlConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_auth_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_auth(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.auth {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_body_size` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_max_body_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.max_body_size {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `remove_tags` field from a `CrawlConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_remove_tags(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match serde_json::to_string(&obj.remove_tags) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `content` field from a `CrawlConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_content_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_content(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match insert_handle(obj.content.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `map_limit` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_map_limit(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.map_limit {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `map_search` field from a `CrawlConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_map_search(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.map_search {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `download_assets` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_download_assets(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| obj.download_assets as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `asset_types` field from a `CrawlConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_asset_types(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match serde_json::to_string(&obj.asset_types) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `max_asset_size` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_max_asset_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.max_asset_size {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `browser` field from a `CrawlConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_browser_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_browser(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match insert_handle(obj.browser.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `proxy` field from a `CrawlConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_proxy_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_proxy(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.proxy {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `user_agents` field from a `CrawlConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_user_agents(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match serde_json::to_string(&obj.user_agents) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `capture_screenshot` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_capture_screenshot(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| obj.capture_screenshot as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `follow_document_urls` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_follow_document_urls(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| obj.follow_document_urls as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `document_url_depth` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_document_url_depth(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.document_url_depth {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `download_documents` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_download_documents(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| obj.download_documents as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `document_max_size` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_document_max_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.document_max_size {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `document_mime_types` field from a `CrawlConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_document_mime_types(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| {
            match serde_json::to_string(&obj.document_mime_types) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `document_output_dir` field from a `CrawlConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_document_output_dir(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.document_output_dir {
            Some(val) => match CString::new(val.to_string_lossy().to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI path value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `document_content_encoding` field from a `CrawlConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_document_content_encoding_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_document_content_encoding(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.document_content_encoding {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `warc_output` field from a `CrawlConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_warc_output(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.warc_output {
            Some(val) => match CString::new(val.to_string_lossy().to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI path value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `browser_profile` field from a `CrawlConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_browser_profile(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.browser_profile {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `save_browser_profile` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_save_browser_profile(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| obj.save_browser_profile as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `ssrf` field from a `CrawlConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_ssrf_policy_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_ssrf(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match insert_handle(obj.ssrf.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `ssrf_deny_private_explicit` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_crawl_config_ssrf_deny_private_explicit(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::CrawlConfig, _>(handle, |obj| match &obj.ssrf_deny_private_explicit {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `SitemapUrl` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_sitemap_url_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sitemap_url_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<crawlberg::SitemapUrl>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `SitemapUrl` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sitemap_url_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<crawlberg::SitemapUrl, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `SitemapUrl` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sitemap_url_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<crawlberg::SitemapUrl>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `url` field from a `SitemapUrl`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sitemap_url_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::SitemapUrl, _>(handle, |obj| match CString::new(obj.url.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `lastmod` field from a `SitemapUrl`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sitemap_url_lastmod(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::SitemapUrl, _>(handle, |obj| match &obj.lastmod {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `changefreq` field from a `SitemapUrl`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sitemap_url_changefreq(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::SitemapUrl, _>(handle, |obj| match &obj.changefreq {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `priority` field from a `SitemapUrl`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sitemap_url_priority(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::SitemapUrl, _>(handle, |obj| match &obj.priority {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `MapResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_map_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_map_result_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<crawlberg::MapResult>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `MapResult` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_map_result_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<crawlberg::MapResult, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `MapResult` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_map_result_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<crawlberg::MapResult>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `urls` field from a `MapResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_map_result_urls(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<crawlberg::MapResult, _>(handle, |obj| match serde_json::to_string(&obj.urls) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `SsrfPolicy` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_ssrf_policy_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ssrf_policy_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<crawlberg::SsrfPolicy>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `SsrfPolicy` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ssrf_policy_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<crawlberg::SsrfPolicy, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `SsrfPolicy` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ssrf_policy_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<crawlberg::SsrfPolicy>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `deny_private` field from a `SsrfPolicy`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ssrf_policy_deny_private(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::SsrfPolicy, _>(handle, |obj| obj.deny_private as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `allowlist` field from a `SsrfPolicy`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ssrf_policy_allowlist(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<crawlberg::SsrfPolicy, _>(handle, |obj| match serde_json::to_string(&obj.allowlist) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `max_redirects` field from a `SsrfPolicy`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ssrf_policy_max_redirects(handle: AlefHandle) -> u8 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<crawlberg::SsrfPolicy, _>(handle, |obj| obj.max_redirects);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ConversionOptions` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_conversion_options_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<html_to_markdown_rs::ConversionOptions>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ConversionOptions` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ConversionOptions` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<html_to_markdown_rs::ConversionOptions>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `heading_style` field from a `ConversionOptions`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_heading_style_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_heading_style(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
            match insert_handle(obj.heading_style) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `list_indent_type` field from a `ConversionOptions`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_list_indent_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_list_indent_type(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
            match insert_handle(obj.list_indent_type) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `list_indent_width` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_list_indent_width(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.list_indent_width);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `bullets` field from a `ConversionOptions`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_bullets(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
            match CString::new(obj.bullets.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `strong_em_symbol` field from a `ConversionOptions`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_strong_em_symbol(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
            match CString::new(obj.strong_em_symbol.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `escape_asterisks` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_escape_asterisks(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.escape_asterisks as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `escape_underscores` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_escape_underscores(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.escape_underscores as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `escape_misc` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_escape_misc(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.escape_misc as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `escape_ascii` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_escape_ascii(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.escape_ascii as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `code_language` field from a `ConversionOptions`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_code_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
            match CString::new(obj.code_language.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `autolinks` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_autolinks(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.autolinks as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `default_title` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_default_title(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.default_title as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `br_in_tables` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_br_in_tables(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.br_in_tables as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `compact_tables` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_compact_tables(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.compact_tables as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `highlight_style` field from a `ConversionOptions`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_highlight_style_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_highlight_style(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
            match insert_handle(obj.highlight_style) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `extract_metadata` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_extract_metadata(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.extract_metadata as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `whitespace_mode` field from a `ConversionOptions`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_whitespace_mode_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_whitespace_mode(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
            match insert_handle(obj.whitespace_mode) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `strip_newlines` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_strip_newlines(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.strip_newlines as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `wrap` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_wrap(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.wrap as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `wrap_width` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_wrap_width(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.wrap_width);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `convert_as_inline` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_convert_as_inline(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.convert_as_inline as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `sub_symbol` field from a `ConversionOptions`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_sub_symbol(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
            match CString::new(obj.sub_symbol.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `sup_symbol` field from a `ConversionOptions`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_sup_symbol(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
            match CString::new(obj.sup_symbol.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `newline_style` field from a `ConversionOptions`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_newline_style_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_newline_style(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
            match insert_handle(obj.newline_style) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `code_block_style` field from a `ConversionOptions`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_code_block_style_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_code_block_style(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
            match insert_handle(obj.code_block_style) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `keep_inline_images_in` field from a `ConversionOptions`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_keep_inline_images_in(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
                match serde_json::to_string(&obj.keep_inline_images_in) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `preprocessing` field from a `ConversionOptions`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_preprocessing_options_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_preprocessing(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
            match insert_handle(obj.preprocessing.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `encoding` field from a `ConversionOptions`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_encoding(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
            match CString::new(obj.encoding.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `debug` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_debug(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.debug as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `strip_tags` field from a `ConversionOptions`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_strip_tags(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
                match serde_json::to_string(&obj.strip_tags) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `preserve_tags` field from a `ConversionOptions`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_preserve_tags(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
                match serde_json::to_string(&obj.preserve_tags) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `skip_images` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_skip_images(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.skip_images as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `url_escape_style` field from a `ConversionOptions`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_url_escape_style_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_url_escape_style(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
            match insert_handle(obj.url_escape_style) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `link_style` field from a `ConversionOptions`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_link_style_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_link_style(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
            match insert_handle(obj.link_style) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_image_size` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_max_image_size(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.max_image_size);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `capture_svg` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_capture_svg(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.capture_svg as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `infer_dimensions` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_infer_dimensions(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| obj.infer_dimensions as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_depth` field from a `ConversionOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_max_depth(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| match &obj.max_depth {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `exclude_selectors` field from a `ConversionOptions`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `xberg_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_conversion_options_exclude_selectors(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<html_to_markdown_rs::ConversionOptions, _>(handle, |obj| {
                match serde_json::to_string(&obj.exclude_selectors) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `PreprocessingOptions` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `xberg_preprocessing_options_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preprocessing_options_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<html_to_markdown_rs::PreprocessingOptions>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PreprocessingOptions` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preprocessing_options_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<html_to_markdown_rs::PreprocessingOptions, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PreprocessingOptions` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preprocessing_options_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<html_to_markdown_rs::PreprocessingOptions>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `enabled` field from a `PreprocessingOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preprocessing_options_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::PreprocessingOptions, _>(handle, |obj| obj.enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `preset` field from a `PreprocessingOptions`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `xberg_preprocessing_preset_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preprocessing_options_preset(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::PreprocessingOptions, _>(handle, |obj| {
            match insert_handle(obj.preset) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `remove_navigation` field from a `PreprocessingOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preprocessing_options_remove_navigation(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<html_to_markdown_rs::PreprocessingOptions, _>(handle, |obj| obj.remove_navigation as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `remove_forms` field from a `PreprocessingOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preprocessing_options_remove_forms(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<html_to_markdown_rs::PreprocessingOptions, _>(handle, |obj| obj.remove_forms as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Convert an integer to a `ExecutionProviderType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_execution_provider_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Auto
            1 => 1, // Cpu
            2 => 2, // CoreMl
            3 => 3, // Cuda
            4 => 4, // TensorRt
            _ => {
                set_last_error(1, "Invalid ExecutionProviderType variant");
                -1
            }
        }
    })
}

/// Convert a `ExecutionProviderType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_execution_provider_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "auto" => 0,
            "cpu" => 1,
            "coreml" => 2,
            "cuda" => 3,
            "tensorrt" => 4,
            _ => {
                set_last_error(1, "Unknown ExecutionProviderType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ImageOutputFormat` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_output_format_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Native
            1 => 1, // Png
            2 => 2, // Jpeg
            3 => 3, // Webp
            4 => 4, // Heif
            5 => 5, // Svg
            _ => {
                set_last_error(1, "Invalid ImageOutputFormat variant");
                -1
            }
        }
    })
}

/// Convert a `ImageOutputFormat` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_output_format_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "native" => 0,
            "png" => 1,
            "jpeg" => 2,
            "webp" => 3,
            "heif" => 4,
            "svg" => 5,
            _ => {
                set_last_error(1, "Unknown ImageOutputFormat variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ExtractInputKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract_input_kind_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Bytes
            1 => 1, // Uri
            _ => {
                set_last_error(1, "Invalid ExtractInputKind variant");
                -1
            }
        }
    })
}

/// Convert a `ExtractInputKind` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract_input_kind_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "bytes" => 0,
            "uri" => 1,
            _ => {
                set_last_error(1, "Unknown ExtractInputKind variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `UrlExtractionMode` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_extraction_mode_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Auto
            1 => 1, // Document
            2 => 2, // Crawl
            _ => {
                set_last_error(1, "Invalid UrlExtractionMode variant");
                -1
            }
        }
    })
}

/// Convert a `UrlExtractionMode` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_extraction_mode_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "auto" => 0,
            "document" => 1,
            "crawl" => 2,
            _ => {
                set_last_error(1, "Unknown UrlExtractionMode variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `BreadcrumbTarget` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_breadcrumb_target_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Content
            1 => 1, // Metadata
            _ => {
                set_last_error(1, "Invalid BreadcrumbTarget variant");
                -1
            }
        }
    })
}

/// Convert a `BreadcrumbTarget` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_breadcrumb_target_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "content" => 0,
            "metadata" => 1,
            _ => {
                set_last_error(1, "Unknown BreadcrumbTarget variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `OutputFormat` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_output_format_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Plain
            1 => 1, // Markdown
            2 => 2, // Djot
            3 => 3, // Html
            4 => 4, // Json
            5 => 5, // Structured
            6 => 6, // DocTags
            7 => 7, // Custom
            _ => {
                set_last_error(1, "Invalid OutputFormat variant");
                -1
            }
        }
    })
}

/// Convert a `OutputFormat` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_output_format_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "plain" => 0,
            "markdown" => 1,
            "djot" => 2,
            "html" => 3,
            "json" => 4,
            "structured" => 5,
            "doctags" => 6,
            "custom" => 7,
            _ => {
                set_last_error(1, "Unknown OutputFormat variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `JupyterCellRendering` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_jupyter_cell_rendering_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Source
            1 => 1, // Outputs
            2 => 2, // Both
            _ => {
                set_last_error(1, "Invalid JupyterCellRendering variant");
                -1
            }
        }
    })
}

/// Convert a `JupyterCellRendering` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_jupyter_cell_rendering_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "source" => 0,
            "outputs" => 1,
            "both" => 2,
            _ => {
                set_last_error(1, "Unknown JupyterCellRendering variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `HtmlTheme` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_theme_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Default
            1 => 1, // GitHub
            2 => 2, // Dark
            3 => 3, // Light
            4 => 4, // Unstyled
            _ => {
                set_last_error(1, "Invalid HtmlTheme variant");
                -1
            }
        }
    })
}

/// Convert a `HtmlTheme` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_theme_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "default" => 0,
            "github" => 1,
            "dark" => 2,
            "light" => 3,
            "unstyled" => 4,
            _ => {
                set_last_error(1, "Unknown HtmlTheme variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `LateInteractionModelType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_model_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Preset
            1 => 1, // Custom
            2 => 2, // Plugin
            _ => {
                set_last_error(1, "Invalid LateInteractionModelType variant");
                -1
            }
        }
    })
}

/// Convert a `LateInteractionModelType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_model_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "preset" => 0,
            "custom" => 1,
            "plugin" => 2,
            _ => {
                set_last_error(1, "Unknown LateInteractionModelType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `TableModel` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_model_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Tatr
            1 => 1, // SlanetWired
            2 => 2, // SlanetWireless
            3 => 3, // SlanetPlus
            4 => 4, // SlanetAuto
            5 => 5, // Disabled
            _ => {
                set_last_error(1, "Invalid TableModel variant");
                -1
            }
        }
    })
}

/// Convert a `TableModel` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_model_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "tatr" => 0,
            "slanet_wired" => 1,
            "slanet_wireless" => 2,
            "slanet_plus" => 3,
            "slanet_auto" => 4,
            "disabled" => 5,
            _ => {
                set_last_error(1, "Unknown TableModel variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `TableOverlapPreference` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_overlap_preference_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Content
            1 => 1, // Native
            2 => 2, // Layout
            _ => {
                set_last_error(1, "Invalid TableOverlapPreference variant");
                -1
            }
        }
    })
}

/// Convert a `TableOverlapPreference` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_overlap_preference_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "content" => 0,
            "native" => 1,
            "layout" => 2,
            _ => {
                set_last_error(1, "Unknown TableOverlapPreference variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `LayoutStrategy` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_strategy_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Always
            1 => 1, // Auto
            _ => {
                set_last_error(1, "Invalid LayoutStrategy variant");
                -1
            }
        }
    })
}

/// Convert a `LayoutStrategy` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_strategy_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "always" => 0,
            "auto" => 1,
            _ => {
                set_last_error(1, "Unknown LayoutStrategy variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `CredentialProviderConfig` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_credential_provider_config_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // AzureAd
            1 => 1, // VertexOauth2
            2 => 2, // VertexAdc
            3 => 3, // BedrockWebIdentity
            _ => {
                set_last_error(1, "Invalid CredentialProviderConfig variant");
                -1
            }
        }
    })
}

/// Convert a `CredentialProviderConfig` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_credential_provider_config_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "azure_ad" => 0,
            "vertex_oauth2" => 1,
            "vertex_adc" => 2,
            "bedrock_web_identity" => 3,
            _ => {
                set_last_error(1, "Unknown CredentialProviderConfig variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `CallMode` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_call_mode_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // TextOnly
            1 => 1, // VisionOnly
            2 => 2, // TextPlusVision
            _ => {
                set_last_error(1, "Invalid CallMode variant");
                -1
            }
        }
    })
}

/// Convert a `CallMode` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_call_mode_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "text_only" => 0,
            "vision_only" => 1,
            "text_plus_vision" => 2,
            _ => {
                set_last_error(1, "Unknown CallMode variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `MergeMode` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_merge_mode_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // ObjectMerge
            1 => 1, // ArrayConcat
            2 => 2, // ObjectFirst
            _ => {
                set_last_error(1, "Invalid MergeMode variant");
                -1
            }
        }
    })
}

/// Convert a `MergeMode` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_merge_mode_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "object_merge" => 0,
            "array_concat" => 1,
            "object_first" => 2,
            _ => {
                set_last_error(1, "Unknown MergeMode variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `NerBackendKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ner_backend_kind_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Onnx
            1 => 1, // Llm
            _ => {
                set_last_error(1, "Invalid NerBackendKind variant");
                -1
            }
        }
    })
}

/// Convert a `NerBackendKind` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ner_backend_kind_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "onnx" => 0,
            "llm" => 1,
            _ => {
                set_last_error(1, "Unknown NerBackendKind variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `VlmFallbackPolicy` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_vlm_fallback_policy_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Disabled
            1 => 1, // OnLowQuality
            2 => 2, // Always
            _ => {
                set_last_error(1, "Invalid VlmFallbackPolicy variant");
                -1
            }
        }
    })
}

/// Convert a `VlmFallbackPolicy` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_vlm_fallback_policy_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "disabled" => 0,
            "on_low_quality" => 1,
            "always" => 2,
            _ => {
                set_last_error(1, "Unknown VlmFallbackPolicy variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `OcrStrategy` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_strategy_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Auto
            1 => 1, // ScannedPages
            _ => {
                set_last_error(1, "Invalid OcrStrategy variant");
                -1
            }
        }
    })
}

/// Convert a `OcrStrategy` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_strategy_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "auto" => 0,
            "scanned_pages" => 1,
            _ => {
                set_last_error(1, "Unknown OcrStrategy variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `TableChunkingMode` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_chunking_mode_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Split
            1 => 1, // RepeatHeader
            _ => {
                set_last_error(1, "Invalid TableChunkingMode variant");
                -1
            }
        }
    })
}

/// Convert a `TableChunkingMode` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_chunking_mode_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "split" => 0,
            "repeat_header" => 1,
            _ => {
                set_last_error(1, "Unknown TableChunkingMode variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ChunkerType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunker_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Text
            1 => 1, // Markdown
            2 => 2, // Yaml
            3 => 3, // Semantic
            _ => {
                set_last_error(1, "Invalid ChunkerType variant");
                -1
            }
        }
    })
}

/// Convert a `ChunkerType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunker_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "text" => 0,
            "markdown" => 1,
            "yaml" => 2,
            "semantic" => 3,
            _ => {
                set_last_error(1, "Unknown ChunkerType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ChunkSizing` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_sizing_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Characters
            1 => 1, // Tokenizer
            _ => {
                set_last_error(1, "Invalid ChunkSizing variant");
                -1
            }
        }
    })
}

/// Convert a `ChunkSizing` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_sizing_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "characters" => 0,
            "tokenizer" => 1,
            _ => {
                set_last_error(1, "Unknown ChunkSizing variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `EmbeddingModelType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedding_model_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Preset
            1 => 1, // Custom
            2 => 2, // Llm
            3 => 3, // Plugin
            _ => {
                set_last_error(1, "Invalid EmbeddingModelType variant");
                -1
            }
        }
    })
}

/// Convert a `EmbeddingModelType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedding_model_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "preset" => 0,
            "custom" => 1,
            "llm" => 2,
            "plugin" => 3,
            _ => {
                set_last_error(1, "Unknown EmbeddingModelType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `RerankerHead` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranker_head_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // CrossEncoder
            1 => 1, // Qwen3Generative
            _ => {
                set_last_error(1, "Invalid RerankerHead variant");
                -1
            }
        }
    })
}

/// Convert a `RerankerHead` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranker_head_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "cross_encoder" => 0,
            "qwen3_generative" => 1,
            _ => {
                set_last_error(1, "Unknown RerankerHead variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `RerankerModelType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranker_model_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Preset
            1 => 1, // Custom
            2 => 2, // Llm
            3 => 3, // Plugin
            _ => {
                set_last_error(1, "Invalid RerankerModelType variant");
                -1
            }
        }
    })
}

/// Convert a `RerankerModelType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranker_model_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "preset" => 0,
            "custom" => 1,
            "llm" => 2,
            "plugin" => 3,
            _ => {
                set_last_error(1, "Unknown RerankerModelType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `SparseEmbeddingModelType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_model_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Preset
            1 => 1, // Custom
            2 => 2, // Plugin
            _ => {
                set_last_error(1, "Invalid SparseEmbeddingModelType variant");
                -1
            }
        }
    })
}

/// Convert a `SparseEmbeddingModelType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_model_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "preset" => 0,
            "custom" => 1,
            "plugin" => 2,
            _ => {
                set_last_error(1, "Unknown SparseEmbeddingModelType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `WhisperModel` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_whisper_model_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Tiny
            1 => 1, // Base
            2 => 2, // Small
            3 => 3, // Medium
            4 => 4, // LargeV3
            _ => {
                set_last_error(1, "Invalid WhisperModel variant");
                -1
            }
        }
    })
}

/// Convert a `WhisperModel` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_whisper_model_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "tiny" => 0,
            "base" => 1,
            "small" => 2,
            "medium" => 3,
            "large_v3" => 4,
            _ => {
                set_last_error(1, "Unknown WhisperModel variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `CodeContentMode` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_content_mode_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Chunks
            1 => 1, // Raw
            2 => 2, // Structure
            _ => {
                set_last_error(1, "Invalid CodeContentMode variant");
                -1
            }
        }
    })
}

/// Convert a `CodeContentMode` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_content_mode_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "chunks" => 0,
            "raw" => 1,
            "structure" => 2,
            _ => {
                set_last_error(1, "Unknown CodeContentMode variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ListType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Bullet
            1 => 1, // Numbered
            2 => 2, // Lettered
            3 => 3, // Indented
            _ => {
                set_last_error(1, "Invalid ListType variant");
                -1
            }
        }
    })
}

/// Convert a `ListType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Bullet" => 0,
            "Numbered" => 1,
            "Lettered" => 2,
            "Indented" => 3,
            _ => {
                set_last_error(1, "Unknown ListType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `OcrBackendType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_backend_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Tesseract
            1 => 1, // PaddleOCR
            2 => 2, // Candle
            3 => 3, // Custom
            _ => {
                set_last_error(1, "Invalid OcrBackendType variant");
                -1
            }
        }
    })
}

/// Convert a `OcrBackendType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_backend_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Tesseract" => 0,
            "PaddleOCR" => 1,
            "Candle" => 2,
            "Custom" => 3,
            _ => {
                set_last_error(1, "Unknown OcrBackendType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ProcessingStage` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_processing_stage_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Early
            1 => 1, // Middle
            2 => 2, // Late
            _ => {
                set_last_error(1, "Invalid ProcessingStage variant");
                -1
            }
        }
    })
}

/// Convert a `ProcessingStage` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_processing_stage_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Early" => 0,
            "Middle" => 1,
            "Late" => 2,
            _ => {
                set_last_error(1, "Unknown ProcessingStage variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ReductionLevel` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reduction_level_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Off
            1 => 1, // Light
            2 => 2, // Moderate
            3 => 3, // Aggressive
            4 => 4, // Maximum
            _ => {
                set_last_error(1, "Invalid ReductionLevel variant");
                -1
            }
        }
    })
}

/// Convert a `ReductionLevel` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reduction_level_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Off" => 0,
            "Light" => 1,
            "Moderate" => 2,
            "Aggressive" => 3,
            "Maximum" => 4,
            _ => {
                set_last_error(1, "Unknown ReductionLevel variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `PdfAnnotationType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0,   // Text
            1 => 1,   // Highlight
            2 => 2,   // Link
            3 => 3,   // Stamp
            4 => 4,   // Underline
            5 => 5,   // StrikeOut
            6 => 6,   // Squiggly
            7 => 7,   // Ink
            8 => 8,   // Square
            9 => 9,   // Circle
            10 => 10, // Polygon
            11 => 11, // PolyLine
            12 => 12, // Line
            13 => 13, // Caret
            14 => 14, // FileAttachment
            15 => 15, // Sound
            16 => 16, // Movie
            17 => 17, // Other
            _ => {
                set_last_error(1, "Invalid PdfAnnotationType variant");
                -1
            }
        }
    })
}

/// Convert a `PdfAnnotationType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "text" => 0,
            "highlight" => 1,
            "link" => 2,
            "stamp" => 3,
            "underline" => 4,
            "strike_out" => 5,
            "squiggly" => 6,
            "ink" => 7,
            "square" => 8,
            "circle" => 9,
            "polygon" => 10,
            "poly_line" => 11,
            "line" => 12,
            "caret" => 13,
            "file_attachment" => 14,
            "sound" => 15,
            "movie" => 16,
            "other" => 17,
            _ => {
                set_last_error(1, "Unknown PdfAnnotationType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `BlockType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_block_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0,   // Paragraph
            1 => 1,   // Heading
            2 => 2,   // Blockquote
            3 => 3,   // CodeBlock
            4 => 4,   // ListItem
            5 => 5,   // OrderedList
            6 => 6,   // BulletList
            7 => 7,   // TaskList
            8 => 8,   // DefinitionList
            9 => 9,   // DefinitionTerm
            10 => 10, // DefinitionDescription
            11 => 11, // Div
            12 => 12, // Section
            13 => 13, // ThematicBreak
            14 => 14, // RawBlock
            15 => 15, // MathDisplay
            _ => {
                set_last_error(1, "Invalid BlockType variant");
                -1
            }
        }
    })
}

/// Convert a `BlockType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_block_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "paragraph" => 0,
            "heading" => 1,
            "blockquote" => 2,
            "code_block" => 3,
            "list_item" => 4,
            "ordered_list" => 5,
            "bullet_list" => 6,
            "task_list" => 7,
            "definition_list" => 8,
            "definition_term" => 9,
            "definition_description" => 10,
            "div" => 11,
            "section" => 12,
            "thematic_break" => 13,
            "raw_block" => 14,
            "math_display" => 15,
            _ => {
                set_last_error(1, "Unknown BlockType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `InlineType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_inline_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0,   // Text
            1 => 1,   // Strong
            2 => 2,   // Emphasis
            3 => 3,   // Highlight
            4 => 4,   // Subscript
            5 => 5,   // Superscript
            6 => 6,   // Insert
            7 => 7,   // Delete
            8 => 8,   // Code
            9 => 9,   // Link
            10 => 10, // Image
            11 => 11, // Span
            12 => 12, // Math
            13 => 13, // RawInline
            14 => 14, // FootnoteRef
            15 => 15, // Symbol
            _ => {
                set_last_error(1, "Invalid InlineType variant");
                -1
            }
        }
    })
}

/// Convert a `InlineType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_inline_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "text" => 0,
            "strong" => 1,
            "emphasis" => 2,
            "highlight" => 3,
            "subscript" => 4,
            "superscript" => 5,
            "insert" => 6,
            "delete" => 7,
            "code" => 8,
            "link" => 9,
            "image" => 10,
            "span" => 11,
            "math" => 12,
            "raw_inline" => 13,
            "footnote_ref" => 14,
            "symbol" => 15,
            _ => {
                set_last_error(1, "Unknown InlineType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `RelationshipKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_relationship_kind_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // FootnoteReference
            1 => 1, // CitationReference
            2 => 2, // InternalLink
            3 => 3, // Caption
            4 => 4, // Label
            5 => 5, // TocEntry
            6 => 6, // CrossReference
            _ => {
                set_last_error(1, "Invalid RelationshipKind variant");
                -1
            }
        }
    })
}

/// Convert a `RelationshipKind` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_relationship_kind_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "footnote_reference" => 0,
            "citation_reference" => 1,
            "internal_link" => 2,
            "caption" => 3,
            "label" => 4,
            "toc_entry" => 5,
            "cross_reference" => 6,
            _ => {
                set_last_error(1, "Unknown RelationshipKind variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ContentLayer` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_layer_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Body
            1 => 1, // Header
            2 => 2, // Footer
            3 => 3, // Footnote
            _ => {
                set_last_error(1, "Invalid ContentLayer variant");
                -1
            }
        }
    })
}

/// Convert a `ContentLayer` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_layer_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "body" => 0,
            "header" => 1,
            "footer" => 2,
            "footnote" => 3,
            _ => {
                set_last_error(1, "Unknown ContentLayer variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `NodeContent` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_node_content_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0,   // Title
            1 => 1,   // Heading
            2 => 2,   // Paragraph
            3 => 3,   // List
            4 => 4,   // ListItem
            5 => 5,   // Table
            6 => 6,   // Image
            7 => 7,   // Code
            8 => 8,   // Quote
            9 => 9,   // Formula
            10 => 10, // Footnote
            11 => 11, // Comment
            12 => 12, // Group
            13 => 13, // PageBreak
            14 => 14, // Slide
            15 => 15, // DefinitionList
            16 => 16, // DefinitionItem
            17 => 17, // Citation
            18 => 18, // Admonition
            19 => 19, // RawBlock
            20 => 20, // MetadataBlock
            _ => {
                set_last_error(1, "Invalid NodeContent variant");
                -1
            }
        }
    })
}

/// Convert a `NodeContent` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_node_content_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "title" => 0,
            "heading" => 1,
            "paragraph" => 2,
            "list" => 3,
            "list_item" => 4,
            "table" => 5,
            "image" => 6,
            "code" => 7,
            "quote" => 8,
            "formula" => 9,
            "footnote" => 10,
            "comment" => 11,
            "group" => 12,
            "page_break" => 13,
            "slide" => 14,
            "definition_list" => 15,
            "definition_item" => 16,
            "citation" => 17,
            "admonition" => 18,
            "raw_block" => 19,
            "metadata_block" => 20,
            _ => {
                set_last_error(1, "Unknown NodeContent variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `AnnotationKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_annotation_kind_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0,   // Bold
            1 => 1,   // Italic
            2 => 2,   // Underline
            3 => 3,   // Strikethrough
            4 => 4,   // Code
            5 => 5,   // Subscript
            6 => 6,   // Superscript
            7 => 7,   // Link
            8 => 8,   // Highlight
            9 => 9,   // Color
            10 => 10, // FontSize
            11 => 11, // Custom
            _ => {
                set_last_error(1, "Invalid AnnotationKind variant");
                -1
            }
        }
    })
}

/// Convert a `AnnotationKind` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_annotation_kind_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "bold" => 0,
            "italic" => 1,
            "underline" => 2,
            "strikethrough" => 3,
            "code" => 4,
            "subscript" => 5,
            "superscript" => 6,
            "link" => 7,
            "highlight" => 8,
            "color" => 9,
            "font_size" => 10,
            "custom" => 11,
            _ => {
                set_last_error(1, "Unknown AnnotationKind variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `EntityCategory` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_entity_category_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0,   // Person
            1 => 1,   // Organization
            2 => 2,   // Location
            3 => 3,   // Date
            4 => 4,   // Time
            5 => 5,   // Money
            6 => 6,   // Percent
            7 => 7,   // Email
            8 => 8,   // Phone
            9 => 9,   // Url
            10 => 10, // Custom
            _ => {
                set_last_error(1, "Invalid EntityCategory variant");
                -1
            }
        }
    })
}

/// Convert a `EntityCategory` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_entity_category_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "person" => 0,
            "organization" => 1,
            "location" => 2,
            "date" => 3,
            "time" => 4,
            "money" => 5,
            "percent" => 6,
            "email" => 7,
            "phone" => 8,
            "url" => 9,
            "custom" => 10,
            _ => {
                set_last_error(1, "Unknown EntityCategory variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ExtractionMethod` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_method_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Native
            1 => 1, // Ocr
            2 => 2, // Mixed
            _ => {
                set_last_error(1, "Invalid ExtractionMethod variant");
                -1
            }
        }
    })
}

/// Convert a `ExtractionMethod` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_method_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "native" => 0,
            "ocr" => 1,
            "mixed" => 2,
            _ => {
                set_last_error(1, "Unknown ExtractionMethod variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ChunkType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0,   // Heading
            1 => 1,   // PartyList
            2 => 2,   // Definitions
            3 => 3,   // OperativeClause
            4 => 4,   // SignatureBlock
            5 => 5,   // Schedule
            6 => 6,   // TableLike
            7 => 7,   // Formula
            8 => 8,   // CodeBlock
            9 => 9,   // Function
            10 => 10, // Class
            11 => 11, // Module
            12 => 12, // Image
            13 => 13, // OrgChart
            14 => 14, // Diagram
            15 => 15, // Unknown
            _ => {
                set_last_error(1, "Invalid ChunkType variant");
                -1
            }
        }
    })
}

/// Convert a `ChunkType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "heading" => 0,
            "party_list" => 1,
            "definitions" => 2,
            "operative_clause" => 3,
            "signature_block" => 4,
            "schedule" => 5,
            "table_like" => 6,
            "formula" => 7,
            "code_block" => 8,
            "function" => 9,
            "class" => 10,
            "module" => 11,
            "image" => 12,
            "org_chart" => 13,
            "diagram" => 14,
            "unknown" => 15,
            _ => {
                set_last_error(1, "Unknown ChunkType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ImageKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_kind_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0,   // Photograph
            1 => 1,   // Diagram
            2 => 2,   // Chart
            3 => 3,   // Drawing
            4 => 4,   // TextBlock
            5 => 5,   // Decoration
            6 => 6,   // Logo
            7 => 7,   // Icon
            8 => 8,   // TileFragment
            9 => 9,   // Mask
            10 => 10, // PageRaster
            11 => 11, // Unknown
            _ => {
                set_last_error(1, "Invalid ImageKind variant");
                -1
            }
        }
    })
}

/// Convert a `ImageKind` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_kind_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "photograph" => 0,
            "diagram" => 1,
            "chart" => 2,
            "drawing" => 3,
            "text_block" => 4,
            "decoration" => 5,
            "logo" => 6,
            "icon" => 7,
            "tile_fragment" => 8,
            "mask" => 9,
            "page_raster" => 10,
            "unknown" => 11,
            _ => {
                set_last_error(1, "Unknown ImageKind variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ResultFormat` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_result_format_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Unified
            1 => 1, // ElementBased
            _ => {
                set_last_error(1, "Invalid ResultFormat variant");
                -1
            }
        }
    })
}

/// Convert a `ResultFormat` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_result_format_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "unified" => 0,
            "element_based" => 1,
            _ => {
                set_last_error(1, "Unknown ResultFormat variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ElementType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0,   // Title
            1 => 1,   // NarrativeText
            2 => 2,   // Heading
            3 => 3,   // ListItem
            4 => 4,   // Table
            5 => 5,   // Image
            6 => 6,   // PageBreak
            7 => 7,   // CodeBlock
            8 => 8,   // BlockQuote
            9 => 9,   // Footer
            10 => 10, // Header
            _ => {
                set_last_error(1, "Invalid ElementType variant");
                -1
            }
        }
    })
}

/// Convert a `ElementType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "title" => 0,
            "narrative_text" => 1,
            "heading" => 2,
            "list_item" => 3,
            "table" => 4,
            "image" => 5,
            "page_break" => 6,
            "code_block" => 7,
            "block_quote" => 8,
            "footer" => 9,
            "header" => 10,
            _ => {
                set_last_error(1, "Unknown ElementType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `FormFieldType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_form_field_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Text
            1 => 1, // Checkbox
            2 => 2, // Radio
            3 => 3, // Choice
            4 => 4, // Signature
            5 => 5, // Button
            6 => 6, // Unknown
            _ => {
                set_last_error(1, "Invalid FormFieldType variant");
                -1
            }
        }
    })
}

/// Convert a `FormFieldType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_form_field_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "text" => 0,
            "checkbox" => 1,
            "radio" => 2,
            "choice" => 3,
            "signature" => 4,
            "button" => 5,
            "unknown" => 6,
            _ => {
                set_last_error(1, "Unknown FormFieldType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `FormatMetadata` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_format_metadata_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0,   // Pdf
            1 => 1,   // Docx
            2 => 2,   // Excel
            3 => 3,   // Email
            4 => 4,   // Pptx
            5 => 5,   // Archive
            6 => 6,   // Image
            7 => 7,   // Xml
            8 => 8,   // Text
            9 => 9,   // Html
            10 => 10, // Ocr
            11 => 11, // Csv
            12 => 12, // Bibtex
            13 => 13, // Citation
            14 => 14, // FictionBook
            15 => 15, // Dbf
            16 => 16, // Jats
            17 => 17, // Epub
            18 => 18, // Pst
            19 => 19, // Audio
            20 => 20, // Code
            _ => {
                set_last_error(1, "Invalid FormatMetadata variant");
                -1
            }
        }
    })
}

/// Convert a `FormatMetadata` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_format_metadata_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "pdf" => 0,
            "docx" => 1,
            "excel" => 2,
            "email" => 3,
            "pptx" => 4,
            "archive" => 5,
            "image" => 6,
            "xml" => 7,
            "text" => 8,
            "html" => 9,
            "ocr" => 10,
            "csv" => 11,
            "bibtex" => 12,
            "citation" => 13,
            "fiction_book" => 14,
            "dbf" => 15,
            "jats" => 16,
            "epub" => 17,
            "pst" => 18,
            "audio" => 19,
            "code" => 20,
            _ => {
                set_last_error(1, "Unknown FormatMetadata variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `CodeDataNodeKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_node_kind_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // KeyValue
            1 => 1, // Element
            2 => 2, // Sequence
            _ => {
                set_last_error(1, "Invalid CodeDataNodeKind variant");
                -1
            }
        }
    })
}

/// Convert a `CodeDataNodeKind` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_node_kind_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "key_value" => 0,
            "element" => 1,
            "sequence" => 2,
            _ => {
                set_last_error(1, "Unknown CodeDataNodeKind variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `TextDirection` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_direction_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // LeftToRight
            1 => 1, // RightToLeft
            2 => 2, // Auto
            _ => {
                set_last_error(1, "Invalid TextDirection variant");
                -1
            }
        }
    })
}

/// Convert a `TextDirection` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_direction_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "ltr" => 0,
            "rtl" => 1,
            "auto" => 2,
            _ => {
                set_last_error(1, "Unknown TextDirection variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `LinkType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_link_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Anchor
            1 => 1, // Internal
            2 => 2, // External
            3 => 3, // Email
            4 => 4, // Phone
            5 => 5, // Other
            _ => {
                set_last_error(1, "Invalid LinkType variant");
                -1
            }
        }
    })
}

/// Convert a `LinkType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_link_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "anchor" => 0,
            "internal" => 1,
            "external" => 2,
            "email" => 3,
            "phone" => 4,
            "other" => 5,
            _ => {
                set_last_error(1, "Unknown LinkType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ImageType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // DataUri
            1 => 1, // InlineSvg
            2 => 2, // External
            3 => 3, // Relative
            _ => {
                set_last_error(1, "Invalid ImageType variant");
                -1
            }
        }
    })
}

/// Convert a `ImageType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "data-uri" => 0,
            "inline-svg" => 1,
            "external" => 2,
            "relative" => 3,
            _ => {
                set_last_error(1, "Unknown ImageType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `StructuredDataType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // JsonLd
            1 => 1, // Microdata
            2 => 2, // RDFa
            _ => {
                set_last_error(1, "Invalid StructuredDataType variant");
                -1
            }
        }
    })
}

/// Convert a `StructuredDataType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "json-ld" => 0,
            "microdata" => 1,
            "rdfa" => 2,
            _ => {
                set_last_error(1, "Unknown StructuredDataType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `OcrBoundingGeometry` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_bounding_geometry_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Rectangle
            1 => 1, // Quadrilateral
            _ => {
                set_last_error(1, "Invalid OcrBoundingGeometry variant");
                -1
            }
        }
    })
}

/// Convert a `OcrBoundingGeometry` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_bounding_geometry_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "rectangle" => 0,
            "quadrilateral" => 1,
            _ => {
                set_last_error(1, "Unknown OcrBoundingGeometry variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `OcrElementLevel` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_level_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Word
            1 => 1, // Line
            2 => 2, // Block
            3 => 3, // Page
            _ => {
                set_last_error(1, "Invalid OcrElementLevel variant");
                -1
            }
        }
    })
}

/// Convert a `OcrElementLevel` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_level_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "word" => 0,
            "line" => 1,
            "block" => 2,
            "page" => 3,
            _ => {
                set_last_error(1, "Unknown OcrElementLevel variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `PageUnitType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_unit_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Page
            1 => 1, // Slide
            2 => 2, // Sheet
            _ => {
                set_last_error(1, "Invalid PageUnitType variant");
                -1
            }
        }
    })
}

/// Convert a `PageUnitType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_unit_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "page" => 0,
            "slide" => 1,
            "sheet" => 2,
            _ => {
                set_last_error(1, "Unknown PageUnitType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `RedactionStrategy` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_strategy_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Mask
            1 => 1, // Hash
            2 => 2, // TokenReplace
            3 => 3, // Drop
            _ => {
                set_last_error(1, "Invalid RedactionStrategy variant");
                -1
            }
        }
    })
}

/// Convert a `RedactionStrategy` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_strategy_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "mask" => 0,
            "hash" => 1,
            "token_replace" => 2,
            "drop" => 3,
            _ => {
                set_last_error(1, "Unknown RedactionStrategy variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `PiiCategory` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pii_category_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0,   // Email
            1 => 1,   // Phone
            2 => 2,   // Ssn
            3 => 3,   // CreditCard
            4 => 4,   // PostalCode
            5 => 5,   // IpAddress
            6 => 6,   // Iban
            7 => 7,   // SwiftBic
            8 => 8,   // DateOfBirth
            9 => 9,   // Person
            10 => 10, // Organization
            11 => 11, // Location
            12 => 12, // Custom
            _ => {
                set_last_error(1, "Invalid PiiCategory variant");
                -1
            }
        }
    })
}

/// Convert a `PiiCategory` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pii_category_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "email" => 0,
            "phone" => 1,
            "ssn" => 2,
            "credit_card" => 3,
            "postal_code" => 4,
            "ip_address" => 5,
            "iban" => 6,
            "swift_bic" => 7,
            "date_of_birth" => 8,
            "person" => 9,
            "organization" => 10,
            "location" => 11,
            "custom" => 12,
            _ => {
                set_last_error(1, "Unknown PiiCategory variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `DiffLine` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_diff_line_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Context
            1 => 1, // Added
            2 => 2, // Removed
            _ => {
                set_last_error(1, "Invalid DiffLine variant");
                -1
            }
        }
    })
}

/// Convert a `DiffLine` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_diff_line_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "context" => 0,
            "added" => 1,
            "removed" => 2,
            _ => {
                set_last_error(1, "Unknown DiffLine variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `RevisionKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_revision_kind_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Insertion
            1 => 1, // Deletion
            2 => 2, // FormatChange
            3 => 3, // Comment
            _ => {
                set_last_error(1, "Invalid RevisionKind variant");
                -1
            }
        }
    })
}

/// Convert a `RevisionKind` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_revision_kind_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "insertion" => 0,
            "deletion" => 1,
            "format_change" => 2,
            "comment" => 3,
            _ => {
                set_last_error(1, "Unknown RevisionKind variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `RevisionAnchor` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_revision_anchor_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Paragraph
            1 => 1, // TableCell
            2 => 2, // Page
            3 => 3, // Slide
            4 => 4, // Sheet
            _ => {
                set_last_error(1, "Invalid RevisionAnchor variant");
                -1
            }
        }
    })
}

/// Convert a `RevisionAnchor` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_revision_anchor_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "paragraph" => 0,
            "table_cell" => 1,
            "page" => 2,
            "slide" => 3,
            "sheet" => 4,
            _ => {
                set_last_error(1, "Unknown RevisionAnchor variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `SummaryStrategy` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_summary_strategy_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Extractive
            1 => 1, // Abstractive
            _ => {
                set_last_error(1, "Invalid SummaryStrategy variant");
                -1
            }
        }
    })
}

/// Convert a `SummaryStrategy` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_summary_strategy_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "extractive" => 0,
            "abstractive" => 1,
            _ => {
                set_last_error(1, "Unknown SummaryStrategy variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `UriKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_uri_kind_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Hyperlink
            1 => 1, // Image
            2 => 2, // Anchor
            3 => 3, // Citation
            4 => 4, // Reference
            5 => 5, // Email
            _ => {
                set_last_error(1, "Invalid UriKind variant");
                -1
            }
        }
    })
}

/// Convert a `UriKind` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_uri_kind_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "hyperlink" => 0,
            "image" => 1,
            "anchor" => 2,
            "citation" => 3,
            "reference" => 4,
            "email" => 5,
            _ => {
                set_last_error(1, "Unknown UriKind variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `RegionKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_region_kind_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Figure
            1 => 1, // DenseTable
            2 => 2, // ComplexLayout
            3 => 3, // Caption
            _ => {
                set_last_error(1, "Invalid RegionKind variant");
                -1
            }
        }
    })
}

/// Convert a `RegionKind` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_region_kind_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Figure" => 0,
            "DenseTable" => 1,
            "ComplexLayout" => 2,
            "Caption" => 3,
            _ => {
                set_last_error(1, "Unknown RegionKind variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `EmbeddingsEmbeddingBackend` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embeddings_embedding_backend_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Onnx
            1 => 1, // Static
            _ => {
                set_last_error(1, "Invalid EmbeddingsEmbeddingBackend variant");
                -1
            }
        }
    })
}

/// Convert a `EmbeddingsEmbeddingBackend` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embeddings_embedding_backend_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "onnx" => 0,
            "static" => 1,
            _ => {
                set_last_error(1, "Unknown EmbeddingsEmbeddingBackend variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `KeywordAlgorithm` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_algorithm_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Yake
            1 => 1, // Rake
            _ => {
                set_last_error(1, "Invalid KeywordAlgorithm variant");
                -1
            }
        }
    })
}

/// Convert a `KeywordAlgorithm` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_algorithm_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "yake" => 0,
            "rake" => 1,
            _ => {
                set_last_error(1, "Unknown KeywordAlgorithm variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `SchemaCompliance` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_schema_compliance_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // AllValid
            1 => 1, // PartialValid
            2 => 2, // AllInvalid
            _ => {
                set_last_error(1, "Invalid SchemaCompliance variant");
                -1
            }
        }
    })
}

/// Convert a `SchemaCompliance` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_schema_compliance_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "all_valid" => 0,
            "partial_valid" => 1,
            "all_invalid" => 2,
            _ => {
                set_last_error(1, "Unknown SchemaCompliance variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `NoChunkingReason` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_no_chunking_reason_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // SmallFile
            1 => 1, // FewPages
            2 => 2, // TextLayerDetected
            3 => 3, // FormatNotChunkable
            4 => 4, // ChunkingDisabled
            5 => 5, // FastTextExtraction
            _ => {
                set_last_error(1, "Invalid NoChunkingReason variant");
                -1
            }
        }
    })
}

/// Convert a `NoChunkingReason` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_no_chunking_reason_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "SmallFile" => 0,
            "FewPages" => 1,
            "TextLayerDetected" => 2,
            "FormatNotChunkable" => 3,
            "ChunkingDisabled" => 4,
            "FastTextExtraction" => 5,
            _ => {
                set_last_error(1, "Unknown NoChunkingReason variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ChunkingReason` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_reason_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // LargeFile
            1 => 1, // ManyPages
            2 => 2, // OcrRequired
            3 => 3, // LargeAndManyPages
            _ => {
                set_last_error(1, "Invalid ChunkingReason variant");
                -1
            }
        }
    })
}

/// Convert a `ChunkingReason` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunking_reason_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "LargeFile" => 0,
            "ManyPages" => 1,
            "OcrRequired" => 2,
            "LargeAndManyPages" => 3,
            _ => {
                set_last_error(1, "Unknown ChunkingReason variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `BoundaryReason` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_boundary_reason_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Start
            1 => 1, // PageOneMarker
            2 => 2, // LetterheadReset
            3 => 3, // DensityShift
            4 => 4, // End
            _ => {
                set_last_error(1, "Invalid BoundaryReason variant");
                -1
            }
        }
    })
}

/// Convert a `BoundaryReason` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_boundary_reason_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "start" => 0,
            "page_one_marker" => 1,
            "letterhead_reset" => 2,
            "density_shift" => 3,
            "end" => 4,
            _ => {
                set_last_error(1, "Unknown BoundaryReason variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `PresetCategory` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_category_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Finance
            1 => 1, // Identity
            2 => 2, // Legal
            3 => 3, // Logistics
            4 => 4, // Medical
            5 => 5, // Hr
            6 => 6, // Other
            _ => {
                set_last_error(1, "Invalid PresetCategory variant");
                -1
            }
        }
    })
}

/// Convert a `PresetCategory` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_category_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "finance" => 0,
            "identity" => 1,
            "legal" => 2,
            "logistics" => 3,
            "medical" => 4,
            "hr" => 5,
            "other" => 6,
            _ => {
                set_last_error(1, "Unknown PresetCategory variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `PSMMode` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_psm_mode_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0,   // OsdOnly
            1 => 1,   // AutoOsd
            2 => 2,   // AutoOnly
            3 => 3,   // Auto
            4 => 4,   // SingleColumn
            5 => 5,   // SingleBlockVertical
            6 => 6,   // SingleBlock
            7 => 7,   // SingleLine
            8 => 8,   // SingleWord
            9 => 9,   // CircleWord
            10 => 10, // SingleChar
            _ => {
                set_last_error(1, "Invalid PSMMode variant");
                -1
            }
        }
    })
}

/// Convert a `PSMMode` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_psm_mode_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "OsdOnly" => 0,
            "AutoOsd" => 1,
            "AutoOnly" => 2,
            "Auto" => 3,
            "SingleColumn" => 4,
            "SingleBlockVertical" => 5,
            "SingleBlock" => 6,
            "SingleLine" => 7,
            "SingleWord" => 8,
            "CircleWord" => 9,
            "SingleChar" => 10,
            _ => {
                set_last_error(1, "Unknown PSMMode variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ProbeStatus` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_probe_status_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Pass
            1 => 1, // Warn
            2 => 2, // Fail
            3 => 3, // Skip
            _ => {
                set_last_error(1, "Invalid ProbeStatus variant");
                -1
            }
        }
    })
}

/// Convert a `ProbeStatus` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_probe_status_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "pass" => 0,
            "warn" => 1,
            "fail" => 2,
            "skip" => 3,
            _ => {
                set_last_error(1, "Unknown ProbeStatus variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `PaddleInferenceBackend` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_inference_backend_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Ort
            1 => 1, // Tract
            _ => {
                set_last_error(1, "Invalid PaddleInferenceBackend variant");
                -1
            }
        }
    })
}

/// Convert a `PaddleInferenceBackend` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_inference_backend_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "ort" => 0,
            "tract" => 1,
            _ => {
                set_last_error(1, "Unknown PaddleInferenceBackend variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `PaddleLanguage` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_language_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0,   // English
            1 => 1,   // Chinese
            2 => 2,   // Japanese
            3 => 3,   // Korean
            4 => 4,   // German
            5 => 5,   // French
            6 => 6,   // Latin
            7 => 7,   // Cyrillic
            8 => 8,   // TraditionalChinese
            9 => 9,   // Thai
            10 => 10, // Greek
            11 => 11, // EastSlavic
            12 => 12, // Arabic
            13 => 13, // Devanagari
            14 => 14, // Tamil
            15 => 15, // Telugu
            _ => {
                set_last_error(1, "Invalid PaddleLanguage variant");
                -1
            }
        }
    })
}

/// Convert a `PaddleLanguage` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_language_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "English" => 0,
            "Chinese" => 1,
            "Japanese" => 2,
            "Korean" => 3,
            "German" => 4,
            "French" => 5,
            "Latin" => 6,
            "Cyrillic" => 7,
            "TraditionalChinese" => 8,
            "Thai" => 9,
            "Greek" => 10,
            "EastSlavic" => 11,
            "Arabic" => 12,
            "Devanagari" => 13,
            "Tamil" => 14,
            "Telugu" => 15,
            _ => {
                set_last_error(1, "Unknown PaddleLanguage variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `LayoutClass` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_class_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0,   // Caption
            1 => 1,   // Chart
            2 => 2,   // Footnote
            3 => 3,   // Formula
            4 => 4,   // ListItem
            5 => 5,   // PageFooter
            6 => 6,   // PageHeader
            7 => 7,   // Picture
            8 => 8,   // SectionHeader
            9 => 9,   // Table
            10 => 10, // Text
            11 => 11, // Title
            12 => 12, // DocumentIndex
            13 => 13, // Code
            14 => 14, // CheckboxSelected
            15 => 15, // CheckboxUnselected
            16 => 16, // Form
            17 => 17, // KeyValueRegion
            _ => {
                set_last_error(1, "Invalid LayoutClass variant");
                -1
            }
        }
    })
}

/// Convert a `LayoutClass` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_class_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "caption" => 0,
            "chart" => 1,
            "footnote" => 2,
            "formula" => 3,
            "list_item" => 4,
            "page_footer" => 5,
            "page_header" => 6,
            "picture" => 7,
            "section_header" => 8,
            "table" => 9,
            "text" => 10,
            "title" => 11,
            "document_index" => 12,
            "code" => 13,
            "checkbox_selected" => 14,
            "checkbox_unselected" => 15,
            "form" => 16,
            "key_value_region" => 17,
            _ => {
                set_last_error(1, "Unknown LayoutClass variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `BrowserMode` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_mode_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Auto
            1 => 1, // Always
            2 => 2, // Never
            3 => 3, // Stealth
            _ => {
                set_last_error(1, "Invalid BrowserMode variant");
                -1
            }
        }
    })
}

/// Convert a `BrowserMode` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_mode_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "auto" => 0,
            "always" => 1,
            "never" => 2,
            "stealth" => 3,
            _ => {
                set_last_error(1, "Unknown BrowserMode variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `BrowserWait` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_wait_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // NetworkIdle
            1 => 1, // Selector
            2 => 2, // Fixed
            _ => {
                set_last_error(1, "Invalid BrowserWait variant");
                -1
            }
        }
    })
}

/// Convert a `BrowserWait` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_wait_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "network_idle" => 0,
            "selector" => 1,
            "fixed" => 2,
            _ => {
                set_last_error(1, "Unknown BrowserWait variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `BrowserBackend` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_backend_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Chromiumoxide
            1 => 1, // Native
            _ => {
                set_last_error(1, "Invalid BrowserBackend variant");
                -1
            }
        }
    })
}

/// Convert a `BrowserBackend` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_backend_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "chromiumoxide" => 0,
            "native" => 1,
            _ => {
                set_last_error(1, "Unknown BrowserBackend variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `DocumentContentEncoding` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_content_encoding_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Base64
            _ => {
                set_last_error(1, "Invalid DocumentContentEncoding variant");
                -1
            }
        }
    })
}

/// Convert a `DocumentContentEncoding` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_content_encoding_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "base64" => 0,
            _ => {
                set_last_error(1, "Unknown DocumentContentEncoding variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `AuthConfig` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_auth_config_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Basic
            1 => 1, // Bearer
            2 => 2, // Header
            _ => {
                set_last_error(1, "Invalid AuthConfig variant");
                -1
            }
        }
    })
}

/// Convert a `AuthConfig` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_auth_config_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "basic" => 0,
            "bearer" => 1,
            "header" => 2,
            _ => {
                set_last_error(1, "Unknown AuthConfig variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `AssetCategory` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_asset_category_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Document
            1 => 1, // Image
            2 => 2, // Audio
            3 => 3, // Video
            4 => 4, // Font
            5 => 5, // Stylesheet
            6 => 6, // Script
            7 => 7, // Archive
            8 => 8, // Data
            9 => 9, // Other
            _ => {
                set_last_error(1, "Invalid AssetCategory variant");
                -1
            }
        }
    })
}

/// Convert a `AssetCategory` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_asset_category_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "document" => 0,
            "image" => 1,
            "audio" => 2,
            "video" => 3,
            "font" => 4,
            "stylesheet" => 5,
            "script" => 6,
            "archive" => 7,
            "data" => 8,
            "other" => 9,
            _ => {
                set_last_error(1, "Unknown AssetCategory variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `HostMatcher` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_host_matcher_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Exact
            1 => 1, // Suffix
            2 => 2, // Cidr
            _ => {
                set_last_error(1, "Invalid HostMatcher variant");
                -1
            }
        }
    })
}

/// Convert a `HostMatcher` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_host_matcher_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "exact" => 0,
            "suffix" => 1,
            "cidr" => 2,
            _ => {
                set_last_error(1, "Unknown HostMatcher variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `PreprocessingPreset` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preprocessing_preset_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Minimal
            1 => 1, // Standard
            2 => 2, // Aggressive
            _ => {
                set_last_error(1, "Invalid PreprocessingPreset variant");
                -1
            }
        }
    })
}

/// Convert a `PreprocessingPreset` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preprocessing_preset_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Minimal" => 0,
            "Standard" => 1,
            "Aggressive" => 2,
            _ => {
                set_last_error(1, "Unknown PreprocessingPreset variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `HeadingStyle` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heading_style_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Underlined
            1 => 1, // Atx
            2 => 2, // AtxClosed
            _ => {
                set_last_error(1, "Invalid HeadingStyle variant");
                -1
            }
        }
    })
}

/// Convert a `HeadingStyle` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heading_style_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Underlined" => 0,
            "Atx" => 1,
            "AtxClosed" => 2,
            _ => {
                set_last_error(1, "Unknown HeadingStyle variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ListIndentType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_indent_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Spaces
            1 => 1, // Tabs
            _ => {
                set_last_error(1, "Invalid ListIndentType variant");
                -1
            }
        }
    })
}

/// Convert a `ListIndentType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_indent_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Spaces" => 0,
            "Tabs" => 1,
            _ => {
                set_last_error(1, "Unknown ListIndentType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `WhitespaceMode` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_whitespace_mode_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Normalized
            1 => 1, // Strict
            _ => {
                set_last_error(1, "Invalid WhitespaceMode variant");
                -1
            }
        }
    })
}

/// Convert a `WhitespaceMode` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_whitespace_mode_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Normalized" => 0,
            "Strict" => 1,
            _ => {
                set_last_error(1, "Unknown WhitespaceMode variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `NewlineStyle` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_newline_style_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Spaces
            1 => 1, // Backslash
            _ => {
                set_last_error(1, "Invalid NewlineStyle variant");
                -1
            }
        }
    })
}

/// Convert a `NewlineStyle` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_newline_style_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Spaces" => 0,
            "Backslash" => 1,
            _ => {
                set_last_error(1, "Unknown NewlineStyle variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `CodeBlockStyle` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_block_style_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Indented
            1 => 1, // Backticks
            2 => 2, // Tildes
            _ => {
                set_last_error(1, "Invalid CodeBlockStyle variant");
                -1
            }
        }
    })
}

/// Convert a `CodeBlockStyle` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_block_style_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Indented" => 0,
            "Backticks" => 1,
            "Tildes" => 2,
            _ => {
                set_last_error(1, "Unknown CodeBlockStyle variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `HighlightStyle` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_highlight_style_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // DoubleEqual
            1 => 1, // Html
            2 => 2, // Bold
            3 => 3, // None
            _ => {
                set_last_error(1, "Invalid HighlightStyle variant");
                -1
            }
        }
    })
}

/// Convert a `HighlightStyle` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_highlight_style_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "DoubleEqual" => 0,
            "Html" => 1,
            "Bold" => 2,
            "None" => 3,
            _ => {
                set_last_error(1, "Unknown HighlightStyle variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `LinkStyle` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_link_style_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Inline
            1 => 1, // Reference
            _ => {
                set_last_error(1, "Invalid LinkStyle variant");
                -1
            }
        }
    })
}

/// Convert a `LinkStyle` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_link_style_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Inline" => 0,
            "Reference" => 1,
            _ => {
                set_last_error(1, "Unknown LinkStyle variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `UrlEscapeStyle` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_escape_style_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Angle
            1 => 1, // Percent
            _ => {
                set_last_error(1, "Invalid UrlEscapeStyle variant");
                -1
            }
        }
    })
}

/// Convert a `UrlEscapeStyle` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_escape_style_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Angle" => 0,
            "Percent" => 1,
            _ => {
                set_last_error(1, "Unknown UrlEscapeStyle variant");
                -1
            }
        }
    })
}

/// Free a heap-allocated `ExecutionProviderType` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_execution_provider_type_free(ptr: *mut xberg::ExecutionProviderType) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `ExecutionProviderType` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_execution_provider_type_to_json(
    ptr: *const xberg::ExecutionProviderType,
) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_execution_provider_type_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `ExecutionProviderType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_execution_provider_type_to_string(
    ptr: *const xberg::ExecutionProviderType,
) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_execution_provider_type_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "ExecutionProviderType variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `ImageOutputFormat` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_output_format_free(ptr: *mut xberg::core::config::extraction::ImageOutputFormat) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `ImageOutputFormat` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_output_format_to_json(
    ptr: *const xberg::core::config::extraction::ImageOutputFormat,
) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_image_output_format_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `ImageOutputFormat` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_output_format_to_string(
    ptr: *const xberg::core::config::extraction::ImageOutputFormat,
) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_image_output_format_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "ImageOutputFormat variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `ExtractInputKind` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract_input_kind_free(ptr: *mut xberg::ExtractInputKind) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `ExtractInputKind` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract_input_kind_to_json(ptr: *const xberg::ExtractInputKind) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_extract_input_kind_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `ExtractInputKind` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract_input_kind_to_string(ptr: *const xberg::ExtractInputKind) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_extract_input_kind_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "ExtractInputKind variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `UrlExtractionMode` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_extraction_mode_free(ptr: *mut xberg::UrlExtractionMode) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `UrlExtractionMode` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_extraction_mode_to_json(ptr: *const xberg::UrlExtractionMode) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_url_extraction_mode_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `UrlExtractionMode` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_extraction_mode_to_string(ptr: *const xberg::UrlExtractionMode) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_url_extraction_mode_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "UrlExtractionMode variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `BreadcrumbTarget` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_breadcrumb_target_free(ptr: *mut xberg::BreadcrumbTarget) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `BreadcrumbTarget` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_breadcrumb_target_to_json(ptr: *const xberg::BreadcrumbTarget) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_breadcrumb_target_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `BreadcrumbTarget` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_breadcrumb_target_to_string(ptr: *const xberg::BreadcrumbTarget) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_breadcrumb_target_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "BreadcrumbTarget variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `OutputFormat` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_output_format_free(ptr: *mut xberg::OutputFormat) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `OutputFormat` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_output_format_to_json(ptr: *const xberg::OutputFormat) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_output_format_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `OutputFormat` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_output_format_to_string(ptr: *const xberg::OutputFormat) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_output_format_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "OutputFormat variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `JupyterCellRendering` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_jupyter_cell_rendering_free(ptr: *mut xberg::JupyterCellRendering) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `JupyterCellRendering` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_jupyter_cell_rendering_to_json(ptr: *const xberg::JupyterCellRendering) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_jupyter_cell_rendering_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `JupyterCellRendering` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_jupyter_cell_rendering_to_string(
    ptr: *const xberg::JupyterCellRendering,
) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_jupyter_cell_rendering_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "JupyterCellRendering variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `HtmlTheme` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_theme_free(ptr: *mut xberg::HtmlTheme) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `HtmlTheme` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_theme_to_json(ptr: *const xberg::HtmlTheme) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_html_theme_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `HtmlTheme` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_html_theme_to_string(ptr: *const xberg::HtmlTheme) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_html_theme_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "HtmlTheme variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `LateInteractionModelType` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_model_type_free(ptr: *mut xberg::LateInteractionModelType) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `LateInteractionModelType` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_model_type_to_json(
    ptr: *const xberg::LateInteractionModelType,
) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_late_interaction_model_type_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `LateInteractionModelType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_late_interaction_model_type_to_string(
    ptr: *const xberg::LateInteractionModelType,
) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_late_interaction_model_type_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "LateInteractionModelType variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `TableModel` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_model_free(ptr: *mut xberg::TableModel) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `TableModel` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_model_to_json(ptr: *const xberg::TableModel) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_table_model_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `TableModel` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_model_to_string(ptr: *const xberg::TableModel) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_table_model_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "TableModel variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `TableOverlapPreference` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_overlap_preference_free(
    ptr: *mut xberg::core::config::layout::TableOverlapPreference,
) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `TableOverlapPreference` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_overlap_preference_to_json(
    ptr: *const xberg::core::config::layout::TableOverlapPreference,
) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_table_overlap_preference_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `TableOverlapPreference` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_overlap_preference_to_string(
    ptr: *const xberg::core::config::layout::TableOverlapPreference,
) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_table_overlap_preference_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "TableOverlapPreference variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `LayoutStrategy` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_strategy_free(ptr: *mut xberg::LayoutStrategy) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `LayoutStrategy` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_strategy_to_json(ptr: *const xberg::LayoutStrategy) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_layout_strategy_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `LayoutStrategy` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_strategy_to_string(ptr: *const xberg::LayoutStrategy) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_layout_strategy_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "LayoutStrategy variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `CredentialProviderConfig` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_credential_provider_config_free(ptr: *mut xberg::CredentialProviderConfig) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `CredentialProviderConfig` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_credential_provider_config_to_json(
    ptr: *const xberg::CredentialProviderConfig,
) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_credential_provider_config_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `CredentialProviderConfig` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_credential_provider_config_to_string(
    ptr: *const xberg::CredentialProviderConfig,
) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_credential_provider_config_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "CredentialProviderConfig variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `CallMode` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_call_mode_free(ptr: *mut xberg::CallMode) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `CallMode` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_call_mode_to_json(ptr: *const xberg::CallMode) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_call_mode_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `CallMode` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_call_mode_to_string(ptr: *const xberg::CallMode) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_call_mode_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "CallMode variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `MergeMode` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_merge_mode_free(ptr: *mut xberg::MergeMode) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `MergeMode` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_merge_mode_to_json(ptr: *const xberg::MergeMode) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_merge_mode_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `MergeMode` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_merge_mode_to_string(ptr: *const xberg::MergeMode) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_merge_mode_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "MergeMode variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `NerBackendKind` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ner_backend_kind_free(ptr: *mut xberg::NerBackendKind) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `NerBackendKind` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ner_backend_kind_to_json(ptr: *const xberg::NerBackendKind) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_ner_backend_kind_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `NerBackendKind` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ner_backend_kind_to_string(ptr: *const xberg::NerBackendKind) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_ner_backend_kind_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "NerBackendKind variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `VlmFallbackPolicy` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_vlm_fallback_policy_free(ptr: *mut xberg::VlmFallbackPolicy) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `VlmFallbackPolicy` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_vlm_fallback_policy_to_json(ptr: *const xberg::VlmFallbackPolicy) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_vlm_fallback_policy_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `VlmFallbackPolicy` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_vlm_fallback_policy_to_string(ptr: *const xberg::VlmFallbackPolicy) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_vlm_fallback_policy_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "VlmFallbackPolicy variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `OcrStrategy` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_strategy_free(ptr: *mut xberg::OcrStrategy) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `OcrStrategy` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_strategy_to_json(ptr: *const xberg::OcrStrategy) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_ocr_strategy_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `OcrStrategy` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_strategy_to_string(ptr: *const xberg::OcrStrategy) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_ocr_strategy_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "OcrStrategy variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `TableChunkingMode` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_chunking_mode_free(ptr: *mut xberg::TableChunkingMode) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `TableChunkingMode` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_chunking_mode_to_json(ptr: *const xberg::TableChunkingMode) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_table_chunking_mode_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `TableChunkingMode` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_table_chunking_mode_to_string(ptr: *const xberg::TableChunkingMode) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_table_chunking_mode_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "TableChunkingMode variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `ChunkerType` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunker_type_free(ptr: *mut xberg::ChunkerType) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `ChunkerType` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunker_type_to_json(ptr: *const xberg::ChunkerType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_chunker_type_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `ChunkerType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunker_type_to_string(ptr: *const xberg::ChunkerType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_chunker_type_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "ChunkerType variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `ChunkSizing` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_sizing_free(ptr: *mut xberg::ChunkSizing) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `ChunkSizing` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_sizing_to_json(ptr: *const xberg::ChunkSizing) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_chunk_sizing_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `ChunkSizing` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_sizing_to_string(ptr: *const xberg::ChunkSizing) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_chunk_sizing_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "ChunkSizing variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `EmbeddingModelType` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedding_model_type_free(ptr: *mut xberg::EmbeddingModelType) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `EmbeddingModelType` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedding_model_type_to_json(ptr: *const xberg::EmbeddingModelType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_embedding_model_type_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `EmbeddingModelType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embedding_model_type_to_string(ptr: *const xberg::EmbeddingModelType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_embedding_model_type_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "EmbeddingModelType variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `RerankerModelType` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranker_model_type_free(ptr: *mut xberg::RerankerModelType) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `RerankerModelType` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranker_model_type_to_json(ptr: *const xberg::RerankerModelType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_reranker_model_type_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `RerankerModelType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reranker_model_type_to_string(ptr: *const xberg::RerankerModelType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_reranker_model_type_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "RerankerModelType variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `SparseEmbeddingModelType` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_model_type_free(ptr: *mut xberg::SparseEmbeddingModelType) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `SparseEmbeddingModelType` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_model_type_to_json(
    ptr: *const xberg::SparseEmbeddingModelType,
) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_sparse_embedding_model_type_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `SparseEmbeddingModelType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_sparse_embedding_model_type_to_string(
    ptr: *const xberg::SparseEmbeddingModelType,
) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_sparse_embedding_model_type_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "SparseEmbeddingModelType variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `WhisperModel` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_whisper_model_free(ptr: *mut xberg::WhisperModel) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `WhisperModel` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_whisper_model_to_json(ptr: *const xberg::WhisperModel) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_whisper_model_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `WhisperModel` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_whisper_model_to_string(ptr: *const xberg::WhisperModel) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_whisper_model_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "WhisperModel variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `CodeContentMode` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_content_mode_free(ptr: *mut xberg::CodeContentMode) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `CodeContentMode` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_content_mode_to_json(ptr: *const xberg::CodeContentMode) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_code_content_mode_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `CodeContentMode` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_content_mode_to_string(ptr: *const xberg::CodeContentMode) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_code_content_mode_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "CodeContentMode variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `ReductionLevel` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reduction_level_free(ptr: *mut xberg::ReductionLevel) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `ReductionLevel` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reduction_level_to_json(ptr: *const xberg::ReductionLevel) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_reduction_level_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `ReductionLevel` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_reduction_level_to_string(ptr: *const xberg::ReductionLevel) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_reduction_level_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "ReductionLevel variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `PdfAnnotationType` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_type_free(ptr: *mut xberg::PdfAnnotationType) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `PdfAnnotationType` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_type_to_json(ptr: *const xberg::PdfAnnotationType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_pdf_annotation_type_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `PdfAnnotationType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pdf_annotation_type_to_string(ptr: *const xberg::PdfAnnotationType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_pdf_annotation_type_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "PdfAnnotationType variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `BlockType` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_block_type_free(ptr: *mut xberg::BlockType) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `BlockType` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_block_type_to_json(ptr: *const xberg::BlockType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_block_type_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `BlockType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_block_type_to_string(ptr: *const xberg::BlockType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_block_type_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "BlockType variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `InlineType` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_inline_type_free(ptr: *mut xberg::InlineType) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `InlineType` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_inline_type_to_json(ptr: *const xberg::InlineType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_inline_type_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `InlineType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_inline_type_to_string(ptr: *const xberg::InlineType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_inline_type_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "InlineType variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `RelationshipKind` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_relationship_kind_free(ptr: *mut xberg::RelationshipKind) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `RelationshipKind` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_relationship_kind_to_json(ptr: *const xberg::RelationshipKind) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_relationship_kind_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `RelationshipKind` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_relationship_kind_to_string(ptr: *const xberg::RelationshipKind) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_relationship_kind_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "RelationshipKind variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `ContentLayer` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_layer_free(ptr: *mut xberg::ContentLayer) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `ContentLayer` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_layer_to_json(ptr: *const xberg::ContentLayer) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_content_layer_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `ContentLayer` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_content_layer_to_string(ptr: *const xberg::ContentLayer) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_content_layer_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "ContentLayer variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `NodeContent` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_node_content_free(ptr: *mut xberg::NodeContent) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `NodeContent` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_node_content_to_json(ptr: *const xberg::NodeContent) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_node_content_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `NodeContent` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_node_content_to_string(ptr: *const xberg::NodeContent) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_node_content_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "NodeContent variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `AnnotationKind` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_annotation_kind_free(ptr: *mut xberg::AnnotationKind) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `AnnotationKind` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_annotation_kind_to_json(ptr: *const xberg::AnnotationKind) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_annotation_kind_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `AnnotationKind` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_annotation_kind_to_string(ptr: *const xberg::AnnotationKind) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_annotation_kind_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "AnnotationKind variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `EntityCategory` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_entity_category_free(ptr: *mut xberg::EntityCategory) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `EntityCategory` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_entity_category_to_json(ptr: *const xberg::EntityCategory) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_entity_category_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `EntityCategory` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_entity_category_to_string(ptr: *const xberg::EntityCategory) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_entity_category_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "EntityCategory variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `ExtractionMethod` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_method_free(ptr: *mut xberg::ExtractionMethod) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `ExtractionMethod` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_method_to_json(ptr: *const xberg::ExtractionMethod) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_extraction_method_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `ExtractionMethod` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extraction_method_to_string(ptr: *const xberg::ExtractionMethod) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_extraction_method_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "ExtractionMethod variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `ChunkType` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_type_free(ptr: *mut xberg::ChunkType) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `ChunkType` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_type_to_json(ptr: *const xberg::ChunkType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_chunk_type_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `ChunkType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_chunk_type_to_string(ptr: *const xberg::ChunkType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_chunk_type_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "ChunkType variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `ImageKind` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_kind_free(ptr: *mut xberg::ImageKind) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `ImageKind` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_kind_to_json(ptr: *const xberg::ImageKind) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_image_kind_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `ImageKind` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_kind_to_string(ptr: *const xberg::ImageKind) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_image_kind_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "ImageKind variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `ResultFormat` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_result_format_free(ptr: *mut xberg::ResultFormat) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `ResultFormat` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_result_format_to_json(ptr: *const xberg::ResultFormat) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_result_format_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `ResultFormat` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_result_format_to_string(ptr: *const xberg::ResultFormat) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_result_format_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "ResultFormat variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `ElementType` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_type_free(ptr: *mut xberg::ElementType) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `ElementType` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_type_to_json(ptr: *const xberg::ElementType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_element_type_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `ElementType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_element_type_to_string(ptr: *const xberg::ElementType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_element_type_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "ElementType variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `FormFieldType` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_form_field_type_free(ptr: *mut xberg::FormFieldType) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `FormFieldType` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_form_field_type_to_json(ptr: *const xberg::FormFieldType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_form_field_type_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `FormFieldType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_form_field_type_to_string(ptr: *const xberg::FormFieldType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_form_field_type_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "FormFieldType variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `FormatMetadata` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_format_metadata_free(ptr: *mut xberg::FormatMetadata) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `FormatMetadata` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_format_metadata_to_json(ptr: *const xberg::FormatMetadata) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_format_metadata_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `FormatMetadata` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_format_metadata_to_string(ptr: *const xberg::FormatMetadata) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_format_metadata_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "FormatMetadata variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `CodeDataNodeKind` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_node_kind_free(ptr: *mut xberg::CodeDataNodeKind) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `CodeDataNodeKind` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_node_kind_to_json(ptr: *const xberg::CodeDataNodeKind) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_code_data_node_kind_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `CodeDataNodeKind` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_data_node_kind_to_string(ptr: *const xberg::CodeDataNodeKind) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_code_data_node_kind_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "CodeDataNodeKind variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `TextDirection` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_direction_free(ptr: *mut xberg::TextDirection) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `TextDirection` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_direction_to_json(ptr: *const xberg::TextDirection) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_text_direction_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `TextDirection` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_text_direction_to_string(ptr: *const xberg::TextDirection) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_text_direction_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "TextDirection variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `LinkType` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_link_type_free(ptr: *mut xberg::LinkType) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `LinkType` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_link_type_to_json(ptr: *const xberg::LinkType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_link_type_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `LinkType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_link_type_to_string(ptr: *const xberg::LinkType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_link_type_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "LinkType variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `ImageType` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_type_free(ptr: *mut xberg::ImageType) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `ImageType` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_type_to_json(ptr: *const xberg::ImageType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_image_type_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `ImageType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_image_type_to_string(ptr: *const xberg::ImageType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_image_type_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "ImageType variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `StructuredDataType` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_type_free(ptr: *mut xberg::StructuredDataType) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `StructuredDataType` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_type_to_json(ptr: *const xberg::StructuredDataType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_structured_data_type_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `StructuredDataType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_structured_data_type_to_string(ptr: *const xberg::StructuredDataType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_structured_data_type_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "StructuredDataType variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `OcrBoundingGeometry` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_bounding_geometry_free(ptr: *mut xberg::OcrBoundingGeometry) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `OcrBoundingGeometry` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_bounding_geometry_to_json(ptr: *const xberg::OcrBoundingGeometry) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_ocr_bounding_geometry_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `OcrBoundingGeometry` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_bounding_geometry_to_string(ptr: *const xberg::OcrBoundingGeometry) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_ocr_bounding_geometry_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "OcrBoundingGeometry variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `OcrElementLevel` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_level_free(ptr: *mut xberg::OcrElementLevel) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `OcrElementLevel` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_level_to_json(ptr: *const xberg::OcrElementLevel) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_ocr_element_level_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `OcrElementLevel` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ocr_element_level_to_string(ptr: *const xberg::OcrElementLevel) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_ocr_element_level_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "OcrElementLevel variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `PageUnitType` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_unit_type_free(ptr: *mut xberg::PageUnitType) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `PageUnitType` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_unit_type_to_json(ptr: *const xberg::PageUnitType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_page_unit_type_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `PageUnitType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_page_unit_type_to_string(ptr: *const xberg::PageUnitType) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_page_unit_type_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "PageUnitType variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `RedactionStrategy` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_strategy_free(ptr: *mut xberg::RedactionStrategy) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `RedactionStrategy` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_strategy_to_json(ptr: *const xberg::RedactionStrategy) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_redaction_strategy_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `RedactionStrategy` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_redaction_strategy_to_string(ptr: *const xberg::RedactionStrategy) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_redaction_strategy_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "RedactionStrategy variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `PiiCategory` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pii_category_free(ptr: *mut xberg::PiiCategory) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `PiiCategory` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pii_category_to_json(ptr: *const xberg::PiiCategory) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_pii_category_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `PiiCategory` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_pii_category_to_string(ptr: *const xberg::PiiCategory) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_pii_category_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "PiiCategory variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `RevisionKind` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_revision_kind_free(ptr: *mut xberg::RevisionKind) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `RevisionKind` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_revision_kind_to_json(ptr: *const xberg::RevisionKind) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_revision_kind_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `RevisionKind` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_revision_kind_to_string(ptr: *const xberg::RevisionKind) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_revision_kind_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "RevisionKind variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `RevisionAnchor` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_revision_anchor_free(ptr: *mut xberg::RevisionAnchor) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `RevisionAnchor` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_revision_anchor_to_json(ptr: *const xberg::RevisionAnchor) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_revision_anchor_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `RevisionAnchor` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_revision_anchor_to_string(ptr: *const xberg::RevisionAnchor) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_revision_anchor_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "RevisionAnchor variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `SummaryStrategy` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_summary_strategy_free(ptr: *mut xberg::SummaryStrategy) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `SummaryStrategy` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_summary_strategy_to_json(ptr: *const xberg::SummaryStrategy) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_summary_strategy_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `SummaryStrategy` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_summary_strategy_to_string(ptr: *const xberg::SummaryStrategy) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_summary_strategy_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "SummaryStrategy variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `UriKind` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_uri_kind_free(ptr: *mut xberg::UriKind) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `UriKind` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_uri_kind_to_json(ptr: *const xberg::UriKind) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_uri_kind_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `UriKind` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_uri_kind_to_string(ptr: *const xberg::UriKind) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_uri_kind_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "UriKind variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `KeywordAlgorithm` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_algorithm_free(ptr: *mut xberg::KeywordAlgorithm) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `KeywordAlgorithm` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_algorithm_to_json(ptr: *const xberg::KeywordAlgorithm) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_keyword_algorithm_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `KeywordAlgorithm` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_keyword_algorithm_to_string(ptr: *const xberg::KeywordAlgorithm) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_keyword_algorithm_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "KeywordAlgorithm variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `SchemaCompliance` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_schema_compliance_free(ptr: *mut xberg::SchemaCompliance) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `SchemaCompliance` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_schema_compliance_to_json(ptr: *const xberg::SchemaCompliance) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_schema_compliance_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `SchemaCompliance` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_schema_compliance_to_string(ptr: *const xberg::SchemaCompliance) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_schema_compliance_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "SchemaCompliance variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `BoundaryReason` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_boundary_reason_free(ptr: *mut xberg::BoundaryReason) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `BoundaryReason` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_boundary_reason_to_json(ptr: *const xberg::BoundaryReason) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_boundary_reason_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `BoundaryReason` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_boundary_reason_to_string(ptr: *const xberg::BoundaryReason) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_boundary_reason_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "BoundaryReason variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `PresetCategory` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_category_free(ptr: *mut xberg::PresetCategory) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `PresetCategory` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_category_to_json(ptr: *const xberg::PresetCategory) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_preset_category_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `PresetCategory` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preset_category_to_string(ptr: *const xberg::PresetCategory) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_preset_category_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "PresetCategory variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `ProbeStatus` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_probe_status_free(ptr: *mut xberg::ProbeStatus) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `ProbeStatus` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_probe_status_to_json(ptr: *const xberg::ProbeStatus) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_probe_status_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `ProbeStatus` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_probe_status_to_string(ptr: *const xberg::ProbeStatus) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_probe_status_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "ProbeStatus variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `PaddleInferenceBackend` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_inference_backend_free(ptr: *mut xberg::PaddleInferenceBackend) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `PaddleInferenceBackend` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_inference_backend_to_json(
    ptr: *const xberg::PaddleInferenceBackend,
) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_paddle_inference_backend_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `PaddleInferenceBackend` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_paddle_inference_backend_to_string(
    ptr: *const xberg::PaddleInferenceBackend,
) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_paddle_inference_backend_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "PaddleInferenceBackend variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `LayoutClass` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_class_free(ptr: *mut xberg::LayoutClass) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `LayoutClass` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_class_to_json(ptr: *const xberg::LayoutClass) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_layout_class_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `LayoutClass` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_layout_class_to_string(ptr: *const xberg::LayoutClass) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_layout_class_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "LayoutClass variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `BrowserMode` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_mode_free(ptr: *mut crawlberg::BrowserMode) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `BrowserMode` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_mode_to_json(ptr: *const crawlberg::BrowserMode) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_browser_mode_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `BrowserMode` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_mode_to_string(ptr: *const crawlberg::BrowserMode) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_browser_mode_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "BrowserMode variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `BrowserWait` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_wait_free(ptr: *mut crawlberg::BrowserWait) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `BrowserWait` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_wait_to_json(ptr: *const crawlberg::BrowserWait) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_browser_wait_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `BrowserWait` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_wait_to_string(ptr: *const crawlberg::BrowserWait) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_browser_wait_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "BrowserWait variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `BrowserBackend` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_backend_free(ptr: *mut crawlberg::BrowserBackend) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `BrowserBackend` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_backend_to_json(ptr: *const crawlberg::BrowserBackend) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_browser_backend_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `BrowserBackend` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_browser_backend_to_string(ptr: *const crawlberg::BrowserBackend) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_browser_backend_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "BrowserBackend variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `DocumentContentEncoding` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_content_encoding_free(ptr: *mut crawlberg::DocumentContentEncoding) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `DocumentContentEncoding` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_content_encoding_to_json(
    ptr: *const crawlberg::DocumentContentEncoding,
) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_document_content_encoding_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `DocumentContentEncoding` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_document_content_encoding_to_string(
    ptr: *const crawlberg::DocumentContentEncoding,
) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_document_content_encoding_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "DocumentContentEncoding variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `AuthConfig` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_auth_config_free(ptr: *mut crawlberg::AuthConfig) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Serialize a heap-allocated `AuthConfig` to a JSON string.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_auth_config_to_json(ptr: *const crawlberg::AuthConfig) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_auth_config_to_json");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        match serde_json::to_string(val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        }
    })
}

/// Render a heap-allocated `AuthConfig` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `xberg` function.
/// The returned string must be freed with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_auth_config_to_string(ptr: *const crawlberg::AuthConfig) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if ptr.is_null() {
            set_last_error(1, "Null pointer passed to xberg_auth_config_to_string");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ptr is valid; no mutable aliases held.
        let val = unsafe { &*ptr };
        let s: String = serde_json::to_value(val)
            .ok()
            .and_then(|v| v.as_str().map(str::to_owned))
            .unwrap_or_default();
        match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(1, "AuthConfig variant contained interior NUL byte");
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a heap-allocated `PreprocessingPreset` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_preprocessing_preset_free(ptr: *mut html_to_markdown_rs::PreprocessingPreset) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Free a heap-allocated `HeadingStyle` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_heading_style_free(ptr: *mut html_to_markdown_rs::HeadingStyle) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Free a heap-allocated `ListIndentType` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_indent_type_free(ptr: *mut html_to_markdown_rs::ListIndentType) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Free a heap-allocated `WhitespaceMode` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_whitespace_mode_free(ptr: *mut html_to_markdown_rs::WhitespaceMode) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Free a heap-allocated `NewlineStyle` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_newline_style_free(ptr: *mut html_to_markdown_rs::NewlineStyle) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Free a heap-allocated `CodeBlockStyle` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_code_block_style_free(ptr: *mut html_to_markdown_rs::CodeBlockStyle) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Free a heap-allocated `HighlightStyle` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_highlight_style_free(ptr: *mut html_to_markdown_rs::HighlightStyle) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Free a heap-allocated `LinkStyle` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_link_style_free(ptr: *mut html_to_markdown_rs::LinkStyle) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

/// Free a heap-allocated `UrlEscapeStyle` returned by a pointer-returning FFI function.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_url_escape_style_free(ptr: *mut html_to_markdown_rs::UrlEscapeStyle) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by Box::into_raw; caller ensures no aliases.
            unsafe {
                drop(Box::from_raw(ptr));
            }
        }
    })
}

fn get_ffi_runtime() -> &'static tokio::runtime::Runtime {
    use std::sync::OnceLock;
    static RUNTIME: OnceLock<tokio::runtime::Runtime> = OnceLock::new();
    RUNTIME.get_or_init(|| tokio::runtime::Runtime::new().expect("Failed to create tokio runtime"))
}

/// Extract content from a single bytes or URI input.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract(input: AlefHandle, config: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: input,
            expected_type: std::any::TypeId::of::<xberg::ExtractInput>(),
        });
        __alef_requests.push(HandleRequest {
            handle: config,
            expected_type: std::any::TypeId::of::<xberg::ExtractionConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if input == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'input'",
            );
            return 0;
        }
        let input_rs_ptr = match locked_handle_ptr::<xberg::ExtractInput>(&mut __alef_guards, input) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let input_rs = unsafe { &*input_rs_ptr }.clone();

        if config == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'config'",
            );
            return 0;
        }
        let config_rs_ptr = match locked_handle_ptr::<xberg::ExtractionConfig>(&mut __alef_guards, config) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let config_rs = unsafe { &*config_rs_ptr };

        let result = get_ffi_runtime().block_on(async { xberg::extract(input_rs, &config_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Extract content from multiple bytes or URI inputs.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_extract_batch(inputs: *const std::ffi::c_char, config: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: config,
            expected_type: std::any::TypeId::of::<xberg::ExtractionConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if inputs.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'inputs'");
            return 0;
        }
        // SAFETY: null check above guarantees inputs is a valid pointer; string is valid UTF-8 from caller.
        let inputs_rs_str = match unsafe { CStr::from_ptr(inputs) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'inputs'");
                return 0;
            }
        };
        let inputs_rs = match serde_json::from_str::<Vec<xberg::ExtractInput>>(inputs_rs_str) {
            Ok(v) => v,
            Err(e) => {
                set_last_error(2, &e.to_string());
                return 0;
            }
        };

        if config == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'config'",
            );
            return 0;
        }
        let config_rs_ptr = match locked_handle_ptr::<xberg::ExtractionConfig>(&mut __alef_guards, config) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let config_rs = unsafe { &*config_rs_ptr };

        let result = get_ffi_runtime().block_on(async { xberg::extract_batch(inputs_rs, &config_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Discover all pages and sitemaps reachable from `uri` without extracting document content.
///
/// Builds a `crawlberg.CrawlEngine` from `config.crawl`, calls
/// `CrawlEngine.map`, and returns the set of discovered URLs as a
/// `crawlberg.MapResult` (re-exported as `MapResult`).
///
/// Use this when you need the URL inventory of a site before committing to
/// full document extraction â e.g. to build a crawl queue or validate scope.
/// \note Returns `Validation` if the crawl configuration fails
/// validation or if the map operation itself fails.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "url-ingestion")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_map_url(uri: *const std::ffi::c_char, config: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: config,
            expected_type: std::any::TypeId::of::<xberg::UrlExtractionConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if uri.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'uri'");
            return 0;
        }
        // SAFETY: null check above guarantees uri is a valid pointer; string is valid UTF-8 from caller.
        let uri_rs = match unsafe { CStr::from_ptr(uri) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'uri'");
                return 0;
            }
        };

        if config == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'config'",
            );
            return 0;
        }
        let config_rs_ptr = match locked_handle_ptr::<xberg::UrlExtractionConfig>(&mut __alef_guards, config) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let config_rs = unsafe { &*config_rs_ptr };

        let result = get_ffi_runtime().block_on(async { xberg::map_url(&uri_rs, &config_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// List all supported document formats.
///
/// Returns every file extension Xberg recognizes together with its
/// corresponding MIME type, derived from the central format registry.
/// Formats that have no registered file extension (such as source code,
/// which is detected dynamically) are not included.
///
/// The static `EXT_TO_MIME` table lists every format the *codebase* knows how
/// to describe, regardless of which Cargo features were compiled in. Advertising
/// that table directly would claim support for extractors that may not exist in
/// this build (see GH#1387). To keep the advertised catalogue honest, the table
/// is intersected with the document extractor registry: an extension is only
/// included if some registered extractor actually claims its MIME type in this
/// build. This can never drift from reality and automatically covers
/// third-party extractors registered at runtime.
///
/// The list is sorted alphabetically by file extension.
/// \return A vector of `SupportedFormat` entries sorted by extension, limited to
/// formats with a registered extractor in this build.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use xberg::core::mime::list_supported_formats;
///
/// let formats = list_supported_formats();
/// assert!(!formats.is_empty());
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_supported_formats() -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("xberg_list_supported_formats", 0);
        let result = xberg::list_supported_formats();
        {
            match serde_json::to_string(&result) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("xberg_list_supported_formats", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("xberg_list_supported_formats", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("xberg_list_supported_formats", 0);
                    std::ptr::null_mut()
                }
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `xberg_list_supported_formats` on
/// this thread. Returns 0 when the primary call returned null or failed before producing a string.
/// Enables safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `xberg_list_supported_formats`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_supported_formats_len() -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("xberg_list_supported_formats"))
}

/// Ensure built-in extractors are registered.
///
/// This function is called automatically on first extraction operation.
/// It's safe to call multiple times - registration only happens once,
/// unless the registry was cleared, in which case extractors are re-registered.
///
/// Public so a caller that wants to *inspect* the registry â rather than extract â
/// can populate it directly. Without this the only way to trigger registration is to
/// run a real extraction, which `xberg formats` would otherwise have to fake (#233).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_ensure_initialized() -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = xberg::extractors::ensure_initialized();
        match result {
            Ok(()) => 0,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            -1
        }
    }
}

/// List the names of all registered embedding backends.
///
/// Used by `xberg-cli`, the api/mcp endpoints, and generated language
/// bindings.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_embedding_backends() -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("xberg_list_embedding_backends", 0);
        let result = xberg::list_embedding_backends();
        match result {
            Ok(val) => match serde_json::to_string(&val) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("xberg_list_embedding_backends", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("xberg_list_embedding_backends", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("xberg_list_embedding_backends", 0);
                    std::ptr::null_mut()
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `xberg_list_embedding_backends` on
/// this thread. Returns 0 when the primary call returned null or failed before producing a string.
/// Enables safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `xberg_list_embedding_backends`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_embedding_backends_len() -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("xberg_list_embedding_backends"))
}

/// List names of all registered document extractors.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_document_extractors() -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("xberg_list_document_extractors", 0);
        let result = xberg::list_document_extractors();
        match result {
            Ok(val) => match serde_json::to_string(&val) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("xberg_list_document_extractors", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("xberg_list_document_extractors", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("xberg_list_document_extractors", 0);
                    std::ptr::null_mut()
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `xberg_list_document_extractors` on
/// this thread. Returns 0 when the primary call returned null or failed before producing a string.
/// Enables safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `xberg_list_document_extractors`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_document_extractors_len() -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("xberg_list_document_extractors"))
}

/// List all registered OCR backends.
///
/// Returns the names of all OCR backends currently registered in the global registry.
/// \return A vector of OCR backend names.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use xberg::plugins::list_ocr_backends;
///
/// let backends = list_ocr_backends()?;
/// for name in backends {
///     println!("Registered OCR backend: {}", name);
/// }
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_ocr_backends() -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("xberg_list_ocr_backends", 0);
        let result = xberg::list_ocr_backends();
        match result {
            Ok(val) => match serde_json::to_string(&val) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("xberg_list_ocr_backends", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("xberg_list_ocr_backends", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("xberg_list_ocr_backends", 0);
                    std::ptr::null_mut()
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `xberg_list_ocr_backends` on this
/// thread. Returns 0 when the primary call returned null or failed before producing a string. Enables
/// safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `xberg_list_ocr_backends`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_ocr_backends_len() -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("xberg_list_ocr_backends"))
}

/// List all registered post-processor names.
///
/// Returns a vector of all post-processor names currently registered in the
/// global registry.
/// \return - `Ok(Vec<String>)` - Vector of post-processor names
/// - `Err(...)` if the registry lock is poisoned
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use xberg::plugins::list_post_processors;
///
/// let processors = list_post_processors()?;
/// for name in processors {
///     println!("Registered post-processor: {}", name);
/// }
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_post_processors() -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("xberg_list_post_processors", 0);
        let result = xberg::list_post_processors();
        match result {
            Ok(val) => match serde_json::to_string(&val) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("xberg_list_post_processors", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("xberg_list_post_processors", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("xberg_list_post_processors", 0);
                    std::ptr::null_mut()
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `xberg_list_post_processors` on
/// this thread. Returns 0 when the primary call returned null or failed before producing a string.
/// Enables safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `xberg_list_post_processors`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_post_processors_len() -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("xberg_list_post_processors"))
}

/// List names of all registered renderers.
/// \note Returns an error if the registry lock is poisoned.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_renderers() -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("xberg_list_renderers", 0);
        let result = xberg::list_renderers();
        match result {
            Ok(val) => match serde_json::to_string(&val) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("xberg_list_renderers", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("xberg_list_renderers", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("xberg_list_renderers", 0);
                    std::ptr::null_mut()
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `xberg_list_renderers` on this
/// thread. Returns 0 when the primary call returned null or failed before producing a string. Enables
/// safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `xberg_list_renderers`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_renderers_len() -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("xberg_list_renderers"))
}

/// List the names of all registered reranker backends.
///
/// Used by `xberg-cli`, the api/mcp endpoints, and generated language
/// bindings.
///
/// Since v5.0.0.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_reranker_backends() -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("xberg_list_reranker_backends", 0);
        let result = xberg::list_reranker_backends();
        match result {
            Ok(val) => match serde_json::to_string(&val) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("xberg_list_reranker_backends", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("xberg_list_reranker_backends", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("xberg_list_reranker_backends", 0);
                    std::ptr::null_mut()
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `xberg_list_reranker_backends` on
/// this thread. Returns 0 when the primary call returned null or failed before producing a string.
/// Enables safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `xberg_list_reranker_backends`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_reranker_backends_len() -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("xberg_list_reranker_backends"))
}

/// List the names of all registered tokenizer backends.
///
/// Used by `xberg-cli`, the api/mcp endpoints, and generated language
/// bindings.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_tokenizer_backends() -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("xberg_list_tokenizer_backends", 0);
        let result = xberg::list_tokenizer_backends();
        match result {
            Ok(val) => match serde_json::to_string(&val) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("xberg_list_tokenizer_backends", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("xberg_list_tokenizer_backends", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("xberg_list_tokenizer_backends", 0);
                    std::ptr::null_mut()
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `xberg_list_tokenizer_backends` on
/// this thread. Returns 0 when the primary call returned null or failed before producing a string.
/// Enables safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `xberg_list_tokenizer_backends`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_tokenizer_backends_len() -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("xberg_list_tokenizer_backends"))
}

/// List names of all registered validators.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_validators() -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("xberg_list_validators", 0);
        let result = xberg::list_validators();
        match result {
            Ok(val) => match serde_json::to_string(&val) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("xberg_list_validators", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("xberg_list_validators", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("xberg_list_validators", 0);
                    std::ptr::null_mut()
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `xberg_list_validators` on this
/// thread. Returns 0 when the primary call returned null or failed before producing a string. Enables
/// safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `xberg_list_validators`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_list_validators_len() -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("xberg_list_validators"))
}

/// Run chunk classification against an extraction result.
///
/// Mutates `ChunkMetadata::classifications` on every chunk in
/// `result.chunks` and appends every LLM call's usage to `result.llm_usage`.
/// A chunk whose classification batch call fails (or that the model omitted
/// from its response) is simply left with an empty `classifications` vector for
/// that chunk, unless the failure is a validation error (empty config) or every
/// batch task fails, in which case the first error is returned.
/// \note Returns `Validation` when `config.definitions` is empty.
/// Returns the first batch error encountered when rendering the prompt or
/// calling the LLM fails for every batch; partial failures on a subset of
/// batches are recorded here as a `ProcessingWarning` on `result` instead of
/// aborting the whole run.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_classify_chunks(result: AlefHandle, config: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: result,
            expected_type: std::any::TypeId::of::<xberg::ExtractedDocument>(),
        });
        __alef_requests.push(HandleRequest {
            handle: config,
            expected_type: std::any::TypeId::of::<xberg::ChunkClassificationConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return -1;
                }
            }
        }
        if result == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'result'",
            );
            return -1;
        }
        let result_rs_ptr = match locked_handle_ptr::<xberg::ExtractedDocument>(&mut __alef_guards, result) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let result_rs = unsafe { &mut *result_rs_ptr };

        if config == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'config'",
            );
            return -1;
        }
        let config_rs_ptr = match locked_handle_ptr::<xberg::ChunkClassificationConfig>(&mut __alef_guards, config) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let config_rs = unsafe { &*config_rs_ptr };

        let result = get_ffi_runtime()
            .block_on(async { xberg::text::classification::classify_chunks(result_rs, &config_rs).await });
        match result {
            Ok(()) => 0,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            -1
        }
    }
}

/// Find unmarked claims in markdown text.
///
/// Returns lines that assert a claim but carry neither a footnote citation anchor (`[^...]`)
/// nor an inference marker (`[*inference*]`).
///
/// The heuristic is simple: a line that contains alphabetic words, ends with sentence punctuation,
/// and is not a heading, blank line, or markup-only line is considered a claim.
/// Exclude lines that appear in the citation block (after `---` + `<!-- citations ... -->`).
/// \param markdown The markdown text to search
/// \return A vector of trimmed line text strings for unmarked claims.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// let text = r#"This is a claim without citation.
/// Another claim with citation.[^1]
/// This is a claim with inference.[*inference*]
///
/// [^1]: Citation"#;
/// let unmarked = find_unmarked_claims(text);
/// assert_eq!(unmarked.len(), 1);
/// assert!(unmarked[0].contains("without citation"));
/// \endcode
#[cfg(feature = "markdown-footnotes")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_find_unmarked_claims(markdown: *const std::ffi::c_char) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("xberg_find_unmarked_claims", 0);
        if markdown.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'markdown'");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees markdown is a valid pointer; string is valid UTF-8 from caller.
        let markdown_rs = match unsafe { CStr::from_ptr(markdown) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'markdown'");
                return std::ptr::null_mut();
            }
        };

        let result = xberg::find_unmarked_claims(&markdown_rs);
        {
            match serde_json::to_string(&result) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("xberg_find_unmarked_claims", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("xberg_find_unmarked_claims", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("xberg_find_unmarked_claims", 0);
                    std::ptr::null_mut()
                }
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `xberg_find_unmarked_claims` on
/// this thread. Returns 0 when the primary call returned null or failed before producing a string.
/// Enables safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `xberg_find_unmarked_claims`.
#[cfg(feature = "markdown-footnotes")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_find_unmarked_claims_len(_markdown: *const std::ffi::c_char) -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("xberg_find_unmarked_claims"))
}

/// Verify that an excerpt appears verbatim in source text.
///
/// Performs exact matching by default. Also tries whitespace-normalized matching
/// (collapsing runs of whitespace on both sides) since PDF-extracted text often
/// has irregular spacing.
/// \param excerpt The text snippet to find
/// \param source_text The full source text to search
/// \return `true` if the excerpt appears (exactly or with normalized whitespace), `false` otherwise.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// let source = "The document states: Exact quoted text.";
/// let excerpt = "Exact quoted text";
/// assert!(verify_excerpt(excerpt, source));
///
/// // Whitespace normalization
/// let source2 = "Text with  irregular   spacing.";
/// let excerpt2 = "Text with irregular spacing";
/// assert!(verify_excerpt(excerpt2, source2));
/// \endcode
#[cfg(feature = "markdown-footnotes")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_verify_excerpt(
    excerpt: *const std::ffi::c_char,
    source_text: *const std::ffi::c_char,
) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if excerpt.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'excerpt'");
            return 0;
        }
        // SAFETY: null check above guarantees excerpt is a valid pointer; string is valid UTF-8 from caller.
        let excerpt_rs = match unsafe { CStr::from_ptr(excerpt) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'excerpt'");
                return 0;
            }
        };

        if source_text.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'source_text'");
            return 0;
        }
        // SAFETY: null check above guarantees source_text is a valid pointer; string is valid UTF-8 from caller.
        let source_text_rs = match unsafe { CStr::from_ptr(source_text) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'source_text'");
                return 0;
            }
        };

        let result = xberg::verify_excerpt(&excerpt_rs, &source_text_rs);
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Async wrapper over `embed_sparse`: runs the blocking ONNX inference on a
/// bounded blocking-task pool so it does not stall the async runtime.
///
/// Since v5.0.0.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(
    all(feature = "sparse-embeddings", feature = "tokio-runtime"),
    all(
        feature = "sparse-embedding-presets",
        not(feature = "sparse-embeddings"),
        feature = "tokio-runtime"
    )
))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embed_sparse_async(
    texts: *const std::ffi::c_char,
    config: AlefHandle,
) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("xberg_embed_sparse_async", 0);
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: config,
            expected_type: std::any::TypeId::of::<xberg::SparseEmbeddingConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if texts.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'texts'");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees texts is a valid pointer; string is valid UTF-8 from caller.
        let texts_rs_str = match unsafe { CStr::from_ptr(texts) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'texts'");
                return std::ptr::null_mut();
            }
        };
        let texts_rs = match serde_json::from_str::<Vec<String>>(texts_rs_str) {
            Ok(v) => v,
            Err(e) => {
                set_last_error(2, &e.to_string());
                return std::ptr::null_mut();
            }
        };

        if config == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'config'",
            );
            return std::ptr::null_mut();
        }
        let config_rs_ptr = match locked_handle_ptr::<xberg::SparseEmbeddingConfig>(&mut __alef_guards, config) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let config_rs = unsafe { &*config_rs_ptr };

        let result = get_ffi_runtime().block_on(async { xberg::embed_sparse_async(texts_rs, &config_rs).await });
        match result {
            Ok(val) => match serde_json::to_string(&val) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("xberg_embed_sparse_async", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("xberg_embed_sparse_async", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("xberg_embed_sparse_async", 0);
                    std::ptr::null_mut()
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `xberg_embed_sparse_async` on this
/// thread. Returns 0 when the primary call returned null or failed before producing a string. Enables
/// safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `xberg_embed_sparse_async`.
#[cfg(any(
    all(feature = "sparse-embeddings", feature = "tokio-runtime"),
    all(
        feature = "sparse-embedding-presets",
        not(feature = "sparse-embeddings"),
        feature = "tokio-runtime"
    )
))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embed_sparse_async_len(_texts: *const std::ffi::c_char, _config: AlefHandle) -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("xberg_embed_sparse_async"))
}

/// Score a query against a document using ColBERT's MaxSim operator: for each
/// query token vector, take the maximum dot product against any document
/// token vector, then sum across query tokens.
///
/// Returns `0.0` if `query` and `doc` have mismatched dimensionality, if either
/// has zero tokens, or if either is not well-formed per
/// `MultiVectorEmbedding.is_well_formed` (its `data` length does not match
/// `num_tokens * dim`).
///
/// Pure CPU primitive â available without ONNX Runtime.
///
/// Since v5.0.0.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(
    any(feature = "late-interaction-presets", feature = "late-interaction"),
    feature = "late-interaction-presets"
))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_max_sim_score(query: AlefHandle, doc: AlefHandle) -> f64 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: query,
            expected_type: std::any::TypeId::of::<xberg::MultiVectorEmbedding>(),
        });
        __alef_requests.push(HandleRequest {
            handle: doc,
            expected_type: std::any::TypeId::of::<xberg::MultiVectorEmbedding>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0.0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0.0;
                }
            }
        }
        if query == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'query'",
            );
            return 0.0;
        }
        let query_rs_ptr = match locked_handle_ptr::<xberg::MultiVectorEmbedding>(&mut __alef_guards, query) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0.0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let query_rs = unsafe { &*query_rs_ptr };

        if doc == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'doc'",
            );
            return 0.0;
        }
        let doc_rs_ptr = match locked_handle_ptr::<xberg::MultiVectorEmbedding>(&mut __alef_guards, doc) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0.0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let doc_rs = unsafe { &*doc_rs_ptr };

        xberg::max_sim_score(&query_rs, &doc_rs)
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0.0
        }
    }
}

/// Rank a set of documents against a query by MaxSim score, descending.
///
/// Mirrors the sort/truncate shape of `crate::reranking`'s `build_results`,
/// minus top-k truncation (callers slice the returned `Vec` themselves).
///
/// Pure CPU primitive â available without ONNX Runtime.
///
/// Since v5.0.0.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(
    any(feature = "late-interaction-presets", feature = "late-interaction"),
    feature = "late-interaction-presets"
))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_max_sim_rank(query: AlefHandle, docs: *const std::ffi::c_char) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("xberg_max_sim_rank", 0);
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: query,
            expected_type: std::any::TypeId::of::<xberg::MultiVectorEmbedding>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if query == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'query'",
            );
            return std::ptr::null_mut();
        }
        let query_rs_ptr = match locked_handle_ptr::<xberg::MultiVectorEmbedding>(&mut __alef_guards, query) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let query_rs = unsafe { &*query_rs_ptr };

        if docs.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'docs'");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees docs is a valid pointer; string is valid UTF-8 from caller.
        let docs_rs_str = match unsafe { CStr::from_ptr(docs) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'docs'");
                return std::ptr::null_mut();
            }
        };
        let docs_rs = match serde_json::from_str::<Vec<xberg::MultiVectorEmbedding>>(docs_rs_str) {
            Ok(v) => v,
            Err(e) => {
                set_last_error(2, &e.to_string());
                return std::ptr::null_mut();
            }
        };

        let result = xberg::max_sim_rank(&query_rs, &docs_rs);
        {
            match serde_json::to_string(&result) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("xberg_max_sim_rank", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("xberg_max_sim_rank", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("xberg_max_sim_rank", 0);
                    std::ptr::null_mut()
                }
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `xberg_max_sim_rank` on this
/// thread. Returns 0 when the primary call returned null or failed before producing a string. Enables
/// safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `xberg_max_sim_rank`.
#[cfg(any(
    any(feature = "late-interaction-presets", feature = "late-interaction"),
    feature = "late-interaction-presets"
))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_max_sim_rank_len(_query: AlefHandle, _docs: *const std::ffi::c_char) -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("xberg_max_sim_rank"))
}

/// Async wrapper over `embed_multi_vector`: runs the blocking ONNX inference
/// on a bounded blocking-task pool so it does not stall the async runtime.
///
/// Since v5.0.0.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(
    all(feature = "late-interaction", feature = "tokio-runtime"),
    all(
        feature = "late-interaction-presets",
        not(feature = "late-interaction"),
        feature = "tokio-runtime"
    )
))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embed_multi_vector_async(
    texts: *const std::ffi::c_char,
    config: AlefHandle,
    is_query: i32,
) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("xberg_embed_multi_vector_async", 0);
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: config,
            expected_type: std::any::TypeId::of::<xberg::LateInteractionConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if texts.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'texts'");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees texts is a valid pointer; string is valid UTF-8 from caller.
        let texts_rs_str = match unsafe { CStr::from_ptr(texts) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'texts'");
                return std::ptr::null_mut();
            }
        };
        let texts_rs = match serde_json::from_str::<Vec<String>>(texts_rs_str) {
            Ok(v) => v,
            Err(e) => {
                set_last_error(2, &e.to_string());
                return std::ptr::null_mut();
            }
        };

        if config == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'config'",
            );
            return std::ptr::null_mut();
        }
        let config_rs_ptr = match locked_handle_ptr::<xberg::LateInteractionConfig>(&mut __alef_guards, config) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let config_rs = unsafe { &*config_rs_ptr };

        let is_query_rs = is_query != 0;

        let result = get_ffi_runtime()
            .block_on(async { xberg::embed_multi_vector_async(texts_rs, &config_rs, is_query_rs).await });
        match result {
            Ok(val) => match serde_json::to_string(&val) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("xberg_embed_multi_vector_async", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("xberg_embed_multi_vector_async", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("xberg_embed_multi_vector_async", 0);
                    std::ptr::null_mut()
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `xberg_embed_multi_vector_async` on
/// this thread. Returns 0 when the primary call returned null or failed before producing a string.
/// Enables safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `xberg_embed_multi_vector_async`.
#[cfg(any(
    all(feature = "late-interaction", feature = "tokio-runtime"),
    all(
        feature = "late-interaction-presets",
        not(feature = "late-interaction"),
        feature = "tokio-runtime"
    )
))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_embed_multi_vector_async_len(
    _texts: *const std::ffi::c_char,
    _config: AlefHandle,
    _is_query: i32,
) -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("xberg_embed_multi_vector_async"))
}

/// Probe the backends and settings in `config` and report what will actually
/// execute on this host.
///
/// Runs no downloads and no billable API calls. Backends that are not compiled
/// in or whose models are not cached report `Skip` rather than failing.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_doctor(config: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        __alef_requests.push(HandleRequest {
            handle: config,
            expected_type: std::any::TypeId::of::<xberg::ExtractionConfig>(),
        });
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if config == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'config'",
            );
            return 0;
        }
        let config_rs_ptr = match locked_handle_ptr::<xberg::ExtractionConfig>(&mut __alef_guards, config) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let config_rs = unsafe { &*config_rs_ptr };

        let result = xberg::doctor(&config_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Install `PdfOxideWarningCapture` as the process-wide `log` backend,
/// exactly once.
///
/// If another component already installed a `log::Log` implementation (an
/// application wiring `env_logger`, for instance), `log::set_boxed_logger`
/// fails and this is a no-op: we do not fight over ownership of the global
/// logger slot, and we do not touch `log::set_max_level` unless our install
/// won, so we never silently raise or lower a level someone else configured.
/// In that case `pdf_oxide`'s glyph-drop records go wherever that other
/// logger sends them instead of into `take_pdf_oxide_render_warnings`.
/// **Opt-in.** Nothing calls this automatically, and that is deliberate: xberg
/// is a library, and `log` has exactly one global backend slot per process. A
/// library that claims it on its own behalf breaks its embedder â a host that
/// later calls `env_logger::init()` panics, and until this returns, every
/// `log` record in the process is routed here rather than wherever the host
/// intended. That decision belongs to the application, so it is exposed as a
/// call an application makes knowingly.
///
/// Returns `true` if this call (or an earlier one) installed the capture, and
/// `false` if some other component already owns the `log` backend â in which
/// case `pdf_oxide`'s glyph-drop records go to that logger and
/// `take_pdf_oxide_render_warnings` stays empty.
///
/// Without this call the #1364 warnings are not produced. The glyph drop
/// itself is decided inside `pdf_oxide`, which reports it only through
/// `log::warn!`; there is no return-value channel to read instead.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "pdf")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_install_pdf_render_diagnostics() -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = xberg::pdf::render::install_pdf_render_diagnostics();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Drain the glyph-drop `ProcessingWarning`s accumulated on this thread by
/// render calls since the last call to this function.
///
/// Callers that render pages as part of extraction should call this after
/// their render pass and merge the result into
/// `InternalDocument::processing_warnings` (see the module-level convention
/// in `crate::core::diagnostics`) so a page with missing glyphs is never
/// returned to the user without a signal. Warnings are already deduped
/// per-thread across all pages rendered before this call.
///
/// `pub` (rather than `pub(crate)`) so both in-tree render-consumers and the
/// regression test for #1364 can observe capture without depending on any
/// one extractor's internal state.
///
/// As of #340, `crate::extractors::pdf::mod` drains this unconditionally right
/// after assembling a document's `processing_warnings`, so every PDF
/// extraction that renders at least one page picks up any captured
/// glyph-drop warnings for free. ~keep: that drain only ever observes
/// warnings from render calls that happened on the *same OS thread* before it
/// ran, because `PDF_OXIDE_PENDING_WARNINGS` is thread-local. OCR page
/// rendering runs inline on the extracting task's thread, so it is covered.
/// Layout-detection rasterization runs inside `tokio::task::spawn_blocking`,
/// which always executes on a different OS thread, so this function alone
/// would never see those warnings. As of #353,
/// `extractors::pdf::layout_runner::run_layout_for_pdf_pages_async` drains
/// this function itself from inside its `spawn_blocking` closure â the only
/// place that can observe the blocking-pool thread's thread-local buffer â
/// and threads the drained warnings back through its return value for the
/// caller in `extractors::pdf::mod` to merge, so layout-path glyph drops are
/// no longer silently lost.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "pdf")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_take_pdf_oxide_render_warnings() -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("xberg_take_pdf_oxide_render_warnings", 0);
        let result = xberg::pdf::render::take_pdf_oxide_render_warnings();
        {
            match serde_json::to_string(&result) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("xberg_take_pdf_oxide_render_warnings", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("xberg_take_pdf_oxide_render_warnings", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("xberg_take_pdf_oxide_render_warnings", 0);
                    std::ptr::null_mut()
                }
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by
/// `xberg_take_pdf_oxide_render_warnings` on this thread. Returns 0 when the primary call returned null
/// or failed before producing a string. Enables safe slice construction in Zig and Java FFM Panama
/// without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `xberg_take_pdf_oxide_render_warnings`.
#[cfg(feature = "pdf")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_take_pdf_oxide_render_warnings_len() -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("xberg_take_pdf_oxide_render_warnings"))
}

/// Build the four (or three) token Whisper decoder prompt.
///
/// The canonical Whisper prompt is
/// `[<|startoftranscript|>, <|{lang}|>, <|transcribe|>, <|notimestamps|>]`.
/// When `timestamps` is `true`, the trailing `no_timestamps` token is omitted
/// so the model is free to emit `<|x.xx|>` timestamp tokens in its output
/// instead of being forced to suppress them.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "transcription")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_build_decoder_prompt_tokens(
    start_of_transcript: u32,
    lang_id: u32,
    transcribe: u32,
    no_timestamps: u32,
    timestamps: i32,
) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("xberg_build_decoder_prompt_tokens", 0);
        let start_of_transcript_rs = start_of_transcript;

        let lang_id_rs = lang_id;

        let transcribe_rs = transcribe;

        let no_timestamps_rs = no_timestamps;

        let timestamps_rs = timestamps != 0;

        let result = xberg::transcription::engine::build_decoder_prompt_tokens(
            start_of_transcript_rs,
            lang_id_rs,
            transcribe_rs,
            no_timestamps_rs,
            timestamps_rs,
        );
        {
            match serde_json::to_string(&result) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("xberg_build_decoder_prompt_tokens", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("xberg_build_decoder_prompt_tokens", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("xberg_build_decoder_prompt_tokens", 0);
                    std::ptr::null_mut()
                }
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `xberg_build_decoder_prompt_tokens`
/// on this thread. Returns 0 when the primary call returned null or failed before producing a string.
/// Enables safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `xberg_build_decoder_prompt_tokens`.
#[cfg(feature = "transcription")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_build_decoder_prompt_tokens_len(
    _start_of_transcript: u32,
    _lang_id: u32,
    _transcribe: u32,
    _no_timestamps: u32,
    _timestamps: i32,
) -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("xberg_build_decoder_prompt_tokens"))
}

/// Convert a raw Whisper timestamp token ID to a millisecond offset from the
/// start of the 30-second chunk it was decoded in.
///
/// `token_id` must be `>= timestamp_begin_id`; IDs below that are ordinary
/// vocabulary tokens, not timestamps.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "transcription")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_timestamp_token_to_ms(token_id: u32, timestamp_begin_id: u32) -> u32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let token_id_rs = token_id;

        let timestamp_begin_id_rs = timestamp_begin_id;

        xberg::transcription::engine::timestamp_token_to_ms(token_id_rs, timestamp_begin_id_rs)
    })) {
        Ok(value) => value,
        Err(_) => {
            let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            0
        }
    }
}

/// Write an error message string into an FFI out-error pointer.
///
/// # Safety
///
/// `out_error` must be null or a valid writable `*mut *mut c_char` pointer.
unsafe fn ffi_set_out_error(out_error: *mut *mut std::ffi::c_char, msg: &str) {
    if !out_error.is_null()
        && let Ok(cs) = std::ffi::CString::new(msg)
    {
        // SAFETY: out_error is non-null; caller must free this string.
        unsafe {
            *out_error = cs.into_raw();
        }
    }
}

/// VTable for C plugin bridges implementing the `OcrBackend` trait.
///
/// # Safety
///
/// All function pointers must be valid for the lifetime of any bridge created from
/// this vtable.  `free_user_data`, when non-null, is called once with `user_data`
/// when the bridge is dropped.
#[derive(Copy, Clone)]
#[repr(C)]
pub struct XbergOcrBackendVTable {
    /// Return a null-terminated UTF-8 name string into `out_name`; return 0 on success.
    pub name_fn: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_name: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Return a null-terminated UTF-8 version string into `out_version`; return 0 on success.
    pub version_fn: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_version: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Initialise the plugin; return 0 on success, non-zero on failure (error text in `out_error`).
    pub initialize_fn:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, out_error: *mut *mut std::ffi::c_char) -> i32>,
    /// Shut down the plugin; return 0 on success, non-zero on failure (error text in `out_error`).
    pub shutdown_fn:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, out_error: *mut *mut std::ffi::c_char) -> i32>,
    /// Process an image and extract text via OCR.
    ///
    /// # Arguments
    ///
    /// * `image_bytes` - Raw image data (JPEG, PNG, TIFF, etc.)
    /// * `config` - OCR configuration (language, PSM mode, etc.)
    ///
    /// # Returns
    ///
    /// An `ExtractedDocument` containing the extracted text and metadata.
    ///
    /// # Errors
    ///
    /// - `XbergError::Ocr` - OCR processing failed
    /// - `XbergError::Validation` - Invalid image format or configuration
    /// - `XbergError::Io` - I/O errors (these always bubble up)
    ///
    /// # Reading `backend_options`
    ///
    /// Backends that support runtime tuning can read `config.backend_options` and
    /// deserialize only the keys they care about. Unknown keys are silently ignored,
    /// so multiple backends can coexist in a pipeline without key conflicts.
    ///
    /// ```rust
    /// async fn process_image(&self, image_bytes: &[u8], config: &OcrConfig) -> Result<ExtractedDocument> {
    /// // Read backend-specific options; unknown keys are silently ignored.
    /// let fast_mode = config.backend_options
    /// .as_ref()
    /// .and_then(|v| v.get("mode"))
    /// .and_then(|v| v.as_str())
    /// .map(|s| s == "fast")
    /// .unwrap_or(false);
    ///
    /// if image_bytes.is_empty() {
    /// return Err(xberg::XbergError::Validation {
    /// message: "Empty image data".to_string(),
    /// source: None,
    /// });
    /// }
    ///
    /// let text = if fast_mode {
    /// "Fast OCR result".to_string()
    /// } else {
    /// format!("Extracted text in language: {:?}", config.language)
    /// };
    ///
    /// let mut document = ExtractedDocument::default();
    /// document.content = text;
    /// document.mime_type = Cow::Borrowed("text/plain");
    /// Ok(document)
    /// }
    /// ```
    pub process_image: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            image_bytes: *const u8,
            image_bytes_len: usize,
            config: *const std::ffi::c_char,
            out_result: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Process a file and extract text via OCR.
    ///
    /// Default implementation reads the file and calls `process_image`.
    /// Override for custom file handling or optimizations.
    ///
    /// # Arguments
    ///
    /// * `path` - Path to the image file
    /// * `config` - OCR configuration
    ///
    /// # Errors
    ///
    /// Same as `process_image`, plus file I/O errors.
    pub process_image_file: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            path: *const std::ffi::c_char,
            config: *const std::ffi::c_char,
            out_result: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Check if this backend supports a given language code.
    ///
    /// # Arguments
    ///
    /// * `lang` - ISO 639-2/3 language code (e.g., "eng", "deu", "fra")
    ///
    /// # Returns
    ///
    /// `true` if the language is supported, `false` otherwise.
    ///
    /// # Example
    ///
    /// ```rust
    /// fn supports_language(&self, lang: &str) -> bool {
    /// self.languages.contains(&lang.to_string())
    /// }
    /// ```
    pub supports_language:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, lang: *const std::ffi::c_char) -> i32>,
    /// Get the backend type identifier.
    ///
    /// # Returns
    ///
    /// The backend type enum value.
    ///
    /// # Example
    ///
    /// ```rust
    /// fn backend_type(&self) -> OcrBackendType {
    /// OcrBackendType::Tesseract
    /// }
    /// ```
    pub backend_type: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_result: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Optional: Get a list of all supported languages.
    ///
    /// Defaults to empty list. Override to provide comprehensive language support info.
    pub supported_languages: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_result: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Optional: Check if the backend supports table detection.
    ///
    /// Defaults to `false`. Override if your backend can detect and extract tables.
    pub supports_table_detection: Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void) -> i32>,
    /// Check if the backend supports direct document-level processing (e.g. for PDFs).
    ///
    /// Defaults to `false`. Override if the backend has optimized document processing.
    pub supports_document_processing: Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void) -> i32>,
    /// Declare that this backend emits structured markdown directly (tables, headings, lists)
    /// and downstream layout reconstruction should be skipped.
    ///
    /// Defaults to `false` — classical OCR backends (Tesseract, PaddleOCR classical) return
    /// plain text per detected region. End-to-end VLM backends (PaddleOCR-VL, GOT-OCR 2.0)
    /// emit markdown in one forward pass and should override this to `true`.
    pub emits_structured_markdown: Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void) -> i32>,
    /// Process a document file directly via OCR.
    ///
    /// Only called if `supports_document_processing` returns `true`.
    ///
    /// # Arguments
    ///
    /// * `path` - Path to the document file (e.g. .pdf)
    /// * `config` - OCR configuration
    pub process_document: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            _path: *const std::ffi::c_char,
            _config: *const std::ffi::c_char,
            out_result: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Optional string destructor: called for strings returned by vtable callbacks.
    pub free_string: Option<unsafe extern "C" fn(*mut std::ffi::c_char)>,
    /// Optional destructor: called once with `user_data` when the bridge is dropped.
    pub free_user_data: Option<unsafe extern "C" fn(*mut std::ffi::c_void)>,
}

// SAFETY: all fields are function pointers and free_user_data, which are Send + Sync.
unsafe impl Send for XbergOcrBackendVTable {}
unsafe impl Sync for XbergOcrBackendVTable {}

/// Rust-side bridge that holds a C vtable pointer and opaque `user_data`.
///
/// Implements `OcrBackend` by forwarding calls through the vtable.
pub struct XbergOcrBackendBridge {
    vtable: XbergOcrBackendVTable,
    user_data: *const std::ffi::c_void,
    cached_name: String,
    cached_version: String,
}

impl std::fmt::Debug for XbergOcrBackendBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.debug_struct("XbergOcrBackendBridge")
            .field("cached_name", &self.cached_name)
            .field("cached_version", &self.cached_version)
            .finish_non_exhaustive()
    }
}

// SAFETY: The caller is responsible for ensuring `user_data` is safe to send across
// thread boundaries. This is documented in `XbergOcrBackendVTable` and the registration function.
unsafe impl Send for XbergOcrBackendBridge {}
unsafe impl Sync for XbergOcrBackendBridge {}

impl Drop for XbergOcrBackendBridge {
    fn drop(&mut self) {
        if let Some(free_fn) = self.vtable.free_user_data {
            // SAFETY: free_fn is a valid function pointer; user_data is the pointer
            // originally provided at registration. Called exactly once here.
            unsafe { free_fn(self.user_data as *mut std::ffi::c_void) }
        }
    }
}

impl XbergOcrBackendBridge {
    /// Create a new bridge from a vtable and opaque user_data pointer.
    ///
    /// # Safety
    ///
    /// `vtable` must remain valid for the lifetime of the returned bridge.
    /// `user_data` must be valid for any thread that calls methods on this bridge.
    /// All required fn pointers in `vtable` must be non-null.
    pub unsafe fn new(name: String, vtable: XbergOcrBackendVTable, user_data: *const std::ffi::c_void) -> Self {
        Self {
            vtable,
            user_data,
            cached_name: name,
            cached_version: String::new(),
        }
    }
}

impl xberg::plugins::Plugin for XbergOcrBackendBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }
    fn version(&self) -> String {
        let Some(fp) = self.vtable.version_fn else {
            return String::new();
        };
        let mut _out: *mut std::ffi::c_char = std::ptr::null_mut();
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out, &mut _out_error) };
        if rc != 0 {
            if !_out_error.is_null() {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let _ = unsafe { std::ffi::CStr::from_ptr(_out_error) }.to_string_lossy();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
            }
            return self.cached_version.clone();
        }
        if _out.is_null() {
            return self.cached_version.clone();
        }
        // SAFETY: _out is a callee-allocated NUL-terminated string.
        let version = unsafe { std::ffi::CStr::from_ptr(_out) }.to_string_lossy().into_owned();
        if let Some(free_fn) = self.vtable.free_string {
            // SAFETY: free_fn is the vtable-provided destructor for callback strings.
            unsafe { free_fn(_out) };
        }
        version
    }
    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        let Some(fp) = self.vtable.initialize_fn else {
            return Ok(());
        };
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out_error) };
        if rc != 0 {
            let msg = if _out_error.is_null() {
                format!("initialize returned {}", rc)
            } else {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            return Err(xberg::XbergError::Plugin {
                message: msg,
                plugin_name: String::new(),
            });
        }
        Ok(())
    }
    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        let Some(fp) = self.vtable.shutdown_fn else {
            return Ok(());
        };
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out_error) };
        if rc != 0 {
            let msg = if _out_error.is_null() {
                format!("shutdown returned {}", rc)
            } else {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            return Err(xberg::XbergError::Plugin {
                message: msg,
                plugin_name: String::new(),
            });
        }
        Ok(())
    }
}

#[async_trait::async_trait]
impl xberg::OcrBackend for XbergOcrBackendBridge {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        struct _SendFn<F>(F);
        // SAFETY: caller guarantees vtable fn pointers and user_data are valid across threads.
        unsafe impl<F> Send for _SendFn<F> {}
        impl<F: FnOnce() -> R, R> _SendFn<F> {
            fn call(self) -> R {
                (self.0)()
            }
        }

        let _cached_name = self.cached_name.clone();
        let vtable = self.vtable;
        let user_data = self.user_data;
        let image_bytes = image_bytes.to_vec();
        let config = config.clone();

        let _task = _SendFn(move || {
            // Inline the sync body:
            let Some(fp) = vtable.process_image else {
                return Err(Box::from("vtable.process_image is null — bridge not initialised"));
            };
            let _config_json = serde_json::to_string(&config).unwrap_or_default();
            let _config_cs = match std::ffi::CString::new(_config_json) {
                Ok(s) => s,
                Err(_) => {
                    return Err(Box::from("nul byte in serialized param config"));
                }
            };
            let config_ptr = _config_cs.as_ptr();
            let mut _out_result: *mut std::ffi::c_char = std::ptr::null_mut();
            let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
            // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
            // user_data validity is the caller's responsibility (documented in the vtable API).
            let _rc = unsafe {
                fp(
                    user_data,
                    image_bytes.as_ptr(),
                    image_bytes.len(),
                    config_ptr,
                    &mut _out_result,
                    &mut _out_error,
                )
            };
            if _rc != 0 {
                let msg = if _out_error.is_null() {
                    format!("vtable.process_image returned error code {}", _rc)
                } else {
                    // SAFETY: out_error was written by the callee as a valid NUL-terminated string.
                    let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                        .to_string_lossy()
                        .into_owned();
                    if let Some(free_fn) = vtable.free_string {
                        // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                        unsafe { free_fn(_out_error) };
                    }
                    msg
                };
                return Err(Box::from(msg));
            }
            if _out_result.is_null() {
                return Err(Box::from("vtable.process_image returned null out_result"));
            }
            // SAFETY: out_result was written by the callee as a valid NUL-terminated string.
            let json = unsafe { std::ffi::CStr::from_ptr(_out_result) }
                .to_string_lossy()
                .into_owned();
            if let Some(free_fn) = vtable.free_string {
                // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                unsafe { free_fn(_out_result) };
            }
            serde_json::from_str::<xberg::ExtractedDocument>(&json)
                .map_err(|e| Box::from(e.to_string()) as Box<dyn std::error::Error + Send + Sync>)
        });
        tokio::task::spawn_blocking(move || _task.call())
            .await
            .map_err(|e| xberg::XbergError::Other(format!("spawn_blocking failed in process_image: {}", e)))?
            .map_err(|e: Box<dyn std::error::Error + Send + Sync>| xberg::XbergError::Other(e.to_string()))
    }

    async fn process_image_file(
        &self,
        path: &std::path::Path,
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        struct _SendFn<F>(F);
        // SAFETY: caller guarantees vtable fn pointers and user_data are valid across threads.
        unsafe impl<F> Send for _SendFn<F> {}
        impl<F: FnOnce() -> R, R> _SendFn<F> {
            fn call(self) -> R {
                (self.0)()
            }
        }

        let _cached_name = self.cached_name.clone();
        let vtable = self.vtable;
        let user_data = self.user_data;
        let path = path.to_path_buf();
        let config = config.clone();

        let _task = _SendFn(move || {
            // Inline the sync body:
            let Some(fp) = vtable.process_image_file else {
                return Err(Box::from("vtable.process_image_file is null — bridge not initialised"));
            };
            let _path_cs = match std::ffi::CString::new(path.to_string_lossy().as_ref()) {
                Ok(s) => s,
                Err(_) => {
                    return Err(Box::from("nul byte in param path"));
                }
            };
            let path_ptr = _path_cs.as_ptr();
            let _config_json = serde_json::to_string(&config).unwrap_or_default();
            let _config_cs = match std::ffi::CString::new(_config_json) {
                Ok(s) => s,
                Err(_) => {
                    return Err(Box::from("nul byte in serialized param config"));
                }
            };
            let config_ptr = _config_cs.as_ptr();
            let mut _out_result: *mut std::ffi::c_char = std::ptr::null_mut();
            let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
            // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
            // user_data validity is the caller's responsibility (documented in the vtable API).
            let _rc = unsafe { fp(user_data, path_ptr, config_ptr, &mut _out_result, &mut _out_error) };
            if _rc != 0 {
                let msg = if _out_error.is_null() {
                    format!("vtable.process_image_file returned error code {}", _rc)
                } else {
                    // SAFETY: out_error was written by the callee as a valid NUL-terminated string.
                    let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                        .to_string_lossy()
                        .into_owned();
                    if let Some(free_fn) = vtable.free_string {
                        // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                        unsafe { free_fn(_out_error) };
                    }
                    msg
                };
                return Err(Box::from(msg));
            }
            if _out_result.is_null() {
                return Err(Box::from("vtable.process_image_file returned null out_result"));
            }
            // SAFETY: out_result was written by the callee as a valid NUL-terminated string.
            let json = unsafe { std::ffi::CStr::from_ptr(_out_result) }
                .to_string_lossy()
                .into_owned();
            if let Some(free_fn) = vtable.free_string {
                // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                unsafe { free_fn(_out_result) };
            }
            serde_json::from_str::<xberg::ExtractedDocument>(&json)
                .map_err(|e| Box::from(e.to_string()) as Box<dyn std::error::Error + Send + Sync>)
        });
        tokio::task::spawn_blocking(move || _task.call())
            .await
            .map_err(|e| xberg::XbergError::Other(format!("spawn_blocking failed in process_image_file: {}", e)))?
            .map_err(|e: Box<dyn std::error::Error + Send + Sync>| xberg::XbergError::Other(e.to_string()))
    }

    fn supports_language(&self, lang: &str) -> bool {
        let Some(fp) = self.vtable.supports_language else {
            tracing::warn!(
                wrapper = "XbergOcrBackendBridge",
                method = "supports_language",
                "vtable slot for host callback is not initialised; returning default"
            );
            return false;
        };
        let _lang_cs = match std::ffi::CString::new(lang) {
            Ok(s) => s,
            Err(_) => {
                return false;
            }
        };
        let lang_ptr = _lang_cs.as_ptr();
        // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
        // user_data validity is the caller's responsibility (documented in the vtable API).
        let _rc = unsafe { fp(self.user_data, lang_ptr) };
        _rc != 0
    }

    fn backend_type(&self) -> xberg::OcrBackendType {
        let Some(fp) = self.vtable.backend_type else {
            tracing::warn!(
                wrapper = "XbergOcrBackendBridge",
                method = "backend_type",
                "vtable slot for host callback is not initialised; returning default"
            );
            return Default::default();
        };
        let mut _out_result: *mut std::ffi::c_char = std::ptr::null_mut();
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
        // user_data validity is the caller's responsibility (documented in the vtable API).
        let _rc = unsafe { fp(self.user_data, &mut _out_result, &mut _out_error) };
        if _rc != 0 {
            let msg = if _out_error.is_null() {
                format!("vtable.backend_type returned error code {}", _rc)
            } else {
                // SAFETY: out_error was written by the callee as a valid NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            tracing::warn!(wrapper = "XbergOcrBackendBridge", method = "backend_type", error = %msg, "host callback failed; returning default");
            return Default::default();
        }
        if _out_result.is_null() {
            tracing::warn!(
                wrapper = "XbergOcrBackendBridge",
                method = "backend_type",
                "host wrote no result; returning default"
            );
            return Default::default();
        }
        // SAFETY: out_result was written by the callee as a valid NUL-terminated string.
        let json = unsafe { std::ffi::CStr::from_ptr(_out_result) }
            .to_string_lossy()
            .into_owned();
        if let Some(free_fn) = self.vtable.free_string {
            // SAFETY: free_fn is the vtable-provided destructor for callback strings.
            unsafe { free_fn(_out_result) };
        }
        serde_json::from_str::<xberg::OcrBackendType>(&json).unwrap_or_else(|e| {
            tracing::warn!(wrapper = "XbergOcrBackendBridge", method = "backend_type", error = %e, "host returned an unparseable value; returning default");
            Default::default()
        })
    }

    fn supported_languages(&self) -> Vec<String> {
        let Some(fp) = self.vtable.supported_languages else {
            tracing::warn!(
                wrapper = "XbergOcrBackendBridge",
                method = "supported_languages",
                "vtable slot for host callback is not initialised; returning default"
            );
            return Vec::new();
        };
        let mut _out_result: *mut std::ffi::c_char = std::ptr::null_mut();
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
        // user_data validity is the caller's responsibility (documented in the vtable API).
        let _rc = unsafe { fp(self.user_data, &mut _out_result, &mut _out_error) };
        if _rc != 0 {
            let msg = if _out_error.is_null() {
                format!("vtable.supported_languages returned error code {}", _rc)
            } else {
                // SAFETY: out_error was written by the callee as a valid NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            tracing::warn!(wrapper = "XbergOcrBackendBridge", method = "supported_languages", error = %msg, "host callback failed; returning default");
            return Default::default();
        }
        if _out_result.is_null() {
            tracing::warn!(
                wrapper = "XbergOcrBackendBridge",
                method = "supported_languages",
                "host wrote no result; returning default"
            );
            return Default::default();
        }
        // SAFETY: out_result was written by the callee as a valid NUL-terminated string.
        let json = unsafe { std::ffi::CStr::from_ptr(_out_result) }
            .to_string_lossy()
            .into_owned();
        if let Some(free_fn) = self.vtable.free_string {
            // SAFETY: free_fn is the vtable-provided destructor for callback strings.
            unsafe { free_fn(_out_result) };
        }
        serde_json::from_str::<Vec<String>>(&json).unwrap_or_else(|e| {
            tracing::warn!(wrapper = "XbergOcrBackendBridge", method = "supported_languages", error = %e, "host returned an unparseable value; returning default");
            Default::default()
        })
    }

    fn supports_table_detection(&self) -> bool {
        let Some(fp) = self.vtable.supports_table_detection else {
            tracing::warn!(
                wrapper = "XbergOcrBackendBridge",
                method = "supports_table_detection",
                "vtable slot for host callback is not initialised; returning default"
            );
            return false;
        };
        // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
        // user_data validity is the caller's responsibility (documented in the vtable API).
        let _rc = unsafe { fp(self.user_data) };
        _rc != 0
    }

    fn supports_document_processing(&self) -> bool {
        let Some(fp) = self.vtable.supports_document_processing else {
            tracing::warn!(
                wrapper = "XbergOcrBackendBridge",
                method = "supports_document_processing",
                "vtable slot for host callback is not initialised; returning default"
            );
            return false;
        };
        // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
        // user_data validity is the caller's responsibility (documented in the vtable API).
        let _rc = unsafe { fp(self.user_data) };
        _rc != 0
    }

    fn emits_structured_markdown(&self) -> bool {
        let Some(fp) = self.vtable.emits_structured_markdown else {
            tracing::warn!(
                wrapper = "XbergOcrBackendBridge",
                method = "emits_structured_markdown",
                "vtable slot for host callback is not initialised; returning default"
            );
            return false;
        };
        // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
        // user_data validity is the caller's responsibility (documented in the vtable API).
        let _rc = unsafe { fp(self.user_data) };
        _rc != 0
    }

    async fn process_document(
        &self,
        _path: &std::path::Path,
        _config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        struct _SendFn<F>(F);
        // SAFETY: caller guarantees vtable fn pointers and user_data are valid across threads.
        unsafe impl<F> Send for _SendFn<F> {}
        impl<F: FnOnce() -> R, R> _SendFn<F> {
            fn call(self) -> R {
                (self.0)()
            }
        }

        let _cached_name = self.cached_name.clone();
        let vtable = self.vtable;
        let user_data = self.user_data;
        let _path = _path.to_path_buf();
        let _config = _config.clone();

        let _task = _SendFn(move || {
            // Inline the sync body:
            let Some(fp) = vtable.process_document else {
                return Err(Box::from("vtable.process_document is null — bridge not initialised"));
            };
            let __path_cs = match std::ffi::CString::new(_path.to_string_lossy().as_ref()) {
                Ok(s) => s,
                Err(_) => {
                    return Err(Box::from("nul byte in param _path"));
                }
            };
            let _path_ptr = __path_cs.as_ptr();
            let __config_json = serde_json::to_string(&_config).unwrap_or_default();
            let __config_cs = match std::ffi::CString::new(__config_json) {
                Ok(s) => s,
                Err(_) => {
                    return Err(Box::from("nul byte in serialized param _config"));
                }
            };
            let _config_ptr = __config_cs.as_ptr();
            let mut _out_result: *mut std::ffi::c_char = std::ptr::null_mut();
            let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
            // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
            // user_data validity is the caller's responsibility (documented in the vtable API).
            let _rc = unsafe { fp(user_data, _path_ptr, _config_ptr, &mut _out_result, &mut _out_error) };
            if _rc != 0 {
                let msg = if _out_error.is_null() {
                    format!("vtable.process_document returned error code {}", _rc)
                } else {
                    // SAFETY: out_error was written by the callee as a valid NUL-terminated string.
                    let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                        .to_string_lossy()
                        .into_owned();
                    if let Some(free_fn) = vtable.free_string {
                        // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                        unsafe { free_fn(_out_error) };
                    }
                    msg
                };
                return Err(Box::from(msg));
            }
            if _out_result.is_null() {
                return Err(Box::from("vtable.process_document returned null out_result"));
            }
            // SAFETY: out_result was written by the callee as a valid NUL-terminated string.
            let json = unsafe { std::ffi::CStr::from_ptr(_out_result) }
                .to_string_lossy()
                .into_owned();
            if let Some(free_fn) = vtable.free_string {
                // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                unsafe { free_fn(_out_result) };
            }
            serde_json::from_str::<xberg::ExtractedDocument>(&json)
                .map_err(|e| Box::from(e.to_string()) as Box<dyn std::error::Error + Send + Sync>)
        });
        tokio::task::spawn_blocking(move || _task.call())
            .await
            .map_err(|e| xberg::XbergError::Other(format!("spawn_blocking failed in process_document: {}", e)))?
            .map_err(|e: Box<dyn std::error::Error + Send + Sync>| xberg::XbergError::Other(e.to_string()))
    }
}

/// Register a C plugin implementing `OcrBackend` via a vtable.
///
/// # Parameters
///
/// - `name`: null-terminated UTF-8 plugin name. Must not be null.
/// - `vtable`: vtable with function pointers implementing the trait.
/// - `user_data`: opaque pointer forwarded to every vtable function.
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// All function pointers in `vtable` must remain valid until the plugin is
/// unregistered. `user_data` must be safe to use from any thread that calls
/// into the plugin.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_register_ocr_backend(
    name: *const std::ffi::c_char,
    vtable: *const XbergOcrBackendVTable,
    user_data: *const std::ffi::c_void,
    out_error: *mut *mut std::ffi::c_char,
) -> i32 {
    catch_ffi_panic(1, || {
        if name.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "name must not be null") };
            return 1;
        }
        if vtable.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable must not be null") };
            return 1;
        }
        // SAFETY: null check above guarantees vtable is a valid pointer.
        let vtable_ref = unsafe { &*vtable };
        if vtable_ref.free_string.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.free_string must not be null") };
            return 1;
        }
        if vtable_ref.process_image.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.process_image must not be null") };
            return 1;
        }
        if vtable_ref.supports_language.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.supports_language must not be null") };
            return 1;
        }
        if vtable_ref.backend_type.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.backend_type must not be null") };
            return 1;
        }

        // SAFETY: name is non-null (checked above); it points to a valid C string.
        let plugin_name = match unsafe { std::ffi::CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_owned(),
            Err(_) => {
                // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
                unsafe { ffi_set_out_error(out_error, "name is not valid UTF-8") };
                return 1;
            }
        };

        // SAFETY: vtable pointer and user_data validity is the caller's responsibility.
        let bridge = unsafe { XbergOcrBackendBridge::new(plugin_name, *vtable_ref, user_data) };
        let arc: Arc<dyn xberg::OcrBackend> = Arc::new(bridge);

        let registry = xberg::plugins::registry::get_ocr_backend_registry();
        let mut registry = registry.write();

        if let Err(e) = registry.register(arc) {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// Unregister a previously registered C plugin by name.
///
/// # Parameters
///
/// - `name`: null-terminated UTF-8 plugin name. Must not be null.
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// `name` must point to a valid null-terminated C string.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_unregister_ocr_backend(
    name: *const std::ffi::c_char,
    out_error: *mut *mut std::ffi::c_char,
) -> i32 {
    catch_ffi_panic(1, || {
        if name.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "name must not be null") };
            return 1;
        }
        // SAFETY: name is non-null (checked above); it points to a valid C string.
        let plugin_name = match unsafe { std::ffi::CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
                unsafe { ffi_set_out_error(out_error, "name is not valid UTF-8") };
                return 1;
            }
        };

        let registry = xberg::plugins::registry::get_ocr_backend_registry();
        let mut registry = registry.write();
        if let Err(e) = registry.remove(plugin_name) {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// Remove all registered C plugins of this trait.
///
/// # Parameters
///
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// `out_error` may be null. When non-null, the caller owns the resulting string
/// and must free it with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_clear_ocr_backend(out_error: *mut *mut std::ffi::c_char) -> i32 {
    catch_ffi_panic(1, || {
        let registry = xberg::plugins::registry::get_ocr_backend_registry();
        let mut registry = registry.write();
        if let Err(e) = registry.clear() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// VTable for C plugin bridges implementing the `PostProcessor` trait.
///
/// # Safety
///
/// All function pointers must be valid for the lifetime of any bridge created from
/// this vtable.  `free_user_data`, when non-null, is called once with `user_data`
/// when the bridge is dropped.
#[derive(Copy, Clone)]
#[repr(C)]
pub struct XbergPostProcessorVTable {
    /// Return a null-terminated UTF-8 name string into `out_name`; return 0 on success.
    pub name_fn: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_name: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Return a null-terminated UTF-8 version string into `out_version`; return 0 on success.
    pub version_fn: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_version: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Initialise the plugin; return 0 on success, non-zero on failure (error text in `out_error`).
    pub initialize_fn:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, out_error: *mut *mut std::ffi::c_char) -> i32>,
    /// Shut down the plugin; return 0 on success, non-zero on failure (error text in `out_error`).
    pub shutdown_fn:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, out_error: *mut *mut std::ffi::c_char) -> i32>,
    /// Process an extraction result.
    ///
    /// Transform or enrich the extraction result. Can modify:
    /// - `content` - The extracted text
    /// - `metadata` - Add or update metadata fields
    /// - `tables` - Modify or enhance table data
    ///
    /// # Arguments
    ///
    /// * `result` - Mutable reference to the extraction result to process
    /// * `config` - Extraction configuration
    ///
    /// # Returns
    ///
    /// `Ok(())` if processing succeeded, `Err(...)` for fatal failures.
    ///
    /// # Errors
    ///
    /// Return errors for fatal processing failures. Non-fatal errors should be
    /// captured in metadata directly on the result.
    ///
    /// # Performance
    ///
    /// This signature avoids unnecessary cloning of large extraction results by
    /// taking a mutable reference instead of ownership. Processors modify the
    /// result in place.
    ///
    /// # Example - Language Detection
    ///
    /// ```rust
    /// async fn process(&self, result: &mut ExtractedDocument, config: &ExtractionConfig)
    /// -> Result<()> {
    /// // Detect language (simplified - use real detection library in practice)
    /// let language = "en"; // Placeholder detection
    ///
    /// // Add to metadata
    /// result.metadata.additional.insert("detected_language".to_string().into(), serde_json::json!(language));
    ///
    /// Ok(())
    /// }
    /// ```
    ///
    /// # Example - Text Cleaning
    ///
    /// ```rust
    /// async fn process(&self, result: &mut ExtractedDocument, config: &ExtractionConfig)
    /// -> Result<()> {
    /// // Remove excessive whitespace
    /// result.content = result
    /// .content
    /// .split_whitespace()
    /// .collect::<Vec<_>>()
    /// .join(" ");
    ///
    /// Ok(())
    /// }
    /// ```
    pub process: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            result: *const std::ffi::c_char,
            config: *const std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Get the processing stage for this post-processor.
    ///
    /// Determines when this processor runs in the pipeline.
    ///
    /// # Returns
    ///
    /// The `ProcessingStage` (Early, Middle, or Late).
    ///
    /// # Example
    ///
    /// ```rust
    /// fn processing_stage(&self) -> ProcessingStage {
    /// ProcessingStage::Early  // Run before other processors
    /// }
    /// ```
    pub processing_stage: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_result: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Optional: Check if this processor should run for a given result.
    ///
    /// Allows conditional processing based on MIME type, metadata, or content.
    /// Defaults to `true` (always run).
    ///
    /// # Arguments
    ///
    /// * `result` - The extraction result to check
    /// * `config` - Extraction configuration
    ///
    /// # Returns
    ///
    /// `true` if the processor should run, `false` to skip.
    ///
    /// # Example
    ///
    /// ```rust
    /// Only process PDF documents
    /// fn should_process(&self, result: &ExtractedDocument, config: &ExtractionConfig) -> bool {
    /// result.mime_type == "application/pdf"
    /// }
    /// ```
    pub should_process: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            _result: *const std::ffi::c_char,
            _config: *const std::ffi::c_char,
        ) -> i32,
    >,
    /// Optional: Estimate processing time in milliseconds.
    ///
    /// Used for logging and debugging. Defaults to 0 (unknown).
    ///
    /// # Arguments
    ///
    /// * `result` - The extraction result to estimate for
    ///
    /// # Returns
    ///
    /// Estimated processing time in milliseconds.
    pub estimated_duration_ms:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, _result: *const std::ffi::c_char) -> u64>,
    /// Execution priority within the processing stage.
    ///
    /// Higher values run first within the same `ProcessingStage`. Defaults to 50.
    /// Use 0-49 for fallback processors, 50 for normal processors, and 51-255
    /// for high-priority processors that should run early in their stage.
    pub priority: Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void) -> i32>,
    /// Optional string destructor: called for strings returned by vtable callbacks.
    pub free_string: Option<unsafe extern "C" fn(*mut std::ffi::c_char)>,
    /// Optional destructor: called once with `user_data` when the bridge is dropped.
    pub free_user_data: Option<unsafe extern "C" fn(*mut std::ffi::c_void)>,
}

// SAFETY: all fields are function pointers and free_user_data, which are Send + Sync.
unsafe impl Send for XbergPostProcessorVTable {}
unsafe impl Sync for XbergPostProcessorVTable {}

/// Rust-side bridge that holds a C vtable pointer and opaque `user_data`.
///
/// Implements `PostProcessor` by forwarding calls through the vtable.
pub struct XbergPostProcessorBridge {
    vtable: XbergPostProcessorVTable,
    user_data: *const std::ffi::c_void,
    cached_name: String,
    cached_version: String,
}

impl std::fmt::Debug for XbergPostProcessorBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.debug_struct("XbergPostProcessorBridge")
            .field("cached_name", &self.cached_name)
            .field("cached_version", &self.cached_version)
            .finish_non_exhaustive()
    }
}

// SAFETY: The caller is responsible for ensuring `user_data` is safe to send across
// thread boundaries. This is documented in `XbergPostProcessorVTable` and the registration function.
unsafe impl Send for XbergPostProcessorBridge {}
unsafe impl Sync for XbergPostProcessorBridge {}

impl Drop for XbergPostProcessorBridge {
    fn drop(&mut self) {
        if let Some(free_fn) = self.vtable.free_user_data {
            // SAFETY: free_fn is a valid function pointer; user_data is the pointer
            // originally provided at registration. Called exactly once here.
            unsafe { free_fn(self.user_data as *mut std::ffi::c_void) }
        }
    }
}

impl XbergPostProcessorBridge {
    /// Create a new bridge from a vtable and opaque user_data pointer.
    ///
    /// # Safety
    ///
    /// `vtable` must remain valid for the lifetime of the returned bridge.
    /// `user_data` must be valid for any thread that calls methods on this bridge.
    /// All required fn pointers in `vtable` must be non-null.
    pub unsafe fn new(name: String, vtable: XbergPostProcessorVTable, user_data: *const std::ffi::c_void) -> Self {
        Self {
            vtable,
            user_data,
            cached_name: name,
            cached_version: String::new(),
        }
    }
}

impl xberg::plugins::Plugin for XbergPostProcessorBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }
    fn version(&self) -> String {
        let Some(fp) = self.vtable.version_fn else {
            return String::new();
        };
        let mut _out: *mut std::ffi::c_char = std::ptr::null_mut();
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out, &mut _out_error) };
        if rc != 0 {
            if !_out_error.is_null() {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let _ = unsafe { std::ffi::CStr::from_ptr(_out_error) }.to_string_lossy();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
            }
            return self.cached_version.clone();
        }
        if _out.is_null() {
            return self.cached_version.clone();
        }
        // SAFETY: _out is a callee-allocated NUL-terminated string.
        let version = unsafe { std::ffi::CStr::from_ptr(_out) }.to_string_lossy().into_owned();
        if let Some(free_fn) = self.vtable.free_string {
            // SAFETY: free_fn is the vtable-provided destructor for callback strings.
            unsafe { free_fn(_out) };
        }
        version
    }
    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        let Some(fp) = self.vtable.initialize_fn else {
            return Ok(());
        };
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out_error) };
        if rc != 0 {
            let msg = if _out_error.is_null() {
                format!("initialize returned {}", rc)
            } else {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            return Err(xberg::XbergError::Plugin {
                message: msg,
                plugin_name: String::new(),
            });
        }
        Ok(())
    }
    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        let Some(fp) = self.vtable.shutdown_fn else {
            return Ok(());
        };
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out_error) };
        if rc != 0 {
            let msg = if _out_error.is_null() {
                format!("shutdown returned {}", rc)
            } else {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            return Err(xberg::XbergError::Plugin {
                message: msg,
                plugin_name: String::new(),
            });
        }
        Ok(())
    }
}

#[async_trait::async_trait]
impl xberg::PostProcessor for XbergPostProcessorBridge {
    async fn process(
        &self,
        result: &mut xberg::ExtractedDocument,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<(), xberg::XbergError> {
        struct _SendFn<F>(F);
        // SAFETY: caller guarantees vtable fn pointers and user_data are valid across threads.
        unsafe impl<F> Send for _SendFn<F> {}
        impl<F: FnOnce() -> R, R> _SendFn<F> {
            fn call(self) -> R {
                (self.0)()
            }
        }

        let _cached_name = self.cached_name.clone();
        let vtable = self.vtable;
        let user_data = self.user_data;
        let result = result.clone();
        let config = config.clone();

        let _task = _SendFn(move || {
            // Inline the sync body:
            let Some(fp) = vtable.process else {
                return Err(Box::from("vtable.process is null — bridge not initialised"));
            };
            let _result_json = serde_json::to_string(&result).unwrap_or_default();
            let _result_cs = match std::ffi::CString::new(_result_json) {
                Ok(s) => s,
                Err(_) => {
                    return Err(Box::from("nul byte in serialized param result"));
                }
            };
            let result_ptr = _result_cs.as_ptr();
            let _config_json = serde_json::to_string(&config).unwrap_or_default();
            let _config_cs = match std::ffi::CString::new(_config_json) {
                Ok(s) => s,
                Err(_) => {
                    return Err(Box::from("nul byte in serialized param config"));
                }
            };
            let config_ptr = _config_cs.as_ptr();
            let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
            // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
            // user_data validity is the caller's responsibility (documented in the vtable API).
            let _rc = unsafe { fp(user_data, result_ptr, config_ptr, &mut _out_error) };
            if _rc != 0 {
                let msg = if _out_error.is_null() {
                    format!("vtable.process returned error code {}", _rc)
                } else {
                    // SAFETY: out_error was written by the callee as a valid NUL-terminated string.
                    let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                        .to_string_lossy()
                        .into_owned();
                    if let Some(free_fn) = vtable.free_string {
                        // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                        unsafe { free_fn(_out_error) };
                    }
                    msg
                };
                return Err(Box::from(msg));
            }
            Ok(())
        });
        tokio::task::spawn_blocking(move || _task.call())
            .await
            .map_err(|e| xberg::XbergError::Other(format!("spawn_blocking failed in process: {}", e)))?
            .map_err(|e: Box<dyn std::error::Error + Send + Sync>| xberg::XbergError::Other(e.to_string()))
    }

    fn processing_stage(&self) -> xberg::ProcessingStage {
        let Some(fp) = self.vtable.processing_stage else {
            tracing::warn!(
                wrapper = "XbergPostProcessorBridge",
                method = "processing_stage",
                "vtable slot for host callback is not initialised; returning default"
            );
            return Default::default();
        };
        let mut _out_result: *mut std::ffi::c_char = std::ptr::null_mut();
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
        // user_data validity is the caller's responsibility (documented in the vtable API).
        let _rc = unsafe { fp(self.user_data, &mut _out_result, &mut _out_error) };
        if _rc != 0 {
            let msg = if _out_error.is_null() {
                format!("vtable.processing_stage returned error code {}", _rc)
            } else {
                // SAFETY: out_error was written by the callee as a valid NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            tracing::warn!(wrapper = "XbergPostProcessorBridge", method = "processing_stage", error = %msg, "host callback failed; returning default");
            return Default::default();
        }
        if _out_result.is_null() {
            tracing::warn!(
                wrapper = "XbergPostProcessorBridge",
                method = "processing_stage",
                "host wrote no result; returning default"
            );
            return Default::default();
        }
        // SAFETY: out_result was written by the callee as a valid NUL-terminated string.
        let json = unsafe { std::ffi::CStr::from_ptr(_out_result) }
            .to_string_lossy()
            .into_owned();
        if let Some(free_fn) = self.vtable.free_string {
            // SAFETY: free_fn is the vtable-provided destructor for callback strings.
            unsafe { free_fn(_out_result) };
        }
        serde_json::from_str::<xberg::ProcessingStage>(&json).unwrap_or_else(|e| {
            tracing::warn!(wrapper = "XbergPostProcessorBridge", method = "processing_stage", error = %e, "host returned an unparseable value; returning default");
            Default::default()
        })
    }

    fn should_process(&self, _result: &xberg::ExtractedDocument, _config: &xberg::ExtractionConfig) -> bool {
        let Some(fp) = self.vtable.should_process else {
            tracing::warn!(
                wrapper = "XbergPostProcessorBridge",
                method = "should_process",
                "vtable slot for host callback is not initialised; returning default"
            );
            return false;
        };
        let __result_json = serde_json::to_string(&_result).unwrap_or_default();
        let __result_cs = match std::ffi::CString::new(__result_json) {
            Ok(s) => s,
            Err(_) => {
                return false;
            }
        };
        let _result_ptr = __result_cs.as_ptr();
        let __config_json = serde_json::to_string(&_config).unwrap_or_default();
        let __config_cs = match std::ffi::CString::new(__config_json) {
            Ok(s) => s,
            Err(_) => {
                return false;
            }
        };
        let _config_ptr = __config_cs.as_ptr();
        // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
        // user_data validity is the caller's responsibility (documented in the vtable API).
        let _rc = unsafe { fp(self.user_data, _result_ptr, _config_ptr) };
        _rc != 0
    }

    fn estimated_duration_ms(&self, _result: &xberg::ExtractedDocument) -> u64 {
        let Some(fp) = self.vtable.estimated_duration_ms else {
            tracing::warn!(
                wrapper = "XbergPostProcessorBridge",
                method = "estimated_duration_ms",
                "vtable slot for host callback is not initialised; returning default"
            );
            return 0;
        };
        let __result_json = serde_json::to_string(&_result).unwrap_or_default();
        let __result_cs = match std::ffi::CString::new(__result_json) {
            Ok(s) => s,
            Err(_) => {
                return 0;
            }
        };
        let _result_ptr = __result_cs.as_ptr();
        // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
        // user_data validity is the caller's responsibility (documented in the vtable API).
        unsafe { fp(self.user_data, _result_ptr) }
    }

    fn priority(&self) -> i32 {
        let Some(fp) = self.vtable.priority else {
            tracing::warn!(
                wrapper = "XbergPostProcessorBridge",
                method = "priority",
                "vtable slot for host callback is not initialised; returning default"
            );
            return 0;
        };
        // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
        // user_data validity is the caller's responsibility (documented in the vtable API).
        unsafe { fp(self.user_data) }
    }
}

/// Register a C plugin implementing `PostProcessor` via a vtable.
///
/// # Parameters
///
/// - `name`: null-terminated UTF-8 plugin name. Must not be null.
/// - `vtable`: vtable with function pointers implementing the trait.
/// - `user_data`: opaque pointer forwarded to every vtable function.
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// All function pointers in `vtable` must remain valid until the plugin is
/// unregistered. `user_data` must be safe to use from any thread that calls
/// into the plugin.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_register_post_processor(
    name: *const std::ffi::c_char,
    vtable: *const XbergPostProcessorVTable,
    user_data: *const std::ffi::c_void,
    out_error: *mut *mut std::ffi::c_char,
) -> i32 {
    catch_ffi_panic(1, || {
        if name.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "name must not be null") };
            return 1;
        }
        if vtable.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable must not be null") };
            return 1;
        }
        // SAFETY: null check above guarantees vtable is a valid pointer.
        let vtable_ref = unsafe { &*vtable };
        if vtable_ref.free_string.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.free_string must not be null") };
            return 1;
        }
        if vtable_ref.process.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.process must not be null") };
            return 1;
        }
        if vtable_ref.processing_stage.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.processing_stage must not be null") };
            return 1;
        }

        // SAFETY: name is non-null (checked above); it points to a valid C string.
        let plugin_name = match unsafe { std::ffi::CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_owned(),
            Err(_) => {
                // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
                unsafe { ffi_set_out_error(out_error, "name is not valid UTF-8") };
                return 1;
            }
        };

        // SAFETY: vtable pointer and user_data validity is the caller's responsibility.
        let bridge = unsafe { XbergPostProcessorBridge::new(plugin_name, *vtable_ref, user_data) };
        let arc: Arc<dyn xberg::PostProcessor> = Arc::new(bridge);

        let registry = xberg::plugins::registry::get_post_processor_registry();
        let mut registry = registry.write();

        if let Err(e) = registry.register(arc) {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// Unregister a previously registered C plugin by name.
///
/// # Parameters
///
/// - `name`: null-terminated UTF-8 plugin name. Must not be null.
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// `name` must point to a valid null-terminated C string.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_unregister_post_processor(
    name: *const std::ffi::c_char,
    out_error: *mut *mut std::ffi::c_char,
) -> i32 {
    catch_ffi_panic(1, || {
        if name.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "name must not be null") };
            return 1;
        }
        // SAFETY: name is non-null (checked above); it points to a valid C string.
        let plugin_name = match unsafe { std::ffi::CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
                unsafe { ffi_set_out_error(out_error, "name is not valid UTF-8") };
                return 1;
            }
        };

        let registry = xberg::plugins::registry::get_post_processor_registry();
        let mut registry = registry.write();
        if let Err(e) = registry.remove(plugin_name) {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// Remove all registered C plugins of this trait.
///
/// # Parameters
///
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// `out_error` may be null. When non-null, the caller owns the resulting string
/// and must free it with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_clear_post_processor(out_error: *mut *mut std::ffi::c_char) -> i32 {
    catch_ffi_panic(1, || {
        let registry = xberg::plugins::registry::get_post_processor_registry();
        let mut registry = registry.write();
        if let Err(e) = registry.clear() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// VTable for C plugin bridges implementing the `Validator` trait.
///
/// # Safety
///
/// All function pointers must be valid for the lifetime of any bridge created from
/// this vtable.  `free_user_data`, when non-null, is called once with `user_data`
/// when the bridge is dropped.
#[derive(Copy, Clone)]
#[repr(C)]
pub struct XbergValidatorVTable {
    /// Return a null-terminated UTF-8 name string into `out_name`; return 0 on success.
    pub name_fn: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_name: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Return a null-terminated UTF-8 version string into `out_version`; return 0 on success.
    pub version_fn: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_version: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Initialise the plugin; return 0 on success, non-zero on failure (error text in `out_error`).
    pub initialize_fn:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, out_error: *mut *mut std::ffi::c_char) -> i32>,
    /// Shut down the plugin; return 0 on success, non-zero on failure (error text in `out_error`).
    pub shutdown_fn:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, out_error: *mut *mut std::ffi::c_char) -> i32>,
    /// Validate an extraction result.
    ///
    /// Check the extraction result and return `Ok(())` if valid, or an error
    /// if validation fails.
    ///
    /// # Arguments
    ///
    /// * `result` - The extraction result to validate
    /// * `config` - Extraction configuration
    ///
    /// # Returns
    ///
    /// - `Ok(())` if validation passes
    /// - `Err(...)` if validation fails (extraction will fail)
    ///
    /// # Errors
    ///
    /// - `XbergError::Validation` - Validation failed
    /// - Any other error type appropriate for the failure
    ///
    /// # Example - Content Length Validation
    ///
    /// ```rust
    /// async fn validate(&self, result: &ExtractedDocument, config: &ExtractionConfig)
    /// -> Result<()> {
    /// let length = result.content.len();
    ///
    /// if length < self.min {
    /// return Err(XbergError::validation(format!(
    /// "Content too short: {} < {} characters",
    /// length, self.min
    /// )));
    /// }
    ///
    /// if length > self.max {
    /// return Err(XbergError::validation(format!(
    /// "Content too long: {} > {} characters",
    /// length, self.max
    /// )));
    /// }
    ///
    /// Ok(())
    /// }
    /// ```
    ///
    /// # Example - Quality Score Validation
    ///
    /// ```rust
    /// async fn validate(&self, result: &ExtractedDocument, config: &ExtractionConfig)
    /// -> Result<()> {
    /// // Check if quality_score exists in metadata
    /// let score = result.metadata
    /// .additional
    /// .get("quality_score")
    /// .and_then(|v| v.as_f64())
    /// .unwrap_or(0.0);
    ///
    /// if score < self.min_score {
    /// return Err(XbergError::validation(format!(
    /// "Quality score too low: {} < {}",
    /// score, self.min_score
    /// )));
    /// }
    ///
    /// Ok(())
    /// }
    /// ```
    ///
    /// # Example - Security Validation
    ///
    /// ```rust
    /// async fn validate(&self, result: &ExtractedDocument, config: &ExtractionConfig)
    /// -> Result<()> {
    /// // Check for blocked patterns
    /// for pattern in &self.blocked_patterns {
    /// if result.content.contains(pattern) {
    /// return Err(XbergError::validation(format!(
    /// "Content contains blocked pattern: {}",
    /// pattern
    /// )));
    /// }
    /// }
    ///
    /// Ok(())
    /// }
    /// ```
    pub validate: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            result: *const std::ffi::c_char,
            config: *const std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Optional: Check if this validator should run for a given result.
    ///
    /// Allows conditional validation based on MIME type, metadata, or content.
    /// Defaults to `true` (always run).
    ///
    /// # Arguments
    ///
    /// * `result` - The extraction result to check
    /// * `config` - Extraction configuration
    ///
    /// # Returns
    ///
    /// `true` if the validator should run, `false` to skip.
    ///
    /// # Example
    ///
    /// ```rust
    /// Only validate PDF documents
    /// fn should_validate(&self, result: &ExtractedDocument, config: &ExtractionConfig) -> bool {
    /// result.mime_type == "application/pdf"
    /// }
    /// ```
    pub should_validate: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            _result: *const std::ffi::c_char,
            _config: *const std::ffi::c_char,
        ) -> i32,
    >,
    /// Optional: Get the validation priority.
    ///
    /// Higher priority validators run first. Useful for ordering validation checks
    /// (e.g., run cheap validations before expensive ones).
    ///
    /// Default priority is 50.
    ///
    /// # Returns
    ///
    /// Priority value (higher = runs earlier).
    ///
    /// # Example
    ///
    /// ```rust
    /// Run this validator first (it's fast)
    /// fn priority(&self) -> i32 {
    /// 100
    /// }
    /// ```
    pub priority: Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void) -> i32>,
    /// Optional string destructor: called for strings returned by vtable callbacks.
    pub free_string: Option<unsafe extern "C" fn(*mut std::ffi::c_char)>,
    /// Optional destructor: called once with `user_data` when the bridge is dropped.
    pub free_user_data: Option<unsafe extern "C" fn(*mut std::ffi::c_void)>,
}

// SAFETY: all fields are function pointers and free_user_data, which are Send + Sync.
unsafe impl Send for XbergValidatorVTable {}
unsafe impl Sync for XbergValidatorVTable {}

/// Rust-side bridge that holds a C vtable pointer and opaque `user_data`.
///
/// Implements `Validator` by forwarding calls through the vtable.
pub struct XbergValidatorBridge {
    vtable: XbergValidatorVTable,
    user_data: *const std::ffi::c_void,
    cached_name: String,
    cached_version: String,
}

impl std::fmt::Debug for XbergValidatorBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.debug_struct("XbergValidatorBridge")
            .field("cached_name", &self.cached_name)
            .field("cached_version", &self.cached_version)
            .finish_non_exhaustive()
    }
}

// SAFETY: The caller is responsible for ensuring `user_data` is safe to send across
// thread boundaries. This is documented in `XbergValidatorVTable` and the registration function.
unsafe impl Send for XbergValidatorBridge {}
unsafe impl Sync for XbergValidatorBridge {}

impl Drop for XbergValidatorBridge {
    fn drop(&mut self) {
        if let Some(free_fn) = self.vtable.free_user_data {
            // SAFETY: free_fn is a valid function pointer; user_data is the pointer
            // originally provided at registration. Called exactly once here.
            unsafe { free_fn(self.user_data as *mut std::ffi::c_void) }
        }
    }
}

impl XbergValidatorBridge {
    /// Create a new bridge from a vtable and opaque user_data pointer.
    ///
    /// # Safety
    ///
    /// `vtable` must remain valid for the lifetime of the returned bridge.
    /// `user_data` must be valid for any thread that calls methods on this bridge.
    /// All required fn pointers in `vtable` must be non-null.
    pub unsafe fn new(name: String, vtable: XbergValidatorVTable, user_data: *const std::ffi::c_void) -> Self {
        Self {
            vtable,
            user_data,
            cached_name: name,
            cached_version: String::new(),
        }
    }
}

impl xberg::plugins::Plugin for XbergValidatorBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }
    fn version(&self) -> String {
        let Some(fp) = self.vtable.version_fn else {
            return String::new();
        };
        let mut _out: *mut std::ffi::c_char = std::ptr::null_mut();
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out, &mut _out_error) };
        if rc != 0 {
            if !_out_error.is_null() {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let _ = unsafe { std::ffi::CStr::from_ptr(_out_error) }.to_string_lossy();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
            }
            return self.cached_version.clone();
        }
        if _out.is_null() {
            return self.cached_version.clone();
        }
        // SAFETY: _out is a callee-allocated NUL-terminated string.
        let version = unsafe { std::ffi::CStr::from_ptr(_out) }.to_string_lossy().into_owned();
        if let Some(free_fn) = self.vtable.free_string {
            // SAFETY: free_fn is the vtable-provided destructor for callback strings.
            unsafe { free_fn(_out) };
        }
        version
    }
    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        let Some(fp) = self.vtable.initialize_fn else {
            return Ok(());
        };
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out_error) };
        if rc != 0 {
            let msg = if _out_error.is_null() {
                format!("initialize returned {}", rc)
            } else {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            return Err(xberg::XbergError::Plugin {
                message: msg,
                plugin_name: String::new(),
            });
        }
        Ok(())
    }
    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        let Some(fp) = self.vtable.shutdown_fn else {
            return Ok(());
        };
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out_error) };
        if rc != 0 {
            let msg = if _out_error.is_null() {
                format!("shutdown returned {}", rc)
            } else {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            return Err(xberg::XbergError::Plugin {
                message: msg,
                plugin_name: String::new(),
            });
        }
        Ok(())
    }
}

#[async_trait::async_trait]
impl xberg::Validator for XbergValidatorBridge {
    async fn validate(
        &self,
        result: &xberg::ExtractedDocument,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<(), xberg::XbergError> {
        struct _SendFn<F>(F);
        // SAFETY: caller guarantees vtable fn pointers and user_data are valid across threads.
        unsafe impl<F> Send for _SendFn<F> {}
        impl<F: FnOnce() -> R, R> _SendFn<F> {
            fn call(self) -> R {
                (self.0)()
            }
        }

        let _cached_name = self.cached_name.clone();
        let vtable = self.vtable;
        let user_data = self.user_data;
        let result = result.clone();
        let config = config.clone();

        let _task = _SendFn(move || {
            // Inline the sync body:
            let Some(fp) = vtable.validate else {
                return Err(Box::from("vtable.validate is null — bridge not initialised"));
            };
            let _result_json = serde_json::to_string(&result).unwrap_or_default();
            let _result_cs = match std::ffi::CString::new(_result_json) {
                Ok(s) => s,
                Err(_) => {
                    return Err(Box::from("nul byte in serialized param result"));
                }
            };
            let result_ptr = _result_cs.as_ptr();
            let _config_json = serde_json::to_string(&config).unwrap_or_default();
            let _config_cs = match std::ffi::CString::new(_config_json) {
                Ok(s) => s,
                Err(_) => {
                    return Err(Box::from("nul byte in serialized param config"));
                }
            };
            let config_ptr = _config_cs.as_ptr();
            let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
            // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
            // user_data validity is the caller's responsibility (documented in the vtable API).
            let _rc = unsafe { fp(user_data, result_ptr, config_ptr, &mut _out_error) };
            if _rc != 0 {
                let msg = if _out_error.is_null() {
                    format!("vtable.validate returned error code {}", _rc)
                } else {
                    // SAFETY: out_error was written by the callee as a valid NUL-terminated string.
                    let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                        .to_string_lossy()
                        .into_owned();
                    if let Some(free_fn) = vtable.free_string {
                        // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                        unsafe { free_fn(_out_error) };
                    }
                    msg
                };
                return Err(Box::from(msg));
            }
            Ok(())
        });
        tokio::task::spawn_blocking(move || _task.call())
            .await
            .map_err(|e| xberg::XbergError::Other(format!("spawn_blocking failed in validate: {}", e)))?
            .map_err(|e: Box<dyn std::error::Error + Send + Sync>| xberg::XbergError::Other(e.to_string()))
    }

    fn should_validate(&self, _result: &xberg::ExtractedDocument, _config: &xberg::ExtractionConfig) -> bool {
        let Some(fp) = self.vtable.should_validate else {
            tracing::warn!(
                wrapper = "XbergValidatorBridge",
                method = "should_validate",
                "vtable slot for host callback is not initialised; returning default"
            );
            return false;
        };
        let __result_json = serde_json::to_string(&_result).unwrap_or_default();
        let __result_cs = match std::ffi::CString::new(__result_json) {
            Ok(s) => s,
            Err(_) => {
                return false;
            }
        };
        let _result_ptr = __result_cs.as_ptr();
        let __config_json = serde_json::to_string(&_config).unwrap_or_default();
        let __config_cs = match std::ffi::CString::new(__config_json) {
            Ok(s) => s,
            Err(_) => {
                return false;
            }
        };
        let _config_ptr = __config_cs.as_ptr();
        // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
        // user_data validity is the caller's responsibility (documented in the vtable API).
        let _rc = unsafe { fp(self.user_data, _result_ptr, _config_ptr) };
        _rc != 0
    }

    fn priority(&self) -> i32 {
        let Some(fp) = self.vtable.priority else {
            tracing::warn!(
                wrapper = "XbergValidatorBridge",
                method = "priority",
                "vtable slot for host callback is not initialised; returning default"
            );
            return 0;
        };
        // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
        // user_data validity is the caller's responsibility (documented in the vtable API).
        unsafe { fp(self.user_data) }
    }
}

/// Register a C plugin implementing `Validator` via a vtable.
///
/// # Parameters
///
/// - `name`: null-terminated UTF-8 plugin name. Must not be null.
/// - `vtable`: vtable with function pointers implementing the trait.
/// - `user_data`: opaque pointer forwarded to every vtable function.
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// All function pointers in `vtable` must remain valid until the plugin is
/// unregistered. `user_data` must be safe to use from any thread that calls
/// into the plugin.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_register_validator(
    name: *const std::ffi::c_char,
    vtable: *const XbergValidatorVTable,
    user_data: *const std::ffi::c_void,
    out_error: *mut *mut std::ffi::c_char,
) -> i32 {
    catch_ffi_panic(1, || {
        if name.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "name must not be null") };
            return 1;
        }
        if vtable.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable must not be null") };
            return 1;
        }
        // SAFETY: null check above guarantees vtable is a valid pointer.
        let vtable_ref = unsafe { &*vtable };
        if vtable_ref.free_string.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.free_string must not be null") };
            return 1;
        }
        if vtable_ref.validate.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.validate must not be null") };
            return 1;
        }

        // SAFETY: name is non-null (checked above); it points to a valid C string.
        let plugin_name = match unsafe { std::ffi::CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_owned(),
            Err(_) => {
                // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
                unsafe { ffi_set_out_error(out_error, "name is not valid UTF-8") };
                return 1;
            }
        };

        // SAFETY: vtable pointer and user_data validity is the caller's responsibility.
        let bridge = unsafe { XbergValidatorBridge::new(plugin_name, *vtable_ref, user_data) };
        let arc: Arc<dyn xberg::Validator> = Arc::new(bridge);

        let registry = xberg::plugins::registry::get_validator_registry();
        let mut registry = registry.write();

        if let Err(e) = registry.register(arc) {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// Unregister a previously registered C plugin by name.
///
/// # Parameters
///
/// - `name`: null-terminated UTF-8 plugin name. Must not be null.
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// `name` must point to a valid null-terminated C string.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_unregister_validator(
    name: *const std::ffi::c_char,
    out_error: *mut *mut std::ffi::c_char,
) -> i32 {
    catch_ffi_panic(1, || {
        if name.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "name must not be null") };
            return 1;
        }
        // SAFETY: name is non-null (checked above); it points to a valid C string.
        let plugin_name = match unsafe { std::ffi::CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
                unsafe { ffi_set_out_error(out_error, "name is not valid UTF-8") };
                return 1;
            }
        };

        let registry = xberg::plugins::registry::get_validator_registry();
        let mut registry = registry.write();
        if let Err(e) = registry.remove(plugin_name) {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// Remove all registered C plugins of this trait.
///
/// # Parameters
///
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// `out_error` may be null. When non-null, the caller owns the resulting string
/// and must free it with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_clear_validator(out_error: *mut *mut std::ffi::c_char) -> i32 {
    catch_ffi_panic(1, || {
        let registry = xberg::plugins::registry::get_validator_registry();
        let mut registry = registry.write();
        if let Err(e) = registry.clear() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// VTable for C plugin bridges implementing the `DocumentExtractor` trait.
///
/// # Safety
///
/// All function pointers must be valid for the lifetime of any bridge created from
/// this vtable.  `free_user_data`, when non-null, is called once with `user_data`
/// when the bridge is dropped.
#[derive(Copy, Clone)]
#[repr(C)]
pub struct XbergDocumentExtractorVTable {
    /// Return a null-terminated UTF-8 name string into `out_name`; return 0 on success.
    pub name_fn: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_name: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Return a null-terminated UTF-8 version string into `out_version`; return 0 on success.
    pub version_fn: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_version: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Initialise the plugin; return 0 on success, non-zero on failure (error text in `out_error`).
    pub initialize_fn:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, out_error: *mut *mut std::ffi::c_char) -> i32>,
    /// Shut down the plugin; return 0 on success, non-zero on failure (error text in `out_error`).
    pub shutdown_fn:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, out_error: *mut *mut std::ffi::c_char) -> i32>,
    /// Binding-safe extraction entry point for foreign-language plugin bridges.
    ///
    /// Accepts the same unified input shape as the public extraction API and
    /// returns one extracted document result.
    pub extract: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            input: *const std::ffi::c_char,
            config: *const std::ffi::c_char,
            out_result: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Get the list of MIME types supported by this extractor.
    ///
    /// Can include exact MIME types and prefix patterns:
    /// - Exact: `"application/pdf"`, `"text/plain"`
    /// - Prefix: `"image/*"` (matches any image type)
    ///
    /// # Returns
    ///
    /// A slice of MIME type strings.
    pub supported_mime_types: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_result: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Get the priority of this extractor.
    ///
    /// Higher priority extractors are preferred when multiple extractors
    /// support the same MIME type.
    ///
    /// # Priority Guidelines
    ///
    /// - **0-25**: Fallback/low-quality extractors
    /// - **26-49**: Alternative extractors
    /// - **50**: Default priority (built-in extractors)
    /// - **51-75**: Premium/enhanced extractors
    /// - **76-100**: Specialized/high-priority extractors
    ///
    /// # Returns
    ///
    /// Priority value (default: 50)
    pub priority: Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void) -> i32>,
    /// Optional: Check if this extractor can handle a specific file.
    ///
    /// Allows for more sophisticated detection beyond MIME types.
    /// Defaults to `true` (rely on MIME type matching).
    ///
    /// # Arguments
    ///
    /// * `path` - Path to the file to check
    /// * `mime_type` - Detected MIME type
    ///
    /// # Returns
    ///
    /// `true` if the extractor can handle this file, `false` otherwise.
    pub can_handle: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            _path: *const std::ffi::c_char,
            _mime_type: *const std::ffi::c_char,
        ) -> i32,
    >,
    /// Optional string destructor: called for strings returned by vtable callbacks.
    pub free_string: Option<unsafe extern "C" fn(*mut std::ffi::c_char)>,
    /// Optional destructor: called once with `user_data` when the bridge is dropped.
    pub free_user_data: Option<unsafe extern "C" fn(*mut std::ffi::c_void)>,
}

// SAFETY: all fields are function pointers and free_user_data, which are Send + Sync.
unsafe impl Send for XbergDocumentExtractorVTable {}
unsafe impl Sync for XbergDocumentExtractorVTable {}

/// Rust-side bridge that holds a C vtable pointer and opaque `user_data`.
///
/// Implements `DocumentExtractor` by forwarding calls through the vtable.
pub struct XbergDocumentExtractorBridge {
    vtable: XbergDocumentExtractorVTable,
    user_data: *const std::ffi::c_void,
    cached_name: String,
    cached_version: String,
    supported_mime_types_strs: &'static [&'static str],
}

impl std::fmt::Debug for XbergDocumentExtractorBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.debug_struct("XbergDocumentExtractorBridge")
            .field("cached_name", &self.cached_name)
            .field("cached_version", &self.cached_version)
            .finish_non_exhaustive()
    }
}

// SAFETY: The caller is responsible for ensuring `user_data` is safe to send across
// thread boundaries. This is documented in `XbergDocumentExtractorVTable` and the registration function.
unsafe impl Send for XbergDocumentExtractorBridge {}
unsafe impl Sync for XbergDocumentExtractorBridge {}

impl Drop for XbergDocumentExtractorBridge {
    fn drop(&mut self) {
        if let Some(free_fn) = self.vtable.free_user_data {
            // SAFETY: free_fn is a valid function pointer; user_data is the pointer
            // originally provided at registration. Called exactly once here.
            unsafe { free_fn(self.user_data as *mut std::ffi::c_void) }
        }
    }
}

impl XbergDocumentExtractorBridge {
    /// Create a new bridge from a vtable and opaque user_data pointer.
    ///
    /// # Safety
    ///
    /// `vtable` must remain valid for the lifetime of the returned bridge.
    /// `user_data` must be valid for any thread that calls methods on this bridge.
    /// All required fn pointers in `vtable` must be non-null.
    pub unsafe fn new(name: String, vtable: XbergDocumentExtractorVTable, user_data: *const std::ffi::c_void) -> Self {
        // Cache supported_mime_types() result for the lifetime of the bridge (required: returns &[&str]).
        let supported_mime_types_strs: &'static [&'static str] = if let Some(fp) = vtable.supported_mime_types {
            let mut _out_result: *mut std::ffi::c_char = std::ptr::null_mut();
            let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
            // SAFETY: fp is a valid non-null function pointer; user_data validity is the caller's
            // responsibility (documented in the vtable API contract).
            let _rc = unsafe { fp(user_data, &mut _out_result, &mut _out_error) };
            if _out_result.is_null() {
                &[]
            } else {
                // SAFETY: out_result was written by the callee as a valid NUL-terminated string.
                let json = unsafe { std::ffi::CStr::from_ptr(_out_result) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_result) };
                }
                let owned: Vec<String> = serde_json::from_str(&json).unwrap_or_default();
                // Leak each string into a `'static str`; these live for the process lifetime.
                // The bridge struct is expected to live for the duration of the program
                // (registered plugins are not typically removed), so this is acceptable.
                let leaked: Vec<&'static str> = owned
                    .into_iter()
                    .map(|s| -> &'static str { Box::leak(s.into_boxed_str()) })
                    .collect();
                Box::leak(leaked.into_boxed_slice())
            }
        } else {
            &[]
        };

        Self {
            vtable,
            user_data,
            cached_name: name,
            cached_version: String::new(),
            supported_mime_types_strs,
        }
    }
}

impl xberg::plugins::Plugin for XbergDocumentExtractorBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }
    fn version(&self) -> String {
        let Some(fp) = self.vtable.version_fn else {
            return String::new();
        };
        let mut _out: *mut std::ffi::c_char = std::ptr::null_mut();
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out, &mut _out_error) };
        if rc != 0 {
            if !_out_error.is_null() {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let _ = unsafe { std::ffi::CStr::from_ptr(_out_error) }.to_string_lossy();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
            }
            return self.cached_version.clone();
        }
        if _out.is_null() {
            return self.cached_version.clone();
        }
        // SAFETY: _out is a callee-allocated NUL-terminated string.
        let version = unsafe { std::ffi::CStr::from_ptr(_out) }.to_string_lossy().into_owned();
        if let Some(free_fn) = self.vtable.free_string {
            // SAFETY: free_fn is the vtable-provided destructor for callback strings.
            unsafe { free_fn(_out) };
        }
        version
    }
    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        let Some(fp) = self.vtable.initialize_fn else {
            return Ok(());
        };
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out_error) };
        if rc != 0 {
            let msg = if _out_error.is_null() {
                format!("initialize returned {}", rc)
            } else {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            return Err(xberg::XbergError::Plugin {
                message: msg,
                plugin_name: String::new(),
            });
        }
        Ok(())
    }
    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        let Some(fp) = self.vtable.shutdown_fn else {
            return Ok(());
        };
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out_error) };
        if rc != 0 {
            let msg = if _out_error.is_null() {
                format!("shutdown returned {}", rc)
            } else {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            return Err(xberg::XbergError::Plugin {
                message: msg,
                plugin_name: String::new(),
            });
        }
        Ok(())
    }
}

#[async_trait::async_trait]
impl xberg::DocumentExtractor for XbergDocumentExtractorBridge {
    async fn extract(
        &self,
        input: xberg::ExtractInput,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        struct _SendFn<F>(F);
        // SAFETY: caller guarantees vtable fn pointers and user_data are valid across threads.
        unsafe impl<F> Send for _SendFn<F> {}
        impl<F: FnOnce() -> R, R> _SendFn<F> {
            fn call(self) -> R {
                (self.0)()
            }
        }

        let _cached_name = self.cached_name.clone();
        let vtable = self.vtable;
        let user_data = self.user_data;
        let input = input.clone();
        let config = config.clone();

        let _task = _SendFn(move || {
            // Inline the sync body:
            let Some(fp) = vtable.extract else {
                return Err(Box::from("vtable.extract is null — bridge not initialised"));
            };
            let _input_json = serde_json::to_string(&input).unwrap_or_default();
            let _input_cs = match std::ffi::CString::new(_input_json) {
                Ok(s) => s,
                Err(_) => {
                    return Err(Box::from("nul byte in serialized param input"));
                }
            };
            let input_ptr = _input_cs.as_ptr();
            let _config_json = serde_json::to_string(&config).unwrap_or_default();
            let _config_cs = match std::ffi::CString::new(_config_json) {
                Ok(s) => s,
                Err(_) => {
                    return Err(Box::from("nul byte in serialized param config"));
                }
            };
            let config_ptr = _config_cs.as_ptr();
            let mut _out_result: *mut std::ffi::c_char = std::ptr::null_mut();
            let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
            // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
            // user_data validity is the caller's responsibility (documented in the vtable API).
            let _rc = unsafe { fp(user_data, input_ptr, config_ptr, &mut _out_result, &mut _out_error) };
            if _rc != 0 {
                let msg = if _out_error.is_null() {
                    format!("vtable.extract returned error code {}", _rc)
                } else {
                    // SAFETY: out_error was written by the callee as a valid NUL-terminated string.
                    let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                        .to_string_lossy()
                        .into_owned();
                    if let Some(free_fn) = vtable.free_string {
                        // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                        unsafe { free_fn(_out_error) };
                    }
                    msg
                };
                return Err(Box::from(msg));
            }
            if _out_result.is_null() {
                return Err(Box::from("vtable.extract returned null out_result"));
            }
            // SAFETY: out_result was written by the callee as a valid NUL-terminated string.
            let json = unsafe { std::ffi::CStr::from_ptr(_out_result) }
                .to_string_lossy()
                .into_owned();
            if let Some(free_fn) = vtable.free_string {
                // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                unsafe { free_fn(_out_result) };
            }
            serde_json::from_str::<xberg::ExtractedDocument>(&json)
                .map_err(|e| Box::from(e.to_string()) as Box<dyn std::error::Error + Send + Sync>)
        });
        tokio::task::spawn_blocking(move || _task.call())
            .await
            .map_err(|e| xberg::XbergError::Other(format!("spawn_blocking failed in extract: {}", e)))?
            .map_err(|e: Box<dyn std::error::Error + Send + Sync>| xberg::XbergError::Other(e.to_string()))
    }

    fn supported_mime_types(&self) -> &[&str] {
        self.supported_mime_types_strs
    }

    fn priority(&self) -> i32 {
        let Some(fp) = self.vtable.priority else {
            tracing::warn!(
                wrapper = "XbergDocumentExtractorBridge",
                method = "priority",
                "vtable slot for host callback is not initialised; returning default"
            );
            return 0;
        };
        // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
        // user_data validity is the caller's responsibility (documented in the vtable API).
        unsafe { fp(self.user_data) }
    }

    fn can_handle(&self, _path: &std::path::Path, _mime_type: &str) -> bool {
        let Some(fp) = self.vtable.can_handle else {
            tracing::warn!(
                wrapper = "XbergDocumentExtractorBridge",
                method = "can_handle",
                "vtable slot for host callback is not initialised; returning default"
            );
            return false;
        };
        let __path_cs = match std::ffi::CString::new(_path.to_string_lossy().as_ref()) {
            Ok(s) => s,
            Err(_) => {
                return false;
            }
        };
        let _path_ptr = __path_cs.as_ptr();
        let __mime_type_cs = match std::ffi::CString::new(_mime_type) {
            Ok(s) => s,
            Err(_) => {
                return false;
            }
        };
        let _mime_type_ptr = __mime_type_cs.as_ptr();
        // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
        // user_data validity is the caller's responsibility (documented in the vtable API).
        let _rc = unsafe { fp(self.user_data, _path_ptr, _mime_type_ptr) };
        _rc != 0
    }
}

/// Register a C plugin implementing `DocumentExtractor` via a vtable.
///
/// # Parameters
///
/// - `name`: null-terminated UTF-8 plugin name. Must not be null.
/// - `vtable`: vtable with function pointers implementing the trait.
/// - `user_data`: opaque pointer forwarded to every vtable function.
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// All function pointers in `vtable` must remain valid until the plugin is
/// unregistered. `user_data` must be safe to use from any thread that calls
/// into the plugin.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_register_document_extractor(
    name: *const std::ffi::c_char,
    vtable: *const XbergDocumentExtractorVTable,
    user_data: *const std::ffi::c_void,
    out_error: *mut *mut std::ffi::c_char,
) -> i32 {
    catch_ffi_panic(1, || {
        if name.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "name must not be null") };
            return 1;
        }
        if vtable.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable must not be null") };
            return 1;
        }
        // SAFETY: null check above guarantees vtable is a valid pointer.
        let vtable_ref = unsafe { &*vtable };
        if vtable_ref.free_string.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.free_string must not be null") };
            return 1;
        }
        if vtable_ref.extract.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.extract must not be null") };
            return 1;
        }
        if vtable_ref.supported_mime_types.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.supported_mime_types must not be null") };
            return 1;
        }

        // SAFETY: name is non-null (checked above); it points to a valid C string.
        let plugin_name = match unsafe { std::ffi::CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_owned(),
            Err(_) => {
                // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
                unsafe { ffi_set_out_error(out_error, "name is not valid UTF-8") };
                return 1;
            }
        };

        // SAFETY: vtable pointer and user_data validity is the caller's responsibility.
        let bridge = unsafe { XbergDocumentExtractorBridge::new(plugin_name, *vtable_ref, user_data) };
        let arc: Arc<dyn xberg::DocumentExtractor> = Arc::new(bridge);

        let registry = xberg::plugins::registry::get_document_extractor_registry();
        let mut registry = registry.write();

        if let Err(e) = registry.register(arc) {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// Unregister a previously registered C plugin by name.
///
/// # Parameters
///
/// - `name`: null-terminated UTF-8 plugin name. Must not be null.
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// `name` must point to a valid null-terminated C string.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_unregister_document_extractor(
    name: *const std::ffi::c_char,
    out_error: *mut *mut std::ffi::c_char,
) -> i32 {
    catch_ffi_panic(1, || {
        if name.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "name must not be null") };
            return 1;
        }
        // SAFETY: name is non-null (checked above); it points to a valid C string.
        let plugin_name = match unsafe { std::ffi::CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
                unsafe { ffi_set_out_error(out_error, "name is not valid UTF-8") };
                return 1;
            }
        };

        let registry = xberg::plugins::registry::get_document_extractor_registry();
        let mut registry = registry.write();
        if let Err(e) = registry.remove(plugin_name) {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// Remove all registered C plugins of this trait.
///
/// # Parameters
///
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// `out_error` may be null. When non-null, the caller owns the resulting string
/// and must free it with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_clear_document_extractor(out_error: *mut *mut std::ffi::c_char) -> i32 {
    catch_ffi_panic(1, || {
        let registry = xberg::plugins::registry::get_document_extractor_registry();
        let mut registry = registry.write();
        if let Err(e) = registry.clear() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// VTable for C plugin bridges implementing the `EmbeddingBackend` trait.
///
/// # Safety
///
/// All function pointers must be valid for the lifetime of any bridge created from
/// this vtable.  `free_user_data`, when non-null, is called once with `user_data`
/// when the bridge is dropped.
#[derive(Copy, Clone)]
#[repr(C)]
pub struct XbergEmbeddingBackendVTable {
    /// Return a null-terminated UTF-8 name string into `out_name`; return 0 on success.
    pub name_fn: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_name: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Return a null-terminated UTF-8 version string into `out_version`; return 0 on success.
    pub version_fn: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_version: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Initialise the plugin; return 0 on success, non-zero on failure (error text in `out_error`).
    pub initialize_fn:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, out_error: *mut *mut std::ffi::c_char) -> i32>,
    /// Shut down the plugin; return 0 on success, non-zero on failure (error text in `out_error`).
    pub shutdown_fn:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, out_error: *mut *mut std::ffi::c_char) -> i32>,
    /// Embedding vector dimension. Must be `> 0` and must match the length of
    /// every vector returned by `embed`.
    pub dimensions: Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void) -> usize>,
    /// Embed a batch of texts, returning one vector per input in order.
    ///
    /// # Errors
    ///
    /// Implementations should return `Plugin` for
    /// backend-specific failures. The dispatcher layers its own validation
    /// (length, per-vector dimension) on top.
    pub embed: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            texts: *const std::ffi::c_char,
            out_result: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Optional string destructor: called for strings returned by vtable callbacks.
    pub free_string: Option<unsafe extern "C" fn(*mut std::ffi::c_char)>,
    /// Optional destructor: called once with `user_data` when the bridge is dropped.
    pub free_user_data: Option<unsafe extern "C" fn(*mut std::ffi::c_void)>,
}

// SAFETY: all fields are function pointers and free_user_data, which are Send + Sync.
unsafe impl Send for XbergEmbeddingBackendVTable {}
unsafe impl Sync for XbergEmbeddingBackendVTable {}

/// Rust-side bridge that holds a C vtable pointer and opaque `user_data`.
///
/// Implements `EmbeddingBackend` by forwarding calls through the vtable.
pub struct XbergEmbeddingBackendBridge {
    vtable: XbergEmbeddingBackendVTable,
    user_data: *const std::ffi::c_void,
    cached_name: String,
    cached_version: String,
}

impl std::fmt::Debug for XbergEmbeddingBackendBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.debug_struct("XbergEmbeddingBackendBridge")
            .field("cached_name", &self.cached_name)
            .field("cached_version", &self.cached_version)
            .finish_non_exhaustive()
    }
}

// SAFETY: The caller is responsible for ensuring `user_data` is safe to send across
// thread boundaries. This is documented in `XbergEmbeddingBackendVTable` and the registration function.
unsafe impl Send for XbergEmbeddingBackendBridge {}
unsafe impl Sync for XbergEmbeddingBackendBridge {}

impl Drop for XbergEmbeddingBackendBridge {
    fn drop(&mut self) {
        if let Some(free_fn) = self.vtable.free_user_data {
            // SAFETY: free_fn is a valid function pointer; user_data is the pointer
            // originally provided at registration. Called exactly once here.
            unsafe { free_fn(self.user_data as *mut std::ffi::c_void) }
        }
    }
}

impl XbergEmbeddingBackendBridge {
    /// Create a new bridge from a vtable and opaque user_data pointer.
    ///
    /// # Safety
    ///
    /// `vtable` must remain valid for the lifetime of the returned bridge.
    /// `user_data` must be valid for any thread that calls methods on this bridge.
    /// All required fn pointers in `vtable` must be non-null.
    pub unsafe fn new(name: String, vtable: XbergEmbeddingBackendVTable, user_data: *const std::ffi::c_void) -> Self {
        Self {
            vtable,
            user_data,
            cached_name: name,
            cached_version: String::new(),
        }
    }
}

impl xberg::plugins::Plugin for XbergEmbeddingBackendBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }
    fn version(&self) -> String {
        let Some(fp) = self.vtable.version_fn else {
            return String::new();
        };
        let mut _out: *mut std::ffi::c_char = std::ptr::null_mut();
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out, &mut _out_error) };
        if rc != 0 {
            if !_out_error.is_null() {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let _ = unsafe { std::ffi::CStr::from_ptr(_out_error) }.to_string_lossy();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
            }
            return self.cached_version.clone();
        }
        if _out.is_null() {
            return self.cached_version.clone();
        }
        // SAFETY: _out is a callee-allocated NUL-terminated string.
        let version = unsafe { std::ffi::CStr::from_ptr(_out) }.to_string_lossy().into_owned();
        if let Some(free_fn) = self.vtable.free_string {
            // SAFETY: free_fn is the vtable-provided destructor for callback strings.
            unsafe { free_fn(_out) };
        }
        version
    }
    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        let Some(fp) = self.vtable.initialize_fn else {
            return Ok(());
        };
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out_error) };
        if rc != 0 {
            let msg = if _out_error.is_null() {
                format!("initialize returned {}", rc)
            } else {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            return Err(xberg::XbergError::Plugin {
                message: msg,
                plugin_name: String::new(),
            });
        }
        Ok(())
    }
    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        let Some(fp) = self.vtable.shutdown_fn else {
            return Ok(());
        };
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out_error) };
        if rc != 0 {
            let msg = if _out_error.is_null() {
                format!("shutdown returned {}", rc)
            } else {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            return Err(xberg::XbergError::Plugin {
                message: msg,
                plugin_name: String::new(),
            });
        }
        Ok(())
    }
}

#[async_trait::async_trait]
impl xberg::EmbeddingBackend for XbergEmbeddingBackendBridge {
    fn dimensions(&self) -> usize {
        let Some(fp) = self.vtable.dimensions else {
            tracing::warn!(
                wrapper = "XbergEmbeddingBackendBridge",
                method = "dimensions",
                "vtable slot for host callback is not initialised; returning default"
            );
            return 0;
        };
        // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
        // user_data validity is the caller's responsibility (documented in the vtable API).
        unsafe { fp(self.user_data) }
    }

    async fn embed(&self, texts: Vec<String>) -> std::result::Result<Vec<Vec<f32>>, xberg::XbergError> {
        struct _SendFn<F>(F);
        // SAFETY: caller guarantees vtable fn pointers and user_data are valid across threads.
        unsafe impl<F> Send for _SendFn<F> {}
        impl<F: FnOnce() -> R, R> _SendFn<F> {
            fn call(self) -> R {
                (self.0)()
            }
        }

        let _cached_name = self.cached_name.clone();
        let vtable = self.vtable;
        let user_data = self.user_data;
        let texts = texts.clone();

        let _task = _SendFn(move || {
            // Inline the sync body:
            let Some(fp) = vtable.embed else {
                return Err(Box::from("vtable.embed is null — bridge not initialised"));
            };
            let _texts_json = serde_json::to_string(&texts).unwrap_or_default();
            let _texts_cs = match std::ffi::CString::new(_texts_json) {
                Ok(s) => s,
                Err(_) => {
                    return Err(Box::from("nul byte in serialized param texts"));
                }
            };
            let texts_ptr = _texts_cs.as_ptr();
            let mut _out_result: *mut std::ffi::c_char = std::ptr::null_mut();
            let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
            // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
            // user_data validity is the caller's responsibility (documented in the vtable API).
            let _rc = unsafe { fp(user_data, texts_ptr, &mut _out_result, &mut _out_error) };
            if _rc != 0 {
                let msg = if _out_error.is_null() {
                    format!("vtable.embed returned error code {}", _rc)
                } else {
                    // SAFETY: out_error was written by the callee as a valid NUL-terminated string.
                    let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                        .to_string_lossy()
                        .into_owned();
                    if let Some(free_fn) = vtable.free_string {
                        // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                        unsafe { free_fn(_out_error) };
                    }
                    msg
                };
                return Err(Box::from(msg));
            }
            if _out_result.is_null() {
                return Err(Box::from("vtable.embed returned null out_result"));
            }
            // SAFETY: out_result was written by the callee as a valid NUL-terminated string.
            let json = unsafe { std::ffi::CStr::from_ptr(_out_result) }
                .to_string_lossy()
                .into_owned();
            if let Some(free_fn) = vtable.free_string {
                // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                unsafe { free_fn(_out_result) };
            }
            serde_json::from_str::<Vec<Vec<f32>>>(&json)
                .map_err(|e| Box::from(e.to_string()) as Box<dyn std::error::Error + Send + Sync>)
        });
        tokio::task::spawn_blocking(move || _task.call())
            .await
            .map_err(|e| xberg::XbergError::Other(format!("spawn_blocking failed in embed: {}", e)))?
            .map_err(|e: Box<dyn std::error::Error + Send + Sync>| xberg::XbergError::Other(e.to_string()))
    }
}

/// Register a C plugin implementing `EmbeddingBackend` via a vtable.
///
/// # Parameters
///
/// - `name`: null-terminated UTF-8 plugin name. Must not be null.
/// - `vtable`: vtable with function pointers implementing the trait.
/// - `user_data`: opaque pointer forwarded to every vtable function.
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// All function pointers in `vtable` must remain valid until the plugin is
/// unregistered. `user_data` must be safe to use from any thread that calls
/// into the plugin.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_register_embedding_backend(
    name: *const std::ffi::c_char,
    vtable: *const XbergEmbeddingBackendVTable,
    user_data: *const std::ffi::c_void,
    out_error: *mut *mut std::ffi::c_char,
) -> i32 {
    catch_ffi_panic(1, || {
        if name.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "name must not be null") };
            return 1;
        }
        if vtable.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable must not be null") };
            return 1;
        }
        // SAFETY: null check above guarantees vtable is a valid pointer.
        let vtable_ref = unsafe { &*vtable };
        if vtable_ref.free_string.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.free_string must not be null") };
            return 1;
        }
        if vtable_ref.dimensions.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.dimensions must not be null") };
            return 1;
        }
        if vtable_ref.embed.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.embed must not be null") };
            return 1;
        }

        // SAFETY: name is non-null (checked above); it points to a valid C string.
        let plugin_name = match unsafe { std::ffi::CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_owned(),
            Err(_) => {
                // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
                unsafe { ffi_set_out_error(out_error, "name is not valid UTF-8") };
                return 1;
            }
        };

        // SAFETY: vtable pointer and user_data validity is the caller's responsibility.
        let bridge = unsafe { XbergEmbeddingBackendBridge::new(plugin_name, *vtable_ref, user_data) };
        let arc: Arc<dyn xberg::EmbeddingBackend> = Arc::new(bridge);

        let registry = xberg::plugins::registry::get_embedding_backend_registry();
        let mut registry = registry.write();

        if let Err(e) = registry.register(arc) {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// Unregister a previously registered C plugin by name.
///
/// # Parameters
///
/// - `name`: null-terminated UTF-8 plugin name. Must not be null.
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// `name` must point to a valid null-terminated C string.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_unregister_embedding_backend(
    name: *const std::ffi::c_char,
    out_error: *mut *mut std::ffi::c_char,
) -> i32 {
    catch_ffi_panic(1, || {
        if name.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "name must not be null") };
            return 1;
        }
        // SAFETY: name is non-null (checked above); it points to a valid C string.
        let plugin_name = match unsafe { std::ffi::CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
                unsafe { ffi_set_out_error(out_error, "name is not valid UTF-8") };
                return 1;
            }
        };

        let registry = xberg::plugins::registry::get_embedding_backend_registry();
        let mut registry = registry.write();
        if let Err(e) = registry.remove(plugin_name) {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// Remove all registered C plugins of this trait.
///
/// # Parameters
///
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// `out_error` may be null. When non-null, the caller owns the resulting string
/// and must free it with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_clear_embedding_backend(out_error: *mut *mut std::ffi::c_char) -> i32 {
    catch_ffi_panic(1, || {
        let registry = xberg::plugins::registry::get_embedding_backend_registry();
        let mut registry = registry.write();
        if let Err(e) = registry.clear() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// VTable for C plugin bridges implementing the `Renderer` trait.
///
/// # Safety
///
/// All function pointers must be valid for the lifetime of any bridge created from
/// this vtable.  `free_user_data`, when non-null, is called once with `user_data`
/// when the bridge is dropped.
#[derive(Copy, Clone)]
#[repr(C)]
pub struct XbergRendererVTable {
    /// Return a null-terminated UTF-8 name string into `out_name`; return 0 on success.
    pub name_fn: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_name: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Return a null-terminated UTF-8 version string into `out_version`; return 0 on success.
    pub version_fn: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_version: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Initialise the plugin; return 0 on success, non-zero on failure (error text in `out_error`).
    pub initialize_fn:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, out_error: *mut *mut std::ffi::c_char) -> i32>,
    /// Shut down the plugin; return 0 on success, non-zero on failure (error text in `out_error`).
    pub shutdown_fn:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, out_error: *mut *mut std::ffi::c_char) -> i32>,
    /// Binding-safe rendering entry point for foreign-language plugin bridges.
    ///
    /// Accepts one public extraction result and returns the rendered output.
    pub render_result: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            result: *const std::ffi::c_char,
            out_result: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Optional string destructor: called for strings returned by vtable callbacks.
    pub free_string: Option<unsafe extern "C" fn(*mut std::ffi::c_char)>,
    /// Optional destructor: called once with `user_data` when the bridge is dropped.
    pub free_user_data: Option<unsafe extern "C" fn(*mut std::ffi::c_void)>,
}

// SAFETY: all fields are function pointers and free_user_data, which are Send + Sync.
unsafe impl Send for XbergRendererVTable {}
unsafe impl Sync for XbergRendererVTable {}

/// Rust-side bridge that holds a C vtable pointer and opaque `user_data`.
///
/// Implements `Renderer` by forwarding calls through the vtable.
pub struct XbergRendererBridge {
    vtable: XbergRendererVTable,
    user_data: *const std::ffi::c_void,
    cached_name: String,
    cached_version: String,
}

impl std::fmt::Debug for XbergRendererBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.debug_struct("XbergRendererBridge")
            .field("cached_name", &self.cached_name)
            .field("cached_version", &self.cached_version)
            .finish_non_exhaustive()
    }
}

// SAFETY: The caller is responsible for ensuring `user_data` is safe to send across
// thread boundaries. This is documented in `XbergRendererVTable` and the registration function.
unsafe impl Send for XbergRendererBridge {}
unsafe impl Sync for XbergRendererBridge {}

impl Drop for XbergRendererBridge {
    fn drop(&mut self) {
        if let Some(free_fn) = self.vtable.free_user_data {
            // SAFETY: free_fn is a valid function pointer; user_data is the pointer
            // originally provided at registration. Called exactly once here.
            unsafe { free_fn(self.user_data as *mut std::ffi::c_void) }
        }
    }
}

impl XbergRendererBridge {
    /// Create a new bridge from a vtable and opaque user_data pointer.
    ///
    /// # Safety
    ///
    /// `vtable` must remain valid for the lifetime of the returned bridge.
    /// `user_data` must be valid for any thread that calls methods on this bridge.
    /// All required fn pointers in `vtable` must be non-null.
    pub unsafe fn new(name: String, vtable: XbergRendererVTable, user_data: *const std::ffi::c_void) -> Self {
        Self {
            vtable,
            user_data,
            cached_name: name,
            cached_version: String::new(),
        }
    }
}

impl xberg::plugins::Plugin for XbergRendererBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }
    fn version(&self) -> String {
        let Some(fp) = self.vtable.version_fn else {
            return String::new();
        };
        let mut _out: *mut std::ffi::c_char = std::ptr::null_mut();
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out, &mut _out_error) };
        if rc != 0 {
            if !_out_error.is_null() {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let _ = unsafe { std::ffi::CStr::from_ptr(_out_error) }.to_string_lossy();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
            }
            return self.cached_version.clone();
        }
        if _out.is_null() {
            return self.cached_version.clone();
        }
        // SAFETY: _out is a callee-allocated NUL-terminated string.
        let version = unsafe { std::ffi::CStr::from_ptr(_out) }.to_string_lossy().into_owned();
        if let Some(free_fn) = self.vtable.free_string {
            // SAFETY: free_fn is the vtable-provided destructor for callback strings.
            unsafe { free_fn(_out) };
        }
        version
    }
    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        let Some(fp) = self.vtable.initialize_fn else {
            return Ok(());
        };
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out_error) };
        if rc != 0 {
            let msg = if _out_error.is_null() {
                format!("initialize returned {}", rc)
            } else {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            return Err(xberg::XbergError::Plugin {
                message: msg,
                plugin_name: String::new(),
            });
        }
        Ok(())
    }
    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        let Some(fp) = self.vtable.shutdown_fn else {
            return Ok(());
        };
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out_error) };
        if rc != 0 {
            let msg = if _out_error.is_null() {
                format!("shutdown returned {}", rc)
            } else {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            return Err(xberg::XbergError::Plugin {
                message: msg,
                plugin_name: String::new(),
            });
        }
        Ok(())
    }
}

impl xberg::Renderer for XbergRendererBridge {
    fn render_result(&self, result: &xberg::ExtractedDocument) -> std::result::Result<String, xberg::XbergError> {
        let Some(fp) = self.vtable.render_result else {
            return Err(xberg::XbergError::Other(
                "vtable.render_result is null — bridge not initialised".to_string(),
            ));
        };
        let _result_json = serde_json::to_string(&result).unwrap_or_default();
        let _result_cs = match std::ffi::CString::new(_result_json) {
            Ok(s) => s,
            Err(_) => {
                return Err(xberg::XbergError::Other(
                    "nul byte in serialized param result".to_string(),
                ));
            }
        };
        let result_ptr = _result_cs.as_ptr();
        let mut _out_result: *mut std::ffi::c_char = std::ptr::null_mut();
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
        // user_data validity is the caller's responsibility (documented in the vtable API).
        let _rc = unsafe { fp(self.user_data, result_ptr, &mut _out_result, &mut _out_error) };
        if _rc != 0 {
            let msg = if _out_error.is_null() {
                format!("vtable.render_result returned error code {}", _rc)
            } else {
                // SAFETY: out_error was written by the callee as a valid NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            return Err(xberg::XbergError::Other(msg));
        }
        if _out_result.is_null() {
            return Ok(String::new());
        }
        // SAFETY: out_result was written by the callee as a valid NUL-terminated string.
        let out = unsafe { std::ffi::CStr::from_ptr(_out_result) }
            .to_string_lossy()
            .into_owned();
        if let Some(free_fn) = self.vtable.free_string {
            // SAFETY: free_fn is the vtable-provided destructor for callback strings.
            unsafe { free_fn(_out_result) };
        }
        Ok(out)
    }
}

/// Register a C plugin implementing `Renderer` via a vtable.
///
/// # Parameters
///
/// - `name`: null-terminated UTF-8 plugin name. Must not be null.
/// - `vtable`: vtable with function pointers implementing the trait.
/// - `user_data`: opaque pointer forwarded to every vtable function.
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// All function pointers in `vtable` must remain valid until the plugin is
/// unregistered. `user_data` must be safe to use from any thread that calls
/// into the plugin.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_register_renderer(
    name: *const std::ffi::c_char,
    vtable: *const XbergRendererVTable,
    user_data: *const std::ffi::c_void,
    out_error: *mut *mut std::ffi::c_char,
) -> i32 {
    catch_ffi_panic(1, || {
        if name.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "name must not be null") };
            return 1;
        }
        if vtable.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable must not be null") };
            return 1;
        }
        // SAFETY: null check above guarantees vtable is a valid pointer.
        let vtable_ref = unsafe { &*vtable };
        if vtable_ref.free_string.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.free_string must not be null") };
            return 1;
        }

        // SAFETY: name is non-null (checked above); it points to a valid C string.
        let plugin_name = match unsafe { std::ffi::CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_owned(),
            Err(_) => {
                // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
                unsafe { ffi_set_out_error(out_error, "name is not valid UTF-8") };
                return 1;
            }
        };

        // SAFETY: vtable pointer and user_data validity is the caller's responsibility.
        let bridge = unsafe { XbergRendererBridge::new(plugin_name, *vtable_ref, user_data) };
        let arc: Arc<dyn xberg::Renderer> = Arc::new(bridge);

        let registry = xberg::plugins::registry::get_renderer_registry();
        let mut registry = registry.write();

        if let Err(e) = registry.register(arc) {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// Unregister a previously registered C plugin by name.
///
/// # Parameters
///
/// - `name`: null-terminated UTF-8 plugin name. Must not be null.
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// `name` must point to a valid null-terminated C string.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_unregister_renderer(
    name: *const std::ffi::c_char,
    out_error: *mut *mut std::ffi::c_char,
) -> i32 {
    catch_ffi_panic(1, || {
        if name.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "name must not be null") };
            return 1;
        }
        // SAFETY: name is non-null (checked above); it points to a valid C string.
        let plugin_name = match unsafe { std::ffi::CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
                unsafe { ffi_set_out_error(out_error, "name is not valid UTF-8") };
                return 1;
            }
        };

        let registry = xberg::plugins::registry::get_renderer_registry();
        let mut registry = registry.write();
        if let Err(e) = registry.remove(plugin_name) {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// Remove all registered C plugins of this trait.
///
/// # Parameters
///
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// `out_error` may be null. When non-null, the caller owns the resulting string
/// and must free it with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_clear_renderer(out_error: *mut *mut std::ffi::c_char) -> i32 {
    catch_ffi_panic(1, || {
        let registry = xberg::plugins::registry::get_renderer_registry();
        let mut registry = registry.write();
        if let Err(e) = registry.clear() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// VTable for C plugin bridges implementing the `RerankerBackend` trait.
///
/// # Safety
///
/// All function pointers must be valid for the lifetime of any bridge created from
/// this vtable.  `free_user_data`, when non-null, is called once with `user_data`
/// when the bridge is dropped.
#[derive(Copy, Clone)]
#[repr(C)]
pub struct XbergRerankerBackendVTable {
    /// Return a null-terminated UTF-8 name string into `out_name`; return 0 on success.
    pub name_fn: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_name: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Return a null-terminated UTF-8 version string into `out_version`; return 0 on success.
    pub version_fn: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_version: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Initialise the plugin; return 0 on success, non-zero on failure (error text in `out_error`).
    pub initialize_fn:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, out_error: *mut *mut std::ffi::c_char) -> i32>,
    /// Shut down the plugin; return 0 on success, non-zero on failure (error text in `out_error`).
    pub shutdown_fn:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, out_error: *mut *mut std::ffi::c_char) -> i32>,
    /// Score a list of documents against a query.
    ///
    /// Returns one raw logit per document in the same order as the input.
    /// The dispatcher applies sigmoid to convert to `[0, 1]` scores.
    ///
    /// # Errors
    ///
    /// Implementations should return `Plugin` for
    /// backend-specific failures. The dispatcher validates the returned length
    /// against `documents.len()` before sorting.
    pub rerank: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            query: *const std::ffi::c_char,
            documents: *const std::ffi::c_char,
            out_result: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Optional string destructor: called for strings returned by vtable callbacks.
    pub free_string: Option<unsafe extern "C" fn(*mut std::ffi::c_char)>,
    /// Optional destructor: called once with `user_data` when the bridge is dropped.
    pub free_user_data: Option<unsafe extern "C" fn(*mut std::ffi::c_void)>,
}

// SAFETY: all fields are function pointers and free_user_data, which are Send + Sync.
unsafe impl Send for XbergRerankerBackendVTable {}
unsafe impl Sync for XbergRerankerBackendVTable {}

/// Rust-side bridge that holds a C vtable pointer and opaque `user_data`.
///
/// Implements `RerankerBackend` by forwarding calls through the vtable.
pub struct XbergRerankerBackendBridge {
    vtable: XbergRerankerBackendVTable,
    user_data: *const std::ffi::c_void,
    cached_name: String,
    cached_version: String,
}

impl std::fmt::Debug for XbergRerankerBackendBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.debug_struct("XbergRerankerBackendBridge")
            .field("cached_name", &self.cached_name)
            .field("cached_version", &self.cached_version)
            .finish_non_exhaustive()
    }
}

// SAFETY: The caller is responsible for ensuring `user_data` is safe to send across
// thread boundaries. This is documented in `XbergRerankerBackendVTable` and the registration function.
unsafe impl Send for XbergRerankerBackendBridge {}
unsafe impl Sync for XbergRerankerBackendBridge {}

impl Drop for XbergRerankerBackendBridge {
    fn drop(&mut self) {
        if let Some(free_fn) = self.vtable.free_user_data {
            // SAFETY: free_fn is a valid function pointer; user_data is the pointer
            // originally provided at registration. Called exactly once here.
            unsafe { free_fn(self.user_data as *mut std::ffi::c_void) }
        }
    }
}

impl XbergRerankerBackendBridge {
    /// Create a new bridge from a vtable and opaque user_data pointer.
    ///
    /// # Safety
    ///
    /// `vtable` must remain valid for the lifetime of the returned bridge.
    /// `user_data` must be valid for any thread that calls methods on this bridge.
    /// All required fn pointers in `vtable` must be non-null.
    pub unsafe fn new(name: String, vtable: XbergRerankerBackendVTable, user_data: *const std::ffi::c_void) -> Self {
        Self {
            vtable,
            user_data,
            cached_name: name,
            cached_version: String::new(),
        }
    }
}

impl xberg::plugins::Plugin for XbergRerankerBackendBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }
    fn version(&self) -> String {
        let Some(fp) = self.vtable.version_fn else {
            return String::new();
        };
        let mut _out: *mut std::ffi::c_char = std::ptr::null_mut();
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out, &mut _out_error) };
        if rc != 0 {
            if !_out_error.is_null() {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let _ = unsafe { std::ffi::CStr::from_ptr(_out_error) }.to_string_lossy();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
            }
            return self.cached_version.clone();
        }
        if _out.is_null() {
            return self.cached_version.clone();
        }
        // SAFETY: _out is a callee-allocated NUL-terminated string.
        let version = unsafe { std::ffi::CStr::from_ptr(_out) }.to_string_lossy().into_owned();
        if let Some(free_fn) = self.vtable.free_string {
            // SAFETY: free_fn is the vtable-provided destructor for callback strings.
            unsafe { free_fn(_out) };
        }
        version
    }
    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        let Some(fp) = self.vtable.initialize_fn else {
            return Ok(());
        };
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out_error) };
        if rc != 0 {
            let msg = if _out_error.is_null() {
                format!("initialize returned {}", rc)
            } else {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            return Err(xberg::XbergError::Plugin {
                message: msg,
                plugin_name: String::new(),
            });
        }
        Ok(())
    }
    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        let Some(fp) = self.vtable.shutdown_fn else {
            return Ok(());
        };
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out_error) };
        if rc != 0 {
            let msg = if _out_error.is_null() {
                format!("shutdown returned {}", rc)
            } else {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            return Err(xberg::XbergError::Plugin {
                message: msg,
                plugin_name: String::new(),
            });
        }
        Ok(())
    }
}

#[async_trait::async_trait]
impl xberg::RerankerBackend for XbergRerankerBackendBridge {
    async fn rerank(&self, query: String, documents: Vec<String>) -> std::result::Result<Vec<f32>, xberg::XbergError> {
        struct _SendFn<F>(F);
        // SAFETY: caller guarantees vtable fn pointers and user_data are valid across threads.
        unsafe impl<F> Send for _SendFn<F> {}
        impl<F: FnOnce() -> R, R> _SendFn<F> {
            fn call(self) -> R {
                (self.0)()
            }
        }

        let _cached_name = self.cached_name.clone();
        let vtable = self.vtable;
        let user_data = self.user_data;
        let query = query.clone();
        let documents = documents.clone();

        let _task = _SendFn(move || {
            // Inline the sync body:
            let Some(fp) = vtable.rerank else {
                return Err(Box::from("vtable.rerank is null — bridge not initialised"));
            };
            let _query_cs = match std::ffi::CString::new(query.as_str()) {
                Ok(s) => s,
                Err(_) => {
                    return Err(Box::from("nul byte in param query"));
                }
            };
            let query_ptr = _query_cs.as_ptr();
            let _documents_json = serde_json::to_string(&documents).unwrap_or_default();
            let _documents_cs = match std::ffi::CString::new(_documents_json) {
                Ok(s) => s,
                Err(_) => {
                    return Err(Box::from("nul byte in serialized param documents"));
                }
            };
            let documents_ptr = _documents_cs.as_ptr();
            let mut _out_result: *mut std::ffi::c_char = std::ptr::null_mut();
            let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
            // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
            // user_data validity is the caller's responsibility (documented in the vtable API).
            let _rc = unsafe { fp(user_data, query_ptr, documents_ptr, &mut _out_result, &mut _out_error) };
            if _rc != 0 {
                let msg = if _out_error.is_null() {
                    format!("vtable.rerank returned error code {}", _rc)
                } else {
                    // SAFETY: out_error was written by the callee as a valid NUL-terminated string.
                    let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                        .to_string_lossy()
                        .into_owned();
                    if let Some(free_fn) = vtable.free_string {
                        // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                        unsafe { free_fn(_out_error) };
                    }
                    msg
                };
                return Err(Box::from(msg));
            }
            if _out_result.is_null() {
                return Err(Box::from("vtable.rerank returned null out_result"));
            }
            // SAFETY: out_result was written by the callee as a valid NUL-terminated string.
            let json = unsafe { std::ffi::CStr::from_ptr(_out_result) }
                .to_string_lossy()
                .into_owned();
            if let Some(free_fn) = vtable.free_string {
                // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                unsafe { free_fn(_out_result) };
            }
            serde_json::from_str::<Vec<f32>>(&json)
                .map_err(|e| Box::from(e.to_string()) as Box<dyn std::error::Error + Send + Sync>)
        });
        tokio::task::spawn_blocking(move || _task.call())
            .await
            .map_err(|e| xberg::XbergError::Other(format!("spawn_blocking failed in rerank: {}", e)))?
            .map_err(|e: Box<dyn std::error::Error + Send + Sync>| xberg::XbergError::Other(e.to_string()))
    }
}

/// Register a C plugin implementing `RerankerBackend` via a vtable.
///
/// # Parameters
///
/// - `name`: null-terminated UTF-8 plugin name. Must not be null.
/// - `vtable`: vtable with function pointers implementing the trait.
/// - `user_data`: opaque pointer forwarded to every vtable function.
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// All function pointers in `vtable` must remain valid until the plugin is
/// unregistered. `user_data` must be safe to use from any thread that calls
/// into the plugin.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_register_reranker_backend(
    name: *const std::ffi::c_char,
    vtable: *const XbergRerankerBackendVTable,
    user_data: *const std::ffi::c_void,
    out_error: *mut *mut std::ffi::c_char,
) -> i32 {
    catch_ffi_panic(1, || {
        if name.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "name must not be null") };
            return 1;
        }
        if vtable.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable must not be null") };
            return 1;
        }
        // SAFETY: null check above guarantees vtable is a valid pointer.
        let vtable_ref = unsafe { &*vtable };
        if vtable_ref.free_string.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.free_string must not be null") };
            return 1;
        }
        if vtable_ref.rerank.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.rerank must not be null") };
            return 1;
        }

        // SAFETY: name is non-null (checked above); it points to a valid C string.
        let plugin_name = match unsafe { std::ffi::CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_owned(),
            Err(_) => {
                // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
                unsafe { ffi_set_out_error(out_error, "name is not valid UTF-8") };
                return 1;
            }
        };

        // SAFETY: vtable pointer and user_data validity is the caller's responsibility.
        let bridge = unsafe { XbergRerankerBackendBridge::new(plugin_name, *vtable_ref, user_data) };
        let arc: Arc<dyn xberg::RerankerBackend> = Arc::new(bridge);

        let registry = xberg::plugins::registry::get_reranker_backend_registry();
        let mut registry = registry.write();

        if let Err(e) = registry.register(arc) {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// Unregister a previously registered C plugin by name.
///
/// # Parameters
///
/// - `name`: null-terminated UTF-8 plugin name. Must not be null.
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// `name` must point to a valid null-terminated C string.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_unregister_reranker_backend(
    name: *const std::ffi::c_char,
    out_error: *mut *mut std::ffi::c_char,
) -> i32 {
    catch_ffi_panic(1, || {
        if name.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "name must not be null") };
            return 1;
        }
        // SAFETY: name is non-null (checked above); it points to a valid C string.
        let plugin_name = match unsafe { std::ffi::CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
                unsafe { ffi_set_out_error(out_error, "name is not valid UTF-8") };
                return 1;
            }
        };

        let registry = xberg::plugins::registry::get_reranker_backend_registry();
        let mut registry = registry.write();
        if let Err(e) = registry.remove(plugin_name) {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// Remove all registered C plugins of this trait.
///
/// # Parameters
///
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// `out_error` may be null. When non-null, the caller owns the resulting string
/// and must free it with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_clear_reranker_backend(out_error: *mut *mut std::ffi::c_char) -> i32 {
    catch_ffi_panic(1, || {
        let registry = xberg::plugins::registry::get_reranker_backend_registry();
        let mut registry = registry.write();
        if let Err(e) = registry.clear() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// VTable for C plugin bridges implementing the `TokenizerBackend` trait.
///
/// # Safety
///
/// All function pointers must be valid for the lifetime of any bridge created from
/// this vtable.  `free_user_data`, when non-null, is called once with `user_data`
/// when the bridge is dropped.
#[derive(Copy, Clone)]
#[repr(C)]
pub struct XbergTokenizerBackendVTable {
    /// Return a null-terminated UTF-8 name string into `out_name`; return 0 on success.
    pub name_fn: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_name: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Return a null-terminated UTF-8 version string into `out_version`; return 0 on success.
    pub version_fn: Option<
        unsafe extern "C" fn(
            user_data: *const std::ffi::c_void,
            out_version: *mut *mut std::ffi::c_char,
            out_error: *mut *mut std::ffi::c_char,
        ) -> i32,
    >,
    /// Initialise the plugin; return 0 on success, non-zero on failure (error text in `out_error`).
    pub initialize_fn:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, out_error: *mut *mut std::ffi::c_char) -> i32>,
    /// Shut down the plugin; return 0 on success, non-zero on failure (error text in `out_error`).
    pub shutdown_fn:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, out_error: *mut *mut std::ffi::c_char) -> i32>,
    /// Count the tokens in `text` according to this backend's tokenizer.
    pub count_tokens:
        Option<unsafe extern "C" fn(user_data: *const std::ffi::c_void, text: *const std::ffi::c_char) -> usize>,
    /// Optional string destructor: called for strings returned by vtable callbacks.
    pub free_string: Option<unsafe extern "C" fn(*mut std::ffi::c_char)>,
    /// Optional destructor: called once with `user_data` when the bridge is dropped.
    pub free_user_data: Option<unsafe extern "C" fn(*mut std::ffi::c_void)>,
}

// SAFETY: all fields are function pointers and free_user_data, which are Send + Sync.
unsafe impl Send for XbergTokenizerBackendVTable {}
unsafe impl Sync for XbergTokenizerBackendVTable {}

/// Rust-side bridge that holds a C vtable pointer and opaque `user_data`.
///
/// Implements `TokenizerBackend` by forwarding calls through the vtable.
pub struct XbergTokenizerBackendBridge {
    vtable: XbergTokenizerBackendVTable,
    user_data: *const std::ffi::c_void,
    cached_name: String,
    cached_version: String,
}

impl std::fmt::Debug for XbergTokenizerBackendBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.debug_struct("XbergTokenizerBackendBridge")
            .field("cached_name", &self.cached_name)
            .field("cached_version", &self.cached_version)
            .finish_non_exhaustive()
    }
}

// SAFETY: The caller is responsible for ensuring `user_data` is safe to send across
// thread boundaries. This is documented in `XbergTokenizerBackendVTable` and the registration function.
unsafe impl Send for XbergTokenizerBackendBridge {}
unsafe impl Sync for XbergTokenizerBackendBridge {}

impl Drop for XbergTokenizerBackendBridge {
    fn drop(&mut self) {
        if let Some(free_fn) = self.vtable.free_user_data {
            // SAFETY: free_fn is a valid function pointer; user_data is the pointer
            // originally provided at registration. Called exactly once here.
            unsafe { free_fn(self.user_data as *mut std::ffi::c_void) }
        }
    }
}

impl XbergTokenizerBackendBridge {
    /// Create a new bridge from a vtable and opaque user_data pointer.
    ///
    /// # Safety
    ///
    /// `vtable` must remain valid for the lifetime of the returned bridge.
    /// `user_data` must be valid for any thread that calls methods on this bridge.
    /// All required fn pointers in `vtable` must be non-null.
    pub unsafe fn new(name: String, vtable: XbergTokenizerBackendVTable, user_data: *const std::ffi::c_void) -> Self {
        Self {
            vtable,
            user_data,
            cached_name: name,
            cached_version: String::new(),
        }
    }
}

impl xberg::plugins::Plugin for XbergTokenizerBackendBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }
    fn version(&self) -> String {
        let Some(fp) = self.vtable.version_fn else {
            return String::new();
        };
        let mut _out: *mut std::ffi::c_char = std::ptr::null_mut();
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out, &mut _out_error) };
        if rc != 0 {
            if !_out_error.is_null() {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let _ = unsafe { std::ffi::CStr::from_ptr(_out_error) }.to_string_lossy();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
            }
            return self.cached_version.clone();
        }
        if _out.is_null() {
            return self.cached_version.clone();
        }
        // SAFETY: _out is a callee-allocated NUL-terminated string.
        let version = unsafe { std::ffi::CStr::from_ptr(_out) }.to_string_lossy().into_owned();
        if let Some(free_fn) = self.vtable.free_string {
            // SAFETY: free_fn is the vtable-provided destructor for callback strings.
            unsafe { free_fn(_out) };
        }
        version
    }
    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        let Some(fp) = self.vtable.initialize_fn else {
            return Ok(());
        };
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out_error) };
        if rc != 0 {
            let msg = if _out_error.is_null() {
                format!("initialize returned {}", rc)
            } else {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            return Err(xberg::XbergError::Plugin {
                message: msg,
                plugin_name: String::new(),
            });
        }
        Ok(())
    }
    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        let Some(fp) = self.vtable.shutdown_fn else {
            return Ok(());
        };
        let mut _out_error: *mut std::ffi::c_char = std::ptr::null_mut();
        // SAFETY: fp is valid; user_data validity is the caller's responsibility.
        let rc = unsafe { fp(self.user_data, &mut _out_error) };
        if rc != 0 {
            let msg = if _out_error.is_null() {
                format!("shutdown returned {}", rc)
            } else {
                // SAFETY: _out_error is a callee-allocated NUL-terminated string.
                let msg = unsafe { std::ffi::CStr::from_ptr(_out_error) }
                    .to_string_lossy()
                    .into_owned();
                if let Some(free_fn) = self.vtable.free_string {
                    // SAFETY: free_fn is the vtable-provided destructor for callback strings.
                    unsafe { free_fn(_out_error) };
                }
                msg
            };
            return Err(xberg::XbergError::Plugin {
                message: msg,
                plugin_name: String::new(),
            });
        }
        Ok(())
    }
}

impl xberg::TokenizerBackend for XbergTokenizerBackendBridge {
    fn count_tokens(&self, text: &str) -> usize {
        let Some(fp) = self.vtable.count_tokens else {
            tracing::warn!(
                wrapper = "XbergTokenizerBackendBridge",
                method = "count_tokens",
                "vtable slot for host callback is not initialised; returning default"
            );
            return 0;
        };
        let _text_cs = match std::ffi::CString::new(text) {
            Ok(s) => s,
            Err(_) => {
                return 0;
            }
        };
        let text_ptr = _text_cs.as_ptr();
        // SAFETY: fp is a valid non-null function pointer; all temporaries outlive this call;
        // user_data validity is the caller's responsibility (documented in the vtable API).
        unsafe { fp(self.user_data, text_ptr) }
    }
}

/// Register a C plugin implementing `TokenizerBackend` via a vtable.
///
/// # Parameters
///
/// - `name`: null-terminated UTF-8 plugin name. Must not be null.
/// - `vtable`: vtable with function pointers implementing the trait.
/// - `user_data`: opaque pointer forwarded to every vtable function.
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// All function pointers in `vtable` must remain valid until the plugin is
/// unregistered. `user_data` must be safe to use from any thread that calls
/// into the plugin.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_register_tokenizer_backend(
    name: *const std::ffi::c_char,
    vtable: *const XbergTokenizerBackendVTable,
    user_data: *const std::ffi::c_void,
    out_error: *mut *mut std::ffi::c_char,
) -> i32 {
    catch_ffi_panic(1, || {
        if name.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "name must not be null") };
            return 1;
        }
        if vtable.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable must not be null") };
            return 1;
        }
        // SAFETY: null check above guarantees vtable is a valid pointer.
        let vtable_ref = unsafe { &*vtable };
        if vtable_ref.free_string.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.free_string must not be null") };
            return 1;
        }
        if vtable_ref.count_tokens.is_none() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "vtable.count_tokens must not be null") };
            return 1;
        }

        // SAFETY: name is non-null (checked above); it points to a valid C string.
        let plugin_name = match unsafe { std::ffi::CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_owned(),
            Err(_) => {
                // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
                unsafe { ffi_set_out_error(out_error, "name is not valid UTF-8") };
                return 1;
            }
        };

        // SAFETY: vtable pointer and user_data validity is the caller's responsibility.
        let bridge = unsafe { XbergTokenizerBackendBridge::new(plugin_name, *vtable_ref, user_data) };
        let arc: Arc<dyn xberg::TokenizerBackend> = Arc::new(bridge);

        let registry = xberg::plugins::registry::get_tokenizer_backend_registry();
        let mut registry = registry.write();

        if let Err(e) = registry.register(arc) {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// Unregister a previously registered C plugin by name.
///
/// # Parameters
///
/// - `name`: null-terminated UTF-8 plugin name. Must not be null.
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// `name` must point to a valid null-terminated C string.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_unregister_tokenizer_backend(
    name: *const std::ffi::c_char,
    out_error: *mut *mut std::ffi::c_char,
) -> i32 {
    catch_ffi_panic(1, || {
        if name.is_null() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, "name must not be null") };
            return 1;
        }
        // SAFETY: name is non-null (checked above); it points to a valid C string.
        let plugin_name = match unsafe { std::ffi::CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
                unsafe { ffi_set_out_error(out_error, "name is not valid UTF-8") };
                return 1;
            }
        };

        let registry = xberg::plugins::registry::get_tokenizer_backend_registry();
        let mut registry = registry.write();
        if let Err(e) = registry.remove(plugin_name) {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}

/// Remove all registered C plugins of this trait.
///
/// # Parameters
///
/// - `out_error`: receives a heap-allocated error string on failure.
///
/// # Safety
///
/// `out_error` may be null. When non-null, the caller owns the resulting string
/// and must free it with `xberg_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn xberg_clear_tokenizer_backend(out_error: *mut *mut std::ffi::c_char) -> i32 {
    catch_ffi_panic(1, || {
        let registry = xberg::plugins::registry::get_tokenizer_backend_registry();
        let mut registry = registry.write();
        if let Err(e) = registry.clear() {
            // SAFETY: out_error is validated by ffi_set_out_error itself (null-checked internally).
            unsafe { ffi_set_out_error(out_error, &e.to_string()) };
            return 1;
        }
        0
    })
}
